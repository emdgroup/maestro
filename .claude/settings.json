{
  "plansDirectory": "./.claude/plans",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "export PATH=\"$HOME/.bun/bin:$PATH\"; [ -d node_modules ] || exit 0; command -v bun >/dev/null 2>&1 || exit 0; bun run format:fix 2>/dev/null || true; output=$(bun run lint:fix 2>&1); code=$?; [ $code -ne 0 ] && { echo \"$output\"; exit 2; }; exit 0",
            "timeout": 60,
            "asyncRewake": true,
            "statusMessage": "Formatting and linting..."
          }
        ]
      }
    ]
  }
}
