/// <reference types="vite/client" />

// Defined by vite.config.ts from TAURI_ENV_DEBUG, which the Tauri CLI sets only
// for debug builds — the same condition under which Tauri compiles devtools in.
declare const __TAURI_DEBUG_BUILD__: boolean;
