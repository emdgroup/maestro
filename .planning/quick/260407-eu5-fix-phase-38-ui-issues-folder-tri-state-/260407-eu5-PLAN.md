---
phase: quick
plan: 260407-eu5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/execution/FileTree.tsx
  - src/components/execution/DiffViewer.tsx
  - src/components/execution/WorktreeDiffPanel.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Folder nodes in tree view show tri-state checkboxes reflecting children aggregate state"
    - "Clicking a folder checkbox toggles all descendant files"
    - "Hunk checkboxes appear inline on the @@ header line, not as separate UI elements above DiffView"
    - "Diff panel content fills entire available viewport height with scrollbar at bottom"
  artifacts:
    - path: "src/components/execution/FileTree.tsx"
      provides: "Folder tri-state checkboxes"
    - path: "src/components/execution/DiffViewer.tsx"
      provides: "Inline hunk checkboxes on @@ header lines"
    - path: "src/components/execution/WorktreeDiffPanel.tsx"
      provides: "Full-height diff panel layout"
  key_links:
    - from: "FileTree.tsx DirectoryNode"
      to: "WorktreeDiffPanel.tsx handleFileToggle"
      via: "onToggleFile callback with folder path"
      pattern: "onToggleFolder.*children"
---

<objective>
Fix three Phase 38 UI issues in the diff/commit panel: (1) add tri-state checkboxes to folder nodes in the file tree, (2) move hunk checkboxes inline onto the @@ header line instead of a separate strip, (3) make the diff panel fill the full available viewport height.

Purpose: Polish the commit panel UX so folder staging, hunk selection, and diff scrolling feel native.
Output: Three modified component files with all three fixes applied.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/execution/FileTree.tsx
@src/components/execution/DiffViewer.tsx
@src/components/execution/WorktreeDiffPanel.tsx
@src/views/WorktreesView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add folder tri-state checkboxes and propagate toggle to children</name>
  <files>src/components/execution/FileTree.tsx, src/components/execution/WorktreeDiffPanel.tsx</files>
  <action>
**FileTree.tsx — Add folder checkboxes to DirectoryNode:**

1. Add a new prop `onToggleFolder` to FileTreeProps (and thread it through FileNode/DirectoryNode):
   ```
   onToggleFolder?: (fileNames: string[]) => void;
   ```
   This callback receives the list of ALL descendant file paths under the folder.

2. Add a helper function `getDescendantFiles(node: FileTreeNode): string[]` that recursively collects all leaf file `fileName` values under a node.

3. Add a helper function `getFolderCheckState(node: FileTreeNode, checkedFiles: Map<string, ...>): "checked" | "unchecked" | "indeterminate"` that:
   - Collects all descendant file names
   - Counts how many are "checked" vs total
   - Returns "checked" if all checked, "unchecked" if none, "indeterminate" otherwise

4. In DirectoryNode's render, insert a CheckboxPrimitive.Root next to the chevron icon (between chevron and folder name), using the same styling as the file checkboxes but size-3.5. Only render when `checkedFiles` and `onToggleFolder` are provided. On click (with e.stopPropagation to not toggle expand), call `onToggleFolder(getDescendantFiles(node))`.

5. The checkbox state: use `getFolderCheckState(node, checkedFiles)`. Checked prop = state === "checked", indeterminate prop = state === "indeterminate". Indicator shows Check or Minus icon accordingly.

**WorktreeDiffPanel.tsx — Add folder toggle handler:**

6. Create a new function `handleFolderToggle(fileNames: string[])` that:
   - Computes how many of the given fileNames are currently in "checked" state (via getFileCheckState)
   - If ALL are checked: remove all from stagedFiles and stagedHunks (uncheck all)
   - Otherwise: add all to stagedFiles, clear their stagedHunks entries (check all)

7. Pass `onToggleFolder={handleFolderToggle}` to the FileTree component in the tree view branch (around line 422-429).
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Folder nodes in tree view display tri-state checkboxes. Clicking a folder toggles all its descendant files between checked/unchecked. Folder state reflects aggregate: all checked = checked, some = indeterminate, none = unchecked.</done>
</task>

<task type="auto">
  <name>Task 2: Move hunk checkboxes inline onto @@ header lines and fix diff panel full height</name>
  <files>src/components/execution/DiffViewer.tsx, src/components/execution/WorktreeDiffPanel.tsx</files>
  <action>
**DiffViewer.tsx — Inline hunk checkboxes on @@ lines:**

The current implementation renders a separate "hunk summary strip" div above the DiffView component (lines 97-121). This must be changed so the checkboxes appear directly on the @@ header lines within the diff output.

Since @git-diff-view/react has no dedicated hunk header render slot (noted in Phase 38 decisions), we must keep the strip approach but style it to look like inline diff headers rather than a separate UI section:

1. Remove the `border-b border-border bg-muted/10 divide-y divide-border/50` container styling. Instead, make each hunk header row look like a native @@ line in the diff. Use styling: `bg-blue-500/8 dark:bg-blue-400/10` (the standard diff hunk header background).

2. Change each row to: checkbox flush left with `pl-2`, then the @@ header text in `font-mono text-xs text-blue-700 dark:text-blue-300` (matching diff hunk header color convention). Remove the gap-2 and px-3 spacing — use tighter `gap-1.5 px-2 py-1` to match diff line density.

3. Remove the outer container's border-b so the hunk strip flows seamlessly into the DiffView below.

4. The strip should only show when `onHunkToggle` is provided AND hunkHeaders.length > 0 (already the case).

**WorktreeDiffPanel.tsx — Full height diff panel:**

The diff content area (line 542) has `overflow-y-auto` on the wrapper div. The issue is that the DiffView component inside may not expand to fill the container.

5. On the diff content wrapper div (line 542: `<div className="flex-1 min-h-0 overflow-y-auto">`), change to `<div className="flex-1 min-h-0 overflow-auto">` — use `overflow-auto` instead of `overflow-y-auto` so horizontal scrollbar also appears at the natural container bottom.

6. Inside the DiffViewer component (DiffViewer.tsx), change the root div from `<div className="min-h-0">` (line 95) to `<div className="min-h-0 flex flex-col h-full">`. This ensures the DiffView content stretches to fill.

7. On the DiffView component itself (line 122), wrap it in a `<div className="flex-1 min-h-0">` so it fills remaining space after hunk headers.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro && npx tsc --noEmit 2>&1 | head -30 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Hunk checkboxes render inline on @@ header lines with diff-native blue styling (no separate strip appearance). Diff panel content fills entire available height — horizontal scrollbar appears at viewport bottom, not mid-screen. Build passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — all changes are purely frontend UI/layout adjustments.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | N/A | UI-only changes | accept | No data flow or auth changes; purely visual |
</threat_model>

<verification>
1. TypeScript compiles with zero errors: `npx tsc --noEmit`
2. Production build succeeds: `pnpm build`
3. In tree view mode, folders show checkboxes that reflect child state
4. Clicking a folder checkbox toggles all children
5. Hunk @@ headers show inline checkboxes with blue diff styling
6. Diff panel fills full viewport height
</verification>

<success_criteria>
- All three UI issues resolved in a single plan
- Zero TypeScript errors, clean production build
- Folder tri-state checkboxes work bidirectionally (parent reflects children, parent toggles children)
- Hunk checkboxes integrated into @@ header display (not separate strip)
- Diff panel uses full available height
</success_criteria>

<output>
After completion, create `.planning/quick/260407-eu5-fix-phase-38-ui-issues-folder-tri-state-/260407-eu5-SUMMARY.md`
</output>
