# Quick Task 260331-d7x: Search backend git command usage

**Created:** 2026-03-31
**Mode:** quick

## Task

Search `src-tauri/` for all git CLI command invocations and produce a complete list.

## Steps

1. Grep `src-tauri/src/git/` for `TokioCommand::new("git")` invocations — local subprocess calls
2. Read `src-tauri/src/git/remote.rs` — SSH-based git command strings
3. Grep remaining `src-tauri/src/ipc/` files for ad-hoc git calls outside the git module
4. Write SUMMARY.md with complete annotated list
