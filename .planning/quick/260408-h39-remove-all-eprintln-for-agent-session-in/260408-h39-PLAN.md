---
phase: quick-260408-h39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/ipc/execution_handlers.rs
  - src-tauri/src/ipc/review_handlers.rs
  - src-tauri/src/ipc/ssh_handlers.rs
  - src-tauri/src/ipc/worktree_handlers.rs
  - src-tauri/src/ipc/project_handlers.rs
  - src-tauri/src/ipc/task_handlers.rs
  - src-tauri/src/ipc/settings_handlers.rs
  - src-tauri/src/ipc/filesystem_handlers.rs
  - src-tauri/src/ssh/session.rs
  - src-tauri/src/process/pty.rs
  - src-tauri/src/process/remote.rs
  - src-tauri/src/websocket/streaming.rs
  - src-tauri/src/main.rs
  - src-tauri/src/models/task.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Zero eprintln! macro invocations remain in the Rust backend"
    - "cargo check passes with no errors"
    - "cargo test passes with no failures"
  artifacts:
    - path: "src-tauri/src/ipc/execution_handlers.rs"
      provides: "Agent execution IPC without debug prints"
    - path: "src-tauri/src/ipc/review_handlers.rs"
      provides: "Review IPC without debug prints"
  key_links: []
---

<objective>
Remove all 178 eprintln! macro invocations across 14 Rust backend files.

Purpose: Clean up noisy stderr debug output that clutters the terminal during agent sessions. These are diagnostic prints (IPC call tracing, status confirmations, PTY lifecycle logging) that served their purpose during development and are no longer needed.

Output: A clean Rust backend with zero eprintln! calls and a passing build.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all eprintln! from IPC handler files</name>
  <files>
    src-tauri/src/ipc/execution_handlers.rs
    src-tauri/src/ipc/review_handlers.rs
    src-tauri/src/ipc/ssh_handlers.rs
    src-tauri/src/ipc/worktree_handlers.rs
    src-tauri/src/ipc/project_handlers.rs
    src-tauri/src/ipc/task_handlers.rs
    src-tauri/src/ipc/settings_handlers.rs
    src-tauri/src/ipc/filesystem_handlers.rs
  </files>
  <action>
Delete every eprintln!(...) statement from these 8 IPC handler files (156 occurrences total):
- execution_handlers.rs: 41 occurrences — IPC call tracing, attach/detach lifecycle, PTY reader logging, status updates
- review_handlers.rs: 30 occurrences — diff generation tracing, merge lifecycle, reject/finalize logging
- ssh_handlers.rs: 26 occurrences — connection tracing, auth method logging, directory listing
- worktree_handlers.rs: 23 occurrences — list/create/delete tracing, zombie cleanup, stage/commit/discard logging
- project_handlers.rs: 14 occurrences — get/create/remove project tracing, stale path validation
- filesystem_handlers.rs: 9 occurrences — directory listing, accent color detection
- task_handlers.rs: 8 occurrences — CRUD call tracing, branch listing
- settings_handlers.rs: 5 occurrences — get/save settings, sync calls

For each file: remove the entire eprintln!() statement including multi-line variants. When the eprintln! is the only statement in a block (e.g., inside an `if` or match arm), keep the block structure intact — do not remove surrounding control flow. If removing the eprintln! leaves an empty line, remove the blank line too.

Do NOT replace with any logging framework — just delete them.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro/src-tauri && grep -r "eprintln!" src/ipc/ | wc -l | xargs test 0 -eq && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Zero eprintln! calls in src-tauri/src/ipc/, cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Remove all eprintln! from non-IPC backend files</name>
  <files>
    src-tauri/src/ssh/session.rs
    src-tauri/src/process/pty.rs
    src-tauri/src/process/remote.rs
    src-tauri/src/websocket/streaming.rs
    src-tauri/src/main.rs
    src-tauri/src/models/task.rs
  </files>
  <action>
Delete every eprintln!(...) statement from these 6 remaining files (22 occurrences total):
- ssh/session.rs: 11 occurrences — SSH PTY reader/writer lifecycle, channel messages (Data, EOF, Close, ExitStatus)
- process/remote.rs: 5 occurrences — poll_remote_log errors, stream_remote_output lifecycle, kill notification
- main.rs: 2 occurrences — stale session cleanup count, app init confirmation
- models/task.rs: 2 occurrences — unknown TaskPriority/TaskStatus enum fallback warnings
- process/pty.rs: 1 occurrence — PTY spawn confirmation
- websocket/streaming.rs: 1 occurrence — stream listener stopped

Same rules as Task 1: remove the full statement, preserve control flow structure, clean up blank lines.

For models/task.rs specifically: the eprintln! calls log unknown enum variants before defaulting. Remove the eprintln! but keep the default return (the match arm body should just return the default value without the print).
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro/src-tauri && grep -r "eprintln!" src/ | wc -l | xargs test 0 -eq && cargo check 2>&1 | tail -5 && cargo test 2>&1 | tail -10</automated>
  </verify>
  <done>Zero eprintln! calls in the entire src-tauri/src/ directory, cargo check and cargo test both pass</done>
</task>

</tasks>

<verification>
```bash
cd /home/m306213/workspace/maestro/src-tauri
# Confirm zero eprintln! in entire backend
grep -r "eprintln!" src/ && echo "FAIL: eprintln! still present" || echo "PASS: all eprintln! removed"
# Confirm build
cargo check
# Confirm tests
cargo test
```
</verification>

<success_criteria>
- grep -r "eprintln!" src-tauri/src/ returns zero matches
- cargo check compiles without errors
- cargo test passes without failures
</success_criteria>

<output>
After completion, create `.planning/quick/260408-h39-remove-all-eprintln-for-agent-session-in/260408-h39-SUMMARY.md`
</output>
