---
phase: quick-260408-s08
plan: "01"
subsystem: frontend/agents-view
tags: [ux, spawn-dialog, worktrees, query-invalidation]
dependency_graph:
  requires: []
  provides: [worktree-based-spawn-dialog]
  affects: [AgentsView]
tech_stack:
  added: []
  patterns: [useQueryClient-invalidate-on-open, worktree-selector]
key_files:
  modified:
    - src/views/AgentsView.tsx
decisions:
  - "Use branch_name as Select value key — always unique per worktree and is the field needed by spawnInteractiveExecution"
  - "Pre-select first worktree on dialog open so Spawn button is immediately enabled"
  - "Invalidate worktreeQueryKeys.all before opening dialog — triggers background refetch so list is fresh"
metrics:
  duration: 0.027h
  completed: "2026-04-08T20:13:55Z"
  tasks: 1
  files: 1
---

# Quick 260408-s08: New Session Dialog — Select Worktree Instead of Branch

**One-liner:** Replaced raw branch dropdown in spawn dialog with worktree selector (branch + running indicator) and added cache invalidation on open.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Replace branch selector with worktree selector and add on-open refresh | 05310aa | src/views/AgentsView.tsx |

## What Changed

`AgentsView.tsx` — single-file change:

- Removed `useProjectBranchesQuery` import from `task.service`; removed `branches` / `currentBranch` derived vars
- Added `useWorktreesQuery` + `worktreeQueryKeys` from `worktree.service`; added `useQueryClient` from `@tanstack/react-query`
- Renamed state `spawnBranch` → `selectedBranchName`
- `onSpawn` handler: calls `queryClient.invalidateQueries({ queryKey: worktreeQueryKeys.all })` then pre-selects first worktree before opening dialog
- Select dropdown maps `worktrees` array: `value={wt.branch_name}`, label shows branch name in monospace with optional `"running"` badge when `agent_status === "running"`
- Label text: "Branch" → "Worktree"; dialog description updated to mention worktrees
- Spawn button disabled on `!selectedBranchName`; mutation call passes `selectedBranchName` as `branchName` — backend API unchanged

## Deviations from Plan

None — plan executed exactly as written.

## Verification

- `pnpm build` passed: 0 TypeScript errors, production bundle built in 2.34s
- `useProjectBranchesQuery` confirmed absent from AgentsView.tsx (grep: no matches)
- `worktreeQueryKeys.all` invalidated in onSpawn handler
- Select maps over `worktrees`, not branches

## Self-Check: PASSED

- File exists: `src/views/AgentsView.tsx` — FOUND
- Commit `05310aa` — FOUND (`git log --oneline -1` confirms)
