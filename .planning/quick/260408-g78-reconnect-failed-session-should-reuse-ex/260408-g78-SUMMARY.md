---
phase: quick-260408-g78
plan: "01"
subsystem: frontend
tags: [agents-view, ux, spawn, reconnect]
dependency_graph:
  requires: []
  provides: [reconnect-reuses-existing-params]
  affects: [AgentsView]
tech_stack:
  added: []
  patterns: [direct-mutation-on-reconnect]
key_files:
  created: []
  modified:
    - src/views/AgentsView.tsx
decisions:
  - "onReconnect calls spawnMutation.mutate() directly — branch is already known from the failed session, no dialog needed"
metrics:
  duration: "0.005h"
  completed_date: "2026-04-08"
---

# Quick Task 260408-g78: Reconnect Reuses Existing Session Params — Summary

**One-liner:** Reconnect button on failed/completed sessions now spawns immediately using the session's branch_name, bypassing the spawn dialog entirely.

## What Was Done

Changed the `onReconnect` callback in `AgentsView.tsx` (lines 121-127) to call `spawnMutation.mutate()` directly instead of populating dialog state and opening the spawn dialog.

The `onSpawn` callback (New Session button) is untouched and continues to open the dialog with branch + label fields.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- [x] `src/views/AgentsView.tsx` modified
- [x] Commit `dc81db5` exists: `fix(quick-260408-g78-01): reconnect skips dialog and spawns directly`
- [x] `pnpm build` passed with 0 TypeScript errors
