---
phase: 11-agent-execution-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TaskCard.tsx
  - src/styles/TaskCard.css
autonomous: true

must_haves:
  truths:
    - "Status badge appears in top-right corner of InProgress tasks"
    - "Running tasks show blue pulsing badge with live elapsed time (updated every 1 second)"
    - "Failed tasks show red static badge (existing Phase 8 colors: #fee2e2 background, #991b1b text)"
    - "Success state shows green badge with checkmark while task remains in InProgress column"
    - "Badge disappears when task moves to Review or Done column"
    - "Badge displays elapsed time in format 'Xm Ys' (e.g., '2m 34s')"
    - "Badge is display-only (pointer-events: none) and does not interfere with card click behavior"
  artifacts:
    - path: "src/components/TaskCard.tsx"
      provides: "Status badge rendering with elapsed time calculation and state management"
      exports: ["TaskCard component with badge rendering"]
    - path: "src/styles/TaskCard.css"
      provides: "Badge positioning, colors, animations (pulsing keyframes)"
      contains: ["@keyframes pulse-badge", ".badge-running", ".badge-failed", ".badge-success"]
    - path: "src/App.css"
      provides: "Spinner animation (already exists, referenced in badge)"
      contains: ["@keyframes spin"]
  key_links:
    - from: "src/components/TaskCard.tsx"
      to: "boardStore.ts (task.status, task.execution_log)"
      via: "props.task with status and execution_log fields"
      pattern: "task.status === 'InProgress'"
    - from: "src/components/TaskCard.tsx"
      to: "src/styles/TaskCard.css"
      via: "className conditional on task.status"
      pattern: "className={task.status === 'InProgress' && task.execution_log ? 'badge-running' : ''}"
    - from: "formatElapsedTime utility"
      to: "task.execution_log.started_at"
      via: "ISO 8601 timestamp parsing"
      pattern: "new Date(task.execution_log.started_at).getTime()"
---

<objective>
Add visual status badges to TaskCard showing execution state with live elapsed time. Makes running execution visible on Kanban board.

Purpose: Phase 4 execution works but is invisible on the board. Users can't see which tasks are actively executing without opening modals. Status badges provide at-a-glance visibility.

Output: TaskCard component displays blue pulsing badge with elapsed time for running tasks, red badge for failed tasks, green checkmark for completed execution.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-execution-ux-polish/11-CONTEXT.md
@.planning/phases/11-agent-execution-ux-polish/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add elapsed time formatting utility and status badge state management to TaskCard</name>
  <files>src/components/TaskCard.tsx</files>
  <action>
Update TaskCard.tsx to track elapsed time and render status badges.

1. Add formatElapsedTime utility function at top of file:
   - Takes ISO 8601 string (from task.execution_log?.started_at)
   - Returns formatted string: "Xm Ys" format
   - Logic: Calculate Date.now() - new Date(startedAt).getTime()
   - Format: seconds < 60 → "Xs", minutes < 60 → "Xm Ys", else "Xh Ym"
   - Handle edge case: startedAt not set → return "0s"

2. In TaskCard component:
   - Import useState, useEffect from React
   - Add state: const [elapsedTime, setElapsedTime] = useState('0s');
   - Add useEffect hook:
     - Runs only when task.status === 'InProgress'
     - Creates setInterval that updates elapsedTime every 1 second
     - Calls formatElapsedTime(task.execution_log?.started_at)
     - Cleanup: clearInterval on unmount or status change
     - Returns () => clearInterval(interval);

3. Determine badge state from task fields:
   - Running: task.status === 'InProgress' && task.execution_log?.status === 'Running'
   - Failed: task.status === 'InProgress' && task.execution_log?.status === 'Failed'
   - Success: task.status === 'InProgress' && task.execution_log?.status === 'Complete'
   - No badge: all other states

Reference RESEARCH.md Example 1 (lines 313-350) for elapsed time pattern.

CRITICAL: Use task.execution_log?.started_at from database (ISO 8601). Never calculate from task.created_at—execution may start after creation.

CRITICAL: Elapsed time updates every 1 second only during InProgress. Must not cause unnecessary re-renders of entire board (use React.memo on TaskCard if needed).
  </action>
  <verify>
    cd /home/m306213/workspace/gsd-demo && pnpm build 2>&1 | grep -E "(error|Failed to compile)" || echo "Build successful"
  </verify>
  <done>
    - TaskCard.tsx imports useState, useEffect
    - formatElapsedTime function exists and handles edge cases
    - elapsedTime state updates every 1 second when task.status === 'InProgress'
    - Badge state determined correctly from task.status and task.execution_log?.status
    - Component does not re-render unnecessarily (interval only runs during InProgress)
</done>
</task>

<task type="auto">
  <name>Task 2: Style status badges with colors, positioning, and pulsing animation</name>
  <files>src/styles/TaskCard.css</files>
  <action>
Add badge styling with position, colors, and animations to TaskCard.css.

1. Add @keyframes pulse-badge animation (before other rules):
   ```css
   @keyframes pulse-badge {
     0%, 100% {
       opacity: 1;
     }
     50% {
       opacity: 0.7;
     }
   }
   ```

2. Add .badge-container positioning (absolute, top-right corner of task card):
   - position: absolute
   - top: 8px
   - right: 8px
   - z-index: 10
   - pointer-events: none (critical: badge must not interfere with card click)

3. Add .badge-running styles (blue, pulsing):
   - background-color: #3b82f6 (blue)
   - color: white
   - padding: 6px 10px
   - border-radius: 12px
   - font-size: 12px
   - font-weight: 600
   - display: inline-flex
   - align-items: center
   - gap: 6px (space between spinner icon and time text)
   - animation: pulse-badge 1.5s ease-in-out infinite

4. Add .badge-failed styles (red, no animation, Phase 8 colors):
   - background-color: #fee2e2
   - color: #991b1b
   - Same padding, border-radius, font-size, font-weight as running
   - animation: none

5. Add .badge-success styles (green, checkmark, no animation):
   - background-color: #dcfce7
   - color: #166534
   - Same layout as failed
   - animation: none

6. Add .spinner-icon styles (small loading spinner, using existing App.css @keyframes spin):
   - width: 14px
   - height: 14px
   - animation: spin 0.6s linear infinite
   - display: inline-block

Reference RESEARCH.md Example 2 (lines 352-397) for badge CSS pattern.

CRITICAL: pointer-events: none on badge-container prevents click bubbling to card's onClick handler. Verify with manual test.

CRITICAL: All badge variants use position: absolute so they don't affect card layout. Badge sits outside card flow, in corner.
  </action>
  <verify>
    grep -A5 "@keyframes pulse-badge" /home/m306213/workspace/gsd-demo/src/styles/TaskCard.css && echo "Keyframes found"
    grep "pointer-events: none" /home/m306213/workspace/gsd-demo/src/styles/TaskCard.css && echo "pointer-events set"
  </verify>
  <done>
    - TaskCard.css has @keyframes pulse-badge animation
    - .badge-running, .badge-failed, .badge-success classes exist
    - Badge positioned absolutely in top-right corner (top: 8px, right: 8px)
    - pointer-events: none prevents click interference
    - Running badge has pulsing animation (1.5s period, opacity 0.7-1.0)
    - Colors match Phase 8 theme (#fee2e2/#991b1b for failed, semantic blue/green for running/success)
</done>
</task>

<task type="auto">
  <name>Task 3: Render badge in TaskCard JSX with conditional className and elapsed time display</name>
  <files>src/components/TaskCard.tsx</files>
  <action>
Add JSX to render status badge in TaskCard component near top of card.

1. Determine badge className:
   - let badgeClassName = '';
   - if (task.status === 'InProgress' && task.execution_log) {
       if (task.execution_log.status === 'Running') badgeClassName = 'badge-running';
       else if (task.execution_log.status === 'Failed') badgeClassName = 'badge-failed';
       else if (task.execution_log.status === 'Complete') badgeClassName = 'badge-success';
     }

2. Add JSX element (inside TaskCard, after opening <div>):
   ```jsx
   {badgeClassName && (
     <div className={`badge-container ${badgeClassName}`}>
       {badgeClassName === 'badge-running' && (
         <>
           <span className="spinner-icon"></span>
           <span>{elapsedTime}</span>
         </>
       )}
       {badgeClassName === 'badge-failed' && (
         <span>Failed</span>
       )}
       {badgeClassName === 'badge-success' && (
         <>
           <span>✓</span>
           <span>Done</span>
         </>
       )}
     </div>
   )}
   ```

3. Ensure badge-container div is inside task-card (position: relative), after opening tag but before card content

Reference RESEARCH.md pattern: Badge appears at top-right, updates every 1s with elapsed time, disappears when status !== InProgress.

CRITICAL: Badge should NOT be clickable (pointer-events: none on container). Test by clicking badge → verify ExecutionHistory modal does NOT open.

CRITICAL: Spinner icon (rotate animation) uses App.css @keyframes spin. Verify this keyframe exists and has smooth rotation.
  </action>
  <verify>
    cd /home/m306213/workspace/gsd-demo && pnpm dev &
    sleep 10
    curl -s http://localhost:5173 | grep -q "badge-container" && echo "JSX renders" || echo "Check manual test"
    pkill -f "pnpm dev"
  </verify>
  <done>
    - Badge renders conditionally based on task.status and task.execution_log?.status
    - Running badge shows spinner icon + elapsed time text
    - Failed badge shows "Failed" text with red styling
    - Success badge shows checkmark + "Done" text with green styling
    - Badge displays in top-right corner of card without affecting layout
    - Elapsed time updates every 1 second during execution
</done>
</task>

</tasks>

<verification>
Manual verification after execution:
1. Create a task in Backlog → move to Ready
2. Click Execute → task moves to InProgress
3. Verify blue badge appears in top-right corner with spinner + elapsed time (e.g., "0s", "1s", "2m 34s")
4. Verify elapsed time increments every 1 second
5. Verify pulsing animation is subtle (not distracting) over 1.5s cycle
6. Open ExecutionHistory modal → verify elapsed time in badge continues updating
7. Click badge → verify ExecutionHistory modal does NOT open (badge is display-only)
8. After execution completes, verify badge color changes to green with checkmark
9. Move task from InProgress to Review → verify badge disappears
10. Simulate execution failure (manual task status change or error condition) → verify badge turns red with "Failed" text
</verification>

<success_criteria>
- Status badge visible for all InProgress tasks with execution_log
- Running badge displays live elapsed time updated every 1 second
- Running badge has subtle pulsing animation (1.5s period, opacity 0.7-1.0)
- Failed and success badges render with correct colors from Phase 8 theme
- Badge is display-only (pointer-events: none) and does not interfere with card interactions
- Badge disappears when task leaves InProgress column
- All badge variants position correctly in top-right corner without affecting card layout
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-execution-ux-polish/11-01-SUMMARY.md`
</output>
