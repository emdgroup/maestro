---
phase: 05-real-time-monitoring
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/components/Terminal.tsx
  - src/components/TaskDetail.tsx
  - src/store/boardStore.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Frontend can open a terminal component embedded in the task detail modal"
    - "Terminal displays live streamed output from backend PTY via Tauri channel"
    - "Terminal can accept keyboard input and send to backend PTY"
    - "Terminal automatically resizes and propagates dimensions to backend"
  artifacts:
    - path: "src/components/Terminal.tsx"
      provides: "Terminal emulator React component using xterm.js"
      exports: ["TerminalComponent", "useTerminal hook"]
    - path: "src/components/TaskDetail.tsx"
      provides: "Integration of Terminal component in modal tabs"
      contains: "Terminal tab showing TerminalComponent"
    - path: "package.json"
      provides: "xterm dependencies"
      contains: "@xterm/xterm and @xterm/addon-fit"
  key_links:
    - from: "src/components/Terminal.tsx"
      to: "attach_terminal IPC handler"
      via: "invoke('attach_terminal', {taskId, outputChannel})"
      pattern: "invoke.*attach_terminal"
    - from: "src/components/Terminal.tsx"
      to: "send_terminal_input IPC handler"
      via: "invoke('send_terminal_input', {taskId, input})"
      pattern: "invoke.*send_terminal_input"
    - from: "src/components/Terminal.tsx onResize"
      to: "resize_terminal IPC handler"
      via: "invoke('resize_terminal', {taskId, cols, rows})"
      pattern: "invoke.*resize_terminal"
    - from: "Plan 01 (backend)"
      to: "Plan 02 (frontend)"
      via: "resize_terminal must be implemented in Plan 01 before Plan 02 can call it"
      pattern: "BLOCKER: resize_terminal handler required"
---

<objective>
Build the React frontend terminal component that displays live PTY output and enables interactive terminal input. This component integrates xterm.js with Tauri channel streaming to create a real-time embedded terminal UI.

Purpose: Provide users with a live terminal view of agent execution inside the task detail modal. The terminal must support input (Ctrl+C, manual commands) and output streaming with automatic resizing.

Output:
- `src/components/Terminal.tsx` with xterm.js integration
- Integration in TaskDetail modal as new "Terminal" tab
- xterm and addon dependencies added to package.json
- Bidirectional channel setup for streaming and input
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-real-time-monitoring/05-RESEARCH.md
@.planning/phases/05-real-time-monitoring/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add xterm.js dependencies to package.json and create Terminal component structure</name>
  <files>package.json</files>
  <action>
1. Add xterm dependencies to package.json dependencies section:
   ```json
   "@xterm/xterm": "^5.3.0",
   "@xterm/addon-fit": "^0.8.0",
   "@xterm/addon-attach": "^0.9.0"
   ```

2. Run `pnpm install` to fetch dependencies (verify installation succeeds)

3. Verify installation:
   - Check node_modules/@xterm/ directory exists with xterm, addon-fit, addon-attach subdirs

Reference: RESEARCH.md "Dependencies to Add" section specifies exact versions.
  </action>
  <verify>
grep -E "@xterm/xterm|@xterm/addon-fit|@xterm/addon-attach" package.json
ls node_modules/@xterm/ 2>/dev/null | wc -l
  </verify>
  <done>
package.json contains all three xterm dependencies (version 5.3+ for xterm, 0.8+ for fit, 0.9+ for attach), dependencies installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Terminal.tsx component with xterm.js integration and Tauri channel setup</name>
  <files>src/components/Terminal.tsx</files>
  <action>
Create new file src/components/Terminal.tsx with:

1. **React component structure** using TypeScript:
   - Import Terminal from @xterm/xterm
   - Import FitAddon from @xterm/addon-fit
   - Import useEffect, useRef from React
   - Import invoke, Channel from @tauri-apps/api/core
   - Import xterm CSS: @xterm/xterm/css/xterm.css

2. **TerminalComponent export:**
   - Accept taskId as prop
   - Create useRef for DOM container
   - Create useRef for Terminal instance
   - Create useRef for Channel

3. **useEffect hook for lifecycle:**
   - Create Terminal instance with options: cursorBlink=true, fontSize=14
   - Create FitAddon and load into terminal
   - Call terminal.open(terminalRef.current)
   - Call fitAddon.fit()
   - Create Channel and set up onmessage to write to terminal
   - Invoke attach_terminal with taskId and channel
   - Set up terminal.onData to invoke send_terminal_input
   - Set up terminal.onResize to invoke resize_terminal
   - Return cleanup: dispose terminal, drop channel

Reference: RESEARCH.md Architecture Pattern section 5 for exact code

Key patterns: useRef not useState, all IPC is async, Channel drop is implicit detach, import xterm CSS
  </action>
  <verify>
grep -q "export.*TerminalComponent" src/components/Terminal.tsx
grep -q "@xterm/xterm" src/components/Terminal.tsx
grep -q "new Channel" src/components/Terminal.tsx
grep -q "attach_terminal\|send_terminal_input\|resize_terminal" src/components/Terminal.tsx
  </verify>
  <done>
File exists with TerminalComponent exported, xterm imports, Channel setup, all handler invocations
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Terminal component into TaskDetail modal and add Terminal tab</name>
  <files>src/components/TaskDetail.tsx</files>
  <action>
1. Import TerminalComponent at top of TaskDetail.tsx

2. Find the tab interface (should have ExecutionHistory tab)

3. Add Terminal tab:
   - Add tab button/label for "Terminal"
   - Add conditional rendering: when Terminal tab selected, show `<TerminalComponent taskId={task.id} />`
   - Ensure Terminal only renders when tab is active (prevents multiple instances)

4. Styling:
   - Terminal container should be full height/width: flex: 1, height: 100%
   - Ensure no fixed heights blocking xterm

Key: Only render TerminalComponent when active tab, prevent premature attachment.
  </action>
  <verify>
grep -q "import.*TerminalComponent" src/components/TaskDetail.tsx
grep -q "<TerminalComponent" src/components/TaskDetail.tsx
grep -q "taskId=" src/components/TaskDetail.tsx
  </verify>
  <done>
TaskDetail.tsx imports TerminalComponent, renders it with taskId prop, terminal tab is part of tabbed interface
  </done>
</task>

</tasks>

<verification>
After completion:

1. **Dependencies:**
   - package.json has all three xterm dependencies
   - node_modules/@xterm/ contains xterm, addon-fit, addon-attach

2. **Component:**
   - src/components/Terminal.tsx exists with TerminalComponent
   - Imports xterm and Tauri modules correctly
   - Sets up Channel and IPC handlers

3. **Integration:**
   - TaskDetail.tsx imports TerminalComponent
   - Terminal tab renders component with taskId prop
   - Terminal only renders when tab is active

4. **Handlers:**
   - attach_terminal invoked on component mount
   - send_terminal_input invoked on keyboard input
   - resize_terminal invoked on container resize

5. **Detach Verification (EXEC-05):**
   - When Terminal component unmounts, channel is automatically dropped
   - Backend detects channel close (output_channel.send() returns Err)
   - PTY process continues running after detach (not terminated)
   - Frontend can re-attach and see fresh output without process restart
</verification>

<success_criteria>
1. ✓ xterm.js dependencies installed (package.json)
2. ✓ Terminal.tsx component created with full lifecycle
3. ✓ Tauri channel setup for output streaming
4. ✓ Terminal accepts keyboard input
5. ✓ Terminal handles resize events
6. ✓ Integrated into TaskDetail modal as tab
7. ✓ Frontend compiles without errors
8. ✓ Terminal displays live output from backend

Plan complete when terminal renders with live agent output.
</success_criteria>

<output>
After completion, create `.planning/phases/05-real-time-monitoring/05-02-SUMMARY.md` with:
- Frontend terminal component created
- xterm.js integration patterns
- TaskDetail integration points
- Tauri channel setup details
- Readiness for Plan 3 (output buffering and history)
</output>
