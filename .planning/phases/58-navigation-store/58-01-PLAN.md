---
phase: 58-navigation-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/navigationStore.ts
  - src/store/navigationStore.test.ts
  - src/components/task/TaskDetailScreen.tsx
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-03
  - DETAIL-04
must_haves:
  truths:
    - "navigationStore exports activeTaskId: number | null with initial value null"
    - "setActiveTaskId(42) sets activeTaskId to 42; setActiveTaskId(null) clears it"
    - "navigate({ taskId: 42 }) sets activeTaskId to 42 and activeTab to kanban"
    - "navigate({ view: 'tasks' }) clears activeTaskId back to null"
    - "SubView type, activeSubView, pendingTaskId, setActiveSubView, clearPendingTask no longer exist in the store"
    - "TaskDetailScreen.tsx exists at src/components/task/ and exports a React component accepting taskId: number"
  artifacts:
    - path: "src/store/navigationStore.ts"
      provides: "Refactored navigation store with activeTaskId"
      exports: ["useNavigationStore", "useActiveTab", "useSlideDirection", "useActiveTaskId", "usePendingAgentId", "usePendingWorktreeId", "useNavigate", "useNavigationActions", "ViewType", "NavigationTarget"]
    - path: "src/store/navigationStore.test.ts"
      provides: "Updated tests for new store API"
      min_lines: 80
    - path: "src/components/task/TaskDetailScreen.tsx"
      provides: "Placeholder component for task detail screen"
      exports: ["TaskDetailScreen"]
  key_links:
    - from: "src/store/navigationStore.ts"
      to: "src/views/KanbanView.tsx"
      via: "useActiveTaskId selector hook"
      pattern: "useActiveTaskId"
    - from: "src/components/task/TaskDetailScreen.tsx"
      to: "src/views/KanbanView.tsx"
      via: "import for conditional render"
      pattern: "TaskDetailScreen"
---

<objective>
Refactor navigationStore to replace activeSubView/pendingTaskId with activeTaskId, rewrite tests, and create the TaskDetailScreen stub.

Purpose: Establish the store API and placeholder component that KanbanView and App.tsx depend on in Plan 02.
Output: Updated store, passing tests, new TaskDetailScreen component file.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-navigation-store/58-CONTEXT.md
@.planning/phases/58-navigation-store/58-PATTERNS.md

<interfaces>
From src/store/navigationStore.ts (current exports to preserve):
```typescript
export type ViewType = "kanban" | "agents" | "worktrees" | "settings";
export const useNavigationStore = create<NavigationState>()(immer((set) => ({...})));
export const useActiveTab = () => useNavigationStore((s) => s.activeTab);
export const useSlideDirection = () => useNavigationStore((s) => s.slideDirection);
export const usePendingAgentId = () => useNavigationStore((s) => s.pendingAgentId);
export const usePendingWorktreeId = () => useNavigationStore((s) => s.pendingWorktreeId);
export const useNavigate = () => useNavigationStore((s) => s.navigate);
export const useNavigationActions = () => useNavigationStore(useShallow((s) => ({...})));
```

Exports to REMOVE:
```typescript
export type SubView = "backlog" | "board" | "archive";
export const useActiveSubView = () => useNavigationStore((s) => s.activeSubView);
export const usePendingTaskId = () => useNavigationStore((s) => s.pendingTaskId);
// setActiveSubView and clearPendingTask inside useNavigationActions
```

Exports to ADD:
```typescript
export const useActiveTaskId = () => useNavigationStore((s) => s.activeTaskId);
// setActiveTaskId inside useNavigationActions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor navigationStore.ts — replace SubView/pendingTaskId with activeTaskId</name>
  <files>src/store/navigationStore.ts</files>
  <read_first>src/store/navigationStore.ts</read_first>
  <action>
Rewrite src/store/navigationStore.ts per decisions D-03, D-05, D-07:

1. Remove `SubView` type export entirely.

2. Change `NavigationTarget` type to:
   - `{ taskId: number }` (was string, per D-07)
   - `{ agentId: string }` (unchanged)
   - `{ worktreeId: string }` (unchanged)
   - `{ view: "tasks" | "agents" | "worktree" | "settings" }` (per D-03: remove backlog/board/archive, add tasks)

3. Update `targetViewToTab`: keep `if (view === "worktree") return "worktrees"`; add `if (view === "tasks") return "kanban"`; remove the backlog/board/archive branch.

4. Change `NavigationState` interface:
   - Remove `activeSubView: SubView`
   - Remove `pendingTaskId: string | null`
   - Add `activeTaskId: number | null`
   - Remove `setActiveSubView: (sub: SubView) => void`
   - Remove `clearPendingTask: () => void`
   - Add `setActiveTaskId: (id: number | null) => void`

5. In the store initial state:
   - Remove `activeSubView: "board"`
   - Remove `pendingTaskId: null`
   - Add `activeTaskId: null`

6. In the `navigate` handler:
   - `"taskId" in target` branch: set `state.activeTab = "kanban"`, set `state.activeTaskId = target.taskId`, keep slideDirection calc. Remove the old `state.activeSubView = "board"` and `state.pendingTaskId = target.taskId` lines.
   - `"view" in target` branch: keep the newTab + slideDirection logic. Remove the `if (backlog/board/archive)` activeSubView assignment. Add: `if (target.view === "tasks") { state.activeTaskId = null; }`

7. Remove the `setActiveSubView` action entirely.
8. Remove the `clearPendingTask` action entirely.
9. Add `setActiveTaskId` action using Immer pattern: `setActiveTaskId: (id: number | null) => set((state) => { state.activeTaskId = id; })`

10. Selector hooks section:
    - Remove `useActiveSubView`
    - Remove `usePendingTaskId`
    - Add `export const useActiveTaskId = () => useNavigationStore((s) => s.activeTaskId);`

11. `useNavigationActions` return object:
    - Remove `setActiveSubView: s.setActiveSubView`
    - Remove `clearPendingTask: s.clearPendingTask`
    - Add `setActiveTaskId: s.setActiveTaskId`
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro && npx tsc --noEmit --pretty 2>&1 | grep -E "navigationStore|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `SubView` type does not appear anywhere in navigationStore.ts
    - `activeSubView` does not appear in navigationStore.ts
    - `pendingTaskId` does not appear in navigationStore.ts
    - `clearPendingTask` does not appear in navigationStore.ts
    - `setActiveSubView` does not appear in navigationStore.ts
    - `useActiveSubView` does not appear in navigationStore.ts
    - `usePendingTaskId` does not appear in navigationStore.ts
    - `activeTaskId: null` appears in initial state
    - `setActiveTaskId` action is defined
    - `useActiveTaskId` selector hook is exported
    - `NavigationTarget` includes `{ taskId: number }` (not string)
    - `NavigationTarget` view union includes `"tasks"` and does NOT include `"backlog" | "board" | "archive"`
    - `targetViewToTab` maps `"tasks"` to `"kanban"`
    - `useNavigationActions` returns `setActiveTaskId` (not `setActiveSubView` or `clearPendingTask`)
  </acceptance_criteria>
  <done>Store compiles with new API; old SubView/pendingTaskId API entirely removed</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite navigationStore.test.ts for new API</name>
  <files>src/store/navigationStore.test.ts</files>
  <read_first>src/store/navigationStore.test.ts, src/store/navigationStore.ts</read_first>
  <action>
Rewrite src/store/navigationStore.test.ts per D-04:

1. Update `resetStore()`:
```
useNavigationStore.setState({
  activeTab: "kanban",
  slideDirection: 1,
  activeTaskId: null,
  pendingAgentId: null,
  pendingWorktreeId: null,
});
```

2. Delete these describe blocks entirely:
   - "navigationStore - setActiveSubView" (the last describe block)
   - The `clearPendingTask` test inside "clear actions"
   - The `navigate({ taskId: "42" })` test (replaced below)
   - The `navigate({ view: 'board' })` test
   - The `navigate({ view: 'backlog' })` test
   - The `navigate({ view: 'archive' })` test

3. Update "initial state" test: replace `activeSubView` and `pendingTaskId` assertions with `expect(state.activeTaskId).toBeNull()`.

4. Add new tests in a describe block "navigationStore - activeTaskId":
   - `navigate({ taskId: 42 })` sets `activeTaskId` to 42 and `activeTab` to "kanban"
   - `navigate({ view: 'tasks' })` clears `activeTaskId` to null
   - `setActiveTaskId(7)` sets `activeTaskId` to 7
   - `setActiveTaskId(null)` clears `activeTaskId` to null

5. Keep unchanged: agentId/worktreeId navigate tests, slideDirection tests, clearPendingAgent/clearPendingWorktree tests, navigate({view:'agents'}), navigate({view:'worktree'}), navigate({view:'settings'}).

6. Update the remaining `navigate` entity tests: `navigate({ agentId: "7" })` and `navigate({ worktreeId: "3" })` assertions stay unchanged.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro && pnpm test navigationStore -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - No test references `activeSubView`, `pendingTaskId`, `clearPendingTask`, or `setActiveSubView`
    - `resetStore` sets `activeTaskId: null` and does NOT set `activeSubView` or `pendingTaskId`
    - Test for `navigate({ taskId: 42 })` asserts `state.activeTaskId === 42`
    - Test for `navigate({ view: "tasks" })` asserts `state.activeTaskId === null`
    - Test for `setActiveTaskId(7)` asserts `state.activeTaskId === 7`
    - Test for `setActiveTaskId(null)` asserts `state.activeTaskId === null`
    - All tests pass (exit code 0 from vitest)
  </acceptance_criteria>
  <done>All navigationStore tests pass with the new API; zero references to removed state fields</done>
</task>

<task type="auto">
  <name>Task 3: Create TaskDetailScreen.tsx stub</name>
  <files>src/components/task/TaskDetailScreen.tsx</files>
  <read_first>src/components/task/TaskDetail.tsx</read_first>
  <action>
Create new file src/components/task/TaskDetailScreen.tsx per D-01:

- Define `interface TaskDetailScreenProps { taskId: number; }`
- Export `const TaskDetailScreen: React.FC<TaskDetailScreenProps>` that renders `<div>Task #{taskId}</div>`
- No imports needed beyond implicit React (JSX transform handles it in this project's tsconfig)
- No hooks, no service calls — pure placeholder. Phase 62 replaces the body; import path must stay identical.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro && npx tsc --noEmit --pretty 2>&1 | grep "TaskDetailScreen" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/task/TaskDetailScreen.tsx
    - File exports `TaskDetailScreen` as a named export (not default)
    - Component accepts `taskId: number` prop
    - Component renders a div containing the text "Task #" followed by the taskId value
    - No TypeScript errors referencing this file
  </acceptance_criteria>
  <done>TaskDetailScreen.tsx exists, exports correctly, compiles without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store state -> component render | Navigation state drives which component renders; no untrusted input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | T (Tampering) | navigationStore.activeTaskId | accept | Client-side UI state only; no security boundary. Task data still fetched from backend with proper authorization when TaskDetailScreen is fully implemented in Phase 62. |
| T-58-02 | I (Information Disclosure) | TaskDetailScreen stub | accept | Stub renders only the numeric taskId already visible in the URL/store; no sensitive data exposed. |
</threat_model>

<verification>
```bash
cd /home/m306213/workspace/maestro && pnpm test navigationStore -- --run
```
All tests pass. No TypeScript errors in the three modified/created files.
</verification>

<success_criteria>
- navigationStore.ts exports new API (activeTaskId, setActiveTaskId, useActiveTaskId) and removes old API (SubView, activeSubView, pendingTaskId, etc.)
- All navigationStore tests pass
- TaskDetailScreen.tsx exists and compiles
- TypeScript compilation succeeds for src/store/navigationStore.ts and src/components/task/TaskDetailScreen.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/58-navigation-store/58-01-SUMMARY.md`
</output>
