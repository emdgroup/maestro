---
phase: 29-v1-3-agents-worktrees-view-polish-and-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/kanban/BacklogTaskSheet.tsx
  - src/components/kanban/TaskCard.tsx
  - src/components/project-picker/CloneProjectDialog.tsx
  - src/components/project-picker/CreateProjectDialog.tsx
  - src/components/project-picker/FilePicker.tsx
  - src/components/project-picker/ProjectsListLayout.tsx
  - src/components/project-picker/__tests__/CloneProjectDialog.test.tsx
  - src/components/task/TaskForm.tsx
  - src/components/views/BacklogView.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "All uncommitted quick-task changes (skills removal + branch dropdown + project picker polish) are committed"
    - "Build passes with the committed changes"
    - "All 110 tests pass with the committed changes"
    - "Pending todo 001 is resolved since Phase 24 already implemented project picker improvements"
  artifacts:
    - path: "src/components/task/TaskForm.tsx"
      provides: "Task form without skills, with branch dropdown"
    - path: "src/components/views/BacklogView.tsx"
      provides: "Redesigned backlog view"
  key_links: []
---

<objective>
Commit the completed but uncommitted quick-task work (skills removal, branch dropdown, project picker polish, BacklogView redesign) and clean up the resolved pending todo.

Purpose: Land existing completed work into the git history so it is not lost and the working tree is clean for subsequent v1.4 work.
Output: Clean git working tree with all v1.3 polish changes committed.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/quick/260328-ty5-remove-skills-selection-from-tasks-and-c/260328-ty5-SUMMARY.md
@.planning/todos/pending/001-improve-project-picker-screen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate and commit uncommitted quick-task changes</name>
  <files>
    src/components/kanban/BacklogTaskSheet.tsx,
    src/components/kanban/TaskCard.tsx,
    src/components/project-picker/CloneProjectDialog.tsx,
    src/components/project-picker/CreateProjectDialog.tsx,
    src/components/project-picker/FilePicker.tsx,
    src/components/project-picker/ProjectsListLayout.tsx,
    src/components/project-picker/__tests__/CloneProjectDialog.test.tsx,
    src/components/task/TaskForm.tsx,
    src/components/views/BacklogView.tsx
  </files>
  <read_first>
    .planning/quick/260328-ty5-remove-skills-selection-from-tasks-and-c/260328-ty5-SUMMARY.md
    src/components/task/TaskForm.tsx
    src/components/views/BacklogView.tsx
    src/components/project-picker/CloneProjectDialog.tsx
  </read_first>
  <action>
**1. Verify the uncommitted changes are complete and working:**

Run `pnpm build` to confirm TypeScript compiles cleanly.
Run `pnpm test --run` to confirm all 110 tests pass.
Run `cd src-tauri && cargo check` to confirm Rust compiles.

**2. Review the diff to understand what is being committed:**

Run `git diff --stat HEAD` to see the 12 modified files.
Run `git diff HEAD -- src/components/task/TaskForm.tsx` to verify skills removal + branch dropdown is complete (per the quick-task SUMMARY).
Run `git diff HEAD -- src/components/views/BacklogView.tsx` to verify BacklogView redesign looks intentional.

**3. Stage and commit the source changes (NOT the .planning/ files):**

Stage the 9 source files:
```bash
git add \
  src/components/kanban/BacklogTaskSheet.tsx \
  src/components/kanban/TaskCard.tsx \
  src/components/project-picker/CloneProjectDialog.tsx \
  src/components/project-picker/CreateProjectDialog.tsx \
  src/components/project-picker/FilePicker.tsx \
  src/components/project-picker/ProjectsListLayout.tsx \
  src/components/project-picker/__tests__/CloneProjectDialog.test.tsx \
  src/components/task/TaskForm.tsx \
  src/components/views/BacklogView.tsx
```

Commit with message: `feat(v1.3): remove skills UI, add branch dropdown, polish project picker and backlog view`

Do NOT stage .planning/ files — those will be committed separately as part of the phase summary workflow.

**4. Verify the commit landed correctly:**

Run `git log --oneline -1` to confirm the commit.
Run `git status` to verify only .planning/ files remain as uncommitted.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro && pnpm build && pnpm test --run</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows a commit message containing "remove skills UI"
    - `git diff --stat HEAD` does NOT list any `src/components/` files (they are committed)
    - `pnpm build` exits 0
    - `pnpm test --run` exits 0 with 110 tests passing
  </acceptance_criteria>
  <done>All 9 uncommitted source files from the quick-task are committed. Working tree is clean for source files. Build and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve pending todo 001 (already implemented in Phase 24)</name>
  <files>.planning/todos/pending/001-improve-project-picker-screen.md</files>
  <read_first>
    .planning/todos/pending/001-improve-project-picker-screen.md
  </read_first>
  <action>
The pending todo `001-improve-project-picker-screen.md` describes improvements to the project picker screen:
- Select action checks git and initializes if needed
- Clone project button
- Create project button

All three acceptance criteria were implemented in Phase 24 (v1.2 milestone). The todo is stale.

Move the file from pending to a resolved state:

```bash
mkdir -p .planning/todos/done
mv .planning/todos/pending/001-improve-project-picker-screen.md .planning/todos/done/001-improve-project-picker-screen.md
```

If the `done` directory does not exist, create it first.

Verify by checking that `.planning/todos/pending/` is now empty.
  </action>
  <verify>
    <automated>ls /home/m306213/workspace/maestro/.planning/todos/pending/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/pending/001-improve-project-picker-screen.md` does NOT exist
    - `.planning/todos/done/001-improve-project-picker-screen.md` exists
    - `ls .planning/todos/pending/ | wc -l` outputs `0`
  </acceptance_criteria>
  <done>Stale pending todo resolved. Pending todos directory is empty.</done>
</task>

</tasks>

<verification>
- `git log --oneline -3` shows the new commit for the quick-task changes
- `git diff --stat HEAD` shows only .planning/ files remaining
- `pnpm build` passes
- `pnpm test --run` passes (110 tests)
- No pending todos in `.planning/todos/pending/`
</verification>

<success_criteria>
- Quick-task changes (skills removal, branch dropdown, project picker polish, backlog redesign) are committed to git
- Build and all tests pass
- Pending todo directory is clean
</success_criteria>

<output>
After completion, create `.planning/phases/29-v1-3-agents-worktrees-view-polish-and-bug-fixes/29-02-SUMMARY.md`
</output>
