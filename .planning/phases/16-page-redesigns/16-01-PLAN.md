---
phase: 16-page-redesigns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/KanbanBoard.tsx
  - src/components/KanbanColumn.tsx
  - src/components/TaskCard.tsx
  - src/styles/KanbanBoard.css
autonomous: true
must_haves:
  truths:
    - "Kanban board displays with 5 columns in a grid layout with subtle shadows and borders"
    - "Each task card shows a small colored status dot matching the column (green=Done, blue=InProgress, yellow=Review, gray=Backlog/Ready)"
    - "In-progress tasks display an animated pulse effect on their status dot"
    - "Cards respond to hover with subtle shadow increase and border color shift to ring color"
    - "Drag feedback displays semi-transparent ghost card and highlighted drop zone with colored border"
  artifacts:
    - path: "src/components/KanbanBoard.tsx"
      provides: "Modern grid-based Kanban layout with Tailwind styling"
      contains: "grid grid-cols-5"
    - path: "src/components/KanbanColumn.tsx"
      provides: "Drop zone with visual feedback on isOver state"
      contains: "useDroppable"
    - path: "src/components/TaskCard.tsx"
      provides: "Card component with status dot, hover effects, and animations"
      contains: "animate-pulse|transition-all"
  key_links:
    - from: "src/components/KanbanColumn.tsx"
      to: "src/components/TaskCard.tsx"
      via: "task rendering in drop zone"
      pattern: "tasks\\.map.*TaskCard"
    - from: "src/components/TaskCard.tsx"
      to: "CSS variables"
      via: "semantic color classes (bg-success, bg-warning, etc.)"
      pattern: "className.*success|warning|error|accent"
    - from: "src/components/KanbanBoard.tsx"
      to: "DndContext"
      via: "DragOverlay for ghost visual feedback"
      pattern: "DragOverlay.*activeTask"
---

<objective>
Redesign the Kanban board with modern card-based layout, semantic status indicators, and smooth interaction patterns. Transform the existing functional board into a visually polished interface matching the design system established in Phase 15.

Purpose: The Kanban board is the primary user interface for task management. Modernizing it with subtle depth, clear status visualization, and responsive feedback dramatically improves the user experience and establishes the visual direction for subsequent page redesigns.

Output: Fully redesigned KanbanBoard, KanbanColumn, and TaskCard components with Tailwind-based styling, status dot animations, hover effects, and drag feedback visual patterns.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-page-redesigns/16-CONTEXT.md
@.planning/phases/16-page-redesigns/16-RESEARCH.md
@.planning/phases/15-component-and-design-system/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Redesign KanbanBoard layout with modern grid structure and DragOverlay styling</name>
  <files>src/components/KanbanBoard.tsx, src/styles/KanbanBoard.css</files>
  <action>
    Update KanbanBoard.tsx to use Tailwind grid layout instead of custom CSS:

    1. Replace the existing CSS file reference with Tailwind classes
    2. Update the main render to use: `grid grid-cols-5 gap-4 p-4 bg-background` for the board container
    3. Ensure columns use consistent spacing with `flex-1` and proper padding
    4. Update DragOverlay to render activeTask with `opacity-50` for ghost effect
    5. Use Tailwind `transition-all duration-200` on all interactive elements

    Per locked decisions:
    - Grid: 5 columns (one per status type)
    - Spacing: gap-4 (16px) between columns, p-4 container padding (16px)
    - Shadows: Tailwind `shadow-sm` for base, `shadow-md` for hover
    - Duration: 200ms transitions for snappy responsiveness (locked decision)

    Delete or minimize src/styles/KanbanBoard.css — move all styling to Tailwind utilities. Keep only if special cases exist (terminal styling only).

    Reference: Phase 15 design system CSS variables and spacing scale are available in tailwind.config.ts.
  </action>
  <verify>
    1. `npm run dev` starts without errors
    2. Kanban board displays 5 columns in a grid layout
    3. Columns have consistent spacing (16px gaps)
    4. Container has subtle background (not white/pure black)
    5. Drag overlay renders semi-transparent card when dragging
  </verify>
  <done>
    KanbanBoard.tsx uses Tailwind grid layout with proper spacing and shadow utilities; DragOverlay renders ghost cards with 50% opacity; custom CSS eliminated or minimal.
  </done>
</task>

<task type="auto">
  <name>Implement status dots with semantic colors and pulse animation for in-progress tasks</name>
  <files>src/components/TaskCard.tsx</files>
  <action>
    Add status dot visualization to TaskCard component:

    1. Create a status dot renderer that shows a small (5-7px) colored circle based on task.status:
       - Backlog/Ready: Gray (muted color)
       - InProgress: Blue (accent/warning color), WITH pulse animation
       - Review: Yellow (warning color)
       - Done: Green (success color)

    2. Use Tailwind utilities for dot styling:
       - `inline-block h-2 w-2 rounded-full` for base size/shape
       - Semantic color classes: `bg-success`, `bg-warning`, `bg-accent`, `bg-muted`
       - Pulse animation: `animate-pulse` for InProgress status only

    3. Position dot inline with task title (left side before text)

    4. Conditional animation based on status:
       ```
       {status === 'InProgress' && 'animate-pulse'}
       ```

    Per locked decisions:
    - Colors: Semantic tokens from CSS variables (success=green, warning=yellow, accent=blue, muted=gray)
    - Dot size: 5-7px (h-2 w-2 in Tailwind = 8px, acceptable modern standard)
    - Animation: Gentle pulse using Tailwind `animate-pulse` (2s ease-in-out cycle)
    - No animations on static dots (idle/done states)

    Reference RESEARCH.md examples for status dot patterns.
  </action>
  <verify>
    1. Backlog tasks show gray dot
    2. InProgress tasks show blue dot with pulse animation
    3. Review tasks show yellow dot
    4. Done tasks show green dot
    5. Dots appear inline with task titles
    6. No animation on non-InProgress tasks
  </verify>
  <done>
    TaskCard displays semantic colored status dots; InProgress status pulses via `animate-pulse`; other statuses show static dots with correct colors.
  </done>
</task>

<task type="auto">
  <name>Add hover effects (shadow lift + border color shift) and drag feedback styling to task cards</name>
  <files>src/components/TaskCard.tsx, src/components/KanbanColumn.tsx</files>
  <action>
    Implement hover and drag feedback patterns per locked decisions:

    **Hover Effects (TaskCard):**
    1. Base card styling:
       - `rounded-lg border border-border bg-card shadow-sm p-3` (modern subtle depth)
       - 8px rounded corners (rounded-lg = 8px per Phase 15)

    2. Hover state:
       - Add `hover:shadow-md` (shadow increases from sm to md)
       - Add `hover:border-ring` (border changes from border color to ring/accent color)
       - Add `transition-all duration-200` for 200ms snappy transition (locked decision)
       - Add `cursor-pointer` to indicate interactivity

    3. Apply to entire card container

    **Drop Zone Highlighting (KanbanColumn):**
    1. Use `useDroppable` hook's `isOver` state (already in place)
    2. Apply conditional Tailwind classes:
       - Base: `rounded-lg border-2 border-transparent transition-all duration-150`
       - When isOver: `border-success bg-success/5` (colored border + light tint)
    3. Only highlight the drop zone, not individual cards while dragging

    Per locked decisions:
    - Hover: Subtle lift via shadow increase + border brightness shift (no scale transform)
    - Transitions: 150-200ms snappy timing
    - Drop feedback: Colored border (semantic success/warning color) + 5% opacity background tint

    Reference RESEARCH.md examples: "Hover Effects (Subtle Lift)" and "Drop Zone Styling (dnd-kit)"
  </action>
  <verify>
    1. Cards have subtle shadow at rest (shadow-sm visible)
    2. Hovering over card increases shadow (shadow-md visible)
    3. Card border changes color on hover (becomes accent/ring color)
    4. Transition is smooth (150-200ms)
    5. Dragging over drop zone highlights it with colored border and light background
    6. Drag ghost overlay shows semi-transparent card
  </verify>
  <done>
    TaskCard hover effects render smoothly with shadow lift and border shift; KanbanColumn drop zones highlight on drag-over with colored border and tinted background; all transitions use 200ms timing.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Start dev server: `pnpm dev`
2. Navigate to project with tasks
3. Kanban board should display:
   - 5 columns in grid layout with clear spacing
   - Task cards with status dots (colored, semantic)
   - InProgress tasks with pulsing status dots
   - Hover effects on cards (shadow + border shift)
4. Test drag-drop:
   - Drag task over a column
   - Drop zone should highlight with colored border and background tint
   - Dragged card shows as semi-transparent ghost
5. Test dark mode:
   - All colors should be visible in both light and dark themes (CSS variables from Phase 15)
   - No hardcoded colors visible
</verification>

<success_criteria>
- Kanban board uses modern grid layout with Tailwind utilities (no custom CSS except special cases)
- All 5 columns display properly with 16px spacing
- Task cards show semantic colored status dots (green/blue/yellow/gray)
- InProgress status dots animate with pulse effect
- Cards have subtle shadows and borders at rest
- Hover effects trigger shadow increase and border color shift (200ms transition)
- Drop zones highlight on drag-over with colored border and background tint
- Drag ghost renders semi-transparent
- All styling uses Phase 15 semantic color tokens and design system
</success_criteria>

<output>
After completion, create `.planning/phases/16-page-redesigns/16-01-SUMMARY.md` with:
- Summary of Kanban redesign changes
- Verification results (colors, animations, hover effects)
- Screenshots/description of final layout
- Next steps pointer to 16-02 for header and settings redesigns
</output>
