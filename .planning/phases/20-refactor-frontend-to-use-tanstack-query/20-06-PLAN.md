---
phase: 20-refactor-frontend-to-use-tanstack-query
plan: 06
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/components/project-picker/FilePicker.tsx
  - src/components/task/ImportSettings.tsx
  - src/utils/hooks/useRecentProjects.ts
autonomous: true

must_haves:
  truths:
    - "FilePicker.tsx uses connection service hooks for file browsing (no direct invoke)"
    - "ImportSettings.tsx uses project settings mutation hooks (no direct invoke)"
    - "useRecentProjects.ts uses query/mutation hooks from settings service (no direct invoke)"
    - "All components migrated from direct invoke() calls to TanStack Query hooks"
    - "Component loading/error states managed by hooks, not useState"
    - "No direct invoke() calls remain in any of the 3 files"
  artifacts:
    - "/home/m306213/workspace/gsd-demo/src/components/project-picker/FilePicker.tsx (refactored)"
    - "/home/m306213/workspace/gsd-demo/src/components/task/ImportSettings.tsx (refactored)"
    - "/home/m306213/workspace/gsd-demo/src/utils/hooks/useRecentProjects.ts (refactored)"
  key_links:
    - "FilePicker likely uses SSH connection hooks from connection.service.ts (Plan 20-03)"
    - "ImportSettings uses project settings mutations from project.service.ts (Plan 20-01)"
    - "useRecentProjects may be renamed/refactored to use settings hooks (Plan 20-02)"
---

<objective>
Migrate final 3 components/hooks (FilePicker, ImportSettings, useRecentProjects) from direct invoke() calls to TanStack Query hooks. These handle project setup, configuration import, and recent project tracking.

Purpose: Complete Wave 2 component migrations. This plan finalizes all 9 component migrations from research phase (3 in Plan 20-04 + 3 in Plan 20-05 + 3 in Plan 20-06 = 9 total). After Wave 2, all direct invoke() calls should be removed from components.

Output: Fully refactored components using TanStack Query hooks throughout the application.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-RESEARCH.md

# Wave 1 outputs (services with hooks)
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-01-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-02-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-03-SUMMARY.md

# Wave 2 prior plans
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-04-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-05-SUMMARY.md

# Service layer with hooks
@/home/m306213/workspace/gsd-demo/src/services/project.service.ts
@/home/m306213/workspace/gsd-demo/src/services/connection.service.ts
@/home/m306213/workspace/gsd-demo/src/services/settings.service.ts

# Components/hooks to migrate
@/home/m306213/workspace/gsd-demo/src/components/project-picker/FilePicker.tsx
@/home/m306213/workspace/gsd-demo/src/components/task/ImportSettings.tsx
@/home/m306213/workspace/gsd-demo/src/utils/hooks/useRecentProjects.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate FilePicker.tsx to use connection service hooks</name>
  <files>/home/m306213/workspace/gsd-demo/src/components/project-picker/FilePicker.tsx</files>
  <action>
Replace direct invoke() calls in FilePicker.tsx (file/directory picker for SSH):

1. Find all invoke() calls for file browsing operations (likely SSH file listing, directory traversal)
2. Replace with connection service hooks from Plan 20-03 (useSshConnectionsQuery, file browsing mutations)
3. Import appropriate hooks from @/services/connection.service
4. Update component to fetch SSH connections: const { data: connections = [] } = useSshConnectionsQuery();
5. For file browsing operations, use appropriate mutation hooks (if they exist in connection.service)
6. Update loading states: show spinner while fetching files
7. Update error states: show toast with error message via Sonner
8. Remove any useState for file list, loading, error
9. Preserve existing file picker UI; only replace data/mutation calls

Likely mutations needed: Browse directory, List files, Select file/directory.

Verify all direct invoke() calls removed from FilePicker.tsx.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in FilePicker.tsx
Verify no direct invoke: grep -c "invoke<" src/components/project-picker/FilePicker.tsx (should be 0)
Verify hooks imported: grep "from @/services" src/components/project-picker/FilePicker.tsx (should show connection service imports)
  </verify>
  <done>
FilePicker.tsx migrated to use connection service hooks. No direct invoke(). File browsing state managed by hooks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ImportSettings.tsx to use project settings mutation hooks</name>
  <files>/home/m306213/workspace/gsd-demo/src/components/task/ImportSettings.tsx</files>
  <action>
Replace direct invoke() calls in ImportSettings.tsx (import task configuration):

1. Find all invoke() calls for import operations (likely saveImportConfig or similar)
2. Replace with: const { mutate: saveConfig, isPending } = useSaveImportConfigMutation();
3. Import useSaveImportConfigMutation from @/services/project.service (created in Plan 20-01)
4. Update form submission to call mutate() with form data
5. Handle mutation states: isPending (disable submit), error (show toast)
6. On success: show toast.success("Configuration saved") and potentially close modal
7. Remove any useState for pending/error
8. Remove all direct invoke() calls

Example flow:
```typescript
const { mutate: saveConfig, isPending } = useSaveImportConfigMutation();
const handleSave = (config) => {
  saveConfig(config, {
    onSuccess: () => { toast.success("Saved"); closeModal(); }
  });
};
```

Verify all direct invoke() calls removed from ImportSettings.tsx.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in ImportSettings.tsx
Verify no direct invoke: grep -c "invoke<" src/components/task/ImportSettings.tsx (should be 0)
Verify mutation hook used: grep "SaveImportConfig" src/components/task/ImportSettings.tsx (should show import + usage)
  </verify>
  <done>
ImportSettings.tsx migrated to use useSaveImportConfigMutation. Form submission uses mutate(). No direct invoke().
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor useRecentProjects.ts to use settings query/mutation hooks</name>
  <files>/home/m306213/workspace/gsd-demo/src/utils/hooks/useRecentProjects.ts</files>
  <action>
Replace direct invoke() calls in useRecentProjects.ts:

This file is a custom hook, not a component. It likely manages recently accessed projects (stored in settings).

1. Find all invoke() calls in the hook (likely for getSettings, saveSettings, etc.)
2. Replace with: const { data: settings } = useSettingsQuery();
3. Replace settings mutations with: const { mutate: saveSettings } = useSaveSettingsMutation();
4. Import both hooks from @/services/settings.service
5. Update hook logic to read settings via query state instead of direct invoke
6. For saving recent projects, use mutation hook instead of invoke
7. Return recentProjects extracted from settings query data
8. Handle loading/error states appropriately
9. Remove any useState, useEffect, or direct invoke() calls

The hook should become a thin wrapper around TanStack Query hooks, similar to useGetConnectionById pattern in research doc (lines 82-88).

Example structure:
```typescript
export function useRecentProjects() {
  const { data: settings } = useSettingsQuery();
  return settings?.recent_projects ?? [];
}
```

Verify all direct invoke() calls removed from useRecentProjects.ts.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in useRecentProjects.ts
Verify no direct invoke: grep -c "invoke<" src/utils/hooks/useRecentProjects.ts (should be 0)
Verify hooks imported: grep "from @/services" src/utils/hooks/useRecentProjects.ts (should show settings service imports)
  </verify>
  <done>
useRecentProjects.ts refactored to use useSettingsQuery. No direct invoke(). Hook now thin wrapper around query hooks.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. npm run build succeeds with no TypeScript errors
2. Verify no direct invoke() in migrated files: grep "invoke<" src/components/project-picker/FilePicker.tsx src/components/task/ImportSettings.tsx src/utils/hooks/useRecentProjects.ts (should be empty)
3. Verify hooks imported: grep -h "from @/services" src/components/project-picker/FilePicker.tsx src/components/task/ImportSettings.tsx src/utils/hooks/useRecentProjects.ts | wc -l (should be ≥3)
4. All 9 components from research phase migrated: 3 (Plan 20-04) + 3 (Plan 20-05) + 3 (Plan 20-06) = 9 total
</verification>

<success_criteria>
- FilePicker.tsx uses connection service hooks (no direct invoke)
- ImportSettings.tsx uses project settings mutation hooks (no direct invoke)
- useRecentProjects.ts uses settings query/mutation hooks (no direct invoke)
- All files compile without TypeScript errors
- All 9 components from research phase successfully migrated
- No component-level useState for data operations (all managed by TanStack Query)
- Wave 2 complete: All component migrations finished
</success_criteria>

<output>
After completion, create `.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-06-SUMMARY.md` with:
- Components migrated (3 total: FilePicker, ImportSettings, useRecentProjects)
- Hooks used in each component/file (with line numbers)
- Build status: "✓ npm run build successful"
- Wave 2 completion: "✓ All 9 components from research phase migrated to TanStack Query"
- Remaining direct invoke(): Expected 0 in components (only remain in services/ipc.ts wrapper)
</output>
