---
phase: 20-refactor-frontend-to-use-tanstack-query
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/App.tsx
  - src/components/common/ApprovalForm.tsx
  - src/components/common/ReviewModal.tsx
autonomous: true

must_haves:
  truths:
    - "src/App.tsx uses useSettingsQuery instead of direct invoke() for loading settings on mount"
    - "ApprovalForm.tsx uses query/mutation hooks for review operations (getDiffForReview via useDiffForReviewQuery)"
    - "ReviewModal.tsx uses mutation hooks for approve/reject operations"
    - "All component-level state for loading/error removed; TanStack Query state used instead"
    - "No direct invoke() calls remain in migrated components for data operations"
    - "Error states properly handled with Sonner toast notifications"
  artifacts:
    - "/home/m306213/workspace/gsd-demo/src/App.tsx (refactored to use hooks)"
    - "/home/m306213/workspace/gsd-demo/src/components/common/ApprovalForm.tsx (refactored to use hooks)"
    - "/home/m306213/workspace/gsd-demo/src/components/common/ReviewModal.tsx (refactored to use hooks)"
  key_links:
    - "App.tsx hooks into useSettingsQuery at app initialization (depends on useSettingsQuery from Plan 20-02)"
    - "Diff-related operations use useDiffForReviewQuery from task.service.ts (Plan 20-01)"
    - "Review operations (approve/reject) delegate to backend mutations"
---

<objective>
Migrate 3 core components (App, ApprovalForm, ReviewModal) from direct invoke() calls to TanStack Query hooks. These components handle app initialization, review workflow, and diff viewing.

Purpose: Begin Wave 2 (component migrations). This plan targets foundation components that other components may depend on. App.tsx is critical path for app startup; review components are used in agent monitoring workflow.

Output: Refactored components using query/mutation hooks with proper loading/error states managed by TanStack Query.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-RESEARCH.md

# Wave 1 outputs
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-01-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-02-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-03-SUMMARY.md

# Service layer with hooks (Wave 1)
@/home/m306213/workspace/gsd-demo/src/services/task.service.ts
@/home/m306213/workspace/gsd-demo/src/services/settings.service.ts

# Components to migrate
@/home/m306213/workspace/gsd-demo/src/App.tsx
@/home/m306213/workspace/gsd-demo/src/components/common/ApprovalForm.tsx
@/home/m306213/workspace/gsd-demo/src/components/common/ReviewModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate src/App.tsx to use useSettingsQuery hook</name>
  <files>/home/m306213/workspace/gsd-demo/src/App.tsx</files>
  <action>
Replace direct invoke() calls in App.tsx with useSettingsQuery hook:

1. Find all direct invoke() calls in App.tsx (likely for settings loading on mount)
2. Replace with: const { data: settings, isLoading, error } = useSettingsQuery();
3. Update loading state: Show loading spinner while isLoading is true
4. Update error state: Show error message if error exists; integrate with Sonner toast
5. Remove any useState/useEffect for manual settings loading
6. Update conditional logic to check settings data vs previous manual state
7. Import useSettingsQuery from @/services/settings.service

Reference pattern from research doc lines 484-520 (component usage of query hooks).

Verify all direct invoke() calls removed from App.tsx.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in App.tsx
Verify no direct invoke: grep -c "invoke<" src/App.tsx (should be 0)
Verify hook imported: grep "useSettingsQuery" src/App.tsx (should show import + usage)
  </verify>
  <done>
App.tsx migrated to useSettingsQuery. No direct invoke() calls remain. Loading/error states managed by hook.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ApprovalForm.tsx to use query hooks for review operations</name>
  <files>/home/m306213/workspace/gsd-demo/src/components/common/ApprovalForm.tsx</files>
  <action>
Replace direct invoke() calls in ApprovalForm.tsx:

1. Find all invoke() calls for review/diff operations (likely getDiffForReview or similar)
2. Replace with: const { data: diff, isLoading } = useDiffForReviewQuery(taskId);
3. Import useDiffForReviewQuery from @/services/task.service
4. Update component to use diff data directly from query state
5. Remove any useState for diff/loading state
6. Handle isLoading state: show placeholder while diff loads
7. Handle error state: show toast with error message via Sonner

Reference useDiffForReviewQuery hook from Plan 20-01 (task.service.ts).

Verify all direct invoke() calls removed from ApprovalForm.tsx.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in ApprovalForm.tsx
Verify no direct invoke: grep -c "invoke<" src/components/common/ApprovalForm.tsx (should be 0)
Verify hook imported: grep "useDiffForReviewQuery" src/components/common/ApprovalForm.tsx (should show import + usage)
  </verify>
  <done>
ApprovalForm.tsx migrated to useDiffForReviewQuery. Query state used instead of manual loading.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate ReviewModal.tsx to use mutation hooks for approve/reject</name>
  <files>/home/m306213/workspace/gsd-demo/src/components/common/ReviewModal.tsx</files>
  <action>
Replace direct invoke() calls in ReviewModal.tsx:

1. Find all invoke() calls for review mutations (approve/reject, etc.)
2. Replace with appropriate mutation hooks from task.service.ts (or review-related mutations)
3. Common mutations: task approval/rejection likely have backend endpoints; check what exists
4. If no specific review mutations exist, use generic task update mutations
5. Import mutation hooks from @/services/task.service
6. Update component to call mutate() instead of invoke()
7. Handle mutation states: isPending, error via onError callback
8. Show success toast on mutation success
9. Remove any useState for pending/error state

Example pattern: const { mutate: approveTask, isPending } = useApproveTaskMutation();

Verify all direct invoke() calls removed from ReviewModal.tsx.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in ReviewModal.tsx
Verify no direct invoke: grep -c "invoke<" src/components/common/ReviewModal.tsx (should be 0)
Verify hooks imported: grep -E "use[A-Z].*Mutation.*from.*service" src/components/common/ReviewModal.tsx (should show imports)
  </verify>
  <done>
ReviewModal.tsx migrated to use mutation hooks. No direct invoke() calls remain. Mutation state managed by hooks.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. npm run build succeeds with no TypeScript errors
2. Verify no direct invoke() in migrated files: grep "invoke<" src/App.tsx src/components/common/{ApprovalForm,ReviewModal}.tsx (should be empty)
3. Verify hooks imported: grep -h "from @/services" src/App.tsx src/components/common/{ApprovalForm,ReviewModal}.tsx | wc -l (should be ≥4)
4. Component functionality preserved: App initializes with settings, ApprovalForm shows diffs, ReviewModal handles approvals
</verification>

<success_criteria>
- src/App.tsx uses useSettingsQuery for settings loading (no direct invoke)
- ApprovalForm.tsx uses useDiffForReviewQuery for diff fetching (no direct invoke)
- ReviewModal.tsx uses mutation hooks for approve/reject (no direct invoke)
- All files compile without TypeScript errors
- Loading/error states managed by TanStack Query instead of component useState
- All components properly import hooks from service layer
- Sonner integration for error/success feedback in mutations
</success_criteria>

<output>
After completion, create `.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-04-SUMMARY.md` with:
- Components migrated (3 total)
- Direct invoke() calls removed from each component
- Hooks imported and used in each component
- Build status: "✓ npm run build successful"
- Component functionality verified: "✓ All 3 components use TanStack Query hooks"
</output>
