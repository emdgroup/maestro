---
phase: 14-ui-foundation
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - src/components/ProjectSettingsModal.tsx
autonomous: false

must_haves:
  truths:
    - "ProjectSettingsModal displays theme selector with three options: Light, Dark, System"
    - "Users can toggle theme from the selector and see immediate visual change"
    - "Theme preference is persisted to database and restored on app restart"
    - "Selector uses dropdown/select control type with text labels only (no icons)"
    - "No toast notification displays on theme change"
  artifacts:
    - path: "src/components/ProjectSettingsModal.tsx"
      provides: "Settings form with theme selector integrated"
      contains: "theme_preference field in form, Select/dropdown control, onChange handler"
  key_links:
    - from: "src/components/ProjectSettingsModal.tsx"
      to: "src/providers/ThemeProvider.tsx"
      via: "useTheme().setTheme() updates theme on select change"
      pattern: "useTheme|setTheme"
    - from: "src/components/ProjectSettingsModal.tsx"
      to: "src/types/bindings.ts"
      via: "AppSettings type used in form, theme_preference field"
      pattern: "AppSettings|theme_preference"

---

<objective>
Integrate theme selector into ProjectSettingsModal. Allow users to toggle theme with instant persistence.

Purpose: User-facing theme control via settings page

Output: Theme toggle functionality complete and testable
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/14-ui-foundation/14-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/components/ProjectSettingsModal.tsx
@src/providers/ThemeProvider.tsx
@src/types/bindings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add theme_preference field to ProjectSettingsModal form</name>
  <files>src/components/ProjectSettingsModal.tsx</files>
  <action>
    Extend ProjectSettingsModal form:
    1. Import useTheme hook: `import { useTheme } from '../providers/ThemeProvider'`
    2. In component state/form, add theme_preference field to form data (use react-hook-form if not already)
    3. In form JSX, add new section for Appearance:
       ```tsx
       <fieldset>
         <legend>Appearance</legend>
         <label>Theme</label>
         <select {...register("theme_preference")}>
           <option value="light">Light</option>
           <option value="dark">Dark</option>
           <option value="system">System</option>
         </select>
       </fieldset>
       ```
    4. Position after other settings (model, MCP, skills) for logical grouping
    5. Load initial value from settings: Set default value to current theme_preference when modal opens
    6. Keep all existing form fields and logic intact (model_default, mcp_allowlist, skills_default)
  </action>
  <verify>
    pnpm build 2>&1 | tail -20 | grep -E "error|warning" || echo "✓ Build OK"
  </verify>
  <done>
    ProjectSettingsModal form includes theme_preference select element, builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement theme change handler in ProjectSettingsModal</name>
  <files>src/components/ProjectSettingsModal.tsx</files>
  <action>
    Add onChange handler for theme selector:
    1. Get useTheme() hook inside component: `const { setTheme } = useTheme()`
    2. On select change event, call: `await setTheme(newValue as 'light' | 'dark' | 'system')`
    3. setTheme updates ThemeProvider state and persists to database via invoke('save_settings')
    4. DOM class (dark) updates immediately, visual change is instant (no need for manual update)
    5. Do NOT add toast notification on change (phase decision: silent update)
    6. Handle errors gracefully: log to console if setTheme fails, don't crash modal
  </action>
  <verify>
    pnpm build 2>&1 | grep -E "error\[" || echo "✓ Build OK"
  </verify>
  <done>
    ProjectSettingsModal includes onChange handler calling useTheme().setTheme(), builds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Theme selector integrated into ProjectSettingsModal with instant persistence and visual updates.
  </what-built>
  <how-to-verify>
    1. Start app with `pnpm tauri:dev`
    2. Open ProjectSettingsModal (via settings button)
    3. Locate "Appearance" / "Theme" section with selector showing "Light", "Dark", "System" options
    4. Select "Dark" and observe:
       - DOM class changes instantly (check DevTools html element)
       - Colors update immediately (background/text colors invert)
       - No unstyled flash
    5. Select "Light" and verify colors revert
    6. Select "System" and verify theme matches OS preference
    7. Close and reopen modal, verify selector shows last selected value
    8. Restart app (`pnpm tauri:dev` again) and verify theme persists
    9. Check console for any theme-related errors
    10. Verify no toast notification appears on theme change
  </how-to-verify>
  <resume-signal>
    Type "approved" if all visual tests pass (theme toggle works, persists across restart, no flash/toast).
    Type "issues" and describe any problems.
  </resume-signal>
</task>

</tasks>

<verification>
1. Visual test: Theme selector renders in settings modal
2. Functional test: Selecting theme updates DOM class instantly
3. Visual test: Dark mode CSS variables apply correctly (colors invert)
4. Persistence test: Close modal, reopen, verify selector shows same value
5. Restart test: Kill app, restart, verify theme persists
6. Error handling: Check console for any errors during theme switch
7. Integration test: Theme switch works with existing UI components (no broken styles)
8. Browser DevTools: Verify html element has/removes 'dark' class appropriately
</verification>

<success_criteria>
- ProjectSettingsModal includes theme selector (Select dropdown with text labels)
- Selector shows "Light", "Dark", "System" options
- Selecting theme calls useTheme().setTheme() with correct value
- Theme change updates DOM class instantly (visual update is immediate)
- Theme preference persists across app restarts
- No toast notification on theme change
- Console has no errors related to theme operations
- Settings modal remains fully functional (other fields unaffected)
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-foundation/14-04-SUMMARY.md` documenting:
- Theme selector implementation in ProjectSettingsModal
- onChange handler implementation details
- Persistence verification results
- Any edge cases discovered (system theme detection, etc.)
- User verification results from checkpoint
</output>
