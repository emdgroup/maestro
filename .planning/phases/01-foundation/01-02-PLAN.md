---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.ts, src/App.tsx, src/main.tsx, src/index.css, tauri.conf.json]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "React app renders in browser when running `npm run dev`"
    - "Tauri window opens with React app inside"
    - "Tauri IPC connection is established (no connection errors in console)"
    - "App can import @tauri-apps/api/core without errors"
  artifacts:
    - path: "package.json"
      provides: "Frontend dependencies (React, Vite, Tauri API)"
      contains: "@tauri-apps/api"
    - path: "vite.config.ts"
      provides: "Vite build configuration for Tauri"
      exports: ["defineConfig"]
    - path: "src/App.tsx"
      provides: "Root React component"
      min_lines: 20
    - path: "src/main.tsx"
      provides: "React entry point with Tauri setup"
      contains: "createRoot"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React.createRoot"
      pattern: "createRoot.*App"
    - from: "src/App.tsx"
      to: "@tauri-apps/api/core"
      via: "import { invoke }"
      pattern: "import.*tauri"
---

<objective>
Set up Tauri 2 + React 18 frontend shell with Vite build tool and working IPC connection to Rust backend.

Purpose: Foundation for all UI components in subsequent phases
Output: Working React app inside Tauri window with established IPC connection
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

Key decisions from CONTEXT:
- React 18 + Vite (user preference for React, Vite is Tauri-endorsed)
- Tauri for IPC and window management (not hand-rolling Node.js sidecar in Phase 1)
- Single Tauri window focused on main task orchestration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize React frontend with Vite and install Tauri API dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json</files>
  <action>
1. Initialize frontend from Tauri scaffolding (or create if not exists):
   `npm init -y`

2. Install core dependencies:
   `npm install react react-dom`
   `npm install --save-dev vite @vitejs/plugin-react typescript @types/react @types/react-dom`
   `npm install @tauri-apps/api @tauri-apps/plugin-shell`

3. Create or verify vite.config.ts:
   - Import defineConfig from 'vite' and react from '@vitejs/plugin-react'
   - Configure:
     * plugins: [react()]
     * server: { port: 5173 }
     * build: { target: 'ES2020', outDir: '../src-tauri/gen/web' }
   - This output matches Tauri's expected build artifact location

4. Create or verify tsconfig.json:
   - target: ES2020
   - jsx: react-jsx
   - moduleResolution: bundler
   - allowSyntheticDefaultImports: true
   - strict: true

5. Update package.json scripts:
   - `"dev": "vite"`
   - `"build": "vite build"`
   - `"preview": "vite preview"`

6. Create .gitignore in root:
   - node_modules/
   - dist/
   - .vite/
  </action>
  <verify>
- `npm install` completes without errors
- `ls node_modules/@tauri-apps/api` confirms Tauri API package installed
- `cat vite.config.ts | grep "@vitejs/plugin-react"` confirms plugin configured
- `npm run build` completes without errors
  </verify>
  <done>Frontend build pipeline configured with Vite and Tauri API available</done>
</task>

<task type="auto">
  <name>Task 2: Create React app entry point and root component</name>
  <files>src/main.tsx, src/App.tsx, src/index.css, index.html</files>
  <action>
1. Create src/ directory: `mkdir -p src`

2. Create index.html in root:
   ```html
   <!doctype html>
   <html lang="en">
   <head>
     <meta charset="UTF-8" />
     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     <title>GSD Agent Orchestrator</title>
   </head>
   <body>
     <div id="root"></div>
     <script type="module" src="/src/main.tsx"></script>
   </body>
   </html>
   ```

3. Create src/index.css with minimal reset and app styles:
   - CSS reset for body, html
   - Basic font sizing
   - Color variables (will expand in later phases)
   - Example: --bg-primary, --text-primary, --border-color

4. Create src/main.tsx:
   - Import React, ReactDOM, App, index.css
   - Call `ReactDOM.createRoot(document.getElementById("root")!).render(<App />)`
   - Add basic error boundary or error handling

5. Create src/App.tsx:
   - Minimal functional component
   - Import { invoke } from '@tauri-apps/api/core'
   - Add useEffect to test IPC connection:
     * Call `invoke("get_projects")` on mount
     * Log result or error to console (won't fail if backend not ready yet)
   - Render: `<div className="app"><h1>GSD Agent Orchestrator</h1><p>Status: Connected</p></div>`
   - Add basic className for styling
  </action>
  <verify>
- `cat src/main.tsx | grep "createRoot"` confirms entry point setup
- `cat src/App.tsx | grep "invoke"` confirms IPC import present
- `npm run build` produces output in dist/ directory
- `ls dist/` shows index.html, assets/ directory
  </verify>
  <done>React app entry point created with IPC test ready</done>
</task>

<task type="auto">
  <name>Task 3: Configure Tauri to serve React frontend and test IPC connection</name>
  <files>tauri.conf.json, src-tauri/src/main.rs</files>
  <action>
1. Verify/update tauri.conf.json (should exist from Tauri init):
   - Set build.frontendDist to "../dist" (Vite output location)
   - Set build.devUrl to "http://localhost:5173" (Vite dev server)
   - Window config:
     * label: "main"
     * title: "GSD Agent Orchestrator"
     * width: 1200, height: 800
     * resizable: true
   - Security: scope all Tauri APIs needed for Phase 1

2. Update src-tauri/src/main.rs to add a test IPC command:
   - Add `use crate::models::Project;` (will define in Plan 03)
   - Create stub command `#[tauri::command] fn get_projects() -> Result<Vec<Project>, String>`
   - For now, return empty Vec or mock data
   - Add to invoke_handler: `.invoke_handler(tauri::generate_handler![get_projects])`

3. Ensure main.rs has setup() hook from Plan 01 complete

4. No async needed for this stub (will add async process spawning in Phase 4)
  </action>
  <verify>
- `grep -q "frontendDist" tauri.conf.json` confirms Vite output path configured
- `grep -q "devUrl" tauri.conf.json` confirms dev server URL
- `grep -q "get_projects" src-tauri/src/main.rs` confirms command registered
- `cargo build -p maestro` from src-tauri/ completes successfully
  </verify>
  <done>Tauri configured to serve React app with stub IPC command</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run dev` starts Vite dev server on localhost:5173
2. In separate terminal: `cargo tauri dev` from root launches Tauri window
3. React app renders in window (displays "GSD Agent Orchestrator" title)
4. Browser console (F12) shows no IPC connection errors
5. Test IPC: Click a button or add event that calls `invoke("get_projects")` and verify response appears in console
6. Tauri window survives reload (verify state persistence ready)
</verification>

<success_criteria>
- React app renders inside Tauri window without crashes
- Vite dev server works for hot reloading during development
- IPC connection established (no console errors from invoke calls)
- Basic styling renders (CSS loaded from index.css)
- Next plan can begin adding React components and IPC handlers
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- What was built (React + Tauri shell)
- Frontend build pipeline (Vite configuration)
- IPC connection test results
- Tauri window configuration
- Development workflow (npm run dev + cargo tauri dev)
- Key files created/modified with file sizes
</output>
