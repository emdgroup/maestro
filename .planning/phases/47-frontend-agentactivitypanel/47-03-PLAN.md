---
phase: 47-frontend-agentactivitypanel
plan: 03
type: execute
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ACTIVITY-02]
gap_closure: true

must_haves:
  truths:
    - "ACTIVITY-02 in REQUIREMENTS.md is marked [x] (complete)"
    - "ACTIVITY-02 description in REQUIREMENTS.md matches the toggle-panel design from CONTEXT.md"
    - "ACTIVITY-02 row in the Traceability table shows Complete"
    - "ROADMAP.md Phase 47 Success Criteria #2 describes the toggleable AcpTerminalPanel design, not a split pane with TerminalComponent"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement status for ACTIVITY-02"
      contains: "[x] **ACTIVITY-02**"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate Phase 47 success criteria"
  key_links:
    - from: "CONTEXT.md (toggle-panel design decision)"
      to: "REQUIREMENTS.md ACTIVITY-02 description"
      via: "documentation alignment"
    - from: "REQUIREMENTS.md ACTIVITY-02 status"
      to: "Traceability table row"
      via: "consistent status field"
---

<objective>
Resolve the ACTIVITY-02 documentation drift gap identified in 47-VERIFICATION.md.

The implementation delivered a correct toggleable bottom panel (AcpTerminalPanel) as locked in CONTEXT.md before planning. The ROADMAP.md Success Criteria #2 and REQUIREMENTS.md ACTIVITY-02 were never updated to reflect this design change. Both documents still describe the pre-discussion "split pane using existing TerminalComponent" design, which was rejected because TerminalComponent requires a PTY session entry that ACP sessions do not have.

Purpose: Bring REQUIREMENTS.md into alignment with the implemented and verified design, and mark ACTIVITY-02 as complete. ROADMAP.md SC#2 was already updated by a prior commit (1fd40d0) and needs only verification.
Output: Updated REQUIREMENTS.md (ACTIVITY-02 marked complete, description updated). ROADMAP.md confirmed correct.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-frontend-agentactivitypanel/47-CONTEXT.md
@.planning/phases/47-frontend-agentactivitypanel/47-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md — mark ACTIVITY-02 complete and fix description</name>
  <read_first>
    .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make exactly three edits to .planning/REQUIREMENTS.md:

**Edit 1 — Fix the requirement checkbox and description (line 60).**

Current text:
```
- [ ] **ACTIVITY-02**: User sees raw terminal output alongside structured output in a split pane using existing TerminalComponent
```

Replace with:
```
- [x] **ACTIVITY-02**: User can toggle a terminal bottom panel to see raw ACP terminal output alongside the structured activity view (AcpTerminalPanel, slide-in from bottom)
```

**Edit 2 — Fix the traceability table row for ACTIVITY-02 (line 128).**

Current text:
```
| ACTIVITY-02 | Phase 47 | Pending |
```

Replace with:
```
| ACTIVITY-02 | Phase 47 | Complete |
```

**Edit 3 — Update the "Last updated" footer line at the bottom of the file.**

Current text:
```
*Last updated: 2026-04-17 after milestone v1.5 initialization*
```

Replace with:
```
*Last updated: 2026-04-22 — ACTIVITY-02 marked complete; description updated to reflect toggle-panel design (AcpTerminalPanel)*
```

No other lines in the file should be modified.
  </action>
  <verify>
    <automated>grep -n "ACTIVITY-02" /home/m306213/workspace/maestro/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\[x\] \*\*ACTIVITY-02\*\*" .planning/REQUIREMENTS.md` returns a match
    - `grep "AcpTerminalPanel" .planning/REQUIREMENTS.md` returns a match
    - `grep "split pane" .planning/REQUIREMENTS.md` returns no match
    - `grep "TerminalComponent" .planning/REQUIREMENTS.md` returns no match (within the ACTIVITY-02 line)
    - `grep "| ACTIVITY-02 | Phase 47 | Complete |" .planning/REQUIREMENTS.md` returns a match
    - `grep "| ACTIVITY-02 | Phase 47 | Pending |" .planning/REQUIREMENTS.md` returns no match
  </acceptance_criteria>
  <done>REQUIREMENTS.md ACTIVITY-02 is marked [x], description reflects toggle-panel design, traceability row shows Complete.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm ROADMAP.md Phase 47 SC#2 is already correct (no edit needed)</name>
  <read_first>
    .planning/ROADMAP.md
  </read_first>
  <files></files>
  <action>
ROADMAP.md was already updated by commit 1fd40d0 ("docs(47-02): complete AgentActivityPanel plan"). No edit is needed.

Read .planning/ROADMAP.md and verify Phase 47 Success Criteria #2 already contains the correct toggle-panel text. Record confirmation in task done note. Do NOT modify the file.
  </action>
  <verify>
    <automated>grep -n "AcpTerminalPanel\|toggleable bottom panel" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AcpTerminalPanel" .planning/ROADMAP.md` returns a match
    - `grep "toggleable bottom panel" .planning/ROADMAP.md` returns a match
    - `grep "split pane using the existing TerminalComponent" .planning/ROADMAP.md` returns no match
  </acceptance_criteria>
  <done>ROADMAP.md Phase 47 SC#2 confirmed to already contain the toggle-panel design. No edit needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Documentation-only edits. No code execution, no external input, no trust boundary crossings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03-01 | Tampering | .planning/REQUIREMENTS.md | accept | Plain text file under git version control; any unintended change is immediately visible in `git diff` |
| T-47-03-02 | Tampering | .planning/ROADMAP.md | accept | Plain text file under git version control; any unintended change is immediately visible in `git diff` |
</threat_model>

<verification>
After both tasks complete, verify the full state:

```bash
# Confirm ACTIVITY-02 is checked and uses new description
grep "ACTIVITY-02" .planning/REQUIREMENTS.md

# Confirm no remnant of old split-pane language in either file
grep -n "split pane" .planning/REQUIREMENTS.md .planning/ROADMAP.md

# Confirm ROADMAP SC#2 uses toggle-panel language (already correct)
grep -n "AcpTerminalPanel\|toggleable bottom panel" .planning/ROADMAP.md

# Confirm traceability table shows Complete
grep "ACTIVITY-02.*Complete" .planning/REQUIREMENTS.md
```

All commands must return results consistent with the acceptance criteria above.
</verification>

<success_criteria>
- REQUIREMENTS.md ACTIVITY-02 is marked `[x]`, description reads "User can toggle a terminal bottom panel to see raw ACP terminal output alongside the structured activity view (AcpTerminalPanel, slide-in from bottom)"
- REQUIREMENTS.md Traceability table shows `| ACTIVITY-02 | Phase 47 | Complete |`
- ROADMAP.md Phase 47 SC#2 already contains "toggleable bottom panel (AcpTerminalPanel)" — confirmed, no edit needed
- `grep "split pane" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns no matches
</success_criteria>

<output>
After completion, create `.planning/phases/47-frontend-agentactivitypanel/47-03-SUMMARY.md`
</output>
