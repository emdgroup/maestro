---
phase: 18-maestro-folder-architecture
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - src/App.tsx
  - CLAUDE.md
  - README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Application window title displays 'Maestro' instead of 'GSD Agent Orchestrator'"
    - "Tauri product name is 'Maestro'"
    - "Bundle identifier is 'com.maestro.app'"
    - "Cargo.toml description includes 'Maestro'"
    - "All user-facing UI strings reference 'Maestro'"
    - "Technical identifiers (gsd_demo, .planning/, etc) remain unchanged"
    - "Documentation updated with new branding"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Updated productName, identifier, and window title"
      contains: "Maestro"
    - path: "src-tauri/Cargo.toml"
      provides: "Updated package description"
      contains: "Maestro"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src/App.tsx"
      via: "UI title consistency"
      pattern: "window.title|productName"
---

<objective>
Systematically rebrand application from "GSD Orchestrator" to "Maestro" across configuration files, UI strings, and documentation, consolidating project identity after v1.1 completion.

Purpose: Completes Phase 18 rebranding requirement. Creates consistent user-facing identity as "Maestro" while maintaining technical identifiers for backwards compatibility.

Output: Rebranded configuration files, UI strings, and updated documentation
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-maestro-folder-architecture/18-RESEARCH.md
@src-tauri/tauri.conf.json
@src-tauri/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Update Tauri configuration with new branding</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>
    1. Open src-tauri/tauri.conf.json

    2. Update productName:
       OLD: "productName": "GSD Agent Orchestrator"
       NEW: "productName": "Maestro"

    3. Update identifier:
       OLD: "identifier": "com.gsd.orchestrator"
       NEW: "identifier": "com.maestro.app"

    4. Update window title:
       OLD: "title": "GSD Agent Orchestrator"
       NEW: "title": "Maestro"
       (inside app.windows[0])

    5. Verify version remains 0.1.0 (or appropriate version)

    6. Do NOT change any other configuration (build commands, paths, etc. remain unchanged)

    7. File should look like:
       ```json
       {
         "$schema": "https://schema.tauri.app/config/2",
         "productName": "Maestro",
         "version": "0.1.0",
         "identifier": "com.maestro.app",
         "build": { ... },
         "app": {
           "windows": [
             {
               "title": "Maestro",
               ...
             }
           ]
         }
       }
       ```

    8. Validate JSON syntax: `jq . src-tauri/tauri.conf.json > /dev/null` (or similar JSON validator)

    Reference: 18-RESEARCH.md Pattern 4 (Application Rebranding), Example 3
  </action>
  <verify>
    1. productName is "Maestro" (not "GSD Agent Orchestrator")
    2. identifier is "com.maestro.app" (not "com.gsd.orchestrator")
    3. window title is "Maestro" (not "GSD Agent Orchestrator")
    4. JSON is valid (no syntax errors)
    5. All other config unchanged
  </verify>
  <done>
    Tauri configuration rebranded. Application window will display "Maestro".
  </done>
</task>

<task type="auto">
  <name>Update Cargo.toml description and UI strings</name>
  <files>src-tauri/Cargo.toml</files>
  <action>
    1. Open src-tauri/Cargo.toml

    2. Find the [package] section

    3. Update description field:
       OLD: description = "AI Agent Orchestrator" (or similar)
       NEW: description = "Maestro: AI Agent Orchestration for Autonomous Coding"

    4. Keep package name as "gsd_demo" (technical identifier, not user-facing)

    5. Keep version field as-is

    6. Verify Cargo.toml syntax: `cargo check -p gsd_demo`

    Reference: 18-RESEARCH.md Pattern 4 - Keep technical names for backwards compatibility
  </action>
  <verify>
    1. description updated to reference Maestro
    2. package name remains "gsd_demo" (unchanged)
    3. Cargo.toml syntax valid (cargo check succeeds)
  </verify>
  <done>
    Cargo.toml metadata updated. Release builds will reflect new branding.
  </done>
</task>

<task type="auto">
  <name>Update documentation files with new branding</name>
  <files>CLAUDE.md, README.md</files>
  <action>
    1. Open CLAUDE.md

    2. Update Project Overview section:
       OLD: "**GSD Agent Orchestrator** - A Tauri desktop app..."
       NEW: "**Maestro** - A Tauri desktop app for orchestrating autonomous AI coding agents..."

    3. Keep .planning/ and gsd-demo references unchanged (these are technical identifiers)

    4. Update any "GSD Orchestrator" references to "Maestro"

    5. Open README.md

    6. Update title/headers:
       OLD: "# GSD Agent Orchestrator" or similar
       NEW: "# Maestro"

    7. Update description text to reference Maestro

    8. Keep "gsd-demo" package name references if they exist (technical identifiers)

    9. Verify markdown syntax is valid

    Reference: 18-RESEARCH.md Pattern 4 - Update documentation systematically
  </action>
  <verify>
    1. CLAUDE.md project overview mentions "Maestro"
    2. README.md title/main heading references "Maestro"
    3. Technical identifiers (.planning/, gsd-demo) remain unchanged where appropriate
    4. Markdown syntax is valid (headers, links, formatting intact)
  </verify>
  <done>
    Documentation rebranded. User-facing materials consistently reference "Maestro".
  </done>
</task>

</tasks>

<verification>
1. tauri.conf.json productName, identifier, and window title all reference "Maestro"
2. Cargo.toml description updated to reference "Maestro"
3. CLAUDE.md project overview updated
4. README.md updated with new branding
5. Technical identifiers (gsd-demo, .planning/) remain unchanged
6. JSON and TOML files are valid (no syntax errors)
</verification>

<success_criteria>
- [ ] tauri.conf.json: productName = "Maestro", identifier = "com.maestro.app", window title = "Maestro"
- [ ] Cargo.toml: description includes "Maestro"
- [ ] CLAUDE.md: Project overview references "Maestro"
- [ ] README.md: Main heading and description reference "Maestro"
- [ ] All config files are valid (JSON/TOML syntax correct)
- [ ] Technical identifiers unchanged (backwards compatibility maintained)
</success_criteria>

<output>
After completion, create `.planning/phases/18-maestro-folder-architecture/18-03-SUMMARY.md`
</output>
