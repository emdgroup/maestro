---
phase: 19-frontend-architecture-refactoring
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - src/components/kanban/KanbanBoard.tsx
  - src/components/kanban/KanbanColumn.tsx
  - src/components/kanban/TaskCard.tsx
  - src/components/kanban/TaskModal.tsx
  - src/components/kanban/index.ts
  - src/components/project/ProjectList.tsx
  - src/components/project/ProjectListItem.tsx
  - src/components/project/ProjectPicker.tsx
  - src/components/project/ConnectionList.tsx
  - src/components/project/index.ts
  - src/components/task/TaskForm.tsx
  - src/components/task/TaskDetail.tsx
  - src/components/task/TaskSettingsModal.tsx
  - src/components/task/index.ts
  - src/components/execution/ExecutionTerminal.tsx
  - src/components/execution/Terminal.tsx
  - src/components/execution/ExecutionHistory.tsx
  - src/components/execution/index.ts
  - src/components/common/AppHeader.tsx
  - src/components/common/ActionBar.tsx
  - src/components/common/ErrorToast.tsx
  - src/components/common/ThemeToggle.tsx
  - src/components/common/index.ts
  - src/components/index.ts
autonomous: true

must_haves:
  truths:
    - "Components are organized into domain folders (kanban, project, task, execution, common) with barrel exports"
    - "Each domain folder has index.ts with exports for its components"
    - "Components can be imported via @/components/kanban or @/components"
    - "No circular dependencies in barrel exports"
    - "All components compile without import errors"
  artifacts:
    - path: "src/components/kanban/index.ts"
      provides: "Barrel export for kanban domain components"
    - path: "src/components/project/index.ts"
      provides: "Barrel export for project domain components"
    - path: "src/components/task/index.ts"
      provides: "Barrel export for task domain components"
    - path: "src/components/execution/index.ts"
      provides: "Barrel export for execution domain components"
    - path: "src/components/common/index.ts"
      provides: "Barrel export for common components"
    - path: "src/components/index.ts"
      provides: "Root barrel export re-exporting all domains"
  key_links:
    - from: "src/views/KanbanView.tsx"
      to: "src/components/kanban/"
      via: "import { KanbanBoard } from @/components/kanban"
      pattern: "from ['\"]@/components/"
---

<objective>
Organize reusable components into domain-specific folders with barrel exports.

Purpose: Establish clear separation of concerns by grouping components by business domain (kanban, project, task, execution, common). This makes the codebase more discoverable and maintainable.

Output: 5 domain folders in src/components/ with barrel exports, plus root barrel export in src/components/index.ts
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-frontend-architecture-refactoring/19-RESEARCH.md
@src/components/
</context>

<tasks>

<task type="auto">
  <name>Organize components into domain folders and create barrel exports</name>
  <files>
    src/components/kanban/
    src/components/project/
    src/components/task/
    src/components/execution/
    src/components/common/
  </files>
  <action>
1. Create domain folders:
```bash
mkdir -p src/components/{kanban,project,task,execution,common}
```

2. Move kanban components:
   - Move KanbanBoard.tsx, KanbanColumn.tsx, TaskCard.tsx, TaskModal.tsx to src/components/kanban/
   - Create src/components/kanban/index.ts:
```typescript
export { KanbanBoard } from "./KanbanBoard";
export { KanbanColumn } from "./KanbanColumn";
export { TaskCard } from "./TaskCard";
export { TaskModal } from "./TaskModal";
export type { KanbanBoardProps } from "./KanbanBoard";
```

3. Move project components:
   - Move ProjectList.tsx, ProjectListItem.tsx, ProjectPicker.tsx, ConnectionList.tsx, FilePicker.tsx, ConnectionHeader.tsx to src/components/project/
   - Create src/components/project/index.ts:
```typescript
export { ProjectList } from "./ProjectList";
export { ProjectListItem } from "./ProjectListItem";
export { ProjectPicker } from "./ProjectPicker";
export { ConnectionList } from "./ConnectionList";
export { FilePicker } from "./FilePicker";
export { ConnectionHeader } from "./ConnectionHeader";
```

4. Move task components:
   - Move TaskForm.tsx, TaskDetail.tsx, TaskSettingsModal.tsx, TaskContextMenu.tsx, ImportSettings.tsx, PasswordModal.tsx to src/components/task/
   - Create src/components/task/index.ts:
```typescript
export { TaskForm } from "./TaskForm";
export { TaskDetail } from "./TaskDetail";
export { TaskSettingsModal } from "./TaskSettingsModal";
export { TaskContextMenu } from "./TaskContextMenu";
export { ImportSettings } from "./ImportSettings";
export { PasswordModal } from "./PasswordModal";
```

5. Move execution components:
   - Move ExecutionTerminal.tsx, Terminal.tsx, ExecutionHistory.tsx, DiffViewer.tsx, FileTree.tsx to src/components/execution/
   - Create src/components/execution/index.ts:
```typescript
export { ExecutionTerminal } from "./ExecutionTerminal";
export { Terminal } from "./Terminal";
export { ExecutionHistory } from "./ExecutionHistory";
export { DiffViewer } from "./DiffViewer";
export { FileTree } from "./FileTree";
```

6. Move common components:
   - Move AppHeader.tsx, ActionBar.tsx, ErrorToast.tsx, ThemeToggle.tsx, SyncButton.tsx, ReviewModal.tsx, ApprovalForm.tsx to src/components/common/
   - Create src/components/common/index.ts:
```typescript
export { AppHeader } from "./AppHeader";
export { ActionBar } from "./ActionBar";
export { ErrorToast, showErrorToast } from "./ErrorToast";
export { ThemeToggle } from "./ThemeToggle";
export { SyncButton } from "./SyncButton";
export { ReviewModal } from "./ReviewModal";
export { ApprovalForm } from "./ApprovalForm";
```

7. Create root barrel export src/components/index.ts:
```typescript
export * from "./kanban";
export * from "./project";
export * from "./task";
export * from "./execution";
export * from "./common";
export * from "./ui";
```

Note: Do NOT move ui/ folder - it's shadcn/ui primitives and stays as is.

8. Verify directory structure:
```bash
ls -1 src/components/*/index.ts
```

9. Verify no files remain at src/components/*.tsx except ui folder and any small utilities
  </action>
  <verify>
- Verify domain folders created: `ls -1d src/components/{kanban,project,task,execution,common}`
- Verify barrel exports exist: `ls -1 src/components/*/index.ts | wc -l` should output 5
- Verify all moved files are in correct folders: `find src/components -maxdepth 1 -name "*.tsx" | wc -l` should be 0 or very small
- Run TypeScript check to find import errors: `pnpm tsc --noEmit`
- Count should stay same: Total components before and after should match
  </verify>
  <done>
- 5 domain folders created (kanban, project, task, execution, common)
- Components moved to appropriate domain folders
- Barrel exports created for each domain
- Root barrel export created at src/components/index.ts
- No components left at src/components/ root level
  </done>
</task>

<task type="auto">
  <name>Update all imports in codebase to use new component organization</name>
  <files>
    src/views/
    src/store/
    src/providers/
    src/App.tsx
  </files>
  <action>
This is a large find-and-replace task. Use these systematic search-and-replace patterns:

1. Replace kanban component imports:
   - Find: `from "@/components/KanbanBoard"` → Replace: `from "@/components/kanban"`
   - Find: `from "@/components/KanbanColumn"` → Replace: `from "@/components/kanban"`
   - Find: `from "@/components/TaskCard"` → Replace: `from "@/components/kanban"`
   - Find: `from "@/components/TaskModal"` → Replace: `from "@/components/kanban"`
   - Find: `from "@/components/Task"` (exact match TaskCard/TaskModal) → Replace: `from "@/components/kanban"`

2. Replace project component imports:
   - Find: `from "@/components/ProjectList"` → Replace: `from "@/components/project"`
   - Find: `from "@/components/ProjectPicker"` → Replace: `from "@/components/project"`
   - Find: `from "@/components/ConnectionList"` → Replace: `from "@/components/project"`
   - Find: `from "@/components/FilePicker"` → Replace: `from "@/components/project"`

3. Replace task component imports:
   - Find: `from "@/components/TaskForm"` → Replace: `from "@/components/task"`
   - Find: `from "@/components/TaskSettingsModal"` → Replace: `from "@/components/task"`
   - Find: `from "@/components/ImportSettings"` → Replace: `from "@/components/task"`

4. Replace execution component imports:
   - Find: `from "@/components/ExecutionTerminal"` → Replace: `from "@/components/execution"`
   - Find: `from "@/components/Terminal"` → Replace: `from "@/components/execution"`
   - Find: `from "@/components/ExecutionHistory"` → Replace: `from "@/components/execution"`

5. Replace common component imports:
   - Find: `from "@/components/AppHeader"` → Replace: `from "@/components/common"`
   - Find: `from "@/components/ActionBar"` → Replace: `from "@/components/common"`
   - Find: `from "@/components/ErrorToast"` → Replace: `from "@/components/common"`
   - Find: `from "@/components/ThemeToggle"` → Replace: `from "@/components/common"`

6. After replacements, also update specific component imports on same line:
   - Example: `import { KanbanBoard, TaskCard }` - both should now import from `@/components/kanban`
   - Consolidate multiple imports from same domain to single line

7. Run through key files and manually verify:
   - src/views/KanbanView.tsx
   - src/views/ProjectPickerView.tsx
   - src/App.tsx
   - src/store/boardStore.ts

8. Fix any relative imports like `../../components/KanbanBoard` to use `@/components/kanban`

9. Verify no broken imports remain: `grep -r "from.*components.*\.tsx" src/ --include="*.tsx"`
  </action>
  <verify>
- Run TypeScript strict check: `pnpm tsc --noEmit` - should have zero import-related errors
- Search for old component paths: `grep -r "from ['\"]@/components/Kanban" src/` should return no results
- Search for old relative imports: `grep -r "from ['\"].*\\.\\./components/" src/` should return no results
- Verify all imports use new domain paths: `grep -r "from ['\"]@/components/" src/ | head -20` should show kanban, project, task, etc.
  </verify>
  <done>
- All component imports updated to use new domain-based organization
- No broken imports remain
- TypeScript compiles without import errors
- All files use @/components/domain pattern consistently
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Domain folder structure exists: `ls -1d src/components/{kanban,project,task,execution,common}`
2. Barrel exports created: `ls -1 src/components/*/index.ts | wc -l` should be 5
3. Root export created: `cat src/components/index.ts` shows all domain re-exports
4. No components at root level: `ls -1 src/components/*.tsx 2>/dev/null | wc -l` should be 0
5. All imports updated: `pnpm tsc --noEmit` has zero errors
6. Full app builds: `pnpm build` succeeds
7. Dev server runs: `pnpm tauri:dev` starts without errors
</verification>

<success_criteria>
- 5 domain folders created (kanban, project, task, execution, common)
- Each domain has barrel export (index.ts)
- All components organized by domain with no components at root level
- All imports throughout codebase updated to use new domain paths (@/components/kanban, etc.)
- Root barrel export created allowing both specific imports (@/components/kanban) and flat imports (@/components)
- TypeScript compiles without errors
- No circular dependency warnings
- All functionality preserved - no behavioral changes
</success_criteria>

<output>
After completion, create `.planning/phases/19-frontend-architecture-refactoring/19-03-SUMMARY.md`
</output>
