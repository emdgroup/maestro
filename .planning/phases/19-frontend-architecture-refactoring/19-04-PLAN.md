---
phase: 19-frontend-architecture-refactoring
plan: 04
type: execute
wave: 2
depends_on: [19-02]
files_modified:
  - src/store/boardStore.ts
  - src/store/projectStore.ts
  - src/components/common/ErrorToast.tsx
  - src/providers/ThemeProvider.tsx
  - src/components/project/ProjectList.tsx
  - src/components/task/TaskForm.tsx
  - src/components/task/ImportSettings.tsx
  - src/components/execution/ExecutionTerminal.tsx
  - src/components/execution/Terminal.tsx
autonomous: true

must_haves:
  truths:
    - "All invoke() calls in stores have been replaced with service layer calls"
    - "All invoke() calls in components have been replaced with service layer calls"
    - "All invoke() calls in providers have been replaced with service layer calls"
    - "Service layer is now the single source of backend communication"
    - "Error handling is centralized through service layer"
  artifacts:
    - path: "src/store/boardStore.ts"
      provides: "Board state management using services"
      pattern: "taskService\\.|executionService\\."
    - path: "src/store/projectStore.ts"
      provides: "Project state management using services"
      pattern: "projectService\\."
    - path: "src/providers/ThemeProvider.tsx"
      provides: "Theme provider using settings service"
      pattern: "settingsService\\."
    - path: "src/components/"
      provides: "All components using services instead of invoke()"
      pattern: "invoke\\(" should not exist
  key_links:
    - from: "src/store/boardStore.ts"
      to: "src/services/task.service.ts"
      via: "import { taskService } from @/services"
      pattern: "from ['\"]@/services['\"]"
    - from: "src/components/project/ProjectList.tsx"
      to: "src/services/project.service.ts"
      via: "projectService.removeProject()"
      pattern: "projectService\\."
---

<objective>
Replace all invoke() calls with centralized service layer calls throughout stores and components.

Purpose: Complete the migration to the service layer by removing all direct Tauri IPC calls from stores and components. This centralizes error handling, improves testability, and creates a clear API contract between frontend and backend.

Output: All stores and components now use service layer for backend communication
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-frontend-architecture-refactoring/19-RESEARCH.md
@src/services/
</context>

<tasks>

<task type="auto">
  <name>Replace invoke() calls in stores with service layer</name>
  <files>
    src/store/boardStore.ts
    src/store/projectStore.ts
  </files>
  <action>
1. Update src/store/boardStore.ts:
   - Add import: `import { taskService, executionService } from "@/services";`
   - Find all `invoke("cancel_execution", ...)` calls → Replace with `executionService.cancelExecution(...)`
   - Find all `invoke("detach_terminal", ...)` calls → Replace with `executionService.detachTerminal(...)`
   - Find all `invoke("attach_terminal", ...)` calls → Replace with `executionService.attachTerminal(...)`
   - Find all `invoke("send_terminal_input", ...)` calls → Replace with `executionService.sendTerminalInput(...)`
   - Find all `invoke("get_tasks", ...)` calls → Replace with `taskService.getTasks(...)`
   - Find all `invoke("update_task_status", ...)` calls → Replace with `taskService.updateTaskStatus(...)`
   - Remove old import: `import { invoke } from "@tauri-apps/api/core";`
   - Verify error handling is consistent (service layer now handles errors)

2. Update src/store/projectStore.ts:
   - Add import: `import { projectService } from "@/services";`
   - Find all `invoke("get_projects", ...)` calls → Replace with `projectService.getProjects()`
   - Find all `invoke("get_settings", ...)` calls → Replace with `projectService.getSettings()`
   - Find all `invoke("get_or_create_project", ...)` calls → Replace with `projectService.getOrCreateProject(...)`
   - Remove old import: `import { invoke } from "@tauri-apps/api/core";`
   - Verify error handling is consistent

3. Verify stores compile: `pnpm tsc --noEmit src/store/`
  </action>
  <verify>
- Search for remaining invoke() in stores: `grep -r "invoke(" src/store/` should return no results
- Verify imports are from @/services: `grep -r "from ['\"]@/services" src/store/`
- Check TypeScript: `pnpm tsc --noEmit src/store/`
- Verify no lingering Tauri imports: `grep -r "@tauri-apps/api/core" src/store/` should return no results
  </verify>
  <done>
- All invoke() calls removed from boardStore.ts
- All invoke() calls removed from projectStore.ts
- Service imports added where needed
- Stores compile without errors
- Error handling delegated to service layer
  </done>
</task>

<task type="auto">
  <name>Replace invoke() calls in components and providers with service layer</name>
  <files>
    src/providers/ThemeProvider.tsx
    src/components/project/ProjectList.tsx
    src/components/task/TaskForm.tsx
    src/components/task/ImportSettings.tsx
    src/components/execution/ExecutionTerminal.tsx
    src/components/execution/Terminal.tsx
    src/components/common/ErrorToast.tsx
  </files>
  <action>
1. Update src/providers/ThemeProvider.tsx:
   - Add import: `import { settingsService } from "@/services";`
   - Find `invoke("save_settings", ...)` → Replace with `settingsService.saveSettings(...)`
   - Remove old import: `import { invoke } from "@tauri-apps/api/core";`

2. Update src/components/project/ProjectList.tsx:
   - Add import: `import { projectService } from "@/services";`
   - Find `invoke("remove_project", ...)` → Replace with `projectService.removeProject(...)`
   - Remove old import

3. Update src/components/task/TaskForm.tsx:
   - Add import: `import { taskService } from "@/services";`
   - Find all task-related invoke() calls → Replace with appropriate taskService methods
   - Remove old import

4. Update src/components/task/ImportSettings.tsx:
   - Add import: `import { settingsService } from "@/services";`
   - Find `invoke("save_import_config", ...)` → Replace with `settingsService.saveImportConfig(...)`
   - Remove old import

5. Update src/components/execution/ExecutionTerminal.tsx:
   - Add import: `import { executionService } from "@/services";`
   - Find `invoke("detach_terminal", ...)` → Replace with `executionService.detachTerminal(...)`
   - Find `invoke("attach_terminal", ...)` → Replace with `executionService.attachTerminal(...)`
   - Find `invoke("send_terminal_input", ...)` → Replace with `executionService.sendTerminalInput(...)`
   - Remove old import

6. Update src/components/execution/Terminal.tsx:
   - Add import: `import { executionService } from "@/services";`
   - Find `invoke("send_terminal_input", ...)` → Replace with `executionService.sendTerminalInput(...)`
   - Find `invoke("attach_terminal", ...)` → Replace with `executionService.attachTerminal(...)`
   - Remove old import

7. Update src/components/common/ErrorToast.tsx (if it has any invoke calls):
   - Check for any invoke() calls
   - If present, replace with appropriate service

8. Verify all files compile: `pnpm tsc --noEmit`

9. Global verification - search entire codebase for remaining invoke():
   - `grep -r "invoke(" src/ --include="*.tsx" --include="*.ts" | grep -v "src/services/"`
   - This should return ZERO results (only allowed in services/)
  </action>
  <verify>
- Search for remaining invoke() outside services: `grep -r "invoke(" src/ --include="*.tsx" --include="*.ts" | grep -v "src/services/"` should return no results
- Verify service imports in components: `grep -r "from ['\"]@/services" src/components/ | wc -l` should show multiple
- Check TypeScript: `pnpm tsc --noEmit`
- Verify no @tauri-apps/api/core imports in components: `grep -r "@tauri-apps/api/core" src/components/ src/providers/` should return no results
  </verify>
  <done>
- All invoke() calls removed from providers
- All invoke() calls removed from components
- Service imports added throughout codebase
- All providers and components compile without errors
- invoke() only exists in src/services/ (centralized)
- No scattered IPC calls remain
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. No invoke() calls exist outside src/services/:
   ```bash
   grep -r "invoke(" src/ --include="*.tsx" --include="*.ts" | grep -v "src/services/"
   ```
   Should return ZERO results.

2. All service imports present:
   ```bash
   grep -r "from ['\"]@/services" src/ | wc -l
   ```
   Should show multiple imports across stores and components.

3. TypeScript compiles cleanly:
   ```bash
   pnpm tsc --noEmit
   ```
   Should show zero errors.

4. No Tauri imports in components/stores:
   ```bash
   grep -r "@tauri-apps/api/core" src/components/ src/store/ src/providers/
   ```
   Should return zero results.

5. Application still functions:
   ```bash
   pnpm tauri:dev
   ```
   Should start without errors and maintain all backend communication.
</verification>

<success_criteria>
- All invoke() calls removed from stores (boardStore, projectStore)
- All invoke() calls removed from providers (ThemeProvider)
- All invoke() calls removed from components (ProjectList, TaskForm, ImportSettings, ExecutionTerminal, Terminal)
- Service layer now is sole entry point for backend communication
- Centralized error handling in service layer for all IPC calls
- TypeScript compiles without errors
- No Tauri IPC imports in components or stores
- All functionality preserved - app works exactly as before
- Service layer enables easier testing and mocking
</success_criteria>

<output>
After completion, create `.planning/phases/19-frontend-architecture-refactoring/19-04-SUMMARY.md`
</output>
