---
phase: 19-frontend-architecture-refactoring
plan: 05
type: execute
wave: 3
depends_on: [19-02, 19-03, 19-04]
files_modified:
  - src/utils/hooks/useProjectSelection/
  - src/utils/hooks/useRecentProjects/
  - src/utils/hooks/useSshConnectionManager/
  - src/utils/hooks/useSshConnectionsQuery/
  - src/utils/hooks/use-mobile.ts
  - src/utils/hooks/index.ts
  - src/utils/helpers/path-utils.ts
  - src/utils/helpers/diff-utils.ts
  - src/utils/helpers/ui-utils.ts
  - src/utils/helpers/index.ts
  - src/utils/index.ts
autonomous: true

must_haves:
  truths:
    - "All hooks are organized in src/utils/hooks/ with barrel export"
    - "Complex hooks have folder structure (hook file + index.ts)"
    - "Simple hooks remain as single files"
    - "All helpers consolidated in src/utils/helpers/"
    - "Root utils barrel export allows clean imports"
  artifacts:
    - path: "src/utils/hooks/"
      provides: "Centralized hooks folder with all custom hooks"
      contains: "useProjectSelection, useRecentProjects, useSshConnectionManager, useSshConnectionsQuery, use-mobile"
    - path: "src/utils/hooks/index.ts"
      provides: "Barrel export for all hooks"
    - path: "src/utils/helpers/"
      provides: "Consolidated helper functions"
      contains: "path-utils.ts, diff-utils.ts, ui-utils.ts"
    - path: "src/utils/helpers/index.ts"
      provides: "Barrel export for all helpers"
    - path: "src/utils/index.ts"
      provides: "Root barrel export for utils"
  key_links:
    - from: "src/components/"
      to: "src/utils/hooks/index.ts"
      via: "import { useProjectSelection } from @/utils/hooks"
      pattern: "from ['\"]@/utils/hooks['\"]"
    - from: "src/"
      to: "src/utils/helpers/index.ts"
      via: "import { formatPath } from @/utils/helpers"
      pattern: "from ['\"]@/utils/helpers['\"]"
---

<objective>
Organize hooks into structured folder layout and consolidate helpers with barrel exports.

Purpose: Establish consistent organization for utilities (hooks and helpers) with clear folder structure and barrel exports. This makes utilities discoverable and maintainable.

Output: Organized src/utils/ with hooks/ and helpers/ subfolders, all with barrel exports
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-frontend-architecture-refactoring/19-RESEARCH.md
@src/hooks/
@src/utils/
@src/lib/
</context>

<tasks>

<task type="auto">
  <name>Reorganize hooks and create utils/helpers structure</name>
  <files>
    src/utils/hooks/
    src/utils/helpers/
  </files>
  <action>
1. Create utils subfolders:
```bash
mkdir -p src/utils/hooks
mkdir -p src/utils/helpers
```

2. For complex hooks (move entire existing hook + create folder):
   - Move src/hooks/useProjectSelection.ts → src/utils/hooks/useProjectSelection/useProjectSelection.ts
   - Create src/utils/hooks/useProjectSelection/index.ts:
```typescript
export { useProjectSelection } from "./useProjectSelection";
```

   - Move src/hooks/useRecentProjects.ts → src/utils/hooks/useRecentProjects/useRecentProjects.ts
   - Create src/utils/hooks/useRecentProjects/index.ts:
```typescript
export { useRecentProjects } from "./useRecentProjects";
```

   - Move src/hooks/useSshConnectionManager.ts → src/utils/hooks/useSshConnectionManager/useSshConnectionManager.ts
   - Create src/utils/hooks/useSshConnectionManager/index.ts:
```typescript
export { useSshConnectionManager } from "./useSshConnectionManager";
```

   - Move src/hooks/useSshConnectionsQuery.ts → src/utils/hooks/useSshConnectionsQuery/useSshConnectionsQuery.ts
   - Create src/utils/hooks/useSshConnectionsQuery/index.ts:
```typescript
export { useSshConnectionsQuery } from "./useSshConnectionsQuery";
```

3. For simple hooks (keep as single files):
   - Move src/hooks/use-mobile.ts → src/utils/hooks/use-mobile.ts (no folder needed)

4. Create src/utils/hooks/index.ts barrel export:
```typescript
export { useProjectSelection } from "./useProjectSelection";
export { useRecentProjects } from "./useRecentProjects";
export { useSshConnectionManager } from "./useSshConnectionManager";
export { useSshConnectionsQuery } from "./useSshConnectionsQuery";
export { useMobile } from "./use-mobile";
```

5. Consolidate helpers:
   - Move src/lib/path-utils.ts → src/utils/helpers/path-utils.ts
   - Move src/utils/diffParser.ts → src/utils/helpers/diff-utils.ts
   - Move src/lib/utils.ts → src/utils/helpers/ui-utils.ts (or rename appropriately)

6. Create src/utils/helpers/index.ts barrel export:
```typescript
export * from "./path-utils";
export * from "./diff-utils";
export * from "./ui-utils";
```

7. Create src/utils/index.ts root barrel export:
```typescript
export * from "./hooks";
export * from "./helpers";
```

8. Delete old folders (if empty):
   - `rmdir src/hooks/` (only if empty after moves)
   - `rmdir src/lib/` (only if empty after moves)

9. Verify structure:
```bash
ls -la src/utils/
ls -la src/utils/hooks/
ls -la src/utils/helpers/
```
  </action>
  <verify>
- Verify utils structure: `ls -1d src/utils/{hooks,helpers}`
- Verify hook folders: `ls -1d src/utils/hooks/use*` should show 4 folders
- Verify simple hooks: `ls -1 src/utils/hooks/*.ts` should show use-mobile.ts
- Verify helper files: `ls -1 src/utils/helpers/*.ts` should show 3 files
- Verify barrel exports: `cat src/utils/hooks/index.ts` and `cat src/utils/helpers/index.ts`
- Verify no files remain in old locations: `ls -1 src/hooks/ src/lib/` should be empty or not exist
  </verify>
  <done>
- Hooks moved to src/utils/hooks/ with folder structure for complex hooks
- Helpers consolidated in src/utils/helpers/
- Barrel exports created for hooks, helpers, and root utils
- Old src/hooks/ and src/lib/ removed or emptied
  </done>
</task>

<task type="auto">
  <name>Update all imports throughout codebase to use new utils structure</name>
  <files>
    src/
  </files>
  <action>
This is another large find-and-replace task for utils imports:

1. Replace hook imports:
   - Find: `from "@/hooks/useProjectSelection"` → Replace: `from "@/utils/hooks"`
   - Find: `from "@/hooks/useRecentProjects"` → Replace: `from "@/utils/hooks"`
   - Find: `from "@/hooks/useSshConnectionManager"` → Replace: `from "@/utils/hooks"`
   - Find: `from "@/hooks/useSshConnectionsQuery"` → Replace: `from "@/utils/hooks"`
   - Find: `from "@/hooks/use-mobile"` → Replace: `from "@/utils/hooks"`
   - Find: `from "../../hooks/use*"` (relative) → Replace: `from "@/utils/hooks"`

2. Replace helper imports:
   - Find: `from "@/lib/path-utils"` → Replace: `from "@/utils/helpers"`
   - Find: `from "@/utils/diffParser"` → Replace: `from "@/utils/helpers"`
   - Find: `from "@/lib/utils"` → Replace: `from "@/utils/helpers"`
   - Find: `from "../../lib/path-utils"` (relative) → Replace: `from "@/utils/helpers"`

3. Update destructuring if necessary:
   - Example: `import { useProjectSelection } from "@/utils/hooks"` (not from subpath)

4. Fix any remaining relative imports:
   - Find: `from "../../utils/diffParser"` → Replace: `from "@/utils/helpers"`
   - Find: `from "../../lib/"` → Replace: `from "@/utils/helpers"`

5. Run global search to verify:
   - `grep -r "from ['\"]@/hooks" src/` should return ZERO results
   - `grep -r "from ['\"]@/lib" src/` should return ZERO results
   - `grep -r "from ['\"]@/utils/helpers" src/` should show multiple hits
   - `grep -r "from ['\"]@/utils/hooks" src/` should show multiple hits

6. Verify TypeScript: `pnpm tsc --noEmit`

7. Verify no broken relative imports to old locations: `grep -r "from ['\"].*\\.\\./\\.\\./\\(hooks\\|lib\\)" src/`
  </action>
  <verify>
- Search for old @/hooks imports: `grep -r "from ['\"]@/hooks" src/` should return no results
- Search for old @/lib imports: `grep -r "from ['\"]@/lib" src/` should return no results
- Search for new @/utils paths: `grep -r "from ['\"]@/utils/" src/ | wc -l` should show multiple
- Check TypeScript: `pnpm tsc --noEmit`
- Verify no relative imports to old locations: `grep -r "from ['\"].*\\.\\./\\.\\./\\(hooks\\|lib\\)" src/`
  </verify>
  <done>
- All hook imports updated to use @/utils/hooks
- All helper imports updated to use @/utils/helpers
- No old @/hooks or @/lib imports remain
- TypeScript compiles without errors
- All relative imports to old locations updated to @/utils paths
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Utils structure is correct:
   ```bash
   ls -1d src/utils/{hooks,helpers}
   ls -1 src/utils/hooks/index.ts src/utils/helpers/index.ts src/utils/index.ts
   ```

2. All hooks in utils/hooks/:
   ```bash
   ls -1 src/utils/hooks/*/index.ts | wc -l
   ls -1 src/utils/hooks/*.ts
   ```

3. No old locations have content:
   ```bash
   ls -1 src/hooks/ src/lib/ 2>/dev/null | wc -l
   ```
   Should return 0 or not exist.

4. Imports are updated:
   ```bash
   grep -r "from ['\"]@/hooks" src/ | wc -l
   grep -r "from ['\"]@/lib" src/ | wc -l
   ```
   Both should return 0.

5. TypeScript compiles:
   ```bash
   pnpm tsc --noEmit
   ```

6. App runs:
   ```bash
   pnpm tauri:dev
   ```
</verification>

<success_criteria>
- src/utils/hooks/ created with folder structure for complex hooks (useProjectSelection, useRecentProjects, useSshConnectionManager, useSshConnectionsQuery) and simple files (use-mobile.ts)
- src/utils/helpers/ created with consolidated helper functions (path-utils.ts, diff-utils.ts, ui-utils.ts)
- Barrel exports created for hooks/, helpers/, and root utils/
- All imports throughout codebase updated to use @/utils/hooks and @/utils/helpers
- No @/hooks or @/lib imports remain
- Old src/hooks/ and src/lib/ directories removed or empty
- TypeScript compiles without errors
- All functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/19-frontend-architecture-refactoring/19-05-SUMMARY.md`
</output>
