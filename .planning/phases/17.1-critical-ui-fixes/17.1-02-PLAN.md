---
phase: 17.1-critical-ui-fixes
plan: 02
type: execute
wave: 2
depends_on: ["17.1-01"]
files_modified:
  - src/components/AppHeader.tsx
  - src/App.tsx
  - src/store/boardStore.ts
autonomous: true

must_haves:
  truths:
    - "User sees project name in header with dropdown to switch projects"
    - "Header shows tab buttons for Tasks, Agents, Worktrees, Settings"
    - "Clicking tabs switches between different views without full page reload"
    - "Header is compact (48px height) and modern-looking"
  artifacts:
    - path: "src/components/AppHeader.tsx"
      provides: "New modern header with project dropdown and tab navigation"
      min_lines: 60
      contains: "Select component"
    - path: "src/App.tsx"
      provides: "Updated App to track activePage state and use new header"
      contains: "activePage"
    - path: "src/store/boardStore.ts"
      provides: "Optional: page navigation state in store if needed"
  key_links:
    - from: "AppHeader.tsx"
      to: "App.tsx"
      via: "onProjectChange and onViewChange props"
      pattern: "onChange|onViewChange"
    - from: "App.tsx"
      to: "AppHeader.tsx"
      via: "currentProject, projects, activeView props"
      pattern: "currentProject|activeView"
    - from: "AppHeader project dropdown"
      to: "App.tsx handleProjectSelected"
      via: "onProjectChange callback"
      pattern: "onProjectChange"
---

<objective>
Redesign the application header to match modern UX patterns from exemple/, featuring inline project dropdown and tab navigation for Tasks/Agents/Worktrees/Settings. This replaces the current separate ProjectPicker screen with a compact, integrated header-based navigation.

Purpose: Create slick modern aesthetic matching reference design, improve UX by allowing project switching without full-screen modal, establish tab-based page navigation.
Output: Modern AppHeader component with project dropdown and tab navigation, integrated into App.tsx page routing.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17.1-critical-ui-fixes/17.1-RESEARCH.md
@src/components/AppHeader.tsx (if exists)
@src/App.tsx
@src/components/ProjectPicker.tsx (reference for patterns)
@exemple/agent-cli-orchestration/components/app-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Create modern AppHeader component with project dropdown and tabs</name>
  <files>src/components/AppHeader.tsx</files>
  <action>
Create or completely rewrite src/components/AppHeader.tsx to implement the modern header design from exemple/agent-cli-orchestration/components/app-shell.tsx:

Header layout structure:
- **Left section** (flex-shrink-0): Logo text + vertical divider + project Select dropdown
  - Logo: "GSD Agent Orchestrator" in text-sm font-semibold
  - Divider: h-4 w-px bg-border
  - Project Select: Use shadcn Select component, h-7 w-[160px], text-xs, show FolderOpen icon + project name
  - Select should list all projects from projects prop
- **Center section** (flex-1): Tab navigation buttons
  - VIEWS: {id: "kanban", label: "Tasks"}, {id: "agents", label: "Agents"}, {id: "worktrees", label: "Worktrees"}, {id: "settings", label: "Settings"}
  - Each tab: flex items-center gap-1.5 rounded-md px-3 py-1.5 text-xs font-medium
  - Active tab: bg-muted text-foreground
  - Inactive tab: text-muted-foreground hover:text-foreground hover:bg-muted/50
  - Each tab should have an icon (LayoutDashboard for Tasks, Bot for Agents, GitBranch for Worktrees, Settings for Settings)
- **Right section** (flex-shrink-0): Status indicator + New Agent button
  - Status: animated pulse dot + "0 running" text in muted color (or {count} running if available)
  - New Agent button: sm size, h-7, gap-1.5, text-xs, with Plus icon + "New Agent" label

Component props:
- currentProject: {name: string, path: string}
- activeView: "kanban" | "agents" | "worktrees" | "settings"
- onViewChange: (view) => void
- projects: Array<{name: string, path: string}>
- onProjectChange: (path: string) => void
- agentCount?: number (optional, for showing "N running")

Styling: h-12 shrink-0 flex items-center justify-between border-b px-4 gap-4 with proper responsive handling.

Import icons from lucide-react: LayoutDashboard, Bot, GitBranch, Settings, FolderOpen, Plus
Import Select components from @/components/ui/select
</action>
  <verify>
    - File exists at src/components/AppHeader.tsx
    - Contains AppHeaderModern function or export (use modern naming)
    - Has header with h-12 height and flex layout
    - Left section: Logo + divider + Select component
    - Center section: Tab navigation with 4 tabs (Tasks, Agents, Worktrees, Settings)
    - Right section: Status indicator and "New Agent" button
    - All icons properly imported from lucide-react
    - Select component shows projects list
    - Tabs have active/inactive styling
    - Props properly typed (TypeScript)
  </verify>
  <done>
    Modern AppHeader component created with project dropdown, tab navigation, and status indicator - ready for integration
  </done>
</task>

<task type="auto">
  <name>Integrate AppHeader into App.tsx and implement page routing via tabs</name>
  <files>src/App.tsx</files>
  <action>
Update App.tsx to use the new AppHeader component and implement tab-based page routing:

1. Import the new AppHeader component (or named export if AppHeaderModern)
2. In the main App component return:
   - If loading, keep existing loading state
   - If not projectSelected, show ProjectPicker (keep existing behavior for now)
   - If projectSelected:
     - Render AppHeader with props:
       - currentProject: currentProject
       - activeView: activePage
       - onViewChange: (view) => setActivePage(view)
       - projects: list of projects (get from state or query)
       - onProjectChange: handleProjectSelected (already exists)
       - agentCount: optional (can be 0 for now or fetch from state)
     - Below header, render page content based on activePage:
       - if activePage === "kanban": render KanbanBoard
       - if activePage === "agents": render AgentMonitor
       - if activePage === "worktrees": render WorktreeManager
       - if activePage === "settings": render ProjectSettingsModal or settings page

3. Ensure activePage state is initialized to "kanban" (already done, keep as is)

4. Update layout structure:
   - Remove old AppHeader if exists
   - New structure: AppHeader (always visible) + Page content (switches based on tab)

5. For project list: Either fetch from invoke("get_projects") or maintain in state - update as needed based on current implementation

Do NOT delete ProjectPicker yet - keep as fallback for first-time setup if projectSelected === false. AppHeader only appears after project is selected.
  </action>
  <verify>
    - App.tsx imports new AppHeader component
    - AppHeader rendered in correct position (only when projectSelected === true)
    - AppHeader receives correct props (currentProject, activeView, onViewChange, projects, onProjectChange)
    - Page content switches based on activePage state:
      - "kanban": KanbanBoard renders
      - "agents": AgentMonitor renders
      - "worktrees": WorktreeManager renders
      - "settings": Settings page renders
    - Tab clicks update activePage state
    - Project dropdown calls handleProjectSelected on change
    - ProjectPicker still used for initial project selection
  </verify>
  <done>
    App.tsx updated with modern header and tab-based page routing - all four main pages accessible via tabs
  </done>
</task>

<task type="auto">
  <name>Ensure all page components render correctly in tab context</name>
  <files>src/App.tsx</files>
  <action>
Verify that all four page components (KanbanBoard, AgentMonitor, WorktreeManager, ProjectSettingsModal/settings) render correctly within the App.tsx page content area:

1. KanbanBoard: Should render below header without horizontal scroll issues
2. AgentMonitor: Should render as a full-height page (or split-pane if already implemented)
3. WorktreeManager: Should render as card grid or list
4. Settings: Should render as a form or modal (decide based on current implementation)

For each page:
- Check that it doesn't overflow the header
- Check that responsive layout works (pages should adapt to window width)
- Ensure all required props are passed from App.tsx
- Verify no console errors when switching tabs

If any page requires full-screen modal (like ProjectSettingsModal currently), decide:
- Option A: Keep as modal overlay on top of current page
- Option B: Integrate into tab view
Use Option A (overlay) for now - simpler, non-breaking change.
  </action>
  <verify>
    - Switch to each tab and verify page renders
    - Switch between tabs and verify content changes
    - No console errors when switching pages
    - No layout overflow or visual glitches
    - All props passed correctly to page components
    - Responsive layout works (try resizing window)
  </verify>
  <done>
    All page components verified to render correctly within tab-based navigation - tab switching works smoothly
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm dev` to start dev server
2. Navigate through initial project selection to main UI
3. Verify header appears at top with:
   - Logo + project name in dropdown (should be selectable)
   - Tab buttons for Tasks, Agents, Worktrees, Settings
   - Status indicator showing "0 running"
   - "New Agent" button
4. Click each tab and verify correct page content appears:
   - Tasks tab: Kanban board
   - Agents tab: Agent monitor page
   - Worktrees tab: Worktree manager page
   - Settings tab: Settings/configuration page
5. Verify header is compact (approximately 48px tall)
6. Verify modern styling matches mockup aesthetic (not unstyled/unpolished)
7. Test project dropdown: should show project name and be clickable
8. Verify no layout shifts when switching tabs
</verification>

<success_criteria>
- AppHeader renders correctly with project dropdown and tab navigation
- All four main pages accessible via tabs
- Switching tabs shows correct page content
- Header compact (h-12) and modern-looking
- No console errors or warnings when navigating
- Responsive layout works on different window sizes
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-critical-ui-fixes/17.1-02-SUMMARY.md`
</output>
