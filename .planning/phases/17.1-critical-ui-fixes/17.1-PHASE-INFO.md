# Phase 17.1: Critical UI Fixes (INSERTED)

**Goal:** Fix production mode project selection, implement slick modern UX patterns from exemple/ (not pixel-perfect copy), and verify with actual screenshots using Playwright

**Depends on:** Phase 17

**Critical Issues to Fix:**
1. Production mode folder selection completes but nothing happens (app doesn't proceed)
2. UI lacks slick modern aesthetics - poor visual quality despite technical implementation
3. Missing UX patterns from exemple/ reference:
   - Project dropdown in header (not separate ProjectPicker screen)
   - Tab navigation for "Tasks", "Agents", "Worktrees", "Settings"
   - Better Kanban column naming and layout
   - Overall element placement and spacing
4. System accent color integration not working properly
5. Missing visual testing - need Playwright screenshots to verify before claiming success

**Plans:** TBD (run /gsd:plan-phase 17.1 to break down)

**Details:**
This is an urgent insertion to fix catastrophic failures in Phase 13-17 execution:
- Phase 17 claimed success but UI is broken
- Verification was paper-only (no actual testing)
- exemple/ folder shows UX patterns and modern aesthetic feel (NOT pixel-perfect color matching)
- Should use OS accent color (not hardcoded colors from example)
- Theme should be SYSTEM-FIRST (follow OS theme preference), not dark-first
- Production mode is non-functional
- Current UI looks unstyled/unpolished despite having Tailwind CSS
