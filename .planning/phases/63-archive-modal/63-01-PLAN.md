---
phase: 63-archive-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/kanban/ArchiveModal.tsx
  - src/components/kanban/__tests__/ArchiveModal.test.tsx
  - src/views/KanbanView.tsx
  - src/components/views/ArchiveView.tsx
autonomous: true
requirements: [ARCHIVE-01, ARCHIVE-02, ARCHIVE-03]

must_haves:
  truths:
    - "Archive button visible in the board action bar opens a modal listing archived/cancelled tasks"
    - "Search input in the archive modal filters tasks by title in real time"
    - "Tab filters (All, Done, Cancelled) update the visible task list"
    - "Clicking a task row closes the modal and navigates to the task detail screen"
    - "ArchiveView.tsx is deleted from the codebase"
  artifacts:
    - path: "src/components/kanban/ArchiveModal.tsx"
      provides: "Archive modal component with search and tab filters"
      exports: ["ArchiveModal"]
    - path: "src/components/kanban/__tests__/ArchiveModal.test.tsx"
      provides: "Unit test stubs covering ARCHIVE-01, ARCHIVE-02, ARCHIVE-03"
      contains: "describe(\"ArchiveModal\""
    - path: "src/views/KanbanView.tsx"
      provides: "Archive button in action bar + ArchiveModal mount"
      contains: "isArchiveModalOpen"
  key_links:
    - from: "src/views/KanbanView.tsx"
      to: "src/components/kanban/ArchiveModal.tsx"
      via: "import + <ArchiveModal> mount with isOpen/onClose/projectId props"
      pattern: "ArchiveModal"
    - from: "src/components/kanban/ArchiveModal.tsx"
      to: "src/services/task.service.ts"
      via: "useTasksQuery(projectId) for archived task data"
      pattern: "useTasksQuery"
    - from: "src/components/kanban/ArchiveModal.tsx"
      to: "src/store/navigationStore.ts"
      via: "useNavigationActions().setActiveTaskId on row click"
      pattern: "setActiveTaskId"
---

<objective>
Create the ArchiveModal component and integrate it into the board action bar, replacing the deleted ArchiveView sub-view with a modal-based archive browser.

Purpose: Users can browse archived and cancelled tasks from the board without navigating away, completing the v1.7 modal-based UX pattern.
Output: Working ArchiveModal accessible from action bar, ArchiveView deleted, test stubs in place.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-archive-modal/63-RESEARCH.md
@.planning/phases/63-archive-modal/63-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/ui/dialog.tsx:
- Dialog: accepts `open: boolean`, `onOpenChange: (open: boolean, eventDetails?) => void`
- DialogContent: renders portal + overlay + content panel; accepts className for width override
- DialogTitle: required for accessibility
- DialogDescription: use with className="sr-only" for accessibility

From src/components/ui/tabs.tsx:
- Tabs: accepts `value: string`, `onValueChange: (value: string) => void`
- TabsList: container for TabsTrigger elements
- TabsTrigger: accepts `value: string`

From src/services/task.service.ts:
- useTasksQuery(projectId: number | null): UseQueryResult<Task[]>

From src/store/navigationStore.ts:
- useNavigationActions(): { setActiveTaskId: (id: number | null) => void, ... }

From src/types/bindings.ts:
- Task: { id: number, title: string, status: TaskStatus, priority: TaskPriority, labels: string[], archived_at: string | null, updated_at: string, ... }
- TaskStatus: "Backlog" | "Ready" | "InProgress" | "Review" | "Done" | "Cancelled"
- TaskPriority: "Critical" | "High" | "Medium" | "Low" | "None"

From src/utils/constants/priority.ts:
- PRIORITY_BADGE_CLASSES: Record<TaskPriority, string>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArchiveModal component and test stubs</name>
  <files>src/components/kanban/ArchiveModal.tsx, src/components/kanban/__tests__/ArchiveModal.test.tsx</files>

  <read_first>
    - src/components/views/ArchiveView.tsx (source of filter logic, formatDate, STATUS_BADGE_CLASSES to migrate)
    - src/components/kanban/CreateTaskModal.tsx (Dialog container pattern, useEffect reset, props interface)
    - src/components/ui/dialog.tsx (Dialog/DialogContent/DialogTitle/DialogDescription exports)
    - src/components/ui/tabs.tsx (Tabs/TabsList/TabsTrigger exports and prop signatures)
    - src/components/kanban/__tests__/CreateTaskModal.test.tsx (test file structure pattern)
  </read_first>

  <action>
    Create `src/components/kanban/ArchiveModal.tsx` with:

    Props interface: `ArchiveModalProps { isOpen: boolean; onClose: () => void; projectId: number }`

    Export: `export function ArchiveModal({ isOpen, onClose, projectId }: ArchiveModalProps)`

    Local state:
    - `search: string` (useState, default "")
    - `filter: ArchiveFilter` (useState, default "all") where `type ArchiveFilter = "all" | "Done" | "Cancelled"`

    Hooks:
    - `useTasksQuery(projectId)` from `@/services/task.service`
    - `useNavigationActions()` from `@/store/navigationStore` — destructure `setActiveTaskId`

    useEffect reset pattern: when `!isOpen`, reset `search` to "" and `filter` to "all"

    useMemo `archiveTasks`: filter `tasks` where `archived_at != null || status === "Cancelled"`, then by `filter` (skip if "all", match status otherwise), then by `search` (case-insensitive title includes), sort by `updated_at` descending

    `handleTaskClick(task: Task)`: call `setActiveTaskId(task.id)` then `onClose()`

    Render structure:
    - `Dialog open={isOpen} onOpenChange={(open) => { if (!open) onClose(); }}`
    - `DialogContent className="sm:max-w-2xl flex flex-col max-h-[80vh]"`
    - `DialogTitle` text "Archive"
    - `DialogDescription className="sr-only"` text "Browse archived and cancelled tasks"
    - `Input` bound to `search` state, placeholder "Search archived tasks...", className "h-8"
    - `Tabs value={filter} onValueChange={(v) => setFilter(v as ArchiveFilter)}`
      - `TabsList` with three `TabsTrigger` values: "all" (label "All"), "Done" (label "Done"), "Cancelled" (label "Cancelled")
    - Scrollable list container: `div className="flex-1 overflow-y-auto min-h-0 custom-scrollbar"`
      - Loading state: when `isLoading`, show "Loading..." text
      - Empty state: when `archiveTasks.length === 0`, show "No archived tasks" centered
      - Task rows: `button` elements with `onClick={() => handleTaskClick(task)}`, showing title (truncated), formatted date, priority badge (using `PRIORITY_BADGE_CLASSES`), status badge (using local `STATUS_BADGE_CLASSES`)

    Copy `formatDate` helper and `STATUS_BADGE_CLASSES` constant from `ArchiveView.tsx` verbatim into `ArchiveModal.tsx` (as module-level declarations, not exported).

    Then create `src/components/kanban/__tests__/ArchiveModal.test.tsx` with:
    - vi.mock for `@/services/task.service` returning `useTasksQuery` with mock data (one Done+archived task, one Cancelled task)
    - vi.mock for `@/store/navigationStore` returning `useNavigationActions` with mock `setActiveTaskId`
    - describe("ArchiveModal") with it.todo stubs: "renders task list when isOpen=true", "filters tasks by search input value", "shows only Done tasks when Done tab selected", "shows only Cancelled tasks when Cancelled tab selected", "calls setActiveTaskId and onClose when a task row is clicked"
  </action>

  <verify>
    <automated>cd /home/m306213/workspace/maestro && pnpm build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/components/kanban/ArchiveModal.tsx exists and exports `ArchiveModal`
    - ArchiveModal.tsx imports Dialog, DialogContent, DialogTitle, DialogDescription from "@/ui/dialog"
    - ArchiveModal.tsx imports Tabs, TabsList, TabsTrigger from "@/ui/tabs"
    - ArchiveModal.tsx imports useTasksQuery from "@/services/task.service"
    - ArchiveModal.tsx imports useNavigationActions from "@/store/navigationStore"
    - ArchiveModal.tsx contains `type ArchiveFilter = "all" | "Done" | "Cancelled"`
    - ArchiveModal.tsx contains `useEffect` that resets search and filter when `!isOpen`
    - ArchiveModal.tsx contains `useMemo` with filter chain matching archived_at/status/search/sort logic from ArchiveView.tsx
    - ArchiveModal.tsx contains `handleTaskClick` that calls `setActiveTaskId(task.id)` and `onClose()`
    - ArchiveModal.tsx contains STATUS_BADGE_CLASSES with Done and Cancelled entries
    - ArchiveModal.tsx contains formatDate helper function
    - src/components/kanban/__tests__/ArchiveModal.test.tsx exists with describe block and 5 it.todo stubs
    - `pnpm build` exits 0
  </acceptance_criteria>

  <done>ArchiveModal component created with full filter/search/navigation logic; test stubs exist; TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Wire ArchiveModal into KanbanView and delete ArchiveView</name>
  <files>src/views/KanbanView.tsx, src/components/views/ArchiveView.tsx</files>

  <read_first>
    - src/views/KanbanView.tsx (current action bar structure, CreateTaskModal mount pattern)
    - src/components/kanban/ArchiveModal.tsx (just created — confirm export name and props)
  </read_first>

  <action>
    Modify `src/views/KanbanView.tsx`:

    Add imports:
    - `import { Archive } from "lucide-react"`
    - `import { ArchiveModal } from "@/components/kanban/ArchiveModal"`

    Add state declaration alongside existing `isCreateModalOpen`:
    - `const [isArchiveModalOpen, setIsArchiveModalOpen] = useState(false)`

    In the action bar div (the `h-12 border-b` div), add an Archive button BEFORE the `ml-auto` div:
    - `Button size="sm" variant="outline" onClick={() => setIsArchiveModalOpen(true)}`
    - Icon: `Archive className="size-4"`
    - Text: "Archive"

    After the existing `CreateTaskModal` mount, add:
    - `ArchiveModal isOpen={isArchiveModalOpen} onClose={() => setIsArchiveModalOpen(false)} projectId={projectId ?? 0}`

    Then DELETE `src/components/views/ArchiveView.tsx` entirely.

    Verify no remaining imports of ArchiveView in the codebase (grep confirms it is only self-referential).
  </action>

  <verify>
    <automated>cd /home/m306213/workspace/maestro && pnpm build 2>&1 | tail -5 &amp;&amp; ! grep -r "ArchiveView" src/ ; echo "exit: $?"</automated>
  </verify>

  <acceptance_criteria>
    - src/views/KanbanView.tsx imports `Archive` from "lucide-react"
    - src/views/KanbanView.tsx imports `ArchiveModal` from "@/components/kanban/ArchiveModal"
    - src/views/KanbanView.tsx contains `isArchiveModalOpen` state declaration
    - src/views/KanbanView.tsx contains a Button with "Archive" text and Archive icon in the action bar before the ml-auto div
    - src/views/KanbanView.tsx mounts ArchiveModal with isOpen, onClose, projectId props
    - src/components/views/ArchiveView.tsx does NOT exist (deleted)
    - `grep -r "ArchiveView" src/` returns no matches
    - `pnpm build` exits 0
  </acceptance_criteria>

  <done>Archive button appears in board action bar; ArchiveModal integrated; ArchiveView.tsx deleted; build passes with zero ArchiveView references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase is entirely client-side — no new data crosses trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Information Disclosure | ArchiveModal task list | accept | All tasks (including archived) are already fetched by useTasksQuery and available in client memory; the modal adds no new data exposure surface |
| T-63-02 | Tampering | Search/filter inputs | accept | Client-side-only filtering on already-fetched data; no server writes; no injection vector (input never sent to backend) |

No ASVS L1 controls are triggered by this phase. All data is already available to the authenticated user via existing IPC.
</threat_model>

<verification>
1. `pnpm build` passes (TypeScript compilation + Vite build)
2. `pnpm test ArchiveModal` runs test file without errors (stubs pass as todo)
3. `grep -r "ArchiveView" src/` returns zero matches
4. `pnpm lint` passes
</verification>

<success_criteria>
- Archive button visible in KanbanView action bar between label filter and ml-auto div
- Clicking Archive button opens the ArchiveModal dialog
- ArchiveModal lists tasks where archived_at is set or status is Cancelled
- Search input filters the list by title in real time
- Tab filters (All/Done/Cancelled) update the visible list
- Clicking a task row calls setActiveTaskId and closes the modal
- ArchiveView.tsx is fully removed from the codebase
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/63-archive-modal/63-01-SUMMARY.md`
</output>
