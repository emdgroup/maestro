---
phase: 33-tauri-backend-code-review-and-refactoring-for-maintainability-dry-solid-kiss
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/ipc/execution_handlers.rs
  - src-tauri/src/process/remote.rs
  - src-tauri/src/ipc/filesystem_handlers.rs
  - src-tauri/src/ipc/worktree_handlers.rs
  - src-tauri/src/error.rs
autonomous: true
requirements: [R12, R13, R14, R15, R16, R17]

must_haves:
  truths:
    - "detect_error_type_and_suggestions dead code is removed from execution_handlers.rs"
    - "canonicalize_repo_path is inlined at its single call site"
    - "process/remote.rs uses log::info!/log::warn! instead of println!/eprintln!"
    - "filesystem_handlers.rs uses log::info!/log::debug! instead of println!"
    - "get_worktree_diff uses a single JOIN query instead of two separate DB calls"
    - "error.rs is removed (empty comment-only stub)"
  artifacts:
    - path: "src-tauri/src/ipc/execution_handlers.rs"
      provides: "Clean execution handlers with no dead code"
    - path: "src-tauri/src/process/remote.rs"
      provides: "Consistent log:: usage"
    - path: "src-tauri/src/ipc/filesystem_handlers.rs"
      provides: "Consistent log:: usage"
    - path: "src-tauri/src/ipc/worktree_handlers.rs"
      provides: "Optimized single-query get_worktree_diff"
  key_links:
    - from: "src-tauri/src/ipc/worktree_handlers.rs"
      to: "worktrees + projects tables"
      via: "JOIN query in get_worktree_diff"
      pattern: "JOIN projects p ON"
---

<objective>
Remove dead code, replace println! with log:: crate, consolidate double DB queries into JOINs, and delete the empty error.rs stub.

Purpose: Final cleanup pass for consistent logging, no dead code, and optimized queries.
Output: Clean execution_handlers, process/remote, filesystem_handlers, worktree_handlers, no error.rs.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tauri-backend-code-review-and-refactoring-for-maintainability-dry-solid-kiss/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code, inline canonicalize_repo_path, replace all println! with log::</name>
  <files>src-tauri/src/ipc/execution_handlers.rs, src-tauri/src/process/remote.rs, src-tauri/src/ipc/filesystem_handlers.rs</files>
  <read_first>
    - src-tauri/src/ipc/execution_handlers.rs
    - src-tauri/src/process/remote.rs
    - src-tauri/src/ipc/filesystem_handlers.rs
  </read_first>
  <action>
**1. Remove `detect_error_type_and_suggestions` dead code (R12):**

In `src-tauri/src/ipc/execution_handlers.rs`, delete the entire `detect_error_type_and_suggestions` function (lines 9-62). It is `pub fn` but has zero call sites in the codebase. Also remove the now-unnecessary `use std::io::Read;` import (line 4) if it was only used by this function — check if `Read` is used elsewhere in the file first.

**2. Inline `canonicalize_repo_path` (R13):**

In `src-tauri/src/ipc/execution_handlers.rs`, the private function `canonicalize_repo_path` (lines 64-69) is only called once (line 696). Inline it at the call site:

Replace line 696:
```rust
canonicalize_repo_path(&repo_path)?
```
with:
```rust
std::path::Path::new(&repo_path)
    .canonicalize()
    .map_err(|e| format!("Invalid repository path '{}': {}. Ensure the project directory exists.", repo_path, e))?
    .to_string_lossy()
    .to_string()
```

Then delete the `canonicalize_repo_path` function definition.

**3. Replace `println!` with `log::` in `process/remote.rs` (R14):**

In `src-tauri/src/process/remote.rs`, make the following replacements:

- Line 67-70: Replace `println!("[Remote] Spawned Claude Code on {} with PID {} in worktree {}", ...)` with `log::info!("[Remote] Spawned Claude Code on {} with PID {} in worktree {}", ...)`

- Line 95: Replace `eprintln!("[poll_remote_log] Failed to check log file size: {}", e)` with `log::warn!("[poll_remote_log] Failed to check log file size: {}", e)`

- Line 112: Replace `eprintln!("[poll_remote_log] Failed to read log file: {}", e)` with `log::warn!("[poll_remote_log] Failed to read log file: {}", e)`

- Line 150: Replace `println!("[stream_remote_output] Background task stopped")` with `log::info!("[stream_remote_output] Background task stopped")`

- Line 167: Replace `println!("[Remote] Killed process {} on remote host", handle.remote_pid)` with `log::info!("[Remote] Killed process {} on remote host", handle.remote_pid)`

**4. Replace `println!` with `log::` in `filesystem_handlers.rs` (R15):**

In `src-tauri/src/ipc/filesystem_handlers.rs`, make the following replacements:

- Line 8: Replace `println!("list_local_directories(path={}) called via IPC", path)` with `log::info!("list_local_directories(path={}) called via IPC", path)`

- Line 45: Replace `println!("Found {} subdirectories in {}", directories.len(), path)` with `log::debug!("Found {} subdirectories in {}", directories.len(), path)`

- Line 89: Replace `println!("Found {} drives: {:?}", drives.len(), drives)` with `log::debug!("Found {} drives: {:?}", drives.len(), drives)`

- Line 116: Replace `println!("[Accent] GNOME accent-color: {}", color_name)` with `log::debug!("[Accent] GNOME accent-color: {}", color_name)`

- Line 137: Replace `println!("[Accent] Using fallback blue accent color")` with `log::debug!("[Accent] Using fallback blue accent color")`

- Line 144: Replace `println!("[Accent] macOS accent color not yet implemented, using fallback")` with `log::debug!("[Accent] macOS accent color not yet implemented, using fallback")`

- Line 163: Replace `println!("[Accent] Windows accent color detected via UISettings: RGB({}, {}, {})", ...)` with `log::debug!("[Accent] Windows accent color detected via UISettings: RGB({}, {}, {})", ...)`

- Line 167: Replace `println!("[Accent] Failed to get accent color from UISettings: {:?}, using fallback", e)` with `log::warn!("[Accent] Failed to get accent color from UISettings: {:?}, using fallback", e)`

- Line 173: Replace `println!("[Accent] Failed to create UISettings instance: {:?}, using fallback", e)` with `log::warn!("[Accent] Failed to create UISettings instance: {:?}, using fallback", e)`

Note: The `log` crate is already a dependency (added in Phase 32). No Cargo.toml changes needed.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - execution_handlers.rs does NOT contain `fn detect_error_type_and_suggestions`
    - execution_handlers.rs does NOT contain `fn canonicalize_repo_path`
    - execution_handlers.rs contains `.canonicalize()` inline at the single usage site
    - process/remote.rs does NOT contain `println!`
    - process/remote.rs does NOT contain `eprintln!`
    - process/remote.rs contains `log::info!` and `log::warn!`
    - filesystem_handlers.rs does NOT contain `println!`
    - filesystem_handlers.rs contains `log::info!` and `log::debug!`
    - `cargo check` exits 0
  </acceptance_criteria>
  <done>Dead code removed, single-use function inlined, all println!/eprintln! replaced with log:: crate.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate get_worktree_diff queries into JOIN, remove error.rs stub</name>
  <files>src-tauri/src/ipc/worktree_handlers.rs, src-tauri/src/error.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/ipc/worktree_handlers.rs
    - src-tauri/src/error.rs
    - src-tauri/src/lib.rs
  </read_first>
  <action>
**1. Consolidate `get_worktree_diff` two-query pattern into a single JOIN (R16):**

In `src-tauri/src/ipc/worktree_handlers.rs`, replace the two separate DB queries in `get_worktree_diff` (lines 232-251 — the worktree query and the project query with two separate lock acquisitions) with a single JOIN query:

```rust
// Step 1: Query DB for worktree path, branch_name, and project repo_path via JOIN
let (wt_path, branch_name, repo_path): (String, String, String) = {
    let conn = app_state.db.lock().map_err(|e| format!("Lock failed: {}", e))?;
    conn.query_row(
        "SELECT w.path, w.branch_name, p.path
         FROM worktrees w
         JOIN projects p ON p.id = w.project_id
         WHERE w.id = ?",
        rusqlite::params![worktree_id],
        |row| Ok((row.get(0)?, row.get(1)?, row.get(2)?)),
    )
    .map_err(|e| format!("Worktree {} not found: {}", worktree_id, e))?
};
```

Remove the `project_id` variable that was previously extracted (it's no longer needed). Update the subsequent code that used `repo_path` — it's now available directly from the JOIN result. The `worktree_abs` construction stays the same: `let worktree_abs = format!("{}/{}", repo_path, wt_path);`

Remove the "Step 2: Get project repo_path" block entirely.

**2. Remove `error.rs` stub (R17):**

Delete the file `src-tauri/src/error.rs`. It contains only a comment:
```
// Error handling: All IPC handlers use Result<T, String> for Tauri serialization.
// No custom error type needed — errors are mapped to descriptive strings at each call site.
```

Then check `src-tauri/src/lib.rs` for any `mod error;` or `pub mod error;` declaration and remove it. Check `src-tauri/src/main.rs` as well for any error module reference.

If removing `mod error;` causes a compilation error because something imports from it, check what imports exist:
```
grep -r "crate::error" src-tauri/src/ 2>/dev/null
grep -r "use.*error" src-tauri/src/lib.rs 2>/dev/null
```

If no imports reference it, safe to remove both the file and the module declaration.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - worktree_handlers.rs `get_worktree_diff` function contains `JOIN projects p ON p.id = w.project_id`
    - worktree_handlers.rs `get_worktree_diff` does NOT contain two separate `app_state.db.lock()` calls for the worktree+project query section
    - src-tauri/src/error.rs does NOT exist (file deleted)
    - lib.rs does NOT contain `mod error`
    - `cargo check` exits 0
  </acceptance_criteria>
  <done>get_worktree_diff uses single JOIN query. error.rs removed. Codebase compiles cleanly.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo check` passes
- `cd src-tauri && cargo test` passes
- grep confirms no `println!` in process/remote.rs or filesystem_handlers.rs
- grep confirms no `detect_error_type_and_suggestions` in execution_handlers.rs
- grep confirms no `error.rs` file exists
- grep confirms `JOIN projects` in get_worktree_diff
</verification>

<success_criteria>
- Zero dead code functions (detect_error_type_and_suggestions removed, canonicalize_repo_path inlined)
- Zero println!/eprintln! in process/remote.rs and filesystem_handlers.rs (all replaced with log::)
- get_worktree_diff uses one DB round-trip instead of two
- error.rs removed from the codebase
- cargo check + cargo test pass
</success_criteria>

<output>
After completion, create `.planning/phases/33-tauri-backend-code-review-and-refactoring-for-maintainability-dry-solid-kiss/33-03-SUMMARY.md`
</output>
