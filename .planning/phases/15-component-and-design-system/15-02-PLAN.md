---
phase: 15-component-and-design-system
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/components/Button.tsx
  - src/components/Card.tsx
  - src/components/KanbanBoard.tsx
  - src/components/KanbanColumn.tsx
  - src/components/TaskCard.tsx
  - src/components/TaskModal.tsx
  - src/components/ProjectPicker.tsx
  - src/components/ProjectSettingsModal.tsx
  - src/components/TaskSettingsModal.tsx
  - src/components/ReviewModal.tsx
  - src/components/ApprovalForm.tsx
  - src/components/DiffViewer.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "All component imports changed from custom components to shadcn/ui (@/components/ui/button, etc.)"
    - "No custom Button.tsx, Card.tsx, or Input.tsx components remain (old implementations removed)"
    - "Components using Button now import from @/components/ui/button instead"
    - "Components using Card now import from @/components/ui/card instead"
    - "All form controls use shadcn/ui components (Select, Checkbox, Label, Textarea)"
    - "No TypeScript errors after migration"
    - "Visual regression test: App still launches and renders without crashes"
  artifacts:
    - path: "src/components/KanbanBoard.tsx"
      provides: "Kanban board using shadcn/ui Card for columns"
      contains: 'import.*Card.*from.*@/components/ui/card|<Card'
    - path: "src/components/TaskCard.tsx"
      provides: "Task card using shadcn/ui Card and Badge"
      contains: 'import.*from.*@/components/ui'
    - path: "src/components/ProjectSettingsModal.tsx"
      provides: "Settings modal using shadcn/ui Dialog, Select, Input"
      contains: 'import.*Dialog.*from.*@/components/ui/dialog'
    - path: "src/components/ui/"
      provides: "No old custom component files"
      not_contains: "Button.tsx|Card.tsx|Input.tsx"
  key_links:
    - from: "src/components/*"
      to: "src/components/ui/*"
      via: "Component imports"
      pattern: 'import.*from.*@/components/ui/'
    - from: "src/App.tsx"
      to: "src/components/ProjectSettingsModal.tsx"
      via: "Dialog injection"
      pattern: "ProjectSettingsModal.*Dialog"

---

<objective>
Migrate all component implementations to use shadcn/ui primitives. Replace custom Button, Card, Input components and old form control implementations with shadcn/ui versions across all major components (KanbanBoard, TaskCard, Modals, etc.).

Purpose: Consolidate component library to single source of truth; reduce duplicate styling code; enable consistent theming

Output: All components use shadcn/ui; old custom component implementations removed
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/15-component-and-design-system/15-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/components/KanbanBoard.tsx
@src/components/TaskCard.tsx
@src/components/TaskModal.tsx
@src/components/ProjectSettingsModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Button, Card, Input components across codebase</name>
  <files>
    src/components/KanbanBoard.tsx
    src/components/KanbanColumn.tsx
    src/components/TaskCard.tsx
    src/components/TaskModal.tsx
    src/components/ProjectPicker.tsx
    src/components/ProjectSettingsModal.tsx
  </files>
  <action>
    Search-and-replace component imports in all files using Button, Card, or Input:

    1. Find all files importing custom Button/Card/Input:
       `grep -r "from './Button\|from './Card\|from './Input" src/components --include="*.tsx"`

    2. For each file, replace imports:
       OLD: `import Button from './Button'`
       NEW: `import { Button } from '@/components/ui/button'`

       OLD: `import Card from './Card'`
       NEW: `import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'`

       OLD: `import Input from './Input'`
       NEW: `import { Input } from '@/components/ui/input'`

    3. Update JSX usage to match shadcn/ui prop API:
       - Button: already compatible (onClick, variant, size, disabled)
       - Card: change structure if needed (CardContent for padding, CardHeader for title section)
       - Input: already compatible (placeholder, value, onChange, type, disabled)

    4. Top-priority files (highest impact):
       - src/components/KanbanBoard.tsx (uses Card for columns)
       - src/components/TaskCard.tsx (uses Card for task display)
       - src/components/ProjectSettingsModal.tsx (uses Input, Select for form)
       - src/components/TaskModal.tsx (uses Input, Button, Dialog)

    5. For form controls (Select, Checkbox, Label, Textarea):
       - Replace react-hook-form bindings with shadcn/ui components
       - Verify select options still render correctly
       - Ensure form submission logic unchanged

    6. Run: `pnpm build` after each major component to catch errors early
  </action>
  <verify>
    grep -r "from './Button\|from './Card\|from './Input" src/components --include="*.tsx" | wc -l
    # Should return 0 (no old imports remaining)
  </verify>
  <done>
    All Button, Card, Input imports migrated to @/components/ui/* ; no old custom component imports remain
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete old custom component implementations</name>
  <files>
    src/components/Button.tsx
    src/components/Card.tsx
    src/components/Input.tsx
  </files>
  <action>
    Remove old custom component files after migration completes:

    1. Verify no files still import from deleted components:
       `grep -r "from './Button'\|from './Card'\|from './Input" src/ --include="*.tsx"`
       Should return NOTHING

    2. Delete old files:
       - rm src/components/Button.tsx
       - rm src/components/Card.tsx
       - rm src/components/Input.tsx

    3. If component-specific CSS exists for old Button/Card/Input:
       - Check src/components/Button.css, Card.css, Input.css
       - Delete these CSS files (styling now in Tailwind + shadcn/ui)

    4. Verify no dangling CSS imports:
       `grep -r "import.*Button.css\|import.*Card.css\|import.*Input.css" src/ --include="*.tsx"`
       Should return NOTHING

    5. Run: `pnpm build` to ensure no broken imports
  </action>
  <verify>
    [ ! -f src/components/Button.tsx ] && [ ! -f src/components/Card.tsx ] && [ ! -f src/components/Input.tsx ] && echo "✓ Old files deleted"
  </verify>
  <done>
    Old Button.tsx, Card.tsx, Input.tsx deleted; no orphaned CSS files; build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Update form components with shadcn/ui Select, Checkbox, Textarea, Label</name>
  <files>
    src/components/ProjectSettingsModal.tsx
    src/components/TaskSettingsModal.tsx
    src/components/ApprovalForm.tsx
  </files>
  <action>
    Migrate form controls to shadcn/ui components:

    1. For Select dropdowns (MCP allowlist, Skills, Model selection):
       OLD: `<select {...register("field")}>`
       NEW: `<Select value={...} onValueChange={...}>`
       Use shadcn/ui Select component with SelectTrigger, SelectContent, SelectItem

    2. For Checkboxes (task skill selection, MCP toggles):
       OLD: `<input type="checkbox" {...register("field")} />`
       NEW: `<Checkbox checked={...} onCheckedChange={...} />`
       Wrap with Label from shadcn/ui for accessibility

    3. For Text inputs (task title, description):
       Already migrated via Task 1 (Button, Card, Input)
       Ensure all use shadcn/ui Input component

    4. For Textarea (task context, acceptance criteria):
       OLD: `<textarea {...register("field")}>`
       NEW: `<Textarea {...register("field")} />`
       Use shadcn/ui Textarea with same react-hook-form integration

    5. For form Labels:
       OLD: `<label htmlFor="field">{text}</label>`
       NEW: `<Label htmlFor="field">{text}</Label>`
       Use shadcn/ui Label for consistent styling

    6. Priority files:
       - src/components/ProjectSettingsModal.tsx (most form controls)
       - src/components/TaskSettingsModal.tsx (select, checkbox)
       - src/components/ApprovalForm.tsx (buttons, labels)

    7. Verify form submission logic unchanged (react-hook-form still handles state)
  </action>
  <verify>
    grep -r "from '@/components/ui/select\|from '@/components/ui/checkbox\|from '@/components/ui/textarea" src/components --include="*.tsx" | wc -l | grep -q "[1-9]" && echo "✓ Form components migrated"
  </verify>
  <done>
    All form controls (Select, Checkbox, Textarea, Label) use shadcn/ui; react-hook-form integration preserved
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify app launches without crashes and TypeScript compiles</name>
  <files>src/App.tsx</files>
  <action>
    Full integration test after component migration:

    1. Build TypeScript:
       `pnpm build 2>&1`
       Should complete with zero errors

    2. Check for console warnings/errors from import issues:
       Look for "Cannot find module" or "Import failed" messages

    3. Verify no missing component exports:
       `grep -r "export.*Button\|export.*Card\|export.*Input" src/components/ui --include="*.tsx"`
       Each component should export a React component

    4. Launch dev server (dry-run, don't actually start):
       `pnpm tauri:dev --help 2>&1 | head -5`
       Should show Tauri help (confirms no syntax errors)

    5. Optional: Manual visual test
       - Start app: `pnpm tauri:dev`
       - Navigate to each major page (Kanban, Settings, Task detail)
       - Verify no console errors
       - Click buttons, open modals, check styling consistency
       - Stop server

    6. Summary: All components render, no TypeScript errors, ready for design token setup
  </action>
  <verify>
    pnpm build 2>&1 | grep -i "error\|failed" || echo "✓ Build OK"
  </verify>
  <done>
    TypeScript builds without errors; component imports valid; app ready for theming (Phase 15-03)
  </done>
</task>

</tasks>

<verification>
1. No old custom component imports remain (grep returns 0)
2. All Button, Card, Input, Select, Checkbox, Textarea, Label components use shadcn/ui (@/components/ui/*)
3. Old Button.tsx, Card.tsx, Input.tsx files deleted
4. Old component CSS files deleted
5. `pnpm build` succeeds with zero TypeScript errors
6. All component exports present in src/components/ui/
7. Form components use shadcn/ui with react-hook-form integration preserved
8. Visual regression: App launches without crashes
</verification>

<success_criteria>
- [ ] All Button components import from @/components/ui/button
- [ ] All Card components import from @/components/ui/card
- [ ] All Input components import from @/components/ui/input
- [ ] All Select, Checkbox, Textarea, Label use shadcn/ui versions
- [ ] Old custom component files (Button.tsx, Card.tsx, Input.tsx) deleted
- [ ] Old component CSS files deleted
- [ ] TypeScript builds without errors
- [ ] No import errors in console
- [ ] App launches and renders without crashes
- [ ] Form submission logic unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-component-and-design-system/15-02-SUMMARY.md` documenting:
- Files migrated to shadcn/ui components
- Old custom files deleted (with line counts)
- Import patterns changed (before/after)
- Form control migration details
- Build validation results
- Visual regression test results (if manual test performed)
</output>
