---
phase: 02-core-orchestration
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - package.json
  - src/components/TaskModal.tsx
  - src/components/TaskForm.tsx
  - src/styles/TaskModal.css
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User can click 'New Task' button and modal opens"
    - "Modal has required fields: title, description, acceptance criteria, skills"
    - "User can submit form and task appears in Backlog column"
    - "Modal closes after successful submit"
    - "Form validation prevents submit without all required fields"
    - "Skills field shows multi-select options"
  artifacts:
    - path: "src/components/TaskModal.tsx"
      provides: "Modal dialog container with form integration"
      min_lines: 40
    - path: "src/components/TaskForm.tsx"
      provides: "React Hook Form with required field validation and skills multi-select"
      min_lines: 60
    - path: "src/App.tsx"
      provides: "TaskModal integration with New Task button and board updates"
      exports: ["showNewTaskModal"]
  key_links:
    - from: "src/components/TaskModal.tsx"
      to: "src/components/TaskForm.tsx"
      via: "child component"
      pattern: "TaskForm.*onSubmit"
    - from: "src/components/TaskForm.tsx"
      to: "create_task IPC"
      via: "useForm + handleSubmit"
      pattern: "invoke.*create_task"
    - from: "create_task IPC"
      to: "useBoardStore"
      via: "success callback"
      pattern: "useBoardStore\\.addTask"
    - from: "src/App.tsx"
      to: "TaskModal"
      via: "render condition and callback"
      pattern: "projectSelected.*TaskModal"
---

<objective>
Implement task creation modal UI with form validation and skills multi-select.

Purpose: Enable users to create tasks with full context (title, description, acceptance criteria, skills) via modal dialog with form validation and automatic Kanban board updates.

Output: TaskModal and TaskForm components with React Hook Form, Radix Dialog, skills multi-select field, and App.tsx integration.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-orchestration/02-CONTEXT.md
@.planning/phases/02-core-orchestration/02-RESEARCH.md

# Prior plan context
@.planning/phases/02-core-orchestration/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add form and dialog libraries to package.json</name>
  <files>package.json</files>
  <action>
    1. Add dependencies to package.json:
       - react-hook-form: ^7.50.0
       - @radix-ui/react-dialog: ^1.1.0
       - @radix-ui/react-select: ^2.0.0 (for skills multi-select)
    2. Run npm install

    Note: React Hook Form is headless (minimal re-renders), Radix Dialog and Select provide accessible WAI-ARIA primitives.
  </action>
  <verify>
    npm list react-hook-form @radix-ui/react-dialog @radix-ui/react-select
  </verify>
  <done>Form and dialog libraries installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskForm component with React Hook Form and skills multi-select</name>
  <files>src/components/TaskForm.tsx</files>
  <action>
    1. Create src/components/TaskForm.tsx:
       - Import useForm, SubmitHandler from react-hook-form
       - Import Select components from @radix-ui/react-select
       - Define interface: TaskFormData = { title: string, description: string, acceptanceCriteria: string, skills: string[] }
       - Create TaskForm component accepting props: { onSubmit: (data) => Promise<void>, isLoading: boolean, onCancel: () => void }
       - Initialize useForm with validation rules:
         * title: required, minLength 3, maxLength 100
         * description: required, minLength 10
         * acceptanceCriteria: required, minLength 10
         * skills: optional (default empty array)
       - Mode: 'onBlur' for efficient validation
       - Render form fields with error display:
         * Input: title (type="text")
         * Textarea: description
         * Textarea: acceptanceCriteria
         * Multi-select: skills (Radix Select with available options, optional)
         * Button: Submit (disabled during isLoading)
         * Button: Cancel
       - On submit: convert form data to match CreateTaskRequest interface, call onSubmit(data), handle errors

    2. Available skills options (hardcoded for now):
       - "debugging", "testing", "documentation", "performance", "security", "refactoring"

    Note: React Hook Form stores field state separately. Skills array can be empty if user doesn't select any.
  </action>
  <verify>
    grep -n "useForm\|SubmitHandler\|acceptanceCriteria" src/components/TaskForm.tsx
    grep -n "skills" src/components/TaskForm.tsx
    grep -n "disabled.*isLoading" src/components/TaskForm.tsx
  </verify>
  <done>TaskForm component created with React Hook Form, required field validation, and skills multi-select field.</done>
</task>

<task type="auto">
  <name>Task 3: Create TaskModal component with Radix Dialog wrapper</name>
  <files>src/components/TaskModal.tsx, src/styles/TaskModal.css</files>
  <action>
    1. Create src/components/TaskModal.tsx:
       - Import Dialog components from @radix-ui/react-dialog
       - Import TaskForm component
       - Create TaskModal accepting props: { isOpen: boolean, onClose: () => void, projectId: number, onTaskCreated: (task: Task) => void }
       - Render Dialog.Root with open={isOpen} onOpenChange={onClose}
       - Inside Dialog.Content:
         * Dialog.Title: "Create New Task"
         * Dialog.Description: "Enter task details (title, description, acceptance criteria required)"
         * Render TaskForm with:
           - onSubmit handler that calls invoke('create_task', {...data})
           - onCancel that calls onClose()
           - isLoading state during submit
           - Show error toast on failure
       - On success: call onTaskCreated(task), invoke useBoardStore.addTask, close modal

    2. Create src/styles/TaskModal.css:
       - Dialog overlay: semi-transparent black background
       - Dialog content: white box, centered, max-width 500px
       - Form fields: full width, padding, border styling
       - Multi-select: Radix Select styling (dropdown, options)
       - Buttons: flex layout, gap between Submit/Cancel
       - Focus management via Radix (automatic)
       - Z-index high enough to overlay Kanban board

    Note: Radix Dialog handles focus trap, Esc to close, keyboard navigation automatically.
  </action>
  <verify>
    grep -n "Dialog\|DialogContent\|TaskForm" src/components/TaskModal.tsx
    grep -n "overlay\|dialog-content" src/styles/TaskModal.css
    grep "skills" src/components/TaskModal.tsx
  </verify>
  <done>TaskModal component created with Radix Dialog primitives, form integration with skills field, and error handling.</done>
</task>

<task type="auto">
  <name>Task 4: Wire TaskModal into App.tsx with New Task button</name>
  <files>src/App.tsx</files>
  <action>
    1. Update src/App.tsx:
       - Import TaskModal component
       - Import useBoardStore from store
       - Add state: const [showNewTaskModal, setShowNewTaskModal] = useState(false)
       - After project selection, render:
         * "New Task" button that calls setShowNewTaskModal(true)
         * TaskModal with props: { isOpen: showNewTaskModal, onClose: () => setShowNewTaskModal(false), projectId: settings.project_id, onTaskCreated: handleTaskCreated }
       - Implement handleTaskCreated callback:
         * Call useBoardStore.addTask(newTask) to update board immediately
         * Or re-fetch tasks via get_tasks IPC
         * Close modal automatically (already handled by onTaskCreated)
    2. Verify the modal button is visible and clickable in the app header/navbar area

    Note: TaskModal state stays in App for global access. When task created, board auto-updates via Zustand store.
  </action>
  <verify>
    grep -n "TaskModal\|showNewTaskModal\|New Task" src/App.tsx
    grep -n "onTaskCreated\|useBoardStore.addTask" src/App.tsx
  </verify>
  <done>TaskModal wired into App.tsx, "New Task" button visible, modal submission updates board state with skills support.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task creation modal with form validation, skills multi-select field, and database persistence. Users can create tasks via modal dialog with required fields (title, description, acceptance criteria) and optional skills assignment.
  </what-built>
  <how-to-verify>
    1. Start app: npm run dev + cargo tauri dev
    2. Select project
    3. Verify "New Task" button is visible in app header
    4. Click "New Task" button
    5. Modal should open with 4 form fields:
       - Title (text input)
       - Description (textarea)
       - Acceptance Criteria (textarea)
       - Skills (multi-select dropdown with options like debugging, testing, documentation, etc.)
       - Submit button (initially enabled)
       - Cancel button
    6. Try submitting with empty required fields - should show validation errors
    7. Fill all required fields with valid text
    8. Select one or more skills (e.g., "testing", "documentation")
    9. Click Submit
    10. Modal should close
    11. Verify new task appears in Backlog column with correct title
    12. Inspect task in database or dev tools to verify skills stored correctly
    13. Refresh page - task should persist with skills intact
    14. Check browser console for errors (should be clean)
  </how-to-verify>
  <resume-signal>Type "approved" if task creation works end-to-end (modal opens, validation works, skills field functional, task appears in Backlog with skills), or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. npm run build succeeds without errors
2. cargo tauri dev builds without warnings
3. TaskModal component renders and is visible
4. Form validation works (prevents submit with invalid data)
5. Skills multi-select field renders and accepts input
6. Task creation via IPC works (new task in database with skills)
7. New task appears in Backlog column immediately with skills preserved
8. Task persists after page refresh with skills data intact
9. Modal closes after successful submit
10. Browser console is error-free
11. TypeScript has no type errors
</verification>

<success_criteria>
- Modal dialog opens on "New Task" button click
- Form has all 4 fields (title, description, acceptance criteria, skills)
- Form validation prevents submission without required fields (title, description, acceptance_criteria)
- Skills field is multi-select, optional (can be empty)
- Successful submission creates task in Backlog column with skills array
- Task persists to database via create_task IPC handler with skills
- Modal closes automatically after submit
- Form state resets for next task creation
- Validation error messages displayed clearly
- Keyboard navigation works (Tab, Esc to close)
- ORCH-07 requirement satisfied: Skills field creates Task with skills field
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-orchestration/02-03-SUMMARY.md`
</output>
