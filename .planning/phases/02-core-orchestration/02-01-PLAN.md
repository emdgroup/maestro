---
phase: 02-core-orchestration
plan: 01
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - package.json
  - src/types/bindings.ts
  - src/store/boardStore.ts
  - src/components/KanbanBoard.tsx
  - src/components/KanbanColumn.tsx
  - src/components/TaskCard.tsx
  - src/styles/KanbanBoard.css
autonomous: true

must_haves:
  truths:
    - "User can see Kanban board with 5 columns (Backlog, Ready, In Progress, Review, Done)"
    - "Tasks from database appear in their respective columns"
    - "Column headers display task count (e.g., 'Backlog (3)')"
    - "User can drag tasks between columns and drop successfully"
    - "Task status persists in database after drop"
    - "Invalid column transitions are prevented with visual feedback"
  artifacts:
    - path: "src/store/boardStore.ts"
      provides: "Zustand state management for board tasks, grouping by status"
      exports: ["useBoardStore", "BoardState"]
    - path: "src/components/KanbanBoard.tsx"
      provides: "Main board orchestration with React Beautiful DnD wrapper"
      min_lines: 60
    - path: "src/components/KanbanColumn.tsx"
      provides: "Column rendering with drop zone and task list"
      min_lines: 50
    - path: "src/components/TaskCard.tsx"
      provides: "Individual task card with title and status indicator"
      min_lines: 30
  key_links:
    - from: "src/components/KanbanBoard.tsx"
      to: "src/store/boardStore.ts"
      via: "useBoardStore hook"
      pattern: "useBoardStore\\(\\)"
    - from: "src/components/KanbanBoard.tsx"
      to: "get_tasks IPC"
      via: "useEffect on mount"
      pattern: "invoke.*get_tasks"
    - from: "src/components/KanbanBoard.tsx"
      to: "update_task IPC"
      via: "onDragEnd handler"
      pattern: "invoke.*update_task"
    - from: "React Beautiful DnD"
      to: "KanbanColumn.tsx"
      via: "Droppable wrapper"
      pattern: "Droppable.*droppableId"
---

<objective>
Implement Kanban board UI with 5 columns, drag-drop functionality, and database-backed state management.

Purpose: Enable users to visualize tasks organized by status and move tasks between workflow columns with real-time persistence.

Output: Complete Kanban board component hierarchy with Zustand state management, React Beautiful DnD integration, and IPC handlers for task status updates.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-orchestration/02-CONTEXT.md
@.planning/phases/02-core-orchestration/02-RESEARCH.md

# Phase 1 context for patterns
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Kanban libraries and update TypeScript bindings</name>
  <files>package.json, src/types/bindings.ts</files>
  <action>
    1. Add dependencies to package.json:
       - react-beautiful-dnd: ^13.1.1
       - zustand: ^4.5.0
       - immer: ^10.0.0
    2. Run npm install
    3. Update TaskStatus enum in src/types/bindings.ts to include: 'backlog' | 'ready' | 'in_progress' | 'review' | 'done'
    4. Add new Task fields from Phase 2 research: acceptance_criteria, external_id, is_imported, import_source, skills (all optional for existing tasks)
    5. Regenerate ts-rs bindings by building Rust: cargo build -p maestro (should auto-generate with ts-rs 7.1)

    Note: These are the core libraries from Phase 2 research. React Beautiful DnD provides drag-drop, Zustand provides state management, Immer enables immutable updates.
    File ownership: This plan owns the base Task type fields in bindings.ts. Plans 02-02 and 02-03 add form/IPC fields as needed but do not re-modify bindings.ts Task definition.
  </action>
  <verify>
    npm list react-beautiful-dnd zustand immer
    cat src/types/bindings.ts | grep -E "backlog|ready|in_progress|review|done"
    cat package.json | grep -E "react-beautiful-dnd|zustand|immer"
  </verify>
  <done>All dependencies installed, TaskStatus enum includes all 5 column states, TypeScript bindings updated with new Task fields.</done>
</task>

<task type="auto">
  <name>Task 2: Create Zustand board state store with task management</name>
  <files>src/store/boardStore.ts</files>
  <action>
    1. Create src/store/ directory
    2. Create boardStore.ts with Zustand + Immer middleware:
       - Define BoardState interface with: tasks: Task[], groupedByStatus: () mapping
       - Implement state hooks:
         * loadTasks(tasks: Task[]) - bulk load from IPC
         * updateTaskStatus(taskId: number, newStatus: TaskStatus) - atomic update
         * addTask(task: Task) - add new task to store
         * getTasks() - getter
         * getTasksByStatus(status: TaskStatus) - filtered getter for columns
    3. Use Immer for mutable updates: set((state) => { state.tasks.push(...) })
    4. Export useBoardStore hook for React components

    Pattern reference: From Phase 2 research section 1.2 - Zustand with Immer middleware allows clean immutable updates without boilerplate.
  </action>
  <verify>
    cat src/store/boardStore.ts | grep -E "create|immer|useBoardStore"
    npm test 2>&1 | grep -i "cannot find module.*boardStore" || echo "Module imports successful"
  </verify>
  <done>Zustand store created with Immer middleware, all state methods implemented, store exports useBoardStore hook.</done>
</task>

<task type="auto">
  <name>Task 3: Build Kanban board component hierarchy with React Beautiful DnD</name>
  <files>src/components/KanbanBoard.tsx, src/components/KanbanColumn.tsx, src/components/TaskCard.tsx, src/styles/KanbanBoard.css</files>
  <action>
    1. Create KanbanBoard.tsx (main component):
       - Import DragDropContext, Droppable from react-beautiful-dnd
       - Import useBoardStore and useEffect from React
       - On mount: invoke('get_tasks', { project_id }) → useBoardStore.loadTasks()
       - Render 5 columns: Backlog, Ready, In Progress, Review, Done (iterate via TaskStatus enum)
       - Wrap columns in DragDropContext with onDragEnd handler:
         * Check validation: allow free movement between Backlog/Ready (user controls)
         * Other columns managed by agent (prevent drag from Review/Done)
         * Call invoke('update_task', { task_id, status: newStatus })
         * Update local store on success
         * Show error toast on validation failure, revert card
       - Pass tasks to KanbanColumn via getTasksByStatus(status)

    2. Create KanbanColumn.tsx (column container):
       - Accept: columnId (string), columnTitle (string), tasks (Task[])
       - Render Droppable wrapper with droppableId={columnId}
       - Render column header with count: "{columnTitle} ({tasks.length})"
       - Render TaskCard for each task in tasks array
       - Apply CSS class for drop zone visual feedback (dim on invalid drop)

    3. Create TaskCard.tsx (task item):
       - Import Draggable from react-beautiful-dnd
       - Accept: task (Task), index (number)
       - Render Draggable wrapper with draggableId={task.id.toString()}
       - Display: task.name (title only, no description preview per spec)
       - Add status badge (e.g., icon or color) if needed
       - Show read-only indicator if task.is_imported

    4. Create KanbanBoard.css (layout and styling):
       - CSS Grid: grid-template-columns: repeat(5, 1fr) for 5 equal columns
       - All columns fit viewport: height: calc(100vh - 60px), columns squeeze on small screens
       - Column containers: flex column, overflow-y: auto for internal scrolling
       - TaskCard: box styling with shadow, hover effects
       - Drag preview: semi-transparent during drag (React Beautiful DnD built-in)
       - Invalid drop zones: dim opacity during drag if drop not allowed
       - Column header: bold font, display count in parentheses

    Reference: From Phase 2 research section 1.1 and 1.3 - React Beautiful DnD handles drop validation, CSS Grid ensures all columns fit without scroll.
  </action>
  <verify>
    # Check component structure
    grep -l "DragDropContext\|Droppable\|Draggable" src/components/Kanban*.tsx

    # Verify imports
    grep "import.*react-beautiful-dnd" src/components/KanbanBoard.tsx

    # Check styling
    cat src/styles/KanbanBoard.css | grep "grid-template-columns\|repeat(5"

    # Verify store usage
    grep "useBoardStore" src/components/KanbanBoard.tsx
  </verify>
  <done>
    KanbanBoard, KanbanColumn, and TaskCard components created and integrated with React Beautiful DnD.
    CSS Grid layout ensures all 5 columns fit viewport without horizontal scroll.
    Drag-drop handlers implemented with validation (Backlog/Ready free, others agent-managed).
    Store integration complete: get_tasks on mount, update_task on drop.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Kanban board UI with 5 columns, task drag-drop between columns with persistence, and Zustand state management.
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev in root, cargo tauri dev in another terminal
    2. Select a project (or use existing project path)
    3. Verify the board renders with 5 columns visible:
       - Columns: Backlog, Ready, In Progress, Review, Done
       - All columns fit on screen without horizontal scroll
       - Column headers show task count (e.g., "Backlog (0)")
    4. Create a test task using database directly (or via Phase 2-02 modal when complete):
       INSERT INTO tasks (project_id, name, description, status, created_at, updated_at) VALUES (1, 'Test', 'Desc', 'backlog', datetime('now'), datetime('now'));
    5. Refresh the board (F5 or close/reopen)
    6. Verify test task appears in Backlog column
    7. Drag task to Ready column
    8. Verify task moves and stays in Ready after page refresh
    9. Verify drag feedback (card dims when dragging, invalid drops show visual hint)
    10. Check browser console for errors (should be clean)
  </how-to-verify>
  <resume-signal>Type "approved" if board renders correctly and drag-drop persists, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. npm run build succeeds without errors
2. cargo tauri dev starts without warnings
3. Kanban board renders with all 5 columns visible
4. Drag-drop works between columns (Backlog/Ready free, others blocked)
5. Task status persists after refresh
6. Browser console is error-free
7. TypeScript has no type errors
</verification>

<success_criteria>
- Kanban board UI displays 5 columns with task counts
- Tasks persist status changes to database via IPC
- Drag-drop functionality works with validation (free movement Backlog↔Ready, others agent-managed)
- All columns fit viewport without horizontal scroll
- Component hierarchy is clean: KanbanBoard > KanbanColumn > TaskCard
- State management via Zustand with Immer enables clean mutations
- React Beautiful DnD integration provides smooth drag feedback and accessibility
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-orchestration/02-01-SUMMARY.md`
</output>
