---
phase: 13-bug-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - CLAUDE.md
  - src-tauri/src/error.rs
  - src/lib/tauri-mock.ts
autonomous: true

must_haves:
  truths:
    - "Development patterns documented for future maintainers"
    - "Code comments explain mock exclusion and warning fixes"
    - "No future developer surprises from removed dead code"

  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated project conventions section"
      contains: "Pattern: Build-time mock exclusion with import.meta.env.DEV"
    - path: "src/lib/tauri-mock.ts"
      provides: "Annotated mock code with build strategy"
      contains: "// Mock code gate: removed in production via import.meta.env.DEV"
    - path: "src-tauri/src/error.rs"
      provides: "Explanation for removed SSH functions"
      contains: "// SSH functions removed - not used in v1.0"

  key_links:
    - from: "CLAUDE.md"
      to: "src/lib/tauri-mock.ts"
      via: "reference documentation"
      pattern: "mock exclusion.*import.meta.env.DEV"
    - from: "source code"
      to: "future developers"
      via: "code comments"
      pattern: "// Build-time|// SSH functions"

---

<objective>
Document the mock exclusion pattern and Rust warning fixes so future maintainers understand the approach and don't accidentally revert these changes.

Purpose: Prevent regression by making the build-time mock exclusion pattern clear in documentation. Ensure future work on SSH features doesn't re-add the dead code we removed.

Output:
- Updated CLAUDE.md with mock exclusion pattern documented
- Code comments explaining removed functions and conditional imports
- Clear reference for future development
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-bug-fixes/13-01-PLAN.md
@.planning/phases/13-bug-fixes/13-CONTEXT.md
@.planning/phases/13-bug-fixes/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document mock exclusion pattern in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Add a new section to CLAUDE.md documenting the build-time mock exclusion pattern so future developers understand the approach:

1. Open CLAUDE.md and find the "## Key Patterns" section (around line 92)

2. Add a new subsection after "Tauri State" section:
   ```markdown
   ### Build-Time Mock Exclusion (Development vs Production)

   **Pattern:** Use `import.meta.env.DEV` to gate development-only code that gets tree-shaken by Vite in production builds.

   **Location:** `src/lib/tauri-mock.ts` and any imports in `src/main.ts`

   **Why this matters:** Development uses mock IPC handlers to test without Tauri runtime. Production builds must exclude all mock code to avoid bundling unnecessary code.

   **Implementation:**
   - All mock invoke functions and mockDB are wrapped in `if (import.meta.env.DEV) { ... }`
   - Vite statically replaces `import.meta.env.DEV` with `true` during dev, `false` during production build
   - Tree-shaking removes `if (false)` branches entirely from production bundle
   - Real Tauri fallback outside the DEV check always available for production

   **Do NOT use runtime checks** like `if (isTauri) { real } else { mock }` — this includes mock code in production bundle because both branches are reachable at runtime.

   **Verification:** Bundle analysis script (`scripts/verify-bundle.mjs`) runs after every production build and fails if mock markers found.

   **Reference:** Phase 13 Bug Fixes (v1.1) — fixed mock leak by implementing this pattern.
   ```

3. Save the file

4. Why: Future developers need to understand why the mock code is structured this way. If someone wants to add new dev features, they'll know to use `import.meta.env.DEV` instead of runtime checks.

  </action>
  <verify>
- CLAUDE.md contains the new subsection
- Section appears under "## Key Patterns"
- Mentions `import.meta.env.DEV` and tree-shaking
- References Phase 13 as context
- Explains why runtime checks are avoided
  </verify>
  <done>
- "Build-Time Mock Exclusion" subsection added to CLAUDE.md under Key Patterns
- Explains Vite tree-shaking mechanism
- Clearly states why runtime checks should not be used
- References bundle analysis verification
- Provides future reference for similar development patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explanatory comments to mock code and removed functions</name>
  <files>
    src/lib/tauri-mock.ts
    src-tauri/src/error.rs
  </files>
  <action>
Add inline code comments explaining the build-time exclusion and dead code removal:

1. In src/lib/tauri-mock.ts:
   - Locate the line: `if (import.meta.env.DEV) {`
   - Add comment above it:
     ```typescript
     // Build-time mock exclusion: This entire block is removed from production via Vite tree-shaking.
     // Vite replaces import.meta.env.DEV with 'false' during production build, and tree-shaking
     // removes the unreachable if (false) branch. This prevents mock code bloat in release builds.
     if (import.meta.env.DEV) {
     ```
   - Locate the real invoke function (outside DEV block)
   - Add comment:
     ```typescript
     // Production fallback: Always available for production use when Tauri is not available.
     export async function invoke<T>(cmd: string, args?: Record<string, any>): Promise<T> {
     ```

2. In src-tauri/src/error.rs:
   - Find where functions were removed (grep for "is_retriable_error" or "calculate_key_fingerprint")
   - If any comments remain suggesting these functions, clarify:
     ```rust
     // SSH helper functions removed in Phase 13 Bug Fixes (v1.1).
     // Not used in v1.0 codebase. When SSH auth is enhanced in future phases,
     // these can be re-implemented from VCS history if needed.
     // Reference: .planning/phases/13-bug-fixes/13-RESEARCH.md
     ```

3. Save both files

4. Why: Comments prevent confusion about why code was removed. Future developers can understand the decision without reading phase documentation.

  </action>
  <verify>
- src/lib/tauri-mock.ts contains comment about Vite tree-shaking above the if (import.meta.env.DEV) line
- Comment explains why build-time exclusion is used instead of runtime checks
- src-tauri/src/error.rs (or relevant file) has comment about removed SSH functions
- Both comments reference Phase 13 for future context
  </verify>
  <done>
- Comment added to src/lib/tauri-mock.ts explaining Vite tree-shaking and mock exclusion
- Comment added to production fallback invoke explaining production use
- Comment added to src-tauri/src/error.rs explaining removed SSH functions and Phase 13 reference
- Comments are clear enough for future developers to understand decisions without reading research docs
  </done>
</task>

<task type="auto">
  <name>Task 3: Create phase summary documenting verification approach</name>
  <files></files>
  <action>
This is a checkpoint task - no files created by this task directly. The checkpoint handler will capture verification results. However, document the key findings:

1. Create a mental summary (for the SUMMARY.md that follows):
   - Two bugs fixed: Mock IPC leak and Rust warnings
   - Verification approach: Automated bundle analysis + manual Rust build check
   - Regression prevention: Bundle verification integrated into build script
   - Documentation: Code comments and CLAUDE.md updated for future reference

2. This forms the basis for the SUMMARY.md that will be created after human verification

3. Key points to track:
   - Mock exclusion strategy: import.meta.env.DEV gates with Vite tree-shaking
   - Rust warning resolution: cargo fix auto-fixed 11, manual removed 4 dead code functions
   - Test plan: dev mode works, production bundle clean, bundle verification catches regressions
   - Documentation: CLAUDE.md updated, code comments added, pattern clear for future work

  </action>
  <verify>
Review checklist for summary:
- [ ] CLAUDE.md has new subsection on Build-Time Mock Exclusion
- [ ] Code comments in src/lib/tauri-mock.ts explain DEV check
- [ ] Code comments in src-tauri/src/error.rs explain removed functions
- [ ] All comments reference Phase 13 for traceability
- [ ] Future developers can understand decisions without external documentation
  </verify>
  <done>
- Documentation approach planned and ready for SUMMARY.md creation
- Code comments added to key files explaining decisions
- CLAUDE.md updated with pattern reference
- Future developers have clear guidance on mock exclusion pattern
- All changes traceable to Phase 13 Bug Fixes
  </done>
</task>

</tasks>

<verification>
Phase 13 documentation verification:

1. **CLAUDE.md Updated:**
   - New "Build-Time Mock Exclusion" subsection in Key Patterns
   - Explains import.meta.env.DEV mechanism
   - Warns against runtime checks
   - References Phase 13

2. **Code Comments Added:**
   - src/lib/tauri-mock.ts has comment explaining tree-shaking above if (import.meta.env.DEV)
   - src-tauri/src/error.rs has comment explaining removed SSH functions
   - Both comments reference Phase 13

3. **Future Developer Clarity:**
   - Someone can read CLAUDE.md and understand why mock code is structured this way
   - Someone can grep for "import.meta.env.DEV" and find the pattern explained in comments
   - Someone reviewing Phase 13 changes sees clear rationale in comments

</verification>

<success_criteria>
Phase 13 documentation is complete when:

✓ CLAUDE.md documents the Build-Time Mock Exclusion pattern
  - Explains why import.meta.env.DEV is used
  - Contrasts with runtime checks (which wouldn't work)
  - References Phase 13 for traceability

✓ Code comments explain key decisions
  - Mock exclusion comment in tauri-mock.ts
  - Dead code removal comment in error.rs
  - Comments reference Phase 13 and are clear without external docs

✓ Future development has clear guidance
  - Developer adding dev features will see the pattern
  - Developer working on SSH features knows not to re-add dead code
  - All decisions traceable back to research and rationale

</success_criteria>

<output>
After completion, update `.planning/phases/13-bug-fixes/13-01-SUMMARY.md`

Add to existing summary:
- Documentation updates (CLAUDE.md, code comments)
- Pattern reference for future development
- Regression prevention through documentation clarity
</output>

