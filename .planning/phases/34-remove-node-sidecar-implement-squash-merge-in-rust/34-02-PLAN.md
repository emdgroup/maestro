---
phase: 34-remove-node-sidecar-implement-squash-merge-in-rust
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src-tauri/src/ipc/execution_handlers.rs
  - src-tauri/src/process/spawner.rs
  - src-tauri/src/process/mod.rs
  - src-tauri/src/models/merge_outcome.rs
  - src-tauri/src/models/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/ipc/review_handlers.rs
autonomous: true
requirements:
  - SM-03
  - SM-04
  - SM-05

must_haves:
  truths:
    - "No dead sidecar code remains in the Rust backend"
    - "sidecar/ directory is completely removed from the repository"
    - "No sidecar references remain in Rust comments or doc strings"
    - "cargo check still passes after all deletions"
    - "spawn_agent_execution IPC command is removed from Tauri command registration"
  artifacts:
    - path: "src-tauri/src/git/mod.rs"
      provides: "squash_merge_to_main (unchanged from Plan 01)"
      contains: "pub async fn squash_merge_to_main"
    - path: "src-tauri/src/lib.rs"
      provides: "Updated command registration without spawn_agent_execution"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/process/mod.rs"
      via: "pub use — spawn_agent_cli removed"
      pattern: "pub use process"
---

<objective>
Delete all dead sidecar code and remove the sidecar directory entirely.

Purpose: Complete the sidecar removal by cleaning up dead code (run_agent_background_task, spawn_agent_cli, spawn_agent_execution IPC, MergeOutcome model) and deleting the sidecar/ directory. After this plan, no trace of the Node.js sidecar exists in the codebase.
Output: Clean codebase with no sidecar artifacts, no dead code, no stale comments.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-remove-node-sidecar-implement-squash-merge-in-rust/34-CONTEXT.md
@.planning/phases/34-remove-node-sidecar-implement-squash-merge-in-rust/34-01-SUMMARY.md

<interfaces>
<!-- What needs deleting and what needs preserving -->

PRESERVE — src-tauri/src/process/mod.rs still needs:
```rust
pub mod pty;
pub mod remote;
pub use pty::{spawn_agent_cli_pty, PtySession};
pub use remote::{spawn_remote_agent_execution, RemoteProcessHandle, ExecutionConfig};
```
The `spawn_agent_execution` function in process/mod.rs (the dispatcher, NOT the IPC command) should be preserved — it routes local/remote agent execution and is separate from the dead sidecar code.

PRESERVE — src-tauri/src/process/spawner.rs contains ProcessOutput struct which is used by process/mod.rs and process/remote.rs. The struct MUST be kept even though spawn_agent_cli is deleted.

DELETE — from execution_handlers.rs:
- `run_agent_background_task` function (lines 8-58)
- `spawn_agent_execution` IPC command (lines 60-106) — frontend does NOT call this (verified: only bindings.ts auto-generated reference)

DELETE — from spawner.rs:
- `spawn_agent_cli` function (lines 24-106) — only caller was run_agent_background_task

DELETE — from models/:
- `merge_outcome.rs` file entirely
- `pub mod merge_outcome;` from mod.rs
- `pub use merge_outcome::MergeOutcome;` from mod.rs

DELETE — from lib.rs:
- `spawn_agent_cli` from `pub use process::` line
- `crate::ipc::spawn_agent_execution` from collect_commands![]
- `MergeOutcome` is not in the pub use models:: line (it's a separate line), but verify

DELETE — entire sidecar/ directory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead sidecar code from Rust backend</name>
  <files>
    src-tauri/src/ipc/execution_handlers.rs,
    src-tauri/src/process/spawner.rs,
    src-tauri/src/process/mod.rs,
    src-tauri/src/models/merge_outcome.rs,
    src-tauri/src/models/mod.rs,
    src-tauri/src/lib.rs
  </files>
  <read_first>
    - src-tauri/src/ipc/execution_handlers.rs (lines 1-106 — the dead code block)
    - src-tauri/src/process/spawner.rs (entire file — delete spawn_agent_cli but keep ProcessOutput)
    - src-tauri/src/process/mod.rs (pub use lines — remove spawn_agent_cli export, keep spawner module for ProcessOutput)
    - src-tauri/src/models/mod.rs (find merge_outcome lines to remove)
    - src-tauri/src/models/merge_outcome.rs (entire file to be deleted)
    - src-tauri/src/lib.rs (find spawn_agent_cli in pub use, spawn_agent_execution in collect_commands!)
    - src-tauri/src/process/remote.rs (verify it uses ProcessOutput — if so, spawner.rs module must remain)
  </read_first>
  <action>
Make these specific deletions across 6 files:

**A. src-tauri/src/ipc/execution_handlers.rs:**
- Delete the entire `run_agent_background_task` async function (lines 8-58)
- Delete the entire `spawn_agent_execution` IPC command function (lines 60-106, including the `#[tauri::command]` and `#[specta::specta]` attributes and doc comment)
- Keep everything else (drain_ready_queue and all functions below line 106)

**B. src-tauri/src/process/spawner.rs:**
- Delete the `spawn_agent_cli` async function (lines 24-106, including doc comment at lines 15-23)
- Keep the `ProcessOutput` struct (lines 6-13) and the `use` imports it needs (`std::process::Stdio` can be removed since only spawn_agent_cli used it; `tokio::io::{AsyncBufReadExt, BufReader}` can also be removed; `tokio::process::Command` can be removed)
- After deletion, spawner.rs should contain only:
```rust
#[derive(Debug, Clone)]
pub struct ProcessOutput {
    pub stdout: String,
    pub stderr: String,
    pub exit_code: i32,
    pub success: bool,
    pub remote_pid: Option<u32>,
    pub is_remote: bool,
}
```

**C. src-tauri/src/process/mod.rs:**
- Change `pub use spawner::{spawn_agent_cli, ProcessOutput};` to `pub use spawner::ProcessOutput;`
- Keep everything else (pub mod spawner, pub mod pty, pub mod remote, the spawn_agent_execution dispatcher function)

**D. src-tauri/src/models/merge_outcome.rs:**
- Delete this entire file

**E. src-tauri/src/models/mod.rs:**
- Remove the line `pub mod merge_outcome;`
- Remove the line `pub use merge_outcome::MergeOutcome;`
- Keep all other pub mod and pub use lines

**F. src-tauri/src/lib.rs:**
- In the `pub use process::` line (line 11), remove `spawn_agent_cli` — change:
  `pub use process::{spawn_agent_cli, ProcessOutput, spawn_agent_cli_pty, PtySession};`
  to:
  `pub use process::{ProcessOutput, spawn_agent_cli_pty, PtySession};`
- In the `collect_commands![]` macro (line 19), remove the line:
  `crate::ipc::spawn_agent_execution,`
- In the `pub use models::` line (line 10), verify `MergeOutcome` is not there (it was on a separate line). If there's a standalone `pub use models::MergeOutcome;` or similar, remove it.

After all deletions, run `cargo check` to verify no dangling references.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/ipc/execution_handlers.rs does NOT contain `run_agent_background_task`
    - src-tauri/src/ipc/execution_handlers.rs does NOT contain `fn spawn_agent_execution(`
    - src-tauri/src/process/spawner.rs does NOT contain `fn spawn_agent_cli(`
    - src-tauri/src/process/spawner.rs contains `pub struct ProcessOutput`
    - src-tauri/src/process/mod.rs does NOT contain `spawn_agent_cli`
    - src-tauri/src/models/merge_outcome.rs does not exist
    - src-tauri/src/models/mod.rs does NOT contain `merge_outcome`
    - src-tauri/src/models/mod.rs does NOT contain `MergeOutcome`
    - src-tauri/src/lib.rs does NOT contain `spawn_agent_cli`
    - src-tauri/src/lib.rs does NOT contain `spawn_agent_execution`
    - cargo check exits 0
  </acceptance_criteria>
  <done>All dead sidecar code removed: run_agent_background_task, spawn_agent_cli, spawn_agent_execution IPC, MergeOutcome model. ProcessOutput struct preserved. cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove sidecar directory and clean up stale references</name>
  <files>
    sidecar/ (deleted),
    src-tauri/src/ipc/review_handlers.rs,
    src-tauri/src/ipc/execution_handlers.rs
  </files>
  <read_first>
    - src-tauri/src/ipc/review_handlers.rs (check for any remaining "sidecar" mentions in comments)
    - src-tauri/src/ipc/execution_handlers.rs (check for any remaining "sidecar" mentions)
    - src-tauri/src/process/mod.rs (check for sidecar mentions in comments)
  </read_first>
  <action>
**A. Delete the sidecar directory:**
```bash
rm -rf sidecar/
```
This removes `sidecar/` entirely: `src/`, `dist/`, `node_modules/`, `package.json`, `package-lock.json`, `tsconfig.json`, and `SIDECAR-RESEARCH.md`.

**B. Clean up stale sidecar references in Rust comments/strings:**

Run `grep -rn "sidecar" src-tauri/src/` to find ALL remaining references. Based on pre-analysis, the following should be cleaned:

1. `src-tauri/src/ipc/review_handlers.rs` — If Plan 01 Task 2 left any doc comments mentioning "sidecar" beyond the one already fixed, update them. The doc comment for `approve_task_and_merge` should now say "native Rust squash merge via git subprocess" not "Node.js sidecar".

2. `src-tauri/src/ipc/execution_handlers.rs` — After Task 1 deleted the dead functions, verify no remaining comments mention "sidecar". If `drain_ready_queue` or other remaining functions have doc comments mentioning sidecar, update them.

3. `src-tauri/src/process/mod.rs` — The `spawn_agent_execution` dispatcher function has a doc comment. Verify it does not mention "sidecar". It currently says "Routes to local or remote spawning" which is fine.

4. Any other files found by grep — update or remove sidecar references.

**C. Verify no build configuration references the sidecar:**
- Check `tauri.conf.json` for any `beforeDevCommand` or `beforeBuildCommand` mentioning sidecar (pre-analysis says none exist, but verify)
- Check `package.json` for any scripts mentioning sidecar

**D. Regenerate TypeScript bindings** since `spawn_agent_execution` IPC command was removed and `MergeOutcome` type was deleted:
```bash
cd src-tauri && cargo test generate_typescript_bindings -- --nocapture
```
Or equivalently from project root: `pnpm tauri:gen`

This will update `src/types/bindings.ts` to remove the `spawn_agent_execution` function and `MergeOutcome` type.
  </action>
  <verify>
    <automated>cd /home/m306213/workspace/maestro && test ! -d sidecar && grep -rn "sidecar" src-tauri/src/ | grep -v "target/" | wc -l | xargs test 0 -eq && echo "PASS: no sidecar references" || echo "FAIL: sidecar references remain"</automated>
  </verify>
  <acceptance_criteria>
    - sidecar/ directory does not exist
    - `grep -rn "sidecar" src-tauri/src/` returns zero matches (excluding target/)
    - src/types/bindings.ts does NOT contain `spawn_agent_execution`
    - src/types/bindings.ts does NOT contain `MergeOutcome`
    - cargo check exits 0
    - pnpm build exits 0 (frontend compiles with updated bindings)
  </acceptance_criteria>
  <done>sidecar/ directory deleted. Zero "sidecar" references remain in Rust source. TypeScript bindings regenerated without MergeOutcome or spawn_agent_execution. Both cargo check and pnpm build pass.</done>
</task>

</tasks>

<verification>
1. `test ! -d sidecar && echo "sidecar removed"` — directory gone
2. `grep -rn "sidecar" src-tauri/src/` — zero matches
3. `grep "MergeOutcome" src-tauri/src/models/mod.rs` — no matches
4. `grep "spawn_agent_cli" src-tauri/src/lib.rs` — no matches
5. `grep "spawn_agent_execution" src-tauri/src/lib.rs` — no matches (removed from collect_commands)
6. `cd src-tauri && cargo check` — passes
7. `pnpm build` — passes (frontend compiles with updated bindings)
</verification>

<success_criteria>
- All dead code removed: run_agent_background_task, spawn_agent_cli, spawn_agent_execution IPC, MergeOutcome
- sidecar/ directory completely deleted
- Zero "sidecar" references in Rust source code
- TypeScript bindings regenerated without deleted items
- Both cargo check and pnpm build pass
</success_criteria>

<output>
After completion, create `.planning/phases/34-remove-node-sidecar-implement-squash-merge-in-rust/34-02-SUMMARY.md`
</output>
