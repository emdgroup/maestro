---
wave: 2
depends_on: [60-01]
files_modified:
  - src/components/kanban/KanbanColumn.tsx
  - src/components/views/BoardView.tsx
  - src/views/KanbanView.tsx
requirements: [CARD-01, CARD-02, CARD-03, CARD-04, CARD-05, CARD-06]
autonomous: true
auto_approve: false
isolated_worktree: false
---

# Plan 60-02: Data Threading + Cleanup

## Objective

Thread `worktreeTaskIds: Set<number>` from `KanbanView` down through `BoardView` → `KanbanColumn` → `TaskCard`. Remove `onSettingsClick`/`TaskSettingsModal` flow from `BoardView` (context menu is gone; settings now belong to the detail screen). Add `useWorktreesQuery` call to `KanbanView`.

## must_haves

```yaml
truths:
  - worktreeTaskIds flows KanbanView → BoardView → KanbanColumn → TaskCard
  - KanbanView calls useWorktreesQuery and derives worktreeTaskIds = new Set of task_id values
  - BoardView has no TaskSettingsModal import or render
  - BoardView has no selectedTaskForSettings state
  - BoardView has no useArchiveTaskMutation import (removed; TaskCard handles archive inline)
  - KanbanColumn has no onSettingsClick or onArchiveClick props
  - ReviewModal and onReviewClick flow remain unchanged in BoardView
  - pnpm build exits 0
  - pnpm test exits 0
```

## Tasks

### Task 1: KanbanColumn prop interface update

<read_first>
- `src/components/kanban/KanbanColumn.tsx` — current file
- `src/components/kanban/TaskCard.tsx` — updated TaskCardProps from Plan 1 (has worktreeTaskIds, onReviewClick; no onSettingsClick or onArchiveClick)
</read_first>

<action>
In KanbanColumn.tsx:
- Remove `onSettingsClick?: (task: Task) => void` from KanbanColumnProps
- Remove `onArchiveClick?: (taskId: number) => void` from KanbanColumnProps
- Add `worktreeTaskIds: Set<number>` to KanbanColumnProps
- Remove `onSettingsClick` and `onArchiveClick` from function destructuring
- In `<TaskCard>`: remove `onSettingsClick={onSettingsClick}` and `onArchiveClick={onArchiveClick}` props
- In `<TaskCard>`: add `worktreeTaskIds={worktreeTaskIds}` prop
- Keep `onReviewClick` pass-through unchanged
- Remove `Task` import if it is only used for the removed props (check; may still be needed for task prop type)
</action>

<acceptance_criteria>
- KanbanColumnProps contains worktreeTaskIds: Set<number> and onReviewClick but NOT onSettingsClick or onArchiveClick
- <TaskCard> in KanbanColumn receives worktreeTaskIds and onReviewClick but NOT onSettingsClick or onArchiveClick
- pnpm build exits 0
</acceptance_criteria>

### Task 2: BoardView cleanup

<read_first>
- `src/components/views/BoardView.tsx` — current file
- `src/components/kanban/KanbanColumn.tsx` — updated KanbanColumnProps from Task 1
</read_first>

<action>
In BoardView.tsx:
- Add `worktreeTaskIds: Set<number>` to BoardViewProps interface
- Add `worktreeTaskIds` to function destructuring
- Remove `import { TaskSettingsModal } from "@/components/task/TaskSettingsModal"`
- Remove `import { useArchiveTaskMutation } from "@/services/task.service"` (archive is now in TaskCard)
- Remove `const archiveTask = useArchiveTaskMutation()`
- Remove `const [selectedTaskForSettings, setSelectedTaskForSettings] = useState<Task | null>(null)`
- Remove `onSettingsClick={(task) => setSelectedTaskForSettings(task)}` from all `<KanbanColumn>` elements
- Remove `onArchiveClick={(taskId) => archiveTask.mutate(taskId)}` from all `<KanbanColumn>` elements
- Add `worktreeTaskIds={worktreeTaskIds}` to each `<KanbanColumn>` element
- Remove the `{selectedTaskForSettings && <TaskSettingsModal ... />}` render block entirely
- Keep ReviewModal flow: selectedTaskId, selectedTaskName state, onReviewClick handler, and <ReviewModal> render all unchanged
- Keep ExecutionTerminal render block unchanged
</action>

<acceptance_criteria>
- BoardViewProps contains worktreeTaskIds: Set<number>
- BoardView.tsx contains no TaskSettingsModal import
- BoardView.tsx contains no selectedTaskForSettings state
- BoardView.tsx contains no useArchiveTaskMutation import
- Each <KanbanColumn> receives worktreeTaskIds
- ReviewModal block is present and unchanged
- pnpm build exits 0
</acceptance_criteria>

### Task 3: KanbanView — hoist useWorktreesQuery, pass worktreeTaskIds

<read_first>
- `src/views/KanbanView.tsx` — current file
- `src/services/worktree.service.ts` — useWorktreesQuery signature (projectId: number | undefined, repoPath: string | undefined)
- `src/store/projectStore.ts` — useSelectedProject return type (check if it includes `path` field)
- `src/components/views/BoardView.tsx` — updated BoardViewProps from Task 2 (needs worktreeTaskIds)
</read_first>

<action>
In KanbanView.tsx:
- Add `import { useWorktreesQuery } from "@/services/worktree.service"`
- Extract projectPath from useSelectedProject: `const projectPath = selectedProject?.path ?? ""`
- Add query: `const { data: worktrees } = useWorktreesQuery(projectId ?? undefined, projectPath || undefined)`
- Derive set: `const worktreeTaskIds = new Set((worktrees ?? []).filter(w => w.task_id != null).map(w => w.task_id!))`
- Pass `worktreeTaskIds={worktreeTaskIds}` to `<BoardView>`

Note: `useSelectedProject()` is already called in KanbanView (`const selectedProject = useSelectedProject()`). Confirm the Project type has a `path` field; if the field is named differently (e.g., `repo_path`), use that name.
</action>

<acceptance_criteria>
- KanbanView.tsx imports useWorktreesQuery from @/services/worktree.service
- KanbanView.tsx calls useWorktreesQuery with (projectId ?? undefined, projectPath || undefined)
- worktreeTaskIds is derived as new Set<number>(...) filtering where task_id != null
- <BoardView> receives worktreeTaskIds={worktreeTaskIds}
- pnpm build exits 0
- pnpm test exits 0
</acceptance_criteria>

## Verification

```bash
pnpm build   # 0 TypeScript errors, 0 build errors
pnpm test    # all existing tests pass
```

Visual regression check via `pnpm dev`:
1. Board renders all 5 columns
2. Tasks with active worktrees show green dot + "worktree" in card footer
3. Tasks without worktrees show no badge
4. ReviewModal still opens when Review button is clicked
5. No broken imports or missing components in the UI
6. TaskSettingsModal no longer appears from any card interaction (context menu is gone)

End-to-end flow:
1. Start a task execution (Ready → Execute) — task moves to InProgress, worktree badge appears
2. Click InProgress card Interrupt — task returns to Backlog, worktree badge disappears
3. Click a Backlog card body — TaskDetailScreen opens
4. Press back from detail — board resumes
