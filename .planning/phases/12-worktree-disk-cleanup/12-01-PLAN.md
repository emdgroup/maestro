---
phase: 12-worktree-disk-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sidecar/src/index.ts
  - src-tauri/src/ipc/handlers.rs
autonomous: true
must_haves:
  truths:
    - "After successful merge, worktree directory is deleted from disk (not just returned to pool)"
    - "Disk space is reclaimed after worktree cleanup completes"
    - "No stale worktree directories accumulate after multiple merge operations"
  artifacts:
    - path: sidecar/src/index.ts
      provides: "CLI handler for --delete-worktree command"
      exports: ["--delete-worktree <repoPath> <worktreePath> <branchName>"]
    - path: src-tauri/src/ipc/handlers.rs
      provides: "finalize_successful_merge handler with disk cleanup integration"
      pattern: "invoke sidecar deleteWorktree after task status transitions"
  key_links:
    - from: "src-tauri/src/ipc/handlers.rs::finalize_successful_merge"
      to: "sidecar/dist/index.js --delete-worktree"
      via: "tokio::process::Command async invocation"
      pattern: "deleteWorktree\\(repo_path, worktree_id, branch_name\\).await"
    - from: "sidecar/src/index.ts::main()"
      to: "gitManager.deleteWorktree()"
      via: "CLI dispatcher"
      pattern: "args.includes.*--delete-worktree"
---

<objective>
Wire disk cleanup into the merge finalization workflow to ensure worktrees are physically deleted from disk after successful merge, reclaiming storage space and preventing stale directory accumulation.

Purpose: Close tech debt from Phase 6 where merge operations completed but worktree directories remained on disk indefinitely, causing disk space to accumulate.

Output: Rust handler invokes sidecar deleteWorktree CLI command after task status transitions complete. Worktree directories cleaned via git worktree remove → branch delete → prune sequence.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-worktree-disk-cleanup/12-RESEARCH.md

## Key Files (reference existing implementation)
@/home/m306213/workspace/gsd-demo/sidecar/src/git-manager.ts (deleteWorktree already implemented)
@/home/m306213/workspace/gsd-demo/src-tauri/src/ipc/handlers.rs (finalize_successful_merge at line 2285)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --delete-worktree CLI handler to sidecar</name>
  <files>sidecar/src/index.ts</files>
  <action>
Add CLI dispatcher for worktree deletion in sidecar/src/index.ts main() function:

1. After the existing --merge handler block (around line 225), add new else-if block:
   - Check for args.includes("--delete-worktree")
   - Parse arguments: repoPath (index+1), worktreePath (index+2), branchName (index+3)
   - Validate all three arguments present, exit with error message if missing
   - Call gitManager.deleteWorktree(repoPath, worktreePath, branchName)
   - On success: log JSON response { success: true, worktreeId } and exit 0
   - On error: log error to stderr and exit 1

2. Update the usage help text (around line 232) to include:
   "  --delete-worktree <repoPath> <worktreePath> <branchName>"

Reference pattern: Use same error handling as --merge handler (try/catch, stderr logging, process.exit)

Why: sidecar needs to receive delete commands from Rust handler as CLI args; this dispatcher makes it callable from finalize_successful_merge via tokio::process::Command
  </action>
  <verify>
Run: npm run build (from sidecar directory) — must succeed with no TypeScript errors
Run: node sidecar/dist/index.js --delete-worktree (no args) — should exit 1 with usage error
Run: grep -n "delete-worktree" sidecar/dist/index.js — should show CLI handler present
  </verify>
  <done>Sidecar index.ts compiles to dist/index.js with --delete-worktree handler. Handler parses three arguments (repoPath, worktreePath, branchName), calls deleteWorktree, exits 0 on success or 1 on error.</done>
</task>

<task type="auto">
  <name>Task 2: Wire deleteWorktree invocation into finalize_successful_merge handler</name>
  <files>src-tauri/src/ipc/handlers.rs</files>
  <action>
Update finalize_successful_merge handler (starts at line 2285):

1. After step 2 (returning worktree to pool, around line 2324), add step 3:

   Comment: "// 3. Delete worktree from disk (mark Dirty before cleanup for crash safety)"

2. Mark worktree as Dirty in DB before calling sidecar:
   - Execute UPDATE: SET status = 'Dirty' on worktrees WHERE id = ?
   - Log: "[finalize] Marked worktree {} as Dirty before cleanup"

3. Invoke sidecar deleteWorktree command using tokio::process::Command:
   - Command path: "node"
   - Args: ["sidecar/dist/index.js", "--delete-worktree", &repo_path, &worktree_path, &branch_name]
   - Use .output().await pattern (same as squash merge call at line 2209)

4. Handle result (match on output.status.success()):
   - Success: Execute DELETE FROM worktrees WHERE id = ? to remove DB entry
     Log: "[finalize] ✓ Worktree {} deleted from disk and DB"
   - Failure: Log error to stderr but do NOT return Err (best-effort cleanup)
     Log: "[finalize] ⚠ Cleanup failed (will retry): {stderr}"
     Note: Worktree stays in Dirty state for recovery on app restart via recover_dirty_worktrees

5. Final log (regardless of cleanup success):
   Log: "[finalize] ✓ Merge finalization complete for task {}"

Replace the TODO comment at line 2326-2327 with this implementation.

Why:
- Dirty state prevents race conditions (lease_worktree skips Dirty worktrees)
- Crash-safe: if process dies mid-cleanup, worktree entry survives for recovery
- Non-blocking: cleanup failures don't fail the merge (task already Done, user unaffected)
- Deferred recovery: recover_dirty_worktrees() handles retries on next app startup
  </action>
  <verify>
Run: cargo build (from src-tauri directory) — must succeed with 0 errors
Run: cargo test (from src-tauri directory) — must pass all existing tests (27/27 passing)
Run: grep -A 40 "fn finalize_successful_merge" src-tauri/src/ipc/handlers.rs — should show complete disk cleanup flow with Dirty marking, sidecar invocation, and error handling
  </verify>
  <done>finalize_successful_merge handler (1) marks worktree Dirty before cleanup, (2) invokes sidecar --delete-worktree with repo_path/worktree_path/branch_name, (3) deletes DB entry on success, (4) logs errors non-fatally on failure, (5) leaves Dirty worktrees for recovery_dirty_worktrees on app startup.</done>
</task>

<task type="auto">
  <name>Task 3: Verify disk cleanup in integration (compile + test)</name>
  <files></files>
  <action>
Perform full integration verification without modifying files:

1. Build frontend:
   Run: pnpm build (from project root)
   Expected: Build succeeds, dist/ output valid

2. Build Rust backend:
   Run: cargo build (from src-tauri directory)
   Expected: 0 compilation errors

3. Run full Rust test suite:
   Run: cargo test (from src-tauri directory)
   Expected: 27/27 tests pass, no regressions

4. Verify sidecar compilation:
   Run: npm run build (from sidecar directory)
   Expected: dist/index.js generated, no TypeScript errors

5. Smoke test sidecar CLI:
   Run: node sidecar/dist/index.js --delete-worktree
   Expected: Exit code 1, usage error in output

6. Verify handler wiring by code inspection:
   Run: grep -B5 -A15 "tokio::process::Command" src-tauri/src/ipc/handlers.rs | grep -A15 "delete-worktree"
   Expected: Shows sidecar invocation with --delete-worktree flag and three arguments

Why: Integration verification confirms no breaking changes, all compilation succeeds, test suite stable.
  </action>
  <verify>
All commands run successfully:
- pnpm build: exit 0, dist/ exists
- cargo build: 0 errors
- cargo test: 27/27 passing
- npm run build (sidecar): exit 0, dist/index.js exists
- node sidecar/dist/index.js --delete-worktree: exit 1 (error for missing args)
- grep finds --delete-worktree invocation in handlers.rs with correct args
  </verify>
  <done>Full compilation stack (frontend + backend + sidecar) succeeds with 0 errors. Rust test suite passes all 27 tests (no regressions). Sidecar CLI handler responds to --delete-worktree flag. Handler wiring visible in code inspection.</done>
</task>

</tasks>

<verification>
Goal-backward verification: All three must_haves truths must be observable:

1. ✓ After successful merge, worktree directory is deleted from disk
   - Inspect: Code shows sidecar.deleteWorktree() invocation in finalize_successful_merge
   - Trace: deleteWorktree calls git worktree remove (line 74 of git-manager.ts)
   - Result: Filesystem directory removed from .worktree-pool/

2. ✓ Disk space is reclaimed after worktree cleanup
   - Mechanism: git worktree remove → git branch -D → git worktree prune frees disk
   - Verification: du -sh .worktree-pool/ would show size reduction after merge (not executed in this plan)
   - Test plan: Manual verify during UAT (merge task, check disk usage before/after)

3. ✓ No stale worktree directories accumulate
   - Prevention: finalize_successful_merge → delete DB entry after successful cleanup
   - Recovery: recover_dirty_worktrees() retries Dirty-marked worktrees on app startup
   - Result: Cannot accidentally accumulate stale directories (either deleted or marked Dirty)

Key links verified:
- finalize_successful_merge calls sidecar deleteWorktree: ✓ (line 2334 approx)
- Sidecar CLI dispatcher routes --delete-worktree: ✓ (sidecar/src/index.ts main())
- Error handling non-blocking: ✓ (logs but doesn't throw)
- Dirty state prevents races: ✓ (marked before cleanup)
</verification>

<success_criteria>
Phase 12 complete when:

1. **Code compiles without errors:**
   - `cargo build` succeeds in src-tauri/
   - `npm run build` succeeds in sidecar/
   - `pnpm build` succeeds in project root

2. **Tests pass without regressions:**
   - `cargo test` passes all 27 tests (no new failures)
   - Integration tests verify merge → cleanup flow works

3. **Implementation verified:**
   - finalize_successful_merge marks worktree Dirty before cleanup
   - Sidecar --delete-worktree handler present and callable
   - Cleanup invocation uses correct arguments (repo_path, worktree_path, branch_name)
   - Errors logged non-fatally (cleanup doesn't block merge)

4. **Observable behavior:**
   - After merge, worktree DB entry is either deleted or marked Dirty
   - No orphaned worktrees left in filesystem
   - recover_dirty_worktrees() can retry failed cleanups on app startup
</success_criteria>

<output>
After completion, create `.planning/phases/12-worktree-disk-cleanup/12-01-SUMMARY.md` documenting:
- Implementation: which files modified, what handlers added
- Integration: how deleteWorktree CLI handler wired into finalize_successful_merge
- Testing: compilation results, test pass rates
- Disk cleanup verification: before/after disk usage (manual test during UAT)
- Phase 12 completion: all 3 success criteria met
</output>
