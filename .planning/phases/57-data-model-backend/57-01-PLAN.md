---
phase: 57-data-model-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/db/schema.rs
  - src-tauri/src/models/task.rs
  - src-tauri/src/models/mod.rs
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Schema version is 18 and migration creates task_attachments table"
    - "Task struct includes auto_approve (bool, default false) and isolated_worktree (bool, default true)"
    - "TaskAttachment struct is exported and available for IPC handlers"
    - "cargo test passes with new schema assertions"
  artifacts:
    - path: "src-tauri/src/db/schema.rs"
      provides: "V18 schema with task_attachments table and new task columns"
      contains: "SCHEMA_VERSION: u32 = 18"
    - path: "src-tauri/src/models/task.rs"
      provides: "Task struct with auto_approve/isolated_worktree + TaskAttachment model"
      contains: "pub struct TaskAttachment"
    - path: "src-tauri/src/models/mod.rs"
      provides: "TaskAttachment re-export"
      contains: "TaskAttachment"
  key_links:
    - from: "src-tauri/src/db/schema.rs"
      to: "src-tauri/src/models/task.rs"
      via: "column indices match TASK_SELECT and from_row"
      pattern: "auto_approve.*isolated_worktree"
---

<objective>
Bump the SQLite schema to V18 with two new task columns (auto_approve, isolated_worktree) and a new task_attachments table, then update the Task model struct and add the TaskAttachment model.

Purpose: Downstream IPC handlers (Plan 02) and all v1.7 frontend phases depend on these fields and the attachments table existing.
Output: Compilable schema migration, updated Task struct with correct from_row indices, new TaskAttachment model exported.
</objective>

<execution_context>
@/home/m306213/workspace/maestro/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/workspace/maestro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-data-model-backend/57-RESEARCH.md
@.planning/phases/57-data-model-backend/57-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema V18 — new columns and task_attachments table</name>
  <files>src-tauri/src/db/schema.rs</files>
  <read_first>
    - src-tauri/src/db/schema.rs (current V17 schema — version constant, DDL, drop block, tests)
  </read_first>
  <action>
1. Change `pub const SCHEMA_VERSION: u32 = 17;` to `pub const SCHEMA_VERSION: u32 = 18;`
2. Rename the SQL constant from `SCHEMA_V17` to `SCHEMA_V18`
3. In the `tasks` CREATE TABLE DDL, after the `labels TEXT DEFAULT '[]'` line (before the FOREIGN KEY constraint), add:
   - `auto_approve INTEGER NOT NULL DEFAULT 0,`
   - `isolated_worktree INTEGER NOT NULL DEFAULT 1,`
4. After the `task_instructions` CREATE TABLE block, add the `task_attachments` table:
   - CREATE TABLE IF NOT EXISTS task_attachments (id INTEGER PRIMARY KEY AUTOINCREMENT, task_id INTEGER NOT NULL, filename TEXT NOT NULL, file_path TEXT NOT NULL, file_size INTEGER NOT NULL, created_at TEXT NOT NULL, FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE);
   - CREATE INDEX IF NOT EXISTS idx_task_attachments_task_id ON task_attachments(task_id);
5. In the destructive migration drop block, add `DROP TABLE IF EXISTS task_attachments;` as the FIRST drop statement (before session_aliases) — FK dependency requires it be dropped before tasks.
6. Update the `conn.execute_batch(SCHEMA_V17)` call to `conn.execute_batch(SCHEMA_V18)`
7. Update schema tests:
   - Assert `task_attachments` is in the tables list
   - Assert `auto_approve` and `isolated_worktree` are in the task columns list
   - Change the version assertion from 17 to 18
  </action>
  <acceptance_criteria>
    - `SCHEMA_VERSION` constant equals 18
    - The SQL constant is named `SCHEMA_V18`
    - `tasks` DDL contains `auto_approve INTEGER NOT NULL DEFAULT 0` and `isolated_worktree INTEGER NOT NULL DEFAULT 1`
    - `task_attachments` DDL has columns: id, task_id, filename, file_path, file_size, created_at
    - `task_attachments` has FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE
    - Index `idx_task_attachments_task_id` is created on task_attachments(task_id)
    - Drop block starts with `DROP TABLE IF EXISTS task_attachments;` before other table drops
    - Schema test asserts version == 18, `task_attachments` in tables, `auto_approve` and `isolated_worktree` in task columns
    - `cd src-tauri && cargo test test_schema_initialization` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /home/m306213/workspace/maestro/src-tauri && cargo test test_schema_initialization -- --nocapture</automated>
  </verify>
  <done>Schema V18 compiles and all schema tests pass with new columns and table present</done>
</task>

<task type="auto">
  <name>Task 2: Task struct extension + TaskAttachment model</name>
  <files>src-tauri/src/models/task.rs, src-tauri/src/models/mod.rs</files>
  <read_first>
    - src-tauri/src/models/task.rs (Task struct, TASK_SELECT constant, from_row method, existing TaskInstruction struct)
    - src-tauri/src/models/mod.rs (re-export line for task module)
  </read_first>
  <action>
In `src-tauri/src/models/task.rs`:
1. Update the TASK_SELECT column comment to include `auto_approve(20), isolated_worktree(21)`
2. Append `, auto_approve, isolated_worktree` to the end of the TASK_SELECT SQL string (after `updated_at`)
3. Add two fields to the Task struct (after `updated_at: String`):
   - `pub auto_approve: bool,`
   - `pub isolated_worktree: bool,`
4. In `from_row`, after the `updated_at: row.get(19)?` line, add:
   - `auto_approve: row.get::<_, bool>(20).unwrap_or(false),`
   - `isolated_worktree: row.get::<_, bool>(21).unwrap_or(true),`
5. After the existing `TaskInstruction` struct definition, add a new `TaskAttachment` struct:
   - Derives: `Debug, Clone, Serialize, Deserialize, Type`
   - Attribute: `#[specta(export)]`
   - Fields: `id: i32`, `task_id: i32`, `filename: String`, `file_path: String`, `file_size: i64`, `created_at: String`

In `src-tauri/src/models/mod.rs`:
6. Add `TaskAttachment` to the `pub use task::{...}` re-export line (after `TaskInstruction`)
  </action>
  <acceptance_criteria>
    - TASK_SELECT string ends with `auto_approve, isolated_worktree FROM tasks`
    - TASK_SELECT column comment lists `auto_approve(20), isolated_worktree(21)`
    - Task struct has `pub auto_approve: bool` and `pub isolated_worktree: bool` fields
    - from_row uses index 20 for auto_approve with `unwrap_or(false)` and index 21 for isolated_worktree with `unwrap_or(true)`
    - TaskAttachment struct exists with derives `Debug, Clone, Serialize, Deserialize, Type` and `#[specta(export)]`
    - TaskAttachment has fields: `id: i32`, `task_id: i32`, `filename: String`, `file_path: String`, `file_size: i64`, `created_at: String`
    - `models/mod.rs` re-exports `TaskAttachment`
    - `cd src-tauri && cargo check` passes without errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/m306213/workspace/maestro/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Task struct has both new bool fields, TaskAttachment model is defined and exported, cargo check passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend IPC → Rust backend | Untrusted input (task_id, file paths) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | task_attachments.file_path | accept | Phase 57 stores metadata only; Phase 62 handles actual file I/O with path validation |
| T-57-02 | Information Disclosure | task_attachments.file_size | accept | file_size is non-sensitive metadata; i64 prevents overflow |
</threat_model>

<verification>
- `cd src-tauri && cargo test` — all tests pass (schema + existing tests)
- `cd src-tauri && cargo check` — no compilation errors
- grep confirms SCHEMA_VERSION is 18: `grep -n "SCHEMA_VERSION.*18" src-tauri/src/db/schema.rs`
- grep confirms TaskAttachment export: `grep "TaskAttachment" src-tauri/src/models/mod.rs`
</verification>

<success_criteria>
- Schema V18 creates task_attachments table with all required columns and CASCADE FK
- Task struct compiles with auto_approve and isolated_worktree fields
- TaskAttachment struct is defined, derives correct traits, and is re-exported from models
- All existing cargo tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/57-data-model-backend/57-01-SUMMARY.md`
</output>
