---
phase: 04-agent-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/process/mod.rs
  - src-tauri/src/process/spawner.rs
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "Tokio process spawner module exists and compiles"
    - "Process spawner can invoke Node.js sidecar with piped I/O"
    - "Process exit codes are captured (success/failure distinction)"
  artifacts:
    - path: "src-tauri/src/process/mod.rs"
      provides: "Process module exports"
    - path: "src-tauri/src/process/spawner.rs"
      provides: "Async process spawning with output capture"
      exports: ["spawn_agent_cli"]
    - path: "Cargo.toml"
      provides: "Tokio async runtime with process feature"
  key_links:
    - from: "src-tauri/src/process/spawner.rs"
      to: "Node.js sidecar"
      via: "tokio::process::Command spawn"
      pattern: "Command::new.*node"
---

<objective>
Create process spawner module for executing Claude Code CLI via Node.js sidecar with non-blocking async spawning and captured output.

Purpose: Phase 4 requires async process execution without blocking Tauri IPC handlers. Tokio's process module provides non-blocking spawn/wait, preventing UI hangs during agent execution.

Output: Spawner module that takes working directory + agent command, spawns via Node sidecar, returns exit code + stdout/stderr.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/04-RESEARCH.md
@.planning/phases/03-git-worktree-infrastructure/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create process module with spawner.rs</name>
  <files>src-tauri/src/process/mod.rs, src-tauri/src/process/spawner.rs</files>
  <action>
Create async process spawner module using tokio::process::Command.

1. Create src-tauri/src/process/ directory
2. Create src-tauri/src/process/mod.rs with public exports:
   - pub mod spawner;
   - pub use spawner::{spawn_agent_cli, ProcessOutput};

3. Create src-tauri/src/process/spawner.rs with:
   - Import: tokio::process::Command, std::process::Stdio, tokio::io::{AsyncBufReadExt, BufReader}
   - Define ProcessOutput struct:
     ```rust
     pub struct ProcessOutput {
         pub stdout: String,
         pub stderr: String,
         pub exit_code: i32,
         pub success: bool,
     }
     ```
   - Implement spawn_agent_cli(working_dir: &str, sidecar_path: &str, task_id: i32) → Result<ProcessOutput, String>
   - Use tokio::process::Command (NOT std::process)
   - Set current_dir to working_dir
   - Pipe stdout and stderr
   - Set kill_on_drop(true) for safety
   - Capture output streams using BufReader
   - Wait for child process completion
   - Extract exit code: status.code().unwrap_or(-1)
   - Set success: status.success()
   - Return ProcessOutput struct

Reference the spawn_agent_cli pattern from 04-RESEARCH.md Pattern 1 (lines 462-507).

CRITICAL: Use tokio::process (not std::process). This is async, doesn't block IPC handlers.
  </action>
  <verify>
    cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | grep -E "(error|warning: unused)" || echo "Build clean"
  </verify>
  <done>
    - src-tauri/src/process/mod.rs exists with correct exports
    - src-tauri/src/process/spawner.rs exists with spawn_agent_cli function
    - Tokio process module imported (not std::process)
    - kill_on_drop(true) set on Command
    - ProcessOutput struct defined with stdout, stderr, exit_code, success fields
    - Function is async (async fn spawn_agent_cli)
    - Cargo build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Export process module in lib.rs</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
Add process module to library exports so it can be used in IPC handlers.

1. Open src-tauri/src/lib.rs
2. Locate pub mod db; pub mod models; etc.
3. Add new line: pub mod process;
4. Save file
5. Run cargo build to verify export works

This makes crate::process accessible from main.rs and ipc/handlers.rs for Phase 4-02 to use.
  </action>
  <verify>
    cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | grep -E "error.*process" && echo "FAILED" || echo "OK"
  </verify>
  <done>
    - src-tauri/src/lib.rs has pub mod process;
    - Cargo build succeeds with no process-related errors
    - crate::process::spawner::spawn_agent_cli is now accessible
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Tokio feature enabled in Cargo.toml</name>
  <files>src-tauri/Cargo.toml</files>
  <action>
Confirm tokio has "full" feature enabled (required for tokio::process).

1. Open src-tauri/Cargo.toml
2. Find tokio = line (should already exist from Phase 1)
3. Verify it has features = ["full"] or at least includes "process" and "io-util"
4. If missing, add: tokio = { version = "^1.0", features = ["full"] }
5. Run cargo build to verify

The "full" feature includes all tokio subsystems including process, io, time, etc.
  </action>
  <verify>
    grep -A1 'tokio.*=' src-tauri/Cargo.toml | grep -E 'features.*full|features.*process'
  </verify>
  <done>
    - Cargo.toml has tokio with "full" features or explicit ["process", "io-util", "macros", "rt"]
    - Cargo build succeeds
    - tokio::process::Command is available
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit process spawner module</name>
  <files>src-tauri/src/process/mod.rs, src-tauri/src/process/spawner.rs, src-tauri/src/lib.rs</files>
  <action>
Commit the process module with async spawner to git.

```bash
cd /home/m306213/workspace/gsd-demo
git add src-tauri/src/process/ src-tauri/src/lib.rs
git commit -m "feat(04-01): create async process spawner module with tokio

- Add process module with spawner.rs for async CLI execution
- Implement spawn_agent_cli using tokio::process::Command (non-blocking)
- Define ProcessOutput struct with stdout, stderr, exit_code, success
- Set kill_on_drop(true) to ensure proper cleanup
- Export process module in lib.rs for IPC handler access"
```
  </action>
  <verify>
    git log --oneline -1 | grep -q "feat(04-01)" && echo "Committed" || echo "Not committed"
  </verify>
  <done>
    - Commit created with message starting with "feat(04-01)"
    - Files staged and committed to git
  </done>
</task>

</tasks>

<verification>
After completion, verify:
- [ ] cargo build --manifest-path src-tauri/Cargo.toml passes
- [ ] spawn_agent_cli function exists and is public
- [ ] ProcessOutput struct has all required fields
- [ ] process module is exported from lib.rs
- [ ] Git commit created
</verification>

<success_criteria>
- Process spawner module compiles without errors
- Async process spawning capability ready for IPC handler integration (Phase 04-02)
- Tokio non-blocking runtime ready to prevent UI hangs
- Output capture infrastructure (stdout/stderr) in place
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-execution/04-01-SUMMARY.md`
</output>
