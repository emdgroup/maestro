---
phase: 02-core-orchestration
plan: 05
type: execute
wave: 2
depends_on: ["01", "04"]
files_modified:
  - package.json
  - src/components/ImportSettings.tsx
  - src/components/SyncButton.tsx
  - src/components/ErrorToast.tsx
  - src/styles/ImportSettings.css
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User can configure GitHub or Jira import provider in settings"
    - "Settings configuration persisted via save_import_config IPC"
    - "User can click Sync button to trigger import"
    - "Sync button calls appropriate sync_github_issues or sync_jira_issues handler"
    - "Imported issues appear in Backlog column"
    - "Error messages displayed as toast notifications"
    - "Imported tasks marked read-only with visual indicator"
  artifacts:
    - path: "src/components/ImportSettings.tsx"
      provides: "Settings UI for GitHub/Jira configuration with Test and Save buttons"
      min_lines: 60
    - path: "src/components/SyncButton.tsx"
      provides: "Sync trigger button with provider detection and error handling"
      min_lines: 30
    - path: "src/components/ErrorToast.tsx"
      provides: "Toast notification component with sonner integration"
      min_lines: 20
    - path: "src/App.tsx"
      provides: "ErrorToast root, ImportSettings modal, SyncButton integration"
  key_links:
    - from: "SyncButton"
      to: "sync_github_issues or sync_jira_issues IPC"
      via: "onClick handler"
      pattern: "invoke.*sync_github|invoke.*sync_jira"
    - from: "ImportSettings"
      to: "save_import_config IPC"
      via: "Save button onClick"
      pattern: "invoke.*save_import_config"
    - from: "SyncButton/ImportSettings"
      to: "ErrorToast"
      via: "showErrorToast/showSuccessToast"
      pattern: "showErrorToast|showSuccessToast"
    - from: "SyncButton"
      to: "useBoardStore"
      via: "onSyncComplete callback"
      pattern: "useBoardStore\\.loadTasks"
---

<objective>
Implement GitHub/Jira import UI with sync button, settings configuration, error handling, and read-only protection.

Purpose: Enable users to configure import providers, manually sync issues from GitHub/Jira, display errors as toasts, and protect imported tasks as read-only.

Output: ImportSettings UI component, SyncButton component, ErrorToast notification component, and App.tsx integration.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-orchestration/02-CONTEXT.md
@.planning/phases/02-core-orchestration/02-RESEARCH.md

# Prior plan context
@.planning/phases/02-core-orchestration/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toast notification library</name>
  <files>package.json</files>
  <action>
    1. Add dependency to package.json:
       - sonner: ^1.5.0 (lightweight toast notifications)
    2. Run npm install

    Note: Sonner provides toast UI component for error/success notifications. Lightweight alternative to heavier toast libraries.
  </action>
  <verify>
    npm list sonner
  </verify>
  <done>Sonner toast notification library installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create ErrorToast component with Sonner integration</name>
  <files>src/components/ErrorToast.tsx</files>
  <action>
    1. Create src/components/ErrorToast.tsx:
       - Import Toaster, toast from sonner
       - Export Toaster component for rendering at root level
       - Export helper functions:
         * export function showErrorToast(message: string) { toast.error(message); }
         * export function showSuccessToast(message: string) { toast.success(message); }
       - Configure Toaster with:
         * position: 'bottom-right'
         * max: 3 (max visible toasts)
         * duration: 4000ms (default toast duration)

    2. In src/App.tsx, render ErrorToast:
       - Import: import { Toaster as ErrorToaster } from '@/components/ErrorToast'
       - Render at root level (before or inside main app content)

    Note: Sonner handles toast management, positioning, animations automatically.
  </action>
  <verify>
    grep -n "Toaster\|toast.error\|toast.success" src/components/ErrorToast.tsx
    grep -n "showErrorToast\|showSuccessToast" src/components/ErrorToast.tsx
    grep "ErrorToast" src/App.tsx
  </verify>
  <done>ErrorToast component created with Sonner, exported helper functions, integrated in App.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Create ImportSettings UI component</name>
  <files>src/components/ImportSettings.tsx, src/styles/ImportSettings.css</files>
  <action>
    1. Create src/components/ImportSettings.tsx:
       - Import useState, useEffect from React
       - Import invoke from @tauri-apps/api/tauri
       - Import showErrorToast, showSuccessToast from ErrorToast
       - Accept props: { isOpen: boolean, onClose: () => void, projectId: number, onConfigSaved: () => void }
       - State: provider ('github' | 'jira' | null), testing: boolean
       - GitHub form fields: owner, repo, token (input type="password")
       - Jira form fields: host, email, token (input type="password"), jql
       - Load existing config on mount via invoke('get_settings', {})
       - For each provider section:
         * Radio/toggle to select provider
         * Form inputs for credentials
         * "Test Connection" button:
           - Calls appropriate sync handler with test parameters
           - Shows success/error toast
           - Sets testing = true during request
         * "Save" button:
           - Calls invoke('save_import_config', {provider, config})
           - Shows success toast
           - Calls onConfigSaved()
       - Render in modal (separate from Kanban, can use Radix Dialog or simple overlay)

    2. Create src/styles/ImportSettings.css:
       - Provider selection: radio buttons or toggle (GitHub | Jira)
       - Form layout: 2-column for desktop, stacked for mobile
       - Input styling: full width, padding, border
       - Password inputs marked with eye icon (optional)
       - Button layout: Test and Save side-by-side
       - Disabled state during testing (loading indicator)
       - Conditional rendering: show only selected provider fields

    Note: Settings stored in SQLite via save_import_config. Test button verifies credentials.
  </action>
  <verify>
    grep -n "useState\|provider\|github\|jira" src/components/ImportSettings.tsx
    grep -n "invoke.*save_import_config" src/components/ImportSettings.tsx
    grep "Test Connection\|password" src/components/ImportSettings.tsx
  </verify>
  <done>ImportSettings component created with GitHub and Jira configuration forms, test and save buttons.</done>
</task>

<task type="auto">
  <name>Task 4: Create SyncButton component</name>
  <files>src/components/SyncButton.tsx</files>
  <action>
    1. Create src/components/SyncButton.tsx:
       - Import useState, useEffect from React
       - Import invoke from @tauri-apps/api/tauri
       - Import showErrorToast, showSuccessToast from ErrorToast
       - Accept props: { projectId: number, onSyncComplete: () => void }
       - State: isLoading, importProvider ('github' | 'jira' | null)
       - On mount: invoke('get_settings', {}) to get import provider
       - If provider is set:
         * Render button: "Sync from {provider}" with loading spinner during request
       - If provider is NOT set:
         * Render grayed-out button: "Configure Import" (disabled)
       - On button click:
         * Set isLoading = true
         * Call appropriate invoke('sync_github_issues' or 'sync_jira_issues', {...config from settings})
         * On success: show toast "Synced X issues from {provider}", call onSyncComplete(), set isLoading = false
         * On error: show error toast with error_message from SyncResult, set isLoading = false
       - Button disabled while isLoading

    Note: Gets config from settings table, retrieves provider and credentials. Calls sync handler asynchronously.
  </action>
  <verify>
    grep -n "useState\|isLoading\|sync_github\|sync_jira" src/components/SyncButton.tsx
    grep -n "showErrorToast\|showSuccessToast" src/components/SyncButton.tsx
    grep "get_settings\|importProvider" src/components/SyncButton.tsx
  </verify>
  <done>SyncButton component created with provider detection, error handling, and board refresh callback.</done>
</task>

<task type="auto">
  <name>Task 5: Wire ImportSettings, SyncButton, and ErrorToast into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    1. Update src/App.tsx:
       - Import ImportSettings, SyncButton, ErrorToast components
       - Render ErrorToast at root (done in Task 2)
       - Add state: showImportSettings (boolean)
       - After Kanban board renders:
         * Add Settings icon/button that toggles showImportSettings
         * Render ImportSettings modal with:
           - isOpen={showImportSettings}
           - onClose={() => setShowImportSettings(false)}
           - onConfigSaved={() => { setShowImportSettings(false); /* optionally refresh board */ }}
         * Add SyncButton in Kanban toolbar area with:
           - onSyncComplete={() => { useBoardStore.loadTasks() or re-invoke get_tasks }}

    2. Verify navigation flow:
       - Settings button visible in header/toolbar
       - Click Settings → ImportSettings modal opens
       - Configure provider (GitHub or Jira)
       - Click Test Connection → test credentials
       - Click Save → settings persisted
       - Close modal → SyncButton becomes active (no longer grayed out)
       - Click SyncButton → sync triggered, progress shown
       - Toast: "Synced X issues from GitHub"
       - Verify new tasks appear in Backlog column
       - SyncButton click triggers board refresh

    Note: Keep import settings in modal. Sync button always visible in toolbar.
  </action>
  <verify>
    grep -n "ImportSettings\|SyncButton\|ErrorToast" src/App.tsx
    grep -n "showImportSettings" src/App.tsx
    grep -n "onSyncComplete\|useBoardStore.loadTasks" src/App.tsx
  </verify>
  <done>ImportSettings, SyncButton, and ErrorToast wired into App.tsx, import workflow complete.</done>
</task>

<task type="auto">
  <name>Task 6: Add read-only protection for imported tasks</name>
  <files>src/components/TaskCard.tsx</files>
  <action>
    1. Update TaskCard.tsx (created in 02-01):
       - Check task.is_imported flag
       - If imported: display "Read-only (imported)" badge/label
       - Conditionally disable drag for imported tasks:
         * Option 1: Don't wrap in Draggable (no drag available)
         * Option 2: Wrap but disable drag via draggableId={task.is_imported ? null : task.id}
       - Show visual hint (e.g., icon, different text color, reduced opacity)

    Note: Read-only enforcement protects sync integrity (prevents conflicting local edits). Re-sync will overwrite anyway.
  </action>
  <verify>
    grep -n "is_imported\|Read-only" src/components/TaskCard.tsx
    grep "Draggable.*is_imported\|disabled.*imported" src/components/TaskCard.tsx
  </verify>
  <done>Imported tasks marked read-only with visual indicator, drag disabled for imported tasks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    GitHub and Jira issue import workflow with sync button, settings configuration, error notifications, and read-only task protection. Users can configure import provider, test credentials, sync issues to Backlog, and see error messages as toasts.
  </what-built>
  <how-to-verify>
    1. Start app: npm run dev + cargo tauri dev
    2. Select project
    3. Verify SyncButton visible in toolbar area, labeled "Configure Import" (grayed out)
    4. Click Settings/Import Settings button
    5. ImportSettings modal opens
    6. Try GitHub configuration:
       - Select "GitHub" radio/toggle
       - Enter owner (e.g., "octocat"), repo (e.g., "Hello-World"), token (valid GitHub PAT)
       - Click "Test Connection" → should show success toast "Connected to GitHub"
       - Click "Save" → settings persisted, modal closes
    7. Back at main board, verify SyncButton now active: "Sync from GitHub" (not grayed out)
    8. Click SyncButton
    9. Loading indicator shows during sync (spinning icon)
    10. Success toast: "Synced X issues from GitHub" (X > 0 if repo has open issues)
    11. Verify new tasks appear in Backlog column with issue titles and descriptions
    12. Scroll to see imported tasks have "Read-only (imported)" badge
    13. Inspect task: try drag-drop (should be prevented or show disabled hint)
    14. Refresh page → imported tasks persist with read-only badge
    15. Test error handling:
        - Click Settings again
        - Change token to invalid value
        - Click Test Connection → error toast with "Unauthorized" or "Bad credentials"
    16. Test Jira configuration:
        - Select "Jira" radio/toggle
        - Enter host (e.g., "mycompany.atlassian.net"), email, API token, JQL
        - Click Test → success/error toast
        - If successful, Save and run sync
    17. Check browser console for errors (should be clean)
  </how-to-verify>
  <resume-signal>Type "approved" if import workflow works end-to-end (settings save, sync imports issues, tasks appear read-only in Backlog, errors show as toasts), or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. npm run build succeeds without errors
2. cargo tauri dev builds without warnings
3. ImportSettings modal opens and configures GitHub/Jira
4. SyncButton triggers import via appropriate IPC handler
5. Issues import to Backlog with correct title and description
6. Syncing existing issues updates rather than duplicates
7. Imported tasks show "Read-only (imported)" badge
8. Drag-drop disabled for imported tasks
9. Error toasts display correctly for network/auth errors
10. Success toasts show imported count
11. TypeScript has no type errors
12. All Phase 2 ROADMAP requirements met:
    - User can import issues from GitHub or Jira project
    - User can view Kanban board with 5 columns
    - User can drag-drop tasks between columns and see changes persist
    - User can create tasks with skills assignment
</verification>

<success_criteria>
- ImportSettings UI allows configuration of GitHub or Jira import provider
- Settings persisted to SQLite via save_import_config IPC
- Test Connection button verifies credentials before saving
- SyncButton disabled until provider configured
- SyncButton triggers sync_github_issues or sync_jira_issues IPC
- Issues imported to Backlog with title and description from remote
- External ID stored for conflict detection (GitHub issue #, Jira key)
- Re-sync updates existing tasks without creating duplicates
- Imported tasks marked as is_imported=true and display "Read-only (imported)" badge
- Imported tasks cannot be dragged (drag-drop disabled)
- Error messages displayed as bottom-right toast notifications
- Success messages shown as green toast with imported count
- Manual sync button (no webhooks or polling)
- All 4 Phase 2 success criteria met (ORCH-01, ORCH-02, ORCH-03, ORCH-04, ORCH-07)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-orchestration/02-05-SUMMARY.md`
</output>
