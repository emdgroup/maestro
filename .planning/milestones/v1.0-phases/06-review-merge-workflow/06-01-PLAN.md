---
phase: 06-review-merge-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ReviewModal.tsx
  - src/components/DiffViewer.tsx
  - src/components/FileTree.tsx
  - src/types/review.ts
  - src/store/reviewStore.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "User can open ReviewModal for task in Review column"
    - "User can see file tree of all changed files on left side"
    - "User can click file in tree and see unified diff with syntax highlighting"
    - "Diff displays 6 context lines around each change hunk"
  artifacts:
    - path: "src/components/ReviewModal.tsx"
      provides: "Main approval workflow UI container"
      min_lines: 50
    - path: "src/components/DiffViewer.tsx"
      provides: "Diff rendering with syntax highlighting"
      min_lines: 40
    - path: "src/components/FileTree.tsx"
      provides: "Collapsible file navigation sidebar"
      min_lines: 30
    - path: "src/store/reviewStore.ts"
      provides: "Zustand store for review state"
      min_lines: 20
    - path: "src/types/review.ts"
      provides: "TypeScript types for review data"
      min_lines: 15
  key_links:
    - from: "src/components/ReviewModal.tsx"
      to: "src/store/reviewStore.ts"
      via: "Zustand hook useReviewStore"
      pattern: "useReviewStore\\(\\)"
    - from: "src/components/ReviewModal.tsx"
      to: "@tauri-apps/api/core"
      via: "invoke to fetch diff"
      pattern: "invoke.*get_diff"
    - from: "src/components/DiffViewer.tsx"
      to: "@git-diff-view/react"
      via: "DiffView component import"
      pattern: "DiffView.*DiffModeEnum"
---

<objective>
Build the diff viewer infrastructure with file tree navigation and syntax highlighting. Users in Review column can inspect agent changes before approval.

Purpose: Provide visual confidence for code review decisions through GitHub-style unified diff with context and syntax highlighting.

Output: ReviewModal component, DiffViewer with @git-diff-view/react, file tree navigation, Zustand store for review state.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-review-merge-workflow/06-CONTEXT.md
@.planning/phases/06-review-merge-workflow/06-RESEARCH.md
@src/components/TaskCard.tsx
@src/store/boardStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install diff viewer dependencies</name>
  <files>package.json</files>
  <action>
    Install @git-diff-view/react, @git-diff-view/shiki, and @git-diff-view/core for diff rendering with syntax highlighting. These are locked per user decision in 06-CONTEXT.md (unified view with syntax highlighting, 5-7 context lines).

    Run:
    ```
    npm install @git-diff-view/react @git-diff-view/shiki @git-diff-view/core
    ```

    Verify installation adds three new packages to package.json.
  </action>
  <verify>npm list @git-diff-view/react @git-diff-view/shiki @git-diff-view/core</verify>
  <done>Three packages installed and listed in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create review types and Zustand store</name>
  <files>src/types/review.ts, src/store/reviewStore.ts</files>
  <action>
    Create TypeScript types for review workflow and Zustand store.

    **src/types/review.ts** — Define review data structures (no ts-rs export needed, frontend-only):
    - `ReviewDecision`: enum with "Approve" and "RequestChanges" values
    - `DiffFile`: { fileName: string; oldContent?: string; newContent: string; hunks: DiffHunk[] }
    - `DiffHunk`: { header: string; lines: DiffLine[] }
    - `DiffLine`: { type: "add" | "remove" | "context" | "header"; content: string; lineNum?: number }
    - `ReviewFeedback`: { taskId: number; decision: ReviewDecision; generalFeedback?: string; perFileComments?: Array<{filePath: string; comment: string}> }

    **src/store/reviewStore.ts** — Zustand store with Immer middleware:
    - `currentTaskId`: number | null
    - `diffData`: DiffFile[] (parsed diff for current task)
    - `selectedFile`: string | null (currently viewed file)
    - `loading`: boolean
    - `error`: string | null
    - Actions: `openReview(taskId)`, `closeReview()`, `selectFile(fileName)`, `setDiffData(files)`, `setError(msg)`

    Follow existing boardStore.ts pattern (Zustand + Immer).
  </action>
  <verify>
    TypeScript compilation succeeds with no errors:
    ```
    npx tsc --noEmit
    ```
  </verify>
  <done>review.ts defines all types, reviewStore.ts creates Zustand store with standard pattern</done>
</task>

<task type="auto">
  <name>Task 3: Create DiffViewer component with @git-diff-view/react</name>
  <files>src/components/DiffViewer.tsx</files>
  <action>
    Create DiffViewer component that renders unified diff with syntax highlighting.

    **Implementation details:**
    - Import DiffView, DiffModeEnum from @git-diff-view/react
    - Import getDiffViewHighlighter from @git-diff-view/shiki
    - Use useEffect to load highlighter asynchronously on mount
    - Accept props: { diffFile: DiffFile | null; loading: boolean; error?: string }
    - Render DiffView with:
      - diffViewMode={DiffModeEnum.Unified} (locked user decision)
      - registerHighlighter={highlighter} for syntax highlighting
      - diffViewTheme="light" (discretionary choice)
      - Full CSS styling to match TaskCard and existing component styling (use CSS variables from Phase 1)
    - Show loading spinner if loading, error message if error
    - Show "No changes" message if diffFile is null
    - Per user decision: 6 context lines (use git diff --unified=6 from backend)

    Import CSS: `import "@git-diff-view/react/styles/diff-view.css"`
  </action>
  <verify>
    Component renders without errors in Storybook or dev mode. Check:
    - npm run dev starts without TypeScript errors
    - DiffView component mounts and accepts diffFile prop
  </verify>
  <done>DiffViewer component renders unified diffs with syntax highlighting, matches app styling</done>
</task>

<task type="auto">
  <name>Task 4: Create FileTree component for file navigation</name>
  <files>src/components/FileTree.tsx</files>
  <action>
    Create FileTree sidebar component for collapsible file navigation (locked user decision per 06-CONTEXT.md).

    **Implementation details:**
    - Accept props: { files: DiffFile[]; selectedFile: string | null; onSelectFile: (fileName: string) => void }
    - Build file hierarchy from flat file list (extract directory structure from fileName paths)
    - Render collapsible folder tree using React recursion or nested lists
    - Each file should show:
      - File name
      - File type indicator (CSS, TS, Rust, etc.)
      - "Added", "Modified", "Deleted" status badge (infer from DiffFile structure)
    - Highlight currently selected file
    - Keyboard navigation: arrow keys to move, Enter to select
    - Click file to select and trigger onSelectFile callback
    - Discretionary: Use simple <details> HTML elements or lightweight collapse library (no new dependency)
    - Styling: Match TaskCard, use CSS variables, left sidebar ~250px width
  </action>
  <verify>
    Check in dev mode:
    - FileTree renders with mock data
    - Click file updates selectedFile
    - Folders collapse/expand
    - Selected file is visually highlighted
  </verify>
  <done>FileTree component provides collapsible file navigation, matches app styling</done>
</task>

<task type="auto">
  <name>Task 5: Create ReviewModal container component</name>
  <files>src/components/ReviewModal.tsx</files>
  <action>
    Create ReviewModal component that orchestrates diff viewer and file tree.

    **Implementation details:**
    - Use @radix-ui/react-dialog (already in project) for modal container
    - Accept props: { taskId: number; isOpen: boolean; onClose: () => void }
    - Use reviewStore to manage state (currentTaskId, diffData, selectedFile, loading, error)
    - On mount, fetch diff for task via IPC: `invoke("get_diff_for_review", { taskId })`
    - Parse diff response into DiffFile[] using simple unified diff parser (or use @git-diff-view/core if needed)
    - Layout: <Modal> containing <div style={{display: "flex"}}>
      - Left: <FileTree files={diffData} selectedFile={selectedFile} onSelectFile={setSelectedFile} />
      - Right: <DiffViewer diffFile={selectedDiffFile} loading={loading} error={error} />
    - Header: Show task name and branch name
    - Footer: "Close" button (Approve/Reject buttons deferred to Plan 06-02)
    - Error handling: Show error message if diff fetch fails, provide retry button
    - Show loading spinner while fetching diff

    Follow existing TaskModal pattern for modal structure.
  </action>
  <verify>
    In dev mode:
    - ReviewModal opens when isOpen prop is true
    - Fetches and displays diff for given taskId
    - File tree and diff viewer render correctly
    - Close button closes modal
  </verify>
  <done>ReviewModal orchestrates diff viewing, ready for approval workflow (Plan 06-02)</done>
</task>

<task type="auto">
  <name>Task 6: Create Rust IPC handler for diff generation</name>
  <files>src-tauri/src/ipc/handlers.rs</files>
  <action>
    Add Tauri IPC command `get_diff_for_review` that invokes Node.js sidecar to generate diff.

    **Implementation:**
    - Create handler: `async fn get_diff_for_review(app_state: State<Arc<AppState>>, task_id: i32) -> Result<String, String>`
    - Query database for task details (id, name, branch_name, worktree_path)
    - Call Node.js sidecar function: `getDiffBetweenBranches(repoPath, branchName, "main")` with --unified=6 (locked per user decision)
    - Return raw unified diff string
    - Error handling: Return descriptive error if task not found, worktree path invalid, or git command fails

    Use existing sidecar integration pattern from Phase 5 (tokio::process::Command to spawn sidecar).
  </action>
  <verify>
    npm run tauri:dev and test via browser console:
    ```
    invoke("get_diff_for_review", { task_id: 1 })
      .then(diff => console.log(diff))
      .catch(err => console.error(err))
    ```
    Should return unified diff string or error message.
  </verify>
  <done>IPC handler generates and returns diff for review modal</done>
</task>

<task type="auto">
  <name>Task 7: Create Node.js sidecar diff functions</name>
  <files>sidecar/src/merge-manager.ts</files>
  <action>
    Extend Node.js sidecar with diff generation functions (or create merge-manager.ts if doesn't exist).

    **Implementation:**
    - Export async function `getDiffBetweenBranches(repoPath: string, fromBranch: string, toBranch: string, contextLines: number = 6): Promise<string>`
    - Use `simpleGit(repoPath).diff([...])` with:
      - `--unified=6` (or contextLines parameter, locked per user decision)
      - `--function-context` (show function names in hunk headers)
      - `--no-ext-diff` (avoid external diff drivers)
    - Export async function `parseDiffString(diffString: string): Promise<DiffFile[]>` to parse unified diff into structured format
      - Extract file names from `diff --git a/... b/...` lines
      - Extract hunks from `@@ ... @@` markers
      - Build DiffLine[] from +, -, and context lines
      - Return array of DiffFile objects matching frontend type

    Reference RESEARCH.md Pattern 1 and Pattern 2 for implementation details.
  </action>
  <verify>
    Test in Node.js:
    ```
    import { getDiffBetweenBranches, parseDiffString } from "./merge-manager";
    const diff = await getDiffBetweenBranches("/path/to/repo", "agent-task-1", "main");
    const parsed = await parseDiffString(diff);
    console.log(parsed);  // Should print array of DiffFile objects
    ```
  </verify>
  <done>Sidecar provides diff generation and parsing functions</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. ReviewModal opens for task in Review column
2. Diff viewer displays unified diff with syntax highlighting
3. File tree shows all changed files with correct status badges
4. Clicking file in tree updates selected file
5. Context lines (6) visible around each change
6. Component styling matches app theme
7. Error handling shows meaningful messages
8. IPC handler successfully fetches diff from sidecar
9. npm run tauri:dev starts without errors
10. TypeScript compilation succeeds
</verification>

<success_criteria>
- User can open ReviewModal for any task in Review column
- Diff viewer renders unified diffs with syntax highlighting per language
- File tree navigation allows selecting and viewing individual file diffs
- 6 context lines displayed around each change hunk (locked user decision)
- ReviewModal integrates with existing TaskCard to launch from Review column
- No new external dependencies beyond @git-diff-view/* packages
- Component styling consistent with existing app (CSS variables, Radix UI)
</success_criteria>

<output>
After completion, create `.planning/phases/06-review-merge-workflow/06-01-SUMMARY.md` documenting:
- DiffViewer component structure and @git-diff-view/react integration
- FileTree file hierarchy algorithm and collapsing logic
- ReviewModal orchestration and IPC call flow
- Sidecar getDiffBetweenBranches function and diff parsing
- GitHub-style diff presentation achieved (unified view, syntax highlighting, context lines)
</output>
