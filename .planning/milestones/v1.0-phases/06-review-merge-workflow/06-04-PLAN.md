---
phase: 06-review-merge-workflow
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TaskCard.tsx
  - src/components/KanbanBoard.tsx
  - src/App.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can click Review button on TaskCard for tasks in Review column"
    - "ReviewModal opens and displays diff viewer for selected task"
    - "User can see file tree with changed files on left sidebar"
    - "User can navigate between files and view diffs"
  artifacts:
    - path: "src/components/TaskCard.tsx"
      provides: "Review button for Review status tasks"
      pattern: "Review.*button.*Review.*status"
    - path: "src/components/KanbanBoard.tsx"
      provides: "ReviewModal state management and rendering"
      pattern: "ReviewModal.*useState.*setSelectedTaskForReview"
    - path: "src/components/ReviewModal.tsx"
      provides: "Diff viewer UI component (already exists)"
      min_lines: 171
  key_links:
    - from: "TaskCard (Review status)"
      to: "KanbanBoard state (setSelectedTaskForReview)"
      via: "onClick handler on Review button"
      pattern: "onClick.*setSelectedTaskForReview"
    - from: "KanbanBoard state"
      to: "ReviewModal component"
      via: "conditional render with task prop"
      pattern: "selectedTaskForReview &&.*ReviewModal"
    - from: "ReviewModal"
      to: "DiffViewer and FileTree"
      via: "useEffect fetching diff data"
      pattern: "get_diff_for_review.*invoke"
---

<objective>
Wire the existing ReviewModal component into the UI so users can access the diff viewer from Review column tasks.

Purpose: Close verification gap where ReviewModal and DiffViewer components exist but are not accessible from the UI. This is the UI integration layer missing from Phase 6 Plan 01 implementation.

Output: Functional Review button on Review-status TaskCards that opens ReviewModal with diff viewer.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-review-merge-workflow/06-01-SUMMARY.md
@.planning/phases/06-review-merge-workflow/06-VERIFICATION.md

# Gap Closure Reference
Gap 1 from verification: ReviewModal exists (171 lines, fully implemented) but NOT imported anywhere. Need to:
- Import ReviewModal into KanbanBoard or App
- Add Review button to TaskCard (conditioned on task.status === 'Review')
- Wire button to open modal with selected task

Existing components already built and substantive:
- src/components/ReviewModal.tsx (171 lines) - full component with DiffViewer + FileTree + ApprovalForm integration
- src/components/DiffViewer.tsx (96 lines) - renders unified diff with syntax highlighting
- src/components/FileTree.tsx (244 lines) - collapsible file navigation
- src/components/ApprovalForm.tsx (229 lines) - approval form already wired to ReviewModal (lines 156-166)
</context>

<tasks>

<task type="auto">
  <name>Add ReviewModal state management to KanbanBoard</name>
  <files>src/components/KanbanBoard.tsx</files>
  <action>
Add ReviewModal state to KanbanBoard component to manage which task is selected for review viewing:

1. Import ReviewModal at top: `import { ReviewModal } from "./ReviewModal";`
2. Add useState hooks for review modal state:
   ```tsx
   const [reviewModalOpen, setReviewModalOpen] = useState(false);
   const [selectedTaskId, setSelectedTaskId] = useState<number | null>(null);
   const [selectedTaskName, setSelectedTaskName] = useState<string>("");
   ```
3. Render ReviewModal conditionally at bottom of component:
   ```tsx
   {reviewModalOpen && selectedTaskId && (
     <ReviewModal
       taskId={selectedTaskId}
       taskName={selectedTaskName}
       isOpen={reviewModalOpen}
       onClose={() => {
         setReviewModalOpen(false);
         setSelectedTaskId(null);
         setSelectedTaskName("");
       }}
     />
   )}
   ```
4. Pass review state setters to TaskCard via prop (see next task)

Implementation notes:
- ReviewModal takes taskId, taskName, isOpen, onClose (NOT task object)
- Use three separate state variables for clean state management
- onClose callback should reset all three state variables
- ReviewModal will fetch diff data internally via useEffect
  </action>
  <verify>
Check that KanbanBoard.tsx imports ReviewModal and contains:
- `const [reviewModalOpen, setReviewModalOpen] = useState(false);`
- `const [selectedTaskId, setSelectedTaskId] = useState<number | null>(null);`
- `const [selectedTaskName, setSelectedTaskName] = useState<string>("");`
- Conditional render: `{reviewModalOpen && selectedTaskId && <ReviewModal ... />`
- ReviewModal props: taskId, taskName, isOpen, onClose (NOT task)
  </verify>
  <done>KanbanBoard state management for ReviewModal is in place with correct prop interface; three state variables exist and ReviewModal is conditionally rendered</done>
</task>

<task type="auto">
  <name>Add Review button to TaskCard for Review status tasks</name>
  <files>src/components/TaskCard.tsx</files>
  <action>
Add a Review button to TaskCard that is only visible for tasks in Review status and triggers the modal:

1. Review function signature: TaskCard should accept prop: `onReviewClick?: (taskId: number, taskName: string) => void;`
2. In TaskCard JSX, add a Review button that only renders when `task.status === 'Review'`:
   ```tsx
   {task.status === 'Review' && (
     <button
       onClick={() => onReviewClick?.(task.id, task.name)}
       className="px-3 py-1 bg-blue-500 hover:bg-blue-600 text-white text-sm rounded"
       title="View diff and approve/reject changes"
     >
       Review
     </button>
   )}
   ```
3. Style button to match existing action buttons on card (check DoneButton or EditButton for style reference)
4. Position button in action buttons area (typically bottom-right of card or action row)

Implementation notes:
- Condition strictly on `task.status === 'Review'` (not other statuses)
- Pass taskId and taskName separately (NOT full task object) to match ReviewModal interface
- Keep button styling consistent with other TaskCard buttons
- Add title/tooltip for UX clarity
  </action>
  <verify>
In browser or test:
1. Navigate to a task in Review column
2. Verify Review button is visible (only on Review status tasks)
3. Verify Review button is NOT visible on other status tasks (Backlog, Ready, InProgress, Done)
4. Click Review button - should trigger onReviewClick prop
  </verify>
  <done>Review button appears on Review-status TaskCards and calls onReviewClick handler</done>
</task>

<task type="auto">
  <name>Wire Review button to ReviewModal state in KanbanBoard</name>
  <files>src/components/KanbanBoard.tsx</files>
  <action>
Connect TaskCard's Review button to KanbanBoard's ReviewModal state:

1. When rendering TaskCard, pass onReviewClick handler:
   ```tsx
   <TaskCard
     task={task}
     onReviewClick={(taskId, taskName) => {
       setSelectedTaskId(taskId);
       setSelectedTaskName(taskName);
       setReviewModalOpen(true);
     }}
     ...otherProps
   />
   ```
2. Verify that clicking Review button sets all three review modal state variables
3. Verify that ReviewModal opens with the correct taskId and taskName

Testing approach:
- No automatic tests needed; manual verification in UI is sufficient
- Clicking Review button should set selectedTaskId, selectedTaskName, and reviewModalOpen
- ReviewModal should show the diff for taskId (fetched via useEffect inside ReviewModal)
- Closing ReviewModal should clear all three state variables
  </action>
  <verify>
Manual test in browser:
1. Create or find a task in Review column
2. Click Review button on task
3. ReviewModal should open
4. DiffViewer should render (check for diff content loading)
5. FileTree should show on left side with changed files
6. Close modal (X button or backdrop click) - modal should close and selectedTaskForReview clears
  </verify>
  <done>Review button is fully wired; clicking it opens ReviewModal with diff viewer for the selected task</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. ReviewModal is imported into KanbanBoard.tsx
2. TaskCard has Review button visible only for Review status tasks
3. Clicking Review button opens ReviewModal with selected task
4. DiffViewer and FileTree render inside ReviewModal
5. Modal closes cleanly and resets state
6. No TypeScript errors; code compiles
</verification>

<success_criteria>
- ReviewModal component is accessible from the UI (not orphaned)
- User can open diff viewer by clicking Review button on Review-status tasks
- DiffViewer displays the diff for the agent's branch vs main
- FileTree shows changed files with selection and navigation
- ApprovalForm is accessible via "Proceed to Approval" button inside ReviewModal
- All existing components (ReviewModal, DiffViewer, FileTree, ApprovalForm) remain unchanged - only wiring is added
</success_criteria>

<output>
After completion, create `.planning/phases/06-review-merge-workflow/06-04-SUMMARY.md` with:
- What was wired (components, state management, event handlers)
- Key files modified (TaskCard.tsx, KanbanBoard.tsx)
- Link to ReviewModal component (src/components/ReviewModal.tsx)
- Next step: Execute Plan 06-05 to fix sidecar --merge CLI handler
</output>
