---
phase: 07-configuration-management
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/ProjectSettingsModal.tsx
  - src/components/ProjectSettingsModal.css
  - src/App.tsx
  - src/store/configStore.ts
autonomous: true

must_haves:
  truths:
    - "User can access project settings via gear icon in header"
    - "Project settings modal allows user to select Claude model from dropdown"
    - "Project settings modal shows checkboxes for MCP servers (opt-in)"
    - "Project settings modal shows checkboxes for Skills (opt-in)"
    - "Configuration changes persist when user saves"
    - "Configuration state is reset when modal closes without saving"
  artifacts:
    - path: "src/components/ProjectSettingsModal.tsx"
      provides: "Modal UI for project-level configuration"
      min_lines: 100
    - path: "src/components/ProjectSettingsModal.css"
      provides: "Modal styling (layout, fieldsets, buttons)"
    - path: "src/store/configStore.ts"
      provides: "Zustand store for project configuration state with lifecycle management"
      exports: ["useConfigStore", "AVAILABLE_MCP_SERVERS", "AVAILABLE_SKILLS", "AVAILABLE_MODELS"]
    - path: "src/App.tsx"
      provides: "Gear icon button to open ProjectSettingsModal"
      pattern: "ProjectSettingsModal"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/ProjectSettingsModal.tsx"
      via: "component import and render with state"
    - from: "src/components/ProjectSettingsModal.tsx"
      to: "src/store/configStore.ts"
      via: "useConfigStore hook to fetch, update, and clear state"
    - from: "src/components/ProjectSettingsModal.tsx"
      to: "IPC handlers"
      via: "invoke('get_project_settings') on open, invoke('update_project_settings') on save"
---

<objective>
Build project-level settings UI where users can configure Claude model defaults and MCP/Skills allowlists that apply to all tasks unless overridden.

Purpose: Implements user decision from CONTEXT.md: "Project-level configuration lives in dedicated settings screen, accessed via gear icon in header."

Output:
- ProjectSettingsModal component with react-hook-form + Radix UI Dialog
- Configurable fields: model dropdown, MCP checkboxes, Skills checkboxes
- Zustand store for configuration state management with proper lifecycle
- Gear icon button in header to open modal
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-configuration-management/07-CONTEXT.md
@.planning/phases/07-configuration-management/07-RESEARCH.md
@.planning/phases/07-configuration-management/07-01-SUMMARY.md

# Prior patterns
@.planning/phases/02-core-orchestration/02-03-SUMMARY.md
@.planning/phases/06-review-merge-workflow/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zustand store for project configuration with lifecycle management</name>
  <files>src/store/configStore.ts</files>
  <action>
    Create new file src/store/configStore.ts with Zustand store for configuration state.

    Define ConfigState interface:
    - model_default: string (e.g., "claude-opus-4-5")
    - mcp_allowlist: string[] (e.g., ["filesystem", "web"])
    - skills_default: string[] (e.g., ["javascript"])
    - isLoading: boolean (for async operations)
    - error: string | null

    Create Zustand store with useConfigStore hook using Immer middleware (consistent with boardStore pattern):
    - setState(config: Partial<ConfigState>) - update configuration state after fetch
    - setModelDefault(model) - update model
    - setMcpAllowlist(list) - update MCP servers
    - setSkillsDefault(list) - update skills
    - setLoading(loading) - set loading state
    - setError(error) - set error state
    - clearError() - clear error state
    - resetConfig() - reset all fields to empty defaults (called when modal closes)
    - clearConfig() - clear all state including error/loading (for project switches)

    resetConfig() should set:
    - model_default: ""
    - mcp_allowlist: []
    - skills_default: []
    - isLoading: false
    - error: null

    Export const AVAILABLE_MCP_SERVERS = ["filesystem", "web", "git"] (hardcoded list for Phase 7)
    Export const AVAILABLE_SKILLS = ["javascript", "python", "react", "rust"] (hardcoded list for Phase 7)
    Export const AVAILABLE_MODELS = ["claude-opus-4-5", "claude-3-5-sonnet"] (two primary models)

    Store pattern should match src/store/boardStore.ts structure (Immer + Zustand).
    This store will be consumed by ProjectSettingsModal to hold fetched configuration state.
  </action>
  <verify>
    Test import: Import useConfigStore from src/store/configStore. No TypeScript errors. Store exports expected functions and constants (AVAILABLE_MCP_SERVERS, AVAILABLE_SKILLS, AVAILABLE_MODELS).

    Test lifecycle: Call useConfigStore(), verify resetConfig() sets all fields to defaults. Call clearConfig(), verify state is empty. No TypeScript errors.
  </verify>
  <done>
    configStore.ts created. Zustand store with configuration state, lifecycle actions (resetConfig, clearConfig), and constants. Store compiles without errors. Lifecycle functions verified to work correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ProjectSettingsModal with configStore integration and IPC verification</name>
  <files>src/components/ProjectSettingsModal.tsx, src/components/ProjectSettingsModal.css</files>
  <action>
    Create ProjectSettingsModal.tsx (React component using react-hook-form + Radix UI Dialog + configStore).

    Props interface:
    - isOpen: boolean
    - onClose: () => void
    - projectId: number

    Component logic:
    - Import useConfigStore and destructure: { model_default, mcp_allowlist, skills_default, isLoading, setState, setLoading, setError, clearError, resetConfig } = useConfigStore()
    - Use useForm<ProjectSettingsFormData>() hook with react-hook-form
    - formData structure: { model_default: string, mcp_servers: Record<string, boolean>, skills: Record<string, boolean> }
    - On modal open (useEffect with [isOpen, projectId] dependency):
      * If isOpen is true: setLoading(true), clearError()
      * Fetch via invoke('get_project_settings', { project_id: projectId })
      * Parse response: convert Vec<String> arrays to Record<string, boolean> (e.g., ["filesystem"] → {"filesystem": true, "web": false, "git": false})
      * setState() with fetched values to update Zustand store
      * Populate form defaultValues with converted checkbox record data
      * setLoading(false)
      * If fetch fails: setError(error_message), setLoading(false)
    - On modal close (onClose callback): Call resetConfig() to clear state
    - On form mount, use Zustand state values as defaultValues in useForm

    Form fields:
    1. Model Dropdown: <select> using register("model_default"), options from AVAILABLE_MODELS
    2. MCP Servers fieldset: Loop over AVAILABLE_MCP_SERVERS (from store export), checkbox input per server using register(`mcp_servers.${server}`)
    3. Skills fieldset: Loop over AVAILABLE_SKILLS (from store export), checkbox input per skill using register(`skills.${skill}`)

    Submit handler (onSubmit):
    - Validate form data: model_default not empty (required)
    - Convert checkbox records back to arrays: Object.entries(data.mcp_servers).filter(([_, enabled]) => enabled).map(([server]) => server)
    - Call invoke('update_project_settings', { project_id, settings: { model_default, mcp_allowlist, skills_default } }) with await
    - Verify response is successful (no error in invoke)
    - On success: setState() to update Zustand store with saved values, display success indicator (optional toast), close modal via onClose()
    - On error: setError() to display error message in modal banner, do NOT close modal

    Radix Dialog wrapper structure (use existing TaskModal.tsx as template):
    - <Dialog.Root open={isOpen} onOpenChange={onClose}>
    - <Dialog.Portal> with <Dialog.Overlay> and <Dialog.Content>
    - <Dialog.Title>Project Configuration</Dialog.Title>
    - <Dialog.Description> explaining what this modal does
    - Form with fieldsets for each section
    - Save and Close buttons (disable Save button while isLoading or form is invalid)
    - Error banner (if error state exists, display in red)

    Reference: Phase 02-03 TaskModal.tsx, react-hook-form docs, and boardStore pattern for Zustand integration.
  </action>
  <verify>
    npm run build

    Component compiles without TypeScript errors. Radix UI imports work. react-hook-form useForm hook works. useConfigStore hook imported and called.

    Manual verification of IPC integration:
    - Verify form loads with values from invoke('get_project_settings')
    - Verify form fields display as checkboxes (with correct null vs array handling)
    - Verify form submit calls invoke('update_project_settings') with correct payload format: { project_id: number, settings: { model_default: string, mcp_allowlist: string[], skills_default: string[] } }
    - Verify setState() is called after successful save (store updated)
    - Verify error state displays error message on IPC failure
    - Verify resetConfig() is called on modal close
    - Verify loading indicator shows during fetch

    No unused variables or props.
  </verify>
  <done>
    ProjectSettingsModal.tsx created. Component accepts isOpen, onClose, projectId props. Uses react-hook-form for validation and useConfigStore for state management. Fetches settings on modal open, validates response, updates Zustand store, saves settings via IPC with proper error handling. Modal closes after successful save and resets state. IPC integration verified end-to-end (fetch, save, error handling).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add modal styling and header integration</name>
  <files>src/components/ProjectSettingsModal.css, src/App.tsx</files>
  <action>
    Create ProjectSettingsModal.css with styles for the modal dialog. Base styling on existing TaskModal.css and ReviewModal.css patterns:
    - .project-settings-modal: Modal container (max-width 500px, centered, light background)
    - fieldset: Bordered containers for form sections (margin-bottom 20px)
    - legend: Section titles (font-weight bold, margin-bottom 10px)
    - label: Checkbox labels (display flex, align-items center, cursor pointer)
    - input[type="checkbox"]: Margin-right 8px
    - input[type="text"], select: Full-width, padding, border (match existing form styling)
    - button: Save and Cancel buttons (primary and secondary styles)
    - .error-banner: Red text or background for error messages
    - .loading-indicator: Optional spinner or disabled state for async operations

    Use CSS variables from existing theme (e.g., --bg-primary, --text-primary, --border-color) for consistency.

    Update App.tsx to add gear icon button in header:
    - Import ProjectSettingsModal component
    - Add useState<boolean> for modalOpen state
    - Add gear icon button in header (right side, near other controls)
    - On button click, set modalOpen(true)
    - Render <ProjectSettingsModal isOpen={modalOpen} onClose={() => setModalOpen(false)} projectId={currentProjectId} />

    Get currentProjectId from existing project context (may be in boardStore or AppState - check App.tsx structure).

    Reference: Existing header in App.tsx or KanbanBoard.tsx for button placement pattern.
  </action>
  <verify>
    pnpm dev

    App starts without errors. Gear icon visible in header. Clicking icon opens ProjectSettingsModal. Modal renders form controls (dropdowns, checkboxes). Loading state shows while fetching. Error messages display correctly. Modal closes after successful save. No styling issues or broken layout.
  </verify>
  <done>
    ProjectSettingsModal.css created with consistent styling. App.tsx updated with gear icon button. Header has visible settings access point. Modal opens and closes correctly. Zustand store integration complete (fetches on open, updates on save, resets on close). Lifecycle management verified.
  </done>
</task>

</tasks>

<verification>
- [ ] configStore.ts exports Zustand store and constants (AVAILABLE_MCP_SERVERS, AVAILABLE_SKILLS, AVAILABLE_MODELS)
- [ ] configStore includes resetConfig() and clearConfig() lifecycle functions
- [ ] ProjectSettingsModal component compiles and renders without errors
- [ ] Form fields (model dropdown, MCP checkboxes, Skills checkboxes) display correctly
- [ ] Fetching settings from backend populates Zustand store and form on open
- [ ] Form payload format verified: { project_id: number, settings: { model_default, mcp_allowlist, skills_default } }
- [ ] Saving settings calls invoke('update_project_settings') with correct payload
- [ ] Zustand store updated with saved values after successful IPC call
- [ ] resetConfig() called on modal close (state cleared)
- [ ] Error handling displays error messages in modal (via store error state)
- [ ] Gear icon in App.tsx header opens/closes modal
- [ ] Modal styling is consistent with existing UI (TaskModal, ReviewModal patterns)
- [ ] No TypeScript errors in component or store
- [ ] configStore is actively used by ProjectSettingsModal
- [ ] IPC integration verified end-to-end (fetch → parse → display → save → error handling)
</verification>

<success_criteria>
- [ ] Zustand configStore created with lifecycle management (reset/clear actions)
- [ ] ProjectSettingsModal component displays all three configuration sections (model, MCP, Skills)
- [ ] Form loads initial settings from backend on modal open and updates Zustand store
- [ ] Saving configuration persists to database via IPC and updates Zustand store
- [ ] Gear icon in header triggers modal open
- [ ] Modal closes after successful save and resets state
- [ ] Error messages display if save fails (from Zustand error state)
- [ ] All form fields populated with current values from Zustand store
- [ ] UI consistent with existing modal patterns (TaskModal, ReviewModal)
- [ ] configStore actively used by ProjectSettingsModal (not orphaned)
- [ ] IPC integration complete: fetch, parse, save, error handling all verified
- [ ] Null vs array handling verified for checkboxes (RFC-04)

</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-management/07-02-SUMMARY.md` documenting:
- ProjectSettingsModal component structure and props
- Zustand configStore actions and state shape (including lifecycle functions)
- Form field mappings (checkbox groups to arrays)
- IPC integration (get_project_settings, update_project_settings)
- Header integration with gear icon
- Styling approach and CSS variables used
- configStore integration points (fetch on open, update on save, reset on close)
- IPC payload format (project_id, settings object)
- Lifecycle management (modal close behavior)
</output>
