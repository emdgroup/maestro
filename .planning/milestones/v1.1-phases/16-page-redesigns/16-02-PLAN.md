---
phase: 16-page-redesigns
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/App.tsx
  - src/components/AppHeader.tsx
  - src/components/AgentMonitor.tsx
  - src/components/WorktreeManager.tsx
  - src/components/ProjectSettingsModal.tsx
autonomous: true
must_haves:
  truths:
    - "App header displays project name, global status indicators (agents running, worktrees count), and horizontal navigation tabs"
    - "Navigation tabs (Kanban, Agent Monitor, Worktree Manager, Settings) switch between pages instantly with active tab highlighted by background color"
    - "Agent monitor displays agent list sidebar with status indicators and terminal output pane with monospace font and semantic prefix coloring"
    - "Worktree manager displays cards with git status (clean checkmark, dirty dot), branch name, and commit info"
    - "Settings panel uses sectioned layout with icons, form controls via shadcn components, and visual hierarchy"
  artifacts:
    - path: "src/App.tsx"
      provides: "Main app structure with state management for page switching"
      contains: "useState.*activePage|page"
    - path: "src/components/AppHeader.tsx"
      provides: "Header with project name, status indicators, and navigation tabs"
      contains: "Tabs|TabsList|TabsTrigger"
    - path: "src/components/AgentMonitor.tsx"
      provides: "Split-pane agent interface (new component)"
      contains: "flex|gap"
    - path: "src/components/WorktreeManager.tsx"
      provides: "Worktree card display with git status (new component)"
      contains: "grid"
    - path: "src/components/ProjectSettingsModal.tsx"
      provides: "Settings panel with sections and form controls"
      contains: "Form|section"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/AppHeader.tsx"
      via: "header component rendering with currentProject"
      pattern: "AppHeader.*currentProject"
    - from: "src/components/AppHeader.tsx"
      to: "Tabs component"
      via: "page navigation via Radix Tabs"
      pattern: "Tabs.*TabsTrigger"
    - from: "src/App.tsx"
      to: "AgentMonitor, WorktreeManager, ProjectSettingsModal"
      via: "conditional rendering based on activePage state"
      pattern: "activePage.*==.*render"
---

<objective>
Redesign the app header with modern navigation structure, create new Agent Monitor and Worktree Manager pages, and update the Settings panel with consistent visual hierarchy. This transforms the application from a single-page Kanban view into a multi-page interface with clear navigation and unified design aesthetic.

Purpose: Navigation is the backbone of the user experience. Establishing a clean, modern header with tab-based page switching and creating dedicated pages for agents and worktrees gives users clear mental models for exploring different aspects of their project. The settings redesign provides a professional, sectioned interface for configuration.

Output: Modern app header with Tabs navigation, three new/redesigned pages (Agent Monitor, Worktree Manager, Settings), and updated App.tsx to orchestrate page switching with proper state management.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-page-redesigns/16-CONTEXT.md
@.planning/phases/16-page-redesigns/16-RESEARCH.md
@.planning/phases/15-component-and-design-system/15-03-SUMMARY.md
@.planning/phases/16-page-redesigns/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create AppHeader component with modern navigation tabs and global status indicators</name>
  <files>src/components/AppHeader.tsx</files>
  <action>
    Create new AppHeader component replacing the inline header in App.tsx:

    1. **Component structure:**
       - Left section: Project name (h1) + global status indicators (agents running, worktrees count)
       - Center section: Horizontal tab navigation (Kanban, Agent Monitor, Worktree Manager, Settings)
       - Right section: Action buttons (placeholders for now)

    2. **Tab navigation:**
       - Use shadcn/ui Tabs component (already installed in Phase 15)
       - Import: `import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs"`
       - Values: "kanban", "agents", "worktrees", "settings"
       - Active tab gets `bg-background` highlight (different from inactive `bg-muted`)
       - Inactive tabs on `bg-muted` background

    3. **Status indicators:**
       - Simple small text in muted foreground color
       - Format: "● X agents running  •  Y worktrees"
       - Use semantic bullet (●) or simple dot

    4. **Styling:**
       - Header container: `border-b bg-card shadow-sm`
       - Padding: `p-3` (compact, 12px per locked decisions)
       - Flex layout for alignment: `flex items-center justify-between`
       - Text sizes: `text-xs` for status, `text-sm` for tabs

    5. **Props:**
       - `currentProject: Project | null`
       - `activePage: string`
       - `onPageChange: (page: string) => void`
       - `agentsRunning: number` (optional, default 0)
       - `worktreesCount: number` (optional, default 0)

    6. **Per locked decisions:**
       - Active indicator: Background highlight (not underline)
       - Transition: Instant (no animation) per "page transitions: instant swap"
       - Color palette: Use semantic colors from Phase 15 (muted, foreground, accent)

    Reference RESEARCH.md "Header Navigation Tab Pattern" for example code.
  </action>
  <verify>
    1. Component renders without errors
    2. Project name displays on left
    3. Status indicators show (e.g., "● 0 agents running  •  0 worktrees")
    4. Four tab triggers visible: Kanban, Agent Monitor, Worktree Manager, Settings
    5. Active tab has different background color from inactive tabs
    6. Tab click calls onPageChange callback
  </verify>
  <done>
    AppHeader component renders modern navigation with project context, status indicators, and active tab styling; ready to integrate into App.tsx.
  </done>
</task>

<task type="auto">
  <name>Create AgentMonitor component with split-pane layout (sidebar + terminal output)</name>
  <files>src/components/AgentMonitor.tsx</files>
  <action>
    Create new AgentMonitor page component showing agent list and execution terminal:

    1. **Layout structure (split-pane):**
       - Left sidebar: Agent list (15-20% width)
       - Right pane: Terminal output (80-85% width)
       - Use flex layout: `flex gap-4 h-screen` or similar
       - Both panes scrollable independently

    2. **Agent sidebar:**
       - List agents with status dots (colored, pulsing for active)
       - Show agent name, status (Running/Idle/Error), metrics if available
       - Compact cards: `p-3 rounded-lg border border-border bg-card`
       - Status dot: semantic colors (blue=running with pulse, gray=idle, red=error)
       - Clickable to select agent (highlight selected agent)
       - Hover effects: shadow lift + border shift (same as task cards)

    3. **Terminal output pane:**
       - Monospace font (use Phase 15 typography: `font-mono` or FiraCode)
       - Dark background appropriate for terminal (bg-muted or darker)
       - Log lines with semantic prefix coloring:
         - `[INFO]` = blue
         - `[WARN]` = yellow
         - `[ERROR]` = red
         - `[SUCCESS]` = green
       - Live scroll (auto-scroll to bottom on new output)
       - Compact line height for density

    4. **Initial state:**
       - Empty agent list message if no agents
       - Empty terminal message if no output
       - Handle loading state gracefully

    5. **Styling:**
       - Container: `flex flex-col h-full p-4 gap-4`
       - Sidebar width: fixed or flex-basis, but compact
       - Use Tailwind classes, no custom CSS
       - Match Kanban design language (subtle shadows, rounded corners, semantic colors)

    6. **Props:**
       - `projectId: number`
       - `agents?: Agent[]` (optional, empty array default)
       - `activeAgentId?: number | null`
       - `onAgentSelect?: (agentId: number) => void`

    **Note:** This component is a layout shell. Actual agent data and terminal integration comes later. For now, render placeholder content.
  </action>
  <verify>
    1. Component renders split-pane layout
    2. Left sidebar shows placeholder agent list
    3. Right pane shows placeholder terminal output
    4. Both panes are scrollable
    5. Status dots render in agent list
    6. Terminal text uses monospace font
    7. Styling matches Kanban design (shadows, rounded corners, colors)
  </verify>
  <done>
    AgentMonitor component renders split-pane layout with sidebar and terminal pane; styling matches design system; ready for data integration in later phases.
  </done>
</task>

<task type="auto">
  <name>Create WorktreeManager component displaying worktree cards with git status indicators</name>
  <files>src/components/WorktreeManager.tsx</files>
  <action>
    Create new WorktreeManager page component showing worktree instances:

    1. **Layout:**
       - Grid of cards (similar to Kanban, but single column or 2 columns depending on space)
       - Use `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 p-4`
       - Each card is a worktree instance

    2. **Worktree card content:**
       - Top section: Branch name (prominent, lg font weight)
       - Middle section: Git status indicator + "clean" or "dirty" label
         - Use semantic colors: Green checkmark for clean, yellow dot for dirty
         - Simple inline icon + text
       - Bottom section: Compact info (last commit, author, timestamp)
       - Metadata: text-xs, muted foreground color

    3. **Git status visualization:**
       - Clean: Green checkmark (✓) + "Clean" text
       - Dirty: Yellow dot (●) + "Dirty" text
       - Use inline semantic icons or simple Unicode symbols
       - Position: Top-right corner of card or inline with branch name

    4. **Card styling:**
       - Base: `rounded-lg border border-border bg-card shadow-sm p-3`
       - Hover: `hover:shadow-md hover:border-ring transition-all duration-200`
       - Match TaskCard aesthetic for consistency
       - Cursor pointer if clickable

    5. **Content structure:**
       ```
       Branch: main
       [✓ Clean]
       Last commit: 2 hours ago
       Author: user@example.com
       ```

    6. **Props:**
       - `projectId: number`
       - `worktrees?: Worktree[]` (optional, empty array default)
       - `onWorktreeClick?: (worktreeId: number) => void`

    **Note:** This is a layout shell with placeholder data. Real worktree data integration comes later.
  </action>
  <verify>
    1. Component renders grid of cards
    2. Each card shows branch name prominently
    3. Git status indicator displays (checkmark or dot)
    4. Card has subtle shadow and border
    5. Hover effects trigger (shadow + border color)
    6. Placeholder content visible
    7. Responsive grid layout on different screen sizes
  </verify>
  <done>
    WorktreeManager component renders grid of worktree cards with git status indicators; styling matches design system; placeholder data renders correctly.
  </done>
</task>

<task type="auto">
  <name>Update ProjectSettingsModal to modern sectioned layout with shadcn form controls</name>
  <files>src/components/ProjectSettingsModal.tsx</files>
  <action>
    Modernize ProjectSettingsModal with sectioned layout and proper typography hierarchy:

    1. **Modal structure:**
       - Keep existing dialog wrapper (shadcn/ui Dialog already in use)
       - Inside dialog, create sections for different setting groups
       - Sections: Project, Model Defaults, MCP Configuration, Skills, Theme

    2. **Section layout:**
       - Each section: `mb-6` spacing (compact but grouped)
       - Section header: `font-semibold text-lg mb-3` (consistent hierarchy)
       - Optional section icon next to title (e.g., ⚙️ Project, 🤖 Model)
       - Subtle divider between sections (border-t) optional

    3. **Form controls:**
       - Use shadcn/ui components:
         - Input: `<Input />` for text fields
         - Select: `<Select />` for dropdowns
         - Checkbox: `<Checkbox />` for toggles
         - Label: `<Label />` for all inputs
         - Textarea: `<Textarea />` for multi-line text

    4. **Form field layout:**
       - Label: `text-sm font-medium mb-2 block` (stack above input)
       - Input: full width, consistent padding
       - Helper text below input: `text-xs text-muted-foreground`
       - Spacing between fields: `mb-4`

    5. **Styling:**
       - Container padding: `p-4` or `p-6` (compact, spacious breathing room)
       - No custom CSS; all Tailwind utilities
       - Text sizes: `text-xs` (labels/help), `text-sm` (body), `text-lg` (headers)
       - Colors: Semantic from Phase 15 (text-muted-foreground for secondary)

    6. **Button actions:**
       - Bottom of modal: Save + Cancel buttons
       - Save: `btn-primary` (accent color)
       - Cancel: `btn-secondary` (muted)
       - Spacing: `gap-3 flex justify-end mt-6`

    7. **Current settings display:**
       - Show current theme selection (if available)
       - Show current model default
       - Show MCP allowlist as badges or list

    **Approach:** Update existing modal incrementally. If modal has custom CSS, replace with Tailwind utilities. Keep form logic intact, update only styling and layout.
  </action>
  <verify>
    1. Modal opens and closes
    2. All form sections render with clear visual hierarchy
    3. Form controls are functional (inputs respond, selects dropdown, checkboxes toggle)
    4. Sections are clearly separated with spacing
    5. Typography hierarchy is clear (headers > labels > body text)
    6. Colors match Phase 15 design system
    7. Modal styling matches modern aesthetic (rounded corners, shadows, borders)
  </verify>
  <done>
    ProjectSettingsModal displays modern sectioned layout with shadcn form controls; typography hierarchy is clear; styling matches design system.
  </done>
</task>

<task type="auto">
  <name>Update App.tsx to use AppHeader and multi-page routing with tab-based state management</name>
  <files>src/App.tsx</files>
  <action>
    Refactor App.tsx to orchestrate page switching via AppHeader tabs:

    1. **State management:**
       - Add new state: `const [activePage, setActivePage] = useState("kanban")`
       - Values: "kanban", "agents", "worktrees", "settings"
       - Default: "kanban" (primary view)

    2. **Replace inline header:**
       - Remove old header JSX (lines ~134-180)
       - Import and render new `<AppHeader />` component
       - Pass props:
         - `currentProject={currentProject}`
         - `activePage={activePage}`
         - `onPageChange={setActivePage}`
         - `agentsRunning={0}` (placeholder, real count later)
         - `worktreesCount={0}` (placeholder, real count later)

    3. **Conditional page rendering:**
       - After AppHeader, render page content based on `activePage`:
       ```
       {activePage === "kanban" && <KanbanBoard />}
       {activePage === "agents" && <AgentMonitor />}
       {activePage === "worktrees" && <WorktreeManager />}
       {activePage === "settings" && <ProjectSettingsModal open={true} />}
       ```
       - Ensure only one page renders at a time

    4. **Container layout:**
       - Main app: `flex flex-col h-screen` (full height, column layout)
       - Header: Takes up top space
       - Content: `flex-1 overflow-auto` (remaining space, scrollable)

    5. **Preserve existing functionality:**
       - Keep task creation modal state (`showNewTaskModal`, etc.)
       - Keep all event handlers
       - Keep settings import/sync functionality
       - Move these to individual page components if needed later

    6. **Per locked decisions:**
       - Page transitions: Instant (no animation) — no transition utilities
       - Navigation: Always visible header (not collapsible)
       - Layout: Simple flex structure with AppHeader on top

    **Approach:** Surgical changes only. Keep existing logic intact, add minimal page switching layer.
  </action>
  <verify>
    1. App starts without errors
    2. AppHeader renders at top with all four tabs visible
    3. Clicking each tab switches page content
    4. Active tab is highlighted with background color
    5. Page switches are instant (no animation)
    6. Project name and status indicators visible in header
    7. All existing functionality preserved (task creation, settings, etc.)
  </verify>
  <done>
    App.tsx orchestrates multi-page interface with AppHeader navigation; tab switching works instantly; all existing functionality preserved.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Start dev server: `pnpm dev`
2. Navigate to a project (post-picker screen)
3. Verify header:
   - AppHeader renders with project name on left
   - Four navigation tabs visible: Kanban, Agent Monitor, Worktree Manager, Settings
   - Status indicators show in header (agents running, worktrees count)
4. Test each page:
   - Kanban tab → shows redesigned Kanban board (from plan 01)
   - Agent Monitor tab → shows split-pane agent/terminal layout
   - Worktree Manager tab → shows grid of worktree cards
   - Settings tab → shows modern sectioned modal
5. Verify styling:
   - All pages match design system (rounded corners, shadows, colors)
   - No visual jarring between pages
   - Text hierarchy is clear
   - Colors use semantic tokens (no hardcoded values)
6. Test dark mode:
   - Switch theme in settings
   - All pages should adapt smoothly (CSS variables from Phase 15)
7. Test responsiveness:
   - Resize window
   - Grid layouts should adapt
   - Text should remain readable
</verification>

<success_criteria>
- AppHeader component renders modern navigation with tabs and status indicators
- App.tsx successfully switches between 4 pages (Kanban, Agents, Worktrees, Settings) via tab clicking
- All pages display with consistent design language (rounded corners, subtle shadows, semantic colors)
- AgentMonitor split-pane layout with sidebar and terminal pane renders correctly
- WorktreeManager grid layout with worktree cards displays git status indicators
- ProjectSettingsModal uses modern sectioned layout with shadcn form controls
- All styling uses Tailwind utilities and Phase 15 design tokens (no hardcoded colors)
- Page transitions are instant (no CSS animations)
- Dark mode works correctly on all pages
</success_criteria>

<output>
After completion, create `.planning/phases/16-page-redesigns/16-02-SUMMARY.md` with:
- Summary of all page redesigns
- Description of AppHeader navigation implementation
- Screenshots/description of each page (Kanban, Agents, Worktrees, Settings)
- Verification results for styling and functionality
- Next steps pointer to Phase 17 for final polish and QA
</output>
