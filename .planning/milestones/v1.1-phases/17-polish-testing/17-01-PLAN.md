---
phase: 17-polish-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/verify-bundle.mjs, src/index.css, src/App.tsx, src/components/KanbanBoard.tsx, src/components/AgentMonitor.tsx, src/components/WorktreeManager.tsx, src/components/ProjectSettingsModal.tsx]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Production build (pnpm tauri build) succeeds without CSS errors or warnings"
    - "Production bundle contains no CSS purging issues (all Tailwind classes present)"
    - "Dark mode theme persists correctly across app restarts with zero flash"
    - "System accent color is properly applied to UI elements in production"
    - "All component styling works identically in production and dev builds"
    - "Bundle verification script passes without detecting mock code or CSS issues"
    - "App remains fully functional at window sizes 800x600, 1200x800, 1600x1000"
    - "No visual regressions detected in Kanban board compared to v1.0 workflow"
  artifacts:
    - path: "dist/assets/*.css"
      provides: "Production CSS bundle with all Tailwind classes"
      contains: "grid-cols-5, gap-4, bg-background, border-ring, animate-pulse"
    - path: "scripts/verify-bundle.mjs"
      provides: "Bundle verification including CSS coverage checks"
      exports: ["ESSENTIAL_CLASSES", "CSS coverage validation"]
    - path: "src/index.css"
      provides: "Theme CSS variables and dark mode implementation"
      contains: "html.dark, --color, color-scheme: dark"
    - path: ".planning/phases/17-polish-testing/17-01-VERIFICATION.md"
      provides: "Production build validation results"
      min_lines: 50
  key_links:
    - from: "tailwind.config.ts"
      to: "src/index.css"
      via: "CSS variable color definitions"
      pattern: "colors:.*hsl\\(var\\(--"
    - from: "src/App.tsx"
      to: "ThemeProvider"
      via: "Theme persistence and dark mode class"
      pattern: "html\\..*dark|color-scheme"
    - from: "pnpm tauri build"
      to: "scripts/verify-bundle.mjs"
      via: "Post-build verification"
      pattern: "postbuild.*verify-bundle"
---

<objective>
Validate production build correctness and CSS system integrity across multiple viewport sizes, ensuring all Tailwind classes properly bundle, dark mode persists without flicker, and no regressions exist in core Kanban functionality.

Purpose: Catch CSS purging issues, theme persistence bugs, and responsive layout regressions before v1.1 release
Output: Production build passes all validation checks with comprehensive test coverage log
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-polish-testing/17-RESEARCH.md
@.planning/phases/16-page-redesigns/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate production build and enhance bundle verification script</name>
  <files>scripts/verify-bundle.mjs, dist/</files>
  <action>
    1. Run `pnpm tauri build` to create production binary and web assets
    2. Capture build output and verify:
       - No CSS purging errors reported by Vite
       - No TypeScript compilation errors
       - No bundle analysis warnings
       - Build succeeds without errors
    3. Run `node scripts/verify-bundle.mjs` to validate current bundle
    4. Enhance verify-bundle.mjs with CSS coverage checks:
       - Add ESSENTIAL_CLASSES array with critical Tailwind classes: 'grid-cols-5', 'gap-4', 'bg-background', 'border-ring', 'text-sm', 'rounded-lg', 'shadow-sm', 'hover:shadow-md', 'animate-pulse', 'flex', 'flex-col', 'absolute', 'relative'
       - After checking mock markers, verify each ESSENTIAL_CLASS appears in dist/assets/*.css
       - If any class missing, log "CSS PURGING DETECTED: class {cls} not in bundle" and exit(1)
       - If all checks pass, log "✓ PASSED: Production bundle verified (CSS coverage OK, no mock code)"
    5. Commit enhanced script: `git add scripts/verify-bundle.mjs && git commit -m "test(17-01): enhance bundle verification with CSS coverage checks"`
  </action>
  <verify>
    - `pnpm tauri build` exits with code 0 (no errors)
    - Build output shows "Finished release" or similar success indicator
    - `node scripts/verify-bundle.mjs` exits with code 0
    - Script output includes "✓ PASSED: Production bundle verified"
    - dist/assets/ contains .js and .css files
  </verify>
  <done>Production build completes without errors, bundle verification passes all checks (mock code excluded, CSS classes present), enhanced script committed</done>
</task>

<task type="auto">
  <name>Task 2: Verify dark mode persistence across app restarts</name>
  <files>src/App.tsx, src/index.css</files>
  <action>
    1. Start dev server: `pnpm dev` (opens http://localhost:5173)
    2. Verify theme initialization:
       - Check browser DevTools > Elements to confirm `<html>` has `class="dark"` or empty depending on system theme
       - Check CSS variables applied: `--color-background`, `--color-foreground`, etc. in computed styles
       - Verify no console errors related to theme loading
    3. Test light → dark → light transition:
       - Click Settings tab
       - In theme selector, toggle: light → dark (should see immediate visual change, no flash)
       - Verify all components respect dark mode (backgrounds darken, text lightens, no bright colors pop)
       - Close and reopen app (kill `pnpm dev`, restart)
       - Verify app starts in dark mode (exact same state as before close)
       - Repeat cycle: dark → light → system
    4. Test system theme fallback:
       - In Settings, select "System" theme mode
       - Close app, change OS theme (light ↔ dark), reopen app
       - Verify app respects new system theme
    5. Inspect theme persistence implementation:
       - Read src/App.tsx to confirm ThemeProvider usage
       - Check Tauri window preload is applying theme class before render
       - Verify theme_preference stored in database (Phase 14-02)
    6. Log results to console for verification: "✓ Dark mode persistence verified"
  </action>
  <verify>
    - Visual inspection: No flash or flicker on app restart
    - Browser DevTools: `<html>` element has correct `class` attribute
    - Settings: Theme preference matches displayed theme after restart
    - Console: No theme-related errors or warnings
    - Responsive: Tabs render correctly in both light and dark modes
  </verify>
  <done>Dark mode persists across restarts without flicker, system theme fallback works, theme class properly applied to HTML element</done>
</task>

<task type="auto">
  <name>Task 3: Verify responsive layout at multiple viewport sizes</name>
  <files>src/components/KanbanBoard.tsx, src/components/AgentMonitor.tsx, src/components/WorktreeManager.tsx</files>
  <action>
    1. Test at four key viewport sizes (use `pnpm dev` with browser resize or `pnpm tauri:dev`):
       - 800x600 (small laptop)
       - 1200x800 (standard desktop)
       - 1600x1000 (large desktop)
       - 2560x1440 (ultrawide)
    2. For each size, verify:
       - Kanban board: All 5 columns visible without horizontal scroll (or graceful scroll if needed)
       - Cards don't overflow without scrollbar
       - Header tabs remain visible and clickable
       - No text truncation or wrapping issues
       - Status dots and animations visible
       - Agent monitor split-pane maintains proportions (sidebar fixed width, content flex)
       - Worktree grid adapts columns: 1 on 800px, 2 on 1200px, 3 on 1600px+
       - Settings modal fits in viewport with scrollable content if needed
    3. Test dynamic resizing:
       - Grab window edge and drag to resize
       - Verify layout reflows smoothly without jumping or breaking
       - No elements disappear or become inaccessible
    4. Verify container queries working (Phase 15 includes @tailwindcss/container-queries):
       - Cards shrink/expand appropriately
       - Text remains readable at all sizes
    5. Document findings: "✓ Responsive layout verified at all viewport sizes"
  </action>
  <verify>
    - Kanban board renders without horizontal scroll at 800px width
    - All columns visible and cards properly laid out at 1200x800
    - Worktree grid adapts to 3 columns at 1600x1000
    - Header tabs clickable at all sizes
    - No text overflow or truncation
    - Dynamic window resize produces smooth layout reflow
  </verify>
  <done>All pages render correctly at 800x600, 1200x800, 1600x1000 viewports, responsive grid and flex layouts work as designed, no overflow without scrollbars</done>
</task>

<task type="auto">
  <name>Task 4: Validate system accent color application</name>
  <files>src/index.css, tailwind.config.ts</files>
  <action>
    1. Inspect accent color CSS variable:
       - Read src/index.css to find `--accent` definition
       - Verify default value (typically from system or fallback to design system color)
       - Check that CSS variable is properly scoped to `:root` or `html` selector
    2. Check Tailwind config:
       - Read tailwind.config.ts
       - Verify `colors.accent` uses `'hsl(var(--accent) / <alpha-value>)'` pattern
       - Confirm `<alpha-value>` placeholder allows opacity variants (bg-accent/50, etc.)
    3. Visual verification in browser (both light and dark modes):
       - Open app with dev tools
       - Inspect elements using accent color: buttons, links, active tabs, focus rings
       - Verify computed styles show accent color properly applied
       - Test accent color with opacity: hover states should lighten/darken appropriately
       - Inspect at least 5 elements using accent class
    4. Verify in production build:
       - Run `pnpm tauri build`
       - Extract and inspect dist/assets/*.css
       - Search for `--accent` in CSS file
       - Verify value is HSL format, not undefined or broken
    5. Document: "✓ System accent color properly applied to UI elements"
  </action>
  <verify>
    - CSS variable `--accent` defined in src/index.css
    - Tailwind config includes accent color with `<alpha-value>` syntax
    - Accent color visible on buttons, tabs, focus rings in dev build
    - Production build CSS contains correct accent color HSL value
    - Opacity variants (bg-accent/50, etc.) work correctly
  </verify>
  <done>Accent color CSS variable properly defined, Tailwind configuration uses `<alpha-value>` syntax, accent applied to UI elements in both dev and production builds</done>
</task>

<task type="auto">
  <name>Task 5: Verify no visual regressions in Kanban board workflow</name>
  <files>src/components/KanbanBoard.tsx</files>
  <action>
    1. Compare Phase 16 Kanban design against Phase 13-16 evolution:
       - Read 16-01-SUMMARY.md to understand Phase 16 Kanban redesign
       - Verify all Phase 16 features present:
         * Grid layout with 5 columns (Backlog, Ready, InProgress, Review, Done)
         * Colored status dots on cards
         * Animated pulse on in-progress cards
         * Drag-drop visual feedback
         * Hover effects (shadow lift, border highlight)
         * Clean card styling with rounded corners and shadows
    2. Functional verification:
       - Create a new task in Backlog column
       - Drag task to Ready column (verify drag visual feedback works)
       - Drag task through In Progress → Review → Done
       - Verify status dots update to correct color for each column
       - Verify animations smooth (pulse on in-progress, no jank)
       - Verify task details can be viewed without layout breaking
    3. Visual consistency check:
       - Compare colors in light mode vs dark mode
       - Verify status dot colors match semantic tokens (success, warning, error, pending)
       - Verify no bright colors clash in dark mode
       - Check card spacing and alignment consistent
    4. Performance check:
       - With 20+ tasks on board, verify no lag during drag-drop
       - Verify animations remain smooth during interactions
    5. Document: "✓ Kanban board visual and functional verification complete"
  </action>
  <verify>
    - Kanban board displays 5 columns with correct status labels
    - Status dots visible on all cards with correct colors
    - Pulse animation on in-progress cards
    - Drag-drop works with visual feedback (no errors in console)
    - Task creation, viewing, and status transitions work without layout breaking
    - Colors consistent in both light and dark modes
    - No performance degradation with multiple tasks
  </verify>
  <done>Kanban board renders with all Phase 16 design features, drag-drop workflow functional, status animations smooth, no visual regressions detected, visual consistency verified in light and dark modes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Build Output:** Capture `pnpm tauri build` output to confirm zero CSS errors or warnings
2. **Bundle Verification:** Confirm `node scripts/verify-bundle.mjs` exits with code 0 and logs success
3. **Theme Persistence:** Verify app starts in correct theme after restart (test 3 times)
4. **Responsive:** Test each viewport size and document results
5. **Accent Color:** Visually confirm accent color applied to buttons/tabs/rings
6. **Kanban:** Verify all Phase 16 features functional and no regressions
7. **Production Build:** Create summary of all validation results in 17-01-VERIFICATION.md

All checks must PASS before proceeding to Phase 17 Plan 02 (Accessibility Audit).
</verification>

<success_criteria>
- [ ] `pnpm tauri build` succeeds with zero errors
- [ ] Bundle verification script runs and outputs "✓ PASSED"
- [ ] CSS coverage check confirms all essential classes present
- [ ] Dark mode persists across app restart without flash
- [ ] System theme fallback works correctly
- [ ] App renders correctly at 800x600, 1200x800, 1600x1000 viewports
- [ ] Kanban board layout stable across all viewport sizes
- [ ] System accent color properly applied and visible
- [ ] All semantic colors respect light/dark modes
- [ ] No regressions in Kanban board workflow (drag-drop, status updates, animations)
- [ ] 17-01-VERIFICATION.md created with comprehensive test results
</success_criteria>

<output>
After completion, create `.planning/phases/17-polish-testing/17-01-VERIFICATION.md` with:
- Build validation results (pnpm tauri build output summary)
- Bundle verification results (mock code check, CSS coverage)
- Theme persistence test results (restart tests, system theme fallback)
- Responsive layout test results (8 viewport sizes × 4 pages = 32 layout checks)
- Accent color verification (CSS variable value, Tailwind config check)
- Kanban regression tests (5 workflow scenarios)
- Overall pass/fail status
- Any issues identified for Phase 17-02 remediation
</output>
