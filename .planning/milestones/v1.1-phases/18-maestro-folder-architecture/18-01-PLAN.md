---
phase: 18-maestro-folder-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/models/project_config.rs
  - src-tauri/src/models/project_state.rs
  - src-tauri/src/models/mod.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Project configuration can be serialized to .maestro/settings.json"
    - "Project state (tasks, worktrees) can be serialized to .maestro/state.json"
    - "TypeScript types are auto-generated for ProjectConfig and ProjectState models"
    - "Models support all existing project settings (model_default, mcp_allowlist, skills_default)"
  artifacts:
    - path: "src-tauri/src/models/project_config.rs"
      provides: "ProjectConfig struct with load/save methods"
      min_lines: 40
    - path: "src-tauri/src/models/project_state.rs"
      provides: "ProjectState struct with TaskSnapshot and WorktreeSnapshot"
      min_lines: 80
    - path: "src/types/bindings.ts"
      provides: "Auto-generated TypeScript types for ProjectConfig, ProjectState, TaskSnapshot, WorktreeSnapshot"
  key_links:
    - from: "src-tauri/src/models/project_config.rs"
      to: "std::fs + serde_json"
      via: "save_to_project() and load_from_project() methods"
      pattern: "serde_json::(from_str|to_string_pretty)"
    - from: "src-tauri/src/models/project_state.rs"
      to: "src-tauri/src/models/task.rs"
      via: "TaskSnapshot struct mirrors Task model fields"
      pattern: "pub struct (TaskSnapshot|WorktreeSnapshot)"
---

<objective>
Create Rust data models for project-local storage (.maestro folder) with JSON serialization support, establishing the foundation for migrating from database-centric to project-local architecture.

Purpose: Phase 18 requires new domain models to represent settings and state stored in .maestro folder. These models are the foundation for all subsequent file I/O and migration work.

Output: Two new Rust modules (project_config.rs, project_state.rs) with serialization methods, plus auto-generated TypeScript types in bindings.ts
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-maestro-folder-architecture/18-RESEARCH.md
@src-tauri/src/models/task.rs
@src-tauri/src/models/project.rs
@src-tauri/src/models/worktree.rs
</context>

<tasks>

<task type="auto">
  <name>Create ProjectConfig model with JSON serialization</name>
  <files>src-tauri/src/models/project_config.rs</files>
  <action>
    Create new file `src-tauri/src/models/project_config.rs` with:

    1. Import statements: serde, serde_json, std::fs, std::path::Path, ts_rs, chrono::Utc

    2. Define ProjectConfig struct with #[derive(Debug, Clone, Serialize, Deserialize, TS)] and #[ts(export)]:
       - model_default: String
       - mcp_allowlist: Vec<String>
       - skills_default: Vec<String>
       - updated_at: String (ISO 8601 timestamp)

    3. Implement ProjectConfig methods:
       - load_from_project(project_path: &str) -> Result<Self, String>
         * Construct path as .maestro/settings.json using std::path::Path::new(project_path).join(".maestro").join("settings.json")
         * Read file with std::fs::read_to_string()
         * Parse JSON with serde_json::from_str()
         * Return error if file not found or JSON invalid

       - save_to_project(&self, project_path: &str) -> Result<(), String>
         * Create .maestro directory with std::fs::create_dir_all()
         * Write settings.json with serde_json::to_string_pretty()
         * Handle errors for directory creation and file write

       - new_default() -> Self
         * Return ProjectConfig with sensible defaults (empty vecs, current timestamp)

    4. Ensure all error messages use format! for detailed context

    Reference: 18-RESEARCH.md Pattern 1 section for implementation details
  </action>
  <verify>
    1. File exists and compiles: `cargo check -p gsd_demo`
    2. Struct has all required fields with correct types
    3. Methods signatures match pattern from research (load_from_project, save_to_project)
    4. Error handling uses Result<T, String> for Tauri IPC compatibility
  </verify>
  <done>
    ProjectConfig struct created with full implementation of load/save methods, ready for TypeScript type generation and integration with IPC handlers.
  </done>
</task>

<task type="auto">
  <name>Create ProjectState model with TaskSnapshot and WorktreeSnapshot</name>
  <files>src-tauri/src/models/project_state.rs</files>
  <action>
    Create new file `src-tauri/src/models/project_state.rs` with:

    1. Import statements: serde, serde_json, std::fs, std::path::Path, ts_rs

    2. Define TaskSnapshot struct with #[derive(Debug, Clone, Serialize, Deserialize, TS)] and #[ts(export)]:
       - id: i32
       - name: String
       - description: String
       - status: String (mirrors TaskStatus enum as string: "Backlog", "Ready", "InProgress", "Review", "Failed", "Done")
       - skills: Vec<String>
       - model_override: Option<String>
       - mcp_allowlist: Option<Vec<String>>
       - skills_override: Option<Vec<String>>
       - acceptance_criteria: Option<String>
       - external_id: Option<String>
       - is_imported: Option<bool>
       - import_source: Option<String>
       - created_at: String
       - updated_at: String

    3. Define WorktreeSnapshot struct with #[derive(Debug, Clone, Serialize, Deserialize, TS)] and #[ts(export)]:
       - id: i32
       - branch_name: String
       - path: String
       - status: String (mirrors WorktreeStatus: "available", "leased", "dirty", etc.)
       - leased_at: Option<String>
       - returned_at: Option<String>
       - created_at: String

    4. Define ProjectState struct with #[derive(Debug, Clone, Serialize, Deserialize, TS)] and #[ts(export)]:
       - tasks: Vec<TaskSnapshot>
       - worktrees: Vec<WorktreeSnapshot>
       - updated_at: String
       - #[serde(default)] schema_version: u32 (allows old state.json without this field)

    5. Implement ProjectState methods:
       - load_from_project(project_path: &str) -> Result<Self, String>
         * Use Path::new(project_path).join(".maestro").join("state.json")
         * Return error if file not found or JSON invalid with context message

       - save_to_project(&self, project_path: &str) -> Result<(), String>
         * Create .maestro directory with create_dir_all()
         * Serialize to JSON with to_string_pretty()
         * Write to state.json

       - empty() -> Self
         * Return ProjectState with empty task/worktree vecs and current timestamp

    6. Add #[serde(default)] to schema_version in ProjectState to support backward compatibility with older state.json files missing this field

    Reference: 18-RESEARCH.md Pattern 2 section for implementation details
  </action>
  <verify>
    1. File exists and compiles: `cargo check -p gsd_demo`
    2. All three structs (TaskSnapshot, WorktreeSnapshot, ProjectState) defined with correct derives
    3. TaskSnapshot has all required fields matching existing Task model structure
    4. WorktreeSnapshot mirrors existing Worktree model
    5. ProjectState has backward-compatible schema_version field with #[serde(default)]
  </verify>
  <done>
    ProjectState module created with complete snapshot structures for tasks, worktrees, and state object. Ready for TypeScript code generation.
  </done>
</task>

<task type="auto">
  <name>Update models/mod.rs and regenerate TypeScript bindings</name>
  <files>src-tauri/src/models/mod.rs</files>
  <action>
    1. Open src-tauri/src/models/mod.rs

    2. Add module declarations:
       ```rust
       pub mod project_config;
       pub mod project_state;
       ```

    3. Add re-exports:
       ```rust
       pub use project_config::ProjectConfig;
       pub use project_state::{ProjectState, TaskSnapshot, WorktreeSnapshot};
       ```

    4. Verify mod.rs compiles: `cargo check -p gsd_demo`

    5. Run TypeScript code generation: `cargo build -p gsd_demo`
       (This triggers ts_rs code gen via Cargo build script)

    6. Verify bindings.ts was updated:
       ```bash
       grep -E "(ProjectConfig|ProjectState|TaskSnapshot|WorktreeSnapshot)" src/types/bindings.ts
       ```
       Should find 4 exported types

    7. Check that generated types have correct structure (no compilation errors in TypeScript):
       ```bash
       npx tsc --noEmit src/types/bindings.ts
       ```
  </action>
  <verify>
    1. src-tauri/src/models/mod.rs updated with new module declarations
    2. `cargo check -p gsd_demo` succeeds with no warnings
    3. `cargo build -p gsd_demo` completes successfully
    4. src/types/bindings.ts contains exported ProjectConfig, ProjectState, TaskSnapshot, WorktreeSnapshot types
    5. TypeScript types are valid (no "any" fallbacks, proper interfaces)
  </verify>
  <done>
    New models integrated into module structure and TypeScript types auto-generated. Phase 18-01 complete; models ready for file I/O implementation in Phase 18-02.
  </done>
</task>

</tasks>

<verification>
1. Both project_config.rs and project_state.rs exist in src-tauri/src/models/
2. ProjectConfig has load_from_project, save_to_project, new_default methods
3. ProjectState has load_from_project, save_to_project, empty methods
4. Both models use serde with #[derive(Serialize, Deserialize, TS)]
5. TypeScript bindings generated with 4 new exported types
6. `cargo check` passes with no errors
7. `cargo build` succeeds
</verification>

<success_criteria>
- [ ] ProjectConfig and ProjectState models created with all required fields
- [ ] Both models implement load/save methods using std::fs and serde_json
- [ ] Models are registered in models/mod.rs with pub mod and pub use statements
- [ ] TypeScript types auto-generated in src/types/bindings.ts
- [ ] cargo check and cargo build succeed
- [ ] No compiler warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/18-maestro-folder-architecture/18-01-SUMMARY.md`
</output>
