---
phase: 20-refactor-frontend-to-use-tanstack-query
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/components/common/SyncButton.tsx
  - src/components/kanban/TaskCard.tsx
  - src/components/kanban/TaskModal.tsx
autonomous: true

must_haves:
  truths:
    - "SyncButton.tsx uses mutation hooks for sync operations (GitHub/Jira issue sync)"
    - "TaskCard.tsx uses useTasksQuery and useUpdateTaskStatusMutation instead of direct invoke()"
    - "TaskModal.tsx uses task-related query/mutation hooks (create, update, settings)"
    - "All direct invoke() calls removed from components; TanStack Query state used"
    - "Loading/error states managed by hooks; no component-level useState for data"
    - "Optimistic updates visible in TaskCard (status change updates immediately)"
  artifacts:
    - "/home/m306213/workspace/gsd-demo/src/components/common/SyncButton.tsx (refactored)"
    - "/home/m306213/workspace/gsd-demo/src/components/kanban/TaskCard.tsx (refactored)"
    - "/home/m306213/workspace/gsd-demo/src/components/kanban/TaskModal.tsx (refactored)"
  key_links:
    - "TaskCard and TaskModal are core Kanban workflow components; heavy invoke() usage expected"
    - "useUpdateTaskStatusMutation from Plan 20-01 provides optimistic updates for drag-drop status changes"
    - "useTasksQuery provides list of tasks for rendering"
---

<objective>
Migrate 3 Kanban workflow components (SyncButton, TaskCard, TaskModal) from direct invoke() calls to TanStack Query hooks. These components are central to task management and agent orchestration workflow.

Purpose: Continue Wave 2 component migrations. Kanban components are high-traffic and likely have 20+ direct invoke() calls. Migration here demonstrates optimistic updates and cache consistency in complex scenarios.

Output: Refactored Kanban components with TanStack Query hooks, optimistic status updates, and proper cache invalidation.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-RESEARCH.md

# Wave 1 outputs
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-01-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-02-SUMMARY.md

# Wave 2 Plan 20-04 (prior component migration)
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-04-SUMMARY.md

# Service layer with hooks
@/home/m306213/workspace/gsd-demo/src/services/task.service.ts
@/home/m306213/workspace/gsd-demo/src/services/project.service.ts

# Components to migrate
@/home/m306213/workspace/gsd-demo/src/components/common/SyncButton.tsx
@/home/m306213/workspace/gsd-demo/src/components/kanban/TaskCard.tsx
@/home/m306213/workspace/gsd-demo/src/components/kanban/TaskModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SyncButton.tsx to use sync mutation hooks</name>
  <files>/home/m306213/workspace/gsd-demo/src/components/common/SyncButton.tsx</files>
  <action>
Replace direct invoke() calls in SyncButton.tsx:

1. Find all invoke() calls for sync operations (likely syncGithubIssues, syncJiraIssues, etc.)
2. Check if mutation hooks exist in project.service.ts or task.service.ts; if not, they may be in separate service
3. Replace with appropriate mutation hooks: const { mutate: syncIssues, isPending } = useSyncIssuesMutation();
4. Import sync-related mutation hooks from service layer
5. Update button disabled state: disabled={isPending}
6. Handle success: show toast.success("Issues synced")
7. Handle error: show toast.error("Failed to sync issues: {error}")
8. Remove any useState for pending state
9. Remove any direct invoke() calls

If sync mutations don't exist, create them in the appropriate service file (likely project.service.ts).

Verify all direct invoke() calls removed from SyncButton.tsx.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in SyncButton.tsx
Verify no direct invoke: grep -c "invoke<" src/components/common/SyncButton.tsx (should be 0)
Verify mutation hook used: grep "Mutation" src/components/common/SyncButton.tsx (should show import + usage)
  </verify>
  <done>
SyncButton.tsx migrated to use sync mutation hooks. No direct invoke() calls. Button state managed by hook isPending.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate TaskCard.tsx to use task query/mutation hooks</name>
  <files>/home/m306213/workspace/gsd-demo/src/components/kanban/TaskCard.tsx</files>
  <action>
Replace direct invoke() calls in TaskCard.tsx (core Kanban component):

1. Find all invoke() calls for task operations (get, update status, etc.)
2. Replace task list fetching with: const { data: tasks = [] } = useTasksQuery(projectId);
3. Replace status updates with: const { mutate: updateStatus } = useUpdateTaskStatusMutation();
4. Import hooks from @/services/task.service
5. Update drag-drop handlers to call mutate() instead of invoke()
6. Example: mutate({ taskId: task.id, status: newStatus }, { onSuccess: () => toast.success(...) })
7. IMPORTANT: useUpdateTaskStatusMutation should show optimistic updates (card UI updates immediately before server response)
8. Handle loading state: show skeleton/placeholder while tasks load
9. Remove any useState for tasks, loading, or error
10. Preserve existing Kanban UI logic; only replace data/mutation calls

This is likely the highest-impact component migration (many invoke() calls, core functionality).

Verify all direct invoke() calls removed from TaskCard.tsx.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in TaskCard.tsx
Verify no direct invoke: grep -c "invoke<" src/components/kanban/TaskCard.tsx (should be 0)
Verify hooks imported: grep -E "useTasksQuery|useUpdateTaskStatusMutation" src/components/kanban/TaskCard.tsx (should show both imports)
Test optimistic update: Manually verify that dragging a task shows immediate status change in UI before server responds
  </verify>
  <done>
TaskCard.tsx migrated to use useTasksQuery and useUpdateTaskStatusMutation. Optimistic updates visible in UI. No direct invoke().
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate TaskModal.tsx to use task mutation hooks</name>
  <files>/home/m306213/workspace/gsd-demo/src/components/kanban/TaskModal.tsx</files>
  <action>
Replace direct invoke() calls in TaskModal.tsx (task creation/editing):

1. Find all invoke() calls for task operations (create, update, get settings, etc.)
2. Replace task creation with: const { mutate: createTask } = useCreateTaskMutation();
3. Replace task updates with: const { mutate: updateTask } = useUpdateTaskMutation();
4. Replace task settings fetch with: const { data: settings } = useTaskSettingsQuery(projectId, taskId);
5. Replace task settings updates with: const { mutate: updateSettings } = useUpdateTaskSettingsMutation();
6. Import all hooks from @/services/task.service
7. Update form submission to call mutate() with form data
8. Handle mutation states: isPending (disable submit button), error (show toast)
9. Remove any useState for pending/loading/error
10. Close modal on success via callback

Example flow:
```typescript
const { mutate: createTask, isPending } = useCreateTaskMutation();
const handleSubmit = (formData) => {
  createTask(formData, {
    onSuccess: () => { toast.success("Task created"); closeModal(); }
  });
};
```

Verify all direct invoke() calls removed from TaskModal.tsx.
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in TaskModal.tsx
Verify no direct invoke: grep -c "invoke<" src/components/kanban/TaskModal.tsx (should be 0)
Verify hooks imported: grep -E "useCreate|useUpdate" src/components/kanban/TaskModal.tsx (should show multiple hooks)
Test form submission: Manually create/edit a task and verify mutation completes successfully
  </verify>
  <done>
TaskModal.tsx migrated to use task mutation hooks. Form submission uses mutate(). No direct invoke(). Modal closes on success.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. npm run build succeeds with no TypeScript errors
2. Verify no direct invoke() in migrated files: grep "invoke<" src/components/common/SyncButton.tsx src/components/kanban/{TaskCard,TaskModal}.tsx (should be empty)
3. Verify hooks imported: grep -h "from @/services" src/components/common/SyncButton.tsx src/components/kanban/{TaskCard,TaskModal}.tsx | wc -l (should be ≥6)
4. Kanban functionality preserved: Tasks render, drag-drop updates status, forms create/edit tasks
5. Optimistic updates working: TaskCard shows immediate status change when dragging
</verification>

<success_criteria>
- SyncButton.tsx uses sync mutation hooks (no direct invoke)
- TaskCard.tsx uses useTasksQuery and useUpdateTaskStatusMutation (no direct invoke)
- TaskModal.tsx uses task mutation hooks for create/update (no direct invoke)
- All files compile without TypeScript errors
- Loading states managed by hooks (no component-level useState for data)
- Optimistic updates visible in TaskCard for status changes
- Kanban workflow fully functional with TanStack Query
</success_criteria>

<output>
After completion, create `.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-05-SUMMARY.md` with:
- Components migrated (3 total: SyncButton, TaskCard, TaskModal)
- Hooks used in each component (with line numbers)
- Optimistic update verification: "✓ Status changes visible immediately in TaskCard"
- Build status: "✓ npm run build successful"
- Kanban workflow status: "✓ Tasks render, drag-drop updates, create/edit forms work"
</output>
