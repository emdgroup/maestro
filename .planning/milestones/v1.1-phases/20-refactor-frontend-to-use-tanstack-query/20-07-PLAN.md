---
phase: 20-refactor-frontend-to-use-tanstack-query
plan: 07
type: execute
wave: 3
depends_on: [04, 05, 06]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "No direct invoke() calls remain in any React components (grep finds 0 in src/components/)"
    - "No direct invoke() calls remain in custom hooks (grep finds 0 in src/utils/hooks/)"
    - "All TanStack Query hooks follow consistent patterns (useQuery, useMutation, optimistic updates)"
    - "All mutation hooks have proper cache invalidation via queryClient.invalidateQueries()"
    - "App builds without TypeScript errors"
    - "App runs without runtime errors from missing query hooks"
  artifacts:
    - "Verification report: All Phase 20 success criteria met"
    - "Migration summary: {task,project,execution,settings,connection}.service.ts with {N} total hooks"
  key_links:
    - "Phase 20 success means no direct invoke() remains in UI layer; all data operations via TanStack Query"
    - "Wave 3 verification gates Phase 21+ work (post-migration optimizations)"
---

<objective>
Comprehensive verification that Phase 20 refactoring is complete: all direct invoke() calls removed from components, all TanStack Query hooks properly implemented, app functional with new query layer.

Purpose: Quality gate for Wave 3. Verify migration success before proceeding to Phase 21 (if scheduled). Phase 20 is complete when all 9 components use hooks, no direct invoke() remains, and app runs without errors.

Output: Verification report confirming migration success or identifying remaining direct invoke() calls for remediation.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-RESEARCH.md

# Wave 1 summaries
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-01-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-02-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-03-SUMMARY.md

# Wave 2 summaries
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-04-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-05-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-06-SUMMARY.md

# Service layer (Wave 1 output)
@/home/m306213/workspace/gsd-demo/src/services/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no direct invoke() calls remain in React components</name>
  <files></files>
  <action>
Comprehensive grep search to ensure all direct invoke() calls removed from UI layer:

1. Search for direct invoke() in components:
   ```bash
   grep -r "invoke<" src/components/ --include="*.tsx" --include="*.ts"
   ```
   Expected result: 0 matches (should be completely empty)

2. Search for direct invoke() in custom hooks:
   ```bash
   grep -r "invoke<" src/utils/hooks/ --include="*.tsx" --include="*.ts"
   ```
   Expected result: 0 matches (should be completely empty)

3. Exceptions (should only appear in services/ipc.ts or services/*.service.ts as wrapped calls):
   ```bash
   grep -r "invoke<" src/services/ --include="*.ts" | grep -v "ipc.ts"
   ```
   Expected result: 0 matches in service files (all invoke() should be delegated to ipc.ts)

4. If any direct invoke() found:
   - Document file paths and line numbers
   - Identify which components still have direct calls (regression)
   - Return to appropriate Wave 2 plan for remediation

5. Report: List all files checked, total invoke() occurrences found (target: 0)
  </action>
  <verify>
Run the three grep commands above and report results.
Expected: All three return 0 matches
If matches found, list them for remediation in follow-up plan
  </verify>
  <done>
No direct invoke() calls found in components or hooks. All invoke() calls properly delegated to services layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify TanStack Query hook consistency and patterns</name>
  <files></files>
  <action>
Audit all hooks across service files for consistency:

1. Verify all query key factories exist and follow consistent structure:
   ```bash
   grep -r "export const.*QueryKeys = " src/services/ --include="*.ts"
   ```
   Expected: 5 factories (taskQueryKeys, projectQueryKeys, executionQueryKeys, settingsQueryKeys, connectionQueryKeys)

2. Verify all useQuery hooks use enabled conditions for null parameters:
   ```bash
   grep -A 3 "useQuery({" src/services/ --include="*.ts" | grep "enabled:"
   ```
   Expected: Multiple lines with enabled conditions for dependent queries

3. Verify all useMutation hooks use queryClient.invalidateQueries():
   ```bash
   grep "queryClient.invalidateQueries" src/services/ --include="*.ts" -c
   ```
   Expected: ≥20 matches (one per mutation hook)

4. Verify optimistic update pattern used in status/priority mutations:
   ```bash
   grep -r "onMutate:" src/services/ --include="*.ts" -c
   ```
   Expected: ≥5 mutations using optimistic updates (status changes, settings updates)

5. If any inconsistencies found:
   - Document which hooks don't follow pattern
   - Determine if pattern deviation is justified (e.g., fire-and-forget mutations for terminal ops)
   - Report findings

6. Report: Hook consistency verification results, pattern deviations (if any) with justifications
  </action>
  <verify>
Run the grep commands above and summarize results.
Expected: All query key factories present, enabled conditions used, cache invalidation consistent
Deviations acceptable only if justified (e.g., execution mutations are fire-and-forget)
  </verify>
  <done>
All TanStack Query hooks follow consistent patterns. Query key factories present across all services. Mutations properly invalidate cache.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify application builds and runs without errors</name>
  <files></files>
  <action>
Build and runtime verification:

1. Build the frontend:
   ```bash
   npm run build
   ```
   Expected: Build succeeds with 0 errors, 0 warnings (if warnings exist, categorize them)

2. Start Tauri dev environment:
   ```bash
   npm run tauri:dev
   ```
   Expected: App launches successfully, no console errors related to missing hooks or query keys

3. Manual smoke tests in running app:
   a. Load app, verify settings load (useSettingsQuery working)
   b. Load project, verify tasks render (useTasksQuery working)
   c. Drag task to update status (useUpdateTaskStatusMutation with optimistic update working)
   d. Create new task (useCreateTaskMutation working)
   e. Check console: No "Cannot find module" or "undefined is not a function" errors

4. If errors found:
   - Document error messages and file paths
   - Identify root cause (missing hook, incorrect import, runtime issue)
   - Return to appropriate plan for remediation

5. Report: Build status, runtime errors (if any), manual test results
  </action>
  <verify>
Run build and app startup commands above.
Expected: Build succeeds, app runs without console errors
Manual tests pass: Settings load, tasks render, mutations work, optimistic updates visible
  </verify>
  <done>
App builds successfully. No TypeScript errors. Tauri app launches and functions correctly. All TanStack Query operations working.
  </done>
</task>

<task type="auto">
  <name>Task 4: Generate Phase 20 completion report</name>
  <files></files>
  <action>
Generate comprehensive Phase 20 completion summary:

1. Create summary document: `.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-COMPLETION-REPORT.md`

2. Document Phase 20 success metrics:
   - ✓ All 21-25 TanStack Query hooks created (Wave 1: 3 service files)
   - ✓ All 9 components migrated (Wave 2: 3 plans, 9 files)
   - ✓ No direct invoke() calls in components (Wave 3 verification)
   - ✓ App builds and runs without errors
   - ✓ TanStack Query patterns consistent across all services
   - ✓ Cache invalidation and optimistic updates working

3. Include statistics:
   - Total hooks created: (count from Wave 1)
   - Components migrated: 9
   - Direct invoke() calls removed: 50+ (from research phase)
   - Lines of code changed: (estimate based on diffs)

4. Include recommendations for Phase 21+ (if applicable):
   - Possible optimizations (selective cache invalidation, request deduplication monitoring)
   - Real-time updates via polling or WebSocket (deferred decision from research)
   - Additional error handling or retry logic
   - Performance monitoring with React Query DevTools (optional)

5. Sign off: Phase 20 complete, ready for Phase 21 or production
  </action>
  <verify>
Completion report generated at `.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-COMPLETION-REPORT.md`
Report includes: Hook counts, component migrations, verification results, recommendations
  </verify>
  <done>
Phase 20 completion report generated. All success criteria documented. Phase ready for sign-off.
  </done>
</task>

</tasks>

<verification>
Wave 3 comprehensive verification:
1. Grep search: 0 direct invoke() in components (verified)
2. Hook consistency: All 5 query key factories present, patterns consistent (verified)
3. Build/runtime: App builds successfully, runs without errors (verified)
4. Manual smoke tests: Settings load, tasks render, mutations work, optimistic updates visible (verified)
5. Completion report generated (verified)
</verification>

<success_criteria>
- Zero direct invoke() calls in src/components/ (grep result: 0 matches)
- Zero direct invoke() calls in src/utils/hooks/ (grep result: 0 matches)
- All 5 query key factories present in services (verified)
- All mutation hooks use cache invalidation (≥20 invalidateQueries calls)
- App builds without TypeScript errors (npm run build succeeds)
- Tauri app runs without runtime errors (npm run tauri:dev succeeds)
- Manual smoke tests pass (settings, tasks, mutations all functional)
- Phase 20 completion report generated and signed off
</success_criteria>

<output>
After completion, create `.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-COMPLETION-REPORT.md` with:
- Phase 20 success metrics (all criteria verified)
- Statistics (hook count, component migrations, invoke() calls removed)
- Build and runtime verification results
- Grep search results (0 matches for direct invoke in components)
- Hook consistency audit results
- Manual smoke test results
- Recommendations for Phase 21+
- Sign-off: "Phase 20 complete. Ready for production / Phase 21."
</output>
