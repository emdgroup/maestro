---
phase: 20-refactor-frontend-to-use-tanstack-query
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/connection.service.ts
autonomous: true

must_haves:
  truths:
    - "connection.service.ts exports all query/mutation hooks for SSH operations (verify existing exports)"
    - "Any missing SSH mutation hooks added (useCreateSshConnectionMutation, useDeleteSshConnectionMutation if not present)"
    - "connectionQueryKeys factory exported for consistency with other services"
    - "All existing hooks in useSshConnectionsQuery.ts verified to use service layer patterns"
    - "No new invoke() calls added to connection.service.ts; all delegated via ipc wrapper"
  artifacts:
    - "/home/m306213/workspace/gsd-demo/src/services/connection.service.ts (verified/extended with missing hooks)"
    - "/home/m306213/workspace/gsd-demo/src/utils/hooks/useSshConnectionsQuery.ts (reference exemplar pattern)"
  key_links:
    - "useSshConnectionsQuery.ts already demonstrates optimistic update pattern (exemplar)"
    - "connection.service.ts may need additional mutation hooks beyond what Phase 19 established"
    - "connectionQueryKeys factory ensures cache consistency across component usage"
---

<objective>
Audit and extend connection.service.ts with complete TanStack Query hook coverage for SSH operations. Connection service already has exemplar pattern (useSshConnectionsQuery.ts exists), so this plan verifies completeness and adds any missing mutations.

Purpose: Complete Wave 1 infrastructure (finalizing 21+ hooks). Connection service is simpler than task/project/execution/settings, so this plan focuses on audit + any gaps rather than full implementation.

Output: Verified/extended connection.service.ts with connectionQueryKeys factory and all necessary query/mutation hooks.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-RESEARCH.md

# Exemplar pattern (already implemented!)
@/home/m306213/workspace/gsd-demo/src/utils/hooks/useSshConnectionsQuery.ts

# Service layer
@/home/m306213/workspace/gsd-demo/src/services/connection.service.ts

# Prior plans (Wave 1 foundation)
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-01-SUMMARY.md
@.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit connection.service.ts and add missing hooks</name>
  <files>/home/m306213/workspace/gsd-demo/src/services/connection.service.ts</files>
  <action>
Audit connection.service.ts for TanStack Query hook coverage:

1. **Check current state**:
   - Verify if connectionQueryKeys factory exists (similar to taskQueryKeys, projectQueryKeys)
   - Identify all service methods (SSH connection CRUD operations)
   - Check if hooks already exist or need to be added
   - Compare against useSshConnectionsQuery.ts exemplar pattern

2. **If connectionQueryKeys missing**, add:
   ```typescript
   export const connectionQueryKeys = {
     all: ["ssh-connections"] as const,
     lists: () => [...connectionQueryKeys.all, "list"] as const,
     list: () => [...connectionQueryKeys.lists()] as const,
     details: () => [...connectionQueryKeys.all, "detail"] as const,
     detail: (connectionId: number) => [...connectionQueryKeys.details(), connectionId] as const,
   };
   ```

3. **Verify/add mutation hooks** (typical SSH CRUD operations):
   - useCreateSshConnectionMutation() - if missing, add hook to create new SSH connection
   - useUpdateSshConnectionMutation() - may already exist (verify in useSshConnectionsQuery.ts or connection.service.ts)
   - useDeleteSshConnectionMutation() - if missing, add hook to delete SSH connection

   For each missing hook:
   - Follow useSshConnectionsQuery.ts pattern (lines 27-76 in codebase)
   - Use connectionQueryKeys for cache keys
   - Implement onSuccess/onError with toast feedback
   - Implement onMutate + onError + onSettled for mutations affecting list

4. **Move hooks if needed**:
   - If hooks exist in useSshConnectionsQuery.ts, consider moving them to connection.service.ts (to co-locate with service methods, matching task/project/execution/settings patterns)
   - Update import in components later (Plan 20-04/20-05/20-06) to import from connection.service instead

5. **Add connectionQueryKeys export** to index.ts (src/services/index.ts) if query key factories exported there
  </action>
  <verify>
Run: npm run build
Expected: No TypeScript errors in connection.service.ts
Verify hooks present: grep -E "export function use|export const connectionQueryKeys" src/services/connection.service.ts
Expected: Query factory + all mutation hooks present
Verify exemplar still works: grep -E "export function useSsh" src/utils/hooks/useSshConnectionsQuery.ts (should still exist)
  </verify>
  <done>
connection.service.ts has connectionQueryKeys factory and all SSH mutation hooks. useSshConnectionsQuery.ts verified as working exemplar. All hooks follow TanStack Query patterns from Plans 20-01/20-02.
  </done>
</task>

</tasks>

<verification>
After task completion:
1. npm run build succeeds with no TypeScript errors
2. connectionQueryKeys factory exported from connection.service.ts: grep "export const connectionQueryKeys" src/services/connection.service.ts
3. All SSH mutation hooks present: grep -E "export function use" src/services/connection.service.ts (should show create/update/delete mutations)
4. Exemplar pattern still functional: src/utils/hooks/useSshConnectionsQuery.ts compiles and can be imported in tests
5. Wave 1 complete: All 5 services (task, project, execution, settings, connection) have query/mutation hooks + query key factories
</verification>

<success_criteria>
- connection.service.ts exports connectionQueryKeys factory
- All SSH CRUD operations wrapped in TanStack Query hooks (create, update, delete at minimum)
- Mutation hooks have error handling via Sonner toast
- Query key structure matches other services (nested selectors)
- useSshConnectionsQuery.ts exemplar pattern verified as working reference
- All files compile without TypeScript errors
- Wave 1 complete: 21+ hooks added across all 5 services
</success_criteria>

<output>
After completion, create `.planning/phases/20-refactor-frontend-to-use-tanstack-query/20-03-SUMMARY.md` with:
- SSH mutation hooks added or verified (with line numbers)
- connectionQueryKeys factory location and structure
- Exemplar pattern reference (useSshConnectionsQuery.ts)
- Build status: "✓ npm run build successful"
- Wave 1 completion: "✓ All 5 services (task, project, execution, settings, connection) have complete TanStack Query hook coverage"
- Estimated hooks: "21-25 hooks across all services"
</output>
