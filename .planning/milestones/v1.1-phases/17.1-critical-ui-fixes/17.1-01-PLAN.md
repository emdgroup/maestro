---
phase: 17.1-critical-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ProjectPicker.tsx
  - src/lib/tauri-safe.ts
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User can select a folder in production mode and app proceeds to main UI"
    - "IPC calls have logging to browser console for production debugging"
    - "Error messages from dialog and settings operations are visible to user"
  artifacts:
    - path: "src/lib/tauri-safe.ts"
      provides: "Production-safe IPC wrapper with error logging"
      min_lines: 30
    - path: "src/components/ProjectPicker.tsx"
      provides: "Enhanced ProjectPicker with try-catch and logging"
      contains: "console.log.*DEBUG"
    - path: "src/App.tsx"
      provides: "Updated handleProjectSelected to use logging"
      contains: "safeInvoke"
  key_links:
    - from: "src/components/ProjectPicker.tsx"
      to: "src/lib/tauri-safe.ts"
      via: "import safeInvoke"
      pattern: "import.*safeInvoke"
    - from: "ProjectPicker dialog callback"
      to: "App.handleProjectSelected"
      via: "onProjectSelected prop"
      pattern: "await.*safeInvoke.*get_or_create_project"
---

<objective>
Fix production mode folder selection flow by adding comprehensive logging and error handling to all IPC calls. This addresses the critical blocker where users can select a folder but the app doesn't proceed to the main UI in production builds.

Purpose: Diagnose and fix the production folder selection failure that prevents users from completing initial project setup.
Output: Production-safe IPC wrapper, enhanced ProjectPicker with logging, debuggable production builds.
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17.1-critical-ui-fixes/17.1-RESEARCH.md
@src/App.tsx
@src/components/ProjectPicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Create production-safe IPC wrapper with logging</name>
  <files>src/lib/tauri-safe.ts</files>
  <action>
Create a new file `src/lib/tauri-safe.ts` that exports a `safeInvoke` function wrapping Tauri's invoke with structured logging:
- Function signature: `safeInvoke<T>(command: string, args?: Record<string, unknown>): Promise<T>`
- Logs all invocations with arguments to console (visible in production via DevTools)
- Logs success with result
- Logs errors with full error object
- Uses `[Tauri]` prefix for easy filtering
- Never throws silently - always logs before throwing
- Example: `[Tauri] Invoking get_or_create_project with args: {path: "/home/user/project"}`

This wrapper makes it easy to debug production builds by capturing all IPC communication. Include example usage comment.
  </action>
  <verify>
    - File exists at src/lib/tauri-safe.ts
    - Contains safeInvoke function with T generic type parameter
    - Has console.log for invoke start with command and args
    - Has console.log for invoke success with result
    - Has console.error for invoke failure with error object
    - Includes JSDoc comment with usage example
  </verify>
  <done>
    Production-safe IPC wrapper created with console logging at all checkpoints (invoke, success, error)
  </done>
</task>

<task type="auto">
  <name>Add logging to ProjectPicker folder selection flow</name>
  <files>src/components/ProjectPicker.tsx</files>
  <action>
Enhance ProjectPicker.tsx to wrap all IPC calls and dialog operations with try-catch and logging:

1. Import safeInvoke from src/lib/tauri-safe
2. In handleFolderPicker or equivalent:
   - Log "[DEBUG] Opening folder dialog"
   - Wrap dialog open call in try-catch
   - Log dialog result (path or null)
   - If no path, log "[DEBUG] Dialog returned no path" and return
   - If path exists, proceed to onProjectSelected with full logging
3. In any IPC call for project creation/loading:
   - Replace invoke() with safeInvoke()
   - Let safeInvoke handle logging
4. Catch block: log error message to user via state (setError or similar)

This makes the production flow visible: each step logs to console, so users/developers can screenshot console to diagnose where flow breaks.
  </action>
  <verify>
    - ProjectPicker imports safeInvoke from src/lib/tauri-safe
    - Folder selection has try-catch wrapper
    - Dialog opening has console.log before and after
    - Dialog result (path or null) is logged
    - No path case logs "[DEBUG] Dialog returned no path"
    - onProjectSelected call has console.log("[DEBUG] onProjectSelected completed") after await
    - Error catch block logs and sets user-visible error state
  </verify>
  <done>
    ProjectPicker folder selection flow fully logged - each step visible in browser console for production debugging
  </done>
</task>

<task type="auto">
  <name>Update App.tsx to use logging and handle errors visibly</name>
  <files>src/App.tsx</files>
  <action>
Update App.tsx's handleProjectSelected to use safeInvoke and improve error visibility:

1. Import safeInvoke from src/lib/tauri-safe
2. In handleProjectSelected function:
   - Replace `invoke<Project>("get_or_create_project", ...)` with `safeInvoke<Project>("get_or_create_project", ...)`
   - Replace `invoke("save_settings", ...)` with `safeInvoke("save_settings", ...)`
   - Add console.log("[DEBUG] handleProjectSelected starting with path:", projectPath)
   - Add console.log("[DEBUG] Project loaded:", project) after project creation
   - Add console.log("[DEBUG] Settings saved successfully") after save_settings
3. In catch block:
   - Log error with full object
   - Set visible error message (already present as console.error, but ensure it's logged)
   - Optionally: set an error state that displays in UI so user knows what went wrong

This ensures both the initial load and the project selection flow are fully traceable in production.
  </action>
  <verify>
    - App.tsx imports safeInvoke from src/lib/tauri-safe
    - Both get_or_create_project and save_settings calls use safeInvoke
    - Console.log calls exist for: start of function, after project load, after settings save
    - Error catch block exists and logs error
    - No code changes beyond adding logging and replacing invoke with safeInvoke
  </verify>
  <done>
    App.tsx project selection flow fully instrumented with logging - production builds now traceable
  </done>
</task>

</tasks>

<verification>
1. Create a test folder (e.g., `/tmp/gsd-test`)
2. Run production build: `pnpm tauri build`
3. Start built app from dist or .app/.exe
4. Open DevTools (F12)
5. Console should show: `[Tauri] Invoking get_settings ...`
6. In ProjectPicker, click "Select Folder"
7. Console should show: `[DEBUG] Opening folder dialog`, then `[DEBUG] Dialog path: /path/to/folder`
8. If app proceeds to main UI: `[DEBUG] onProjectSelected completed` and `[Tauri] Invoking save_settings ...`
9. If app fails: error should be visible in console with full error object for diagnosis
</verification>

<success_criteria>
- Production build executes without errors
- Every IPC call logs to console (visible in DevTools)
- Folder selection flow visible in console from start to finish
- If flow fails, error message is captured and logged
- Next plan can identify exact failure point by reading console logs
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-critical-ui-fixes/17.1-01-SUMMARY.md`
</output>
