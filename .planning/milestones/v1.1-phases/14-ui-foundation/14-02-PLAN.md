---
phase: 14-ui-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/models/settings.rs
  - src-tauri/src/db/settings.rs
  - src/types/bindings.ts
autonomous: true

must_haves:
  truths:
    - "AppSettings model includes theme_preference field storing 'light', 'dark', or 'system'"
    - "Theme preference persists to database and can be read back"
    - "Theme preference is written to the settings table and survives app restart"
    - "React components can access theme_preference from AppSettings type"
    - "Cargo build completes without errors and regenerates TypeScript bindings"
  artifacts:
    - path: "src-tauri/src/models/settings.rs"
      provides: "Rust AppSettings struct with theme_preference field"
      contains: "pub theme_preference: Option<String>, #[ts(export)]"
    - path: "src-tauri/src/db/settings.rs"
      provides: "Database load/save functions handling theme_preference"
      contains: "theme_preference read from/written to settings table"
    - path: "src/types/bindings.ts"
      provides: "TypeScript AppSettings type with theme_preference"
      contains: "theme_preference: string | null"
  key_links:
    - from: "src-tauri/src/models/settings.rs"
      to: "src/types/bindings.ts"
      via: "ts-rs #[derive(TS)] generates TypeScript type on cargo build"
      pattern: "#\\[ts\\(export\\)\\]"
    - from: "src-tauri/src/db/settings.rs"
      to: "src-tauri/src/models/settings.rs"
      via: "load/save functions read/write AppSettings fields"
      pattern: "settings\\.theme_preference"

---

<objective>
Extend Rust AppSettings model and database layer to persist theme preference. Auto-generate TypeScript bindings.

Purpose: Foundation for theme persistence across app restarts

Output: Rust model + DB functions + TypeScript types ready for theme provider integration
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/14-ui-foundation/14-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@src-tauri/src/models/settings.rs
@src-tauri/src/db/settings.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add theme_preference field to AppSettings Rust model</name>
  <files>src-tauri/src/models/settings.rs</files>
  <action>
    Extend the AppSettings struct:
    1. Add new field: `pub theme_preference: Option<String>,  // 'light' | 'dark' | 'system'`
    2. Position after skills_default, before updated_at for logical grouping
    3. Keep Serialize, Deserialize, TS derives unchanged (already on struct)
    4. Update Default impl to include: `theme_preference: Some("system".to_string()),` (respects OS preference)
    5. Keep all existing fields unchanged (project_path, recent_projects, model_default, mcp_allowlist, skills_default, updated_at)
    6. No breaking changes to existing code
  </action>
  <verify>
    cd src-tauri && cargo check 2>&1 | grep -E "error|warning" | head -5
  </verify>
  <done>
    AppSettings struct includes theme_preference: Option<String>, Default impl includes it, cargo check passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update settings.rs load_settings() to read theme_preference from database</name>
  <files>src-tauri/src/db/settings.rs</files>
  <action>
    Modify load_settings() function:
    1. After reading existing settings (project_path, recent_projects, model_default, mcp_allowlist, skills_default)
    2. Add: `let theme_preference = settings_map.get("theme_preference").cloned();` (reads from key-value settings table)
    3. Return constructed AppSettings with theme_preference field
    4. If key doesn't exist (first run), defaults to None which uses Default::default() value "system"
    5. Keep error handling unchanged (return AppError on DB failure)
  </action>
  <verify>
    cd src-tauri && cargo check 2>&1 | grep -E "error\[" | head -1 || echo "Build OK"
  </verify>
  <done>
    load_settings() reads theme_preference from database, builds AppSettings with all fields including theme_preference
  </done>
</task>

<task type="auto">
  <name>Task 3: Update settings.rs save_settings() to write theme_preference to database</name>
  <files>src-tauri/src/db/settings.rs</files>
  <action>
    Modify save_settings() function:
    1. In the INSERT OR REPLACE VALUES (...) statement, add theme_preference as a new column
    2. Extract value: `settings.theme_preference.as_ref().map(|s| s.as_str()).unwrap_or("system")`
    3. Add to VALUES clause: include the extracted value in the right position (after skills, before updated_at order matching struct)
    4. Update column count in INSERT statement if using explicit column list
    5. Keep transaction handling and error handling unchanged
    6. All existing fields (project_path, recent_projects, model_default, mcp_allowlist, skills_default) keep their save logic intact
  </action>
  <verify>
    cd src-tauri && cargo check 2>&1 | grep -E "error\[|warning" | head -5 || echo "Build OK"
  </verify>
  <done>
    save_settings() writes theme_preference to database settings table, SQL insert/replace statement includes new column
  </done>
</task>

<task type="auto">
  <name>Task 4: Rebuild Rust and verify TypeScript bindings auto-generate</name>
  <files>src/types/bindings.ts</files>
  <action>
    Rebuild Rust to trigger ts-rs code generation:
    1. Run: `cd src-tauri && cargo build --lib` (--lib generates TypeScript without creating binary)
    2. Verify ts-rs processes the updated AppSettings struct
    3. Check src/types/bindings.ts was regenerated (timestamp updated)
    4. Verify AppSettings TypeScript type includes: `theme_preference: string | null;`
    5. No other TypeScript files need manual updates (bindings auto-generated)
  </action>
  <verify>
    grep "theme_preference" src/types/bindings.ts && echo "✓ TypeScript binding generated"
  </verify>
  <done>
    cargo build --lib completes, src/types/bindings.ts updated with AppSettings.theme_preference: string | null
  </done>
</task>

</tasks>

<verification>
1. Run `cargo check` in src-tauri directory, verify no errors
2. Check src-tauri/src/models/settings.rs contains theme_preference field with TS derive
3. Check src-tauri/src/db/settings.rs load/save functions handle theme_preference
4. Verify src/types/bindings.ts includes AppSettings with theme_preference: string | null
5. Verify Default impl sets theme_preference to "system"
6. Database test: Settings can be saved and loaded with theme_preference persisted
</verification>

<success_criteria>
- AppSettings Rust struct includes theme_preference: Option<String> field
- Default impl provides theme_preference: Some("system".to_string())
- load_settings() reads theme_preference from database settings table
- save_settings() writes theme_preference to database
- TypeScript bindings regenerated with AppSettings.theme_preference field
- cargo check passes without errors
- No breaking changes to existing settings functionality
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-foundation/14-02-SUMMARY.md` documenting:
- AppSettings struct changes (new theme_preference field)
- Database schema notes (theme_preference stored as key-value in settings table)
- TypeScript binding changes (AppSettings type updated)
- Any issues encountered with ts-rs code generation
</output>
