---
phase: 14-ui-foundation
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - src/providers/ThemeProvider.tsx
  - src/App.tsx
  - src/main.tsx
  - src-tauri/src/main.rs
autonomous: true

must_haves:
  truths:
    - "App initializes with theme loaded from database and applied to document.documentElement"
    - "Users can switch between light, dark, and system theme instantly"
    - "Theme change persists in database and is restored on app restart"
    - "No visible flash or unstyled content appears on startup"
    - "ThemeProvider context available to all components via useTheme() hook"
  artifacts:
    - path: "src/providers/ThemeProvider.tsx"
      provides: "React context for theme state management and persistence"
      contains: "ThemeContext, ThemeProvider, useTheme hook, theme persistence logic"
    - path: "src/App.tsx"
      provides: "App wrapped with ThemeProvider"
      contains: "<ThemeProvider>" around app content
    - path: "src/main.tsx"
      provides: "Entry point with theme loading before React renders"
      contains: "Early theme initialization logic"
    - path: "src-tauri/src/main.rs"
      provides: "Tauri window preload with theme class injection"
      contains: "window.eval or webview initialization hook setting html.dark class"
  key_links:
    - from: "src/providers/ThemeProvider.tsx"
      to: "src/App.tsx"
      via: "App wrapped with <ThemeProvider>"
      pattern: "<ThemeProvider>.*</ThemeProvider>"
    - from: "src/providers/ThemeProvider.tsx"
      to: "src/types/bindings.ts"
      via: "useTheme hook invokes IPC commands via AppSettings type"
      pattern: "invoke.*get_settings|invoke.*save_settings"
    - from: "src-tauri/src/main.rs"
      to: "src/index.css"
      via: "Preload injects dark class, CSS variables apply via html.dark"
      pattern: "html\\.dark|dark"

---

<objective>
Create ThemeProvider React component for theme state management and persistence. Integrate with Tauri window preload for flash-free startup.

Purpose: Complete theme system with instant switching and zero flash on startup

Output: Production-ready theme provider + preload integration
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/14-ui-foundation/14-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/App.tsx
@src/main.tsx
@src/types/bindings.ts
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/providers/ThemeProvider.tsx with context and hook</name>
  <files>src/providers/ThemeProvider.tsx</files>
  <action>
    Create new React component:
    1. Define ThemeValue type: 'light' | 'dark' | 'system'
    2. Create ThemeContextValue interface with: theme (ThemeValue), setTheme function, systemTheme ('light'|'dark')
    3. Create ThemeContext using createContext<ThemeContextValue | null>(null)
    4. Implement ThemeProvider component:
       - useState for theme, systemTheme, isReady
       - useEffect on mount: load settings via invoke('get_settings'), detect system theme via matchMedia, apply initial theme
       - applyTheme(theme, systemTheme) helper: adds/removes 'dark' class from document.documentElement
       - handleSetTheme(newTheme) handler: updates DOM, persists to DB via invoke('save_settings')
       - Return ThemeContext.Provider wrapping children
    5. Implement useTheme() hook: returns context, throws error if used outside provider
    6. Handle errors gracefully: fallback to system theme if DB read fails

    Reference structure from Part 3 of research document (lines 226-336).
  </action>
  <verify>
    tsc --noEmit src/providers/ThemeProvider.tsx 2>&1 | grep -E "error TS" || echo "✓ TypeScript OK"
  </verify>
  <done>
    src/providers/ThemeProvider.tsx exists, compiles without errors, exports ThemeProvider and useTheme
  </done>
</task>

<task type="auto">
  <name>Task 2: Update src/App.tsx to wrap with ThemeProvider</name>
  <files>src/App.tsx</files>
  <action>
    Modify App.tsx:
    1. Import ThemeProvider: `import { ThemeProvider } from './providers/ThemeProvider'`
    2. In the return JSX, wrap the top-level content with <ThemeProvider>
    3. Structure: Keep existing providers/layout, wrap entire App content tree
    4. Example: `return <ThemeProvider>{ /* existing JSX */ }</ThemeProvider>`
    5. Do NOT move state initialization (keep existing logic for project loading, task loading, etc.)
    6. Verify App still loads settings on mount (existing code path unchanged)
  </action>
  <verify>
    pnpm build 2>&1 | tail -20 | grep -E "error|warning" || echo "✓ Build OK"
  </verify>
  <done>
    App.tsx imports ThemeProvider and wraps app content, build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Update src/main.tsx for early theme initialization</name>
  <files>src/main.tsx</files>
  <action>
    Modify main.tsx to initialize theme before React renders:
    1. Add early theme detection code before ReactDOM.render:
       ```typescript
       // Detect and apply system theme synchronously before React renders
       const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
       if (prefersDark) {
         document.documentElement.classList.add('dark');
       }
       ```
    2. This ensures html.dark is set before CSS variables are evaluated
    3. ThemeProvider will refine with DB-persisted preference after React mounts
    4. Order: Early system theme detect → React render → ThemeProvider refines with DB preference
  </action>
  <verify>
    pnpm build 2>&1 | grep -E "error\[" || echo "✓ Build OK"
  </verify>
  <done>
    main.tsx includes early theme initialization before React.render, build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 4: Add Tauri window preload hook in src-tauri/src/main.rs</name>
  <files>src-tauri/src/main.rs</files>
  <action>
    Modify main.rs to inject theme class on window initialization:
    1. Locate the .setup() handler or window creation code in main.rs
    2. After app setup, get the main window: `let main_window = app.get_webview_window("main")?;`
    3. Inject JavaScript to set theme class:
       ```rust
       main_window.eval("
         (function() {
           const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
           if (prefersDark) {
             document.documentElement.classList.add('dark');
           }
         })();
       ")?;
       ```
    4. This runs BEFORE React renders, ensuring html.dark class is present when CSS loads
    5. Keep all other main.rs logic unchanged (IPC handlers, database setup, etc.)
  </action>
  <verify>
    cd src-tauri && cargo build --lib 2>&1 | grep -E "error\[" || echo "✓ Build OK"
  </verify>
  <done>
    main.rs includes window preload hook with theme class injection, cargo build succeeds
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm tauri:dev` and start the app
2. Verify app renders without flash (watch for any unstyled content between window open and render)
3. Open DevTools, check that html element has 'dark' class if system theme is dark
4. Open settings modal, verify theme selector shows current system theme
5. In future plans, test theme toggle from settings page
6. Restart app, verify theme preference persists (manual test until settings UI implemented)
7. Check console for any errors related to theme initialization
8. Build production: `pnpm build` and verify bundle size unchanged significantly
</verification>

<success_criteria>
- ThemeProvider.tsx created with ThemeContext, ThemeProvider component, useTheme hook
- App.tsx wrapped with <ThemeProvider>
- src/main.tsx initializes theme before React renders
- src-tauri/src/main.rs includes window preload with theme class injection
- App starts without visible flash or unstyled content
- Theme state available to all components via useTheme()
- Database persistence ready for next plan (settings UI)
- All TypeScript compiles without errors
- All Rust builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-foundation/14-03-SUMMARY.md` documenting:
- ThemeProvider implementation (context structure, hooks, persistence logic)
- Main.tsx early initialization strategy
- Tauri preload hook implementation details
- Flash prevention verification (manual or automated check)
- Any issues with IPC/database access during provider initialization
</output>
