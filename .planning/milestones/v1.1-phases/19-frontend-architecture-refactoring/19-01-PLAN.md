---
phase: 19-frontend-architecture-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/KanbanView.tsx
  - src/views/AgentsView.tsx
  - src/views/SettingsView.tsx
  - src/views/ProjectPickerView.tsx
  - src/views/index.ts
autonomous: true

must_haves:
  truths:
    - "All page-level components have been renamed to Views and moved to src/views/"
    - "src/views/ contains exactly 5 view files (Kanban, Agents, Settings, ProjectPicker, and WorktreesView)"
    - "Each view is an orchestrator component importing from domain-grouped components"
    - "Views compile without errors and maintain all prior functionality"
  artifacts:
    - path: "src/views/KanbanView.tsx"
      provides: "Main kanban board screen orchestration"
      min_lines: 30
    - path: "src/views/AgentsView.tsx"
      provides: "Agent monitoring screen orchestration"
      min_lines: 30
    - path: "src/views/SettingsView.tsx"
      provides: "Settings/configuration screen orchestration"
      min_lines: 30
    - path: "src/views/ProjectPickerView.tsx"
      provides: "Project selection first-run screen orchestration"
      min_lines: 30
    - path: "src/views/index.ts"
      provides: "Barrel export for all views"
  key_links:
    - from: "App.tsx"
      to: "src/views/*"
      via: "import { KanbanView } from @/views"
      pattern: "from ['\"]@/views['\"]"
---

<objective>
Extract page-level components into views layer to establish foundation for architecture refactoring.

Purpose: Create the `src/views/` directory with orchestrator components for each major application screen. This establishes the page-level layer of the new architecture and separates concerns between view orchestration and reusable components.

Output: Five view components (KanbanView, AgentsView, SettingsView, ProjectPickerView, WorktreesView) and barrel export in `src/views/index.ts`
</objective>

<execution_context>
@/home/m306213/.claude/get-shit-done/workflows/execute-plan.md
@/home/m306213/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-frontend-architecture-refactoring/19-RESEARCH.md
@src/App.tsx
@src/components/KanbanBoard.tsx
@src/components/AgentMonitor.tsx
@src/components/SettingsPage.tsx
@src/components/ProjectsListLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Create views directory structure and extract page-level components</name>
  <files>
    src/views/KanbanView.tsx
    src/views/AgentsView.tsx
    src/views/SettingsView.tsx
    src/views/ProjectPickerView.tsx
    src/views/WorktreesView.tsx
    src/views/index.ts
  </files>
  <action>
1. Create directory: `mkdir -p src/views/`

2. Extract KanbanView: Rename KanbanBoard.tsx logic into KanbanView.tsx. This component should be ~50-100 lines, importing KanbanBoard, KanbanColumn, TaskCard from future domain folders. For now, keep imports from @/components until refactoring. Import store hooks (useBoardStore). Include page-level state management if any.

3. Extract AgentsView: Rename AgentMonitor.tsx into AgentsView.tsx. Orchestrator for agent monitoring screen. Keep agent monitoring components imported from @/components for now.

4. Extract SettingsView: Rename SettingsPage.tsx into SettingsView.tsx. Orchestrator for settings/configuration. Keep internal components imported from @/components for now.

5. Extract ProjectPickerView: Rename ProjectsListLayout.tsx and related picker logic into ProjectPickerView.tsx. First-run project selection screen orchestrator. Will import ProjectList, ProjectPicker from future domain folders.

6. Extract WorktreesView: Rename WorktreeManager.tsx into WorktreesView.tsx. Worktree management screen orchestrator.

7. Create barrel export: src/views/index.ts with:
```typescript
export { KanbanView } from "./KanbanView";
export { AgentsView } from "./AgentsView";
export { SettingsView } from "./SettingsView";
export { ProjectPickerView } from "./ProjectPickerView";
export { WorktreesView } from "./WorktreesView";
```

8. Do NOT change imports in views yet - they should still import from @/components root for now. We will organize domain components in the next plan.

9. Goal: Views are pure orchestrators (30-100 lines) that import and compose domain components. No business logic, no IPC calls in views themselves - those stay in stores or components.
  </action>
  <verify>
- Verify all 5 view files exist: `ls -1 src/views/*.tsx | wc -l` should output 5
- Verify barrel export exists: `cat src/views/index.ts`
- Run TypeScript check: `pnpm tsc --noEmit` - no errors
- Run dev server: `pnpm tauri:dev` - app starts and routes to each view without crashing
- Verify no duplicate files remain (old SettingsPage.tsx, AgentMonitor.tsx, etc. should be deleted or noted for cleanup)
  </verify>
  <done>
- src/views/ directory created with 5 view components (KanbanView, AgentsView, SettingsView, ProjectPickerView, WorktreesView)
- Each view is 50-100 lines, pure orchestrator logic
- Barrel export created at src/views/index.ts
- All views compile without errors
- Views maintain all functionality of original page components
- No views contain IPC calls or direct business logic
  </done>
</task>

<task type="auto">
  <name>Update App.tsx to import from views and verify routing</name>
  <files>
    src/App.tsx
  </files>
  <action>
1. Open src/App.tsx and update imports:
   - Find all imports like `import KanbanBoard from "@/components/KanbanBoard"` (page-level components)
   - Replace with: `import { KanbanView, AgentsView, SettingsView, ProjectPickerView, WorktreesView } from "@/views"`

2. Update the component rendering logic to use views instead of page components:
   - Replace `<KanbanBoard projectId={...} />` with `<KanbanView projectId={...} />`
   - Replace `<AgentMonitor ... />` with `<AgentsView ... />`
   - Replace `<SettingsPage ... />` with `<SettingsView ... />`
   - Replace project picker logic with `<ProjectPickerView />`
   - Replace worktree manager with `<WorktreesView projectId={...} />`

3. Keep all other logic in App.tsx unchanged (routing, state, conditional rendering)

4. Verify imports are consistent: All should use `from "@/views"` with no relative imports

5. Do NOT refactor App.tsx itself yet - it's still the root orchestrator and will stay at src/App.tsx
  </action>
  <verify>
- Run TypeScript check: `pnpm tsc --noEmit` - no errors
- Verify no broken imports: `grep -r "from.*components.*KanbanBoard" src/App.tsx` should return no results
- Run dev server: `pnpm tauri:dev` - app still routes between pages correctly
- All navigation buttons still work (tabs/buttons that switch between views)
  </verify>
  <done>
- App.tsx updated to import and use all views from @/views
- App.tsx routing logic still works correctly
- TypeScript compiles without errors
- No broken references to old component names
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Directory structure is correct: `ls -1 src/views/`
2. Barrel export is valid: `cat src/views/index.ts`
3. App.tsx has correct imports from @/views
4. Full app builds: `pnpm build` succeeds
5. Dev server starts: `pnpm tauri:dev` runs without errors
6. Application routes between pages without errors
7. All views load and display content correctly
8. TypeScript has zero errors: `pnpm tsc --noEmit`
</verification>

<success_criteria>
- src/views/ directory created with 5 view files (KanbanView, AgentsView, SettingsView, ProjectPickerView, WorktreesView)
- Each view file is 50-150 lines, orchestrating internal components with page-level logic
- src/views/index.ts barrel export created and working
- App.tsx updated to import from @/views with no broken references
- Application compiles and runs without errors
- All page routing works correctly
- No duplicate view components remain in old locations
</success_criteria>

<output>
After completion, create `.planning/phases/19-frontend-architecture-refactoring/19-01-SUMMARY.md`
</output>
