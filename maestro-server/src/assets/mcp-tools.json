{
  "$comment": "Maestro's own MCP tool surface, served by `maestro-server mcp` (see mcp_stdio.rs). Descriptions are the ONLY documentation the agent receives, so prose here is behaviour, not commentary. Adding a tool here also needs an arm in the host's `host_tools::handle`, or the gateway will forward calls nobody answers.",
  "tools": [
    {
      "name": "canvas_create",
      "description": "Open a canvas surface in Maestro's side panel. A surface is an HTML document you write — HTML, CSS and JavaScript — rendered in a sandboxed frame beside the conversation. Always the first call for a new surfaceId, before canvas_data or canvas_update. Reuse one surfaceId for a whole answer; a new surface per tool call produces disconnected panels. Never narrate these calls to the user — they see the rendered surface, not the machinery.\n\nWRITING THE DOCUMENT\n- Give every meaningful element an `id`. Controls with an id answer canvas_await, and elements with an id are the ones the user can annotate and point you at.\n- theme `maestro` (the default) gives you Tailwind compiled in the frame with Maestro's own tokens: `bg-card`, `text-muted-foreground`, `border-border`, `rounded-lg`, `bg-primary`, `text-destructive`, `bg-chart-1`. Writing shadcn-flavoured Tailwind produces a surface that looks like the rest of the app — `<div class='rounded-lg border bg-card p-4 shadow-sm'>` is a Maestro card.\n- theme `tailwind` is Tailwind with its own stock tokens, for mocking up a project that uses Tailwind. theme `none` injects nothing, for a mockup carrying the target project's own CSS.\n- Assets are `https:` URLs or `data:` URIs. Relative paths and `file://` do NOT resolve — the frame has no base URL and no access to the disk. Icons are inline `<svg>`, not image files.\n- Charts: inline `<svg>` for bars, lines and sparklines, which always works. For real axes, scales and tooltips add a CDN library — and guard it, because the user may be offline:\n  `<script src='https://cdn.jsdelivr.net/npm/uplot@1.6.32/dist/uPlot.iife.min.js' onerror=\"document.getElementById('chart').textContent='Chart library unavailable offline'\"></script>`\n- `window.maestro` is available in the frame: `maestro.emit(id, kind, value)` answers a pending canvas_await from your own code, `maestro.record(id, value)` notes a value without answering, `maestro.onData(cb)` reads what canvas_data pushed, and `maestro.fetch(url, init)` performs a request from the host machine against the origins you declared in `sources`.\n- A control your own script handles should either call `preventDefault()` or carry `data-maestro-ignore`. Otherwise Maestro also reports the click, and a button that only redraws something in the frame consumes the canvas_await you were holding for a real answer.\n- Anything the frame fails to load or run — a blocked asset, an offline CDN, a thrown exception — comes back to you on the result of your next canvas call on this surface.",
      "inputSchema": {
        "type": "object",
        "required": ["surfaceId", "title", "html"],
        "properties": {
          "surfaceId": {
            "type": "string",
            "description": "Your identifier for this surface; reuse it in every later call."
          },
          "title": {
            "type": "string",
            "description": "Shown above the surface."
          },
          "html": {
            "type": "string",
            "description": "The document. A full `<html>` page or a fragment — Maestro prepends its own head either way."
          },
          "theme": {
            "type": "string",
            "enum": ["maestro", "tailwind", "none"],
            "default": "maestro",
            "description": "`maestro` for anything that should look like the app. `none` for a mockup of someone else's product."
          },
          "sources": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Origins `maestro.fetch` may reach, e.g. [\"https://api.example.com\"]. Anything else is refused. The user is shown this list. Secrets in request headers end up in the transcript and in the saved file."
          }
        }
      }
    },
    {
      "name": "canvas_update",
      "description": "Change a surface. With `target`, the element with that id is replaced in the live frame: no reload, so scroll position, focus, canvas bitmaps and anything the user typed survive — prefer this for content. Without `target`, the whole document is replaced and the frame reloads, losing all of that and re-running your script from the top.\n\nThe replacement is assigned as `outerHTML`, so the element itself is a new node: listeners your script bound to it are gone, and a `<script>` inside the replacement never runs. A control your own JavaScript drives has to be rewired, which means a document-level listener that survives the swap — or a full replace. Static content, rows, values and labels are what `target` is for.\n\nElement ids are scoped to their surface, so two forms may both use `branch` without colliding. Neither `title` nor `sources` can be changed after canvas_create.",
      "inputSchema": {
        "type": "object",
        "required": ["surfaceId", "html"],
        "properties": {
          "surfaceId": {
            "type": "string"
          },
          "html": {
            "type": "string",
            "description": "With `target`, the replacement for that element — include the element itself, with its id, or you lose the anchor. Without `target`, the whole document."
          },
          "target": {
            "type": "string",
            "description": "Id of the element to replace. Omit to replace the document."
          }
        }
      }
    },
    {
      "name": "canvas_data",
      "description": "Store data on a surface at a JSON Pointer path. The document subscribes with `maestro.onData(function (data) { ... })`, reading `data['/rows']`. Use it for values that change while the surface stays the same — a table's rows, a chart's series — so a refresh is one small call instead of a document rewrite.\n\nSubscribe; do not read `maestro.data` while your script is initialising. It is empty then — the bag arrives just after the document loads, and `onData` fires at that moment with everything in it.\n\nThe bag outlives the document. A full canvas_update reloads the frame, and `onData` then delivers every value you have ever pushed as though it were new — so a handler that acts on what it receives (appending a row, playing a move) must be able to tell a replay from a change. Push a counter alongside the value and ignore anything not newer than what the document has already seen.",
      "inputSchema": {
        "type": "object",
        "required": ["surfaceId", "path", "value"],
        "properties": {
          "surfaceId": {
            "type": "string"
          },
          "path": {
            "type": "string",
            "description": "JSON Pointer, e.g. /rows"
          },
          "value": {
            "description": "Any JSON value."
          }
        }
      }
    },
    {
      "name": "canvas_await",
      "description": "Wait for the user to act on a surface's controls, and return what they did. This is what makes the controls answer — render the form with canvas_create or canvas_update first, then call this. Returns `{event:{surfaceId,componentId,kind,value,values}}`, where `componentId` is the element's `id` and `values` holds every field on that surface, so one form is one round trip. A `button[id]` click, a `form[id]` submit, and any `maestro.emit` call all answer it; inputs with an `id` or `name` fill `values` without answering. Omit `surfaceId` to take whichever canvas the user acts on: they can page between every surface you have drawn, and the event names the one they used. Pass `surfaceId` only to insist on a particular canvas — one that does not match a canvas you created waits for nothing. Returns `{timeout:true}` if nothing happened in the window; call again to keep waiting. Keep an open wait for as long as the surface is worth interacting with, re-arming after every event and every timeout: while one is open the user's actions reach you directly, and while none is open each one costs a whole turn. Nothing is lost either way — an action with no wait open is held and delivered to the next wait you arm, or as a prompt once you go idle — but a held action is one the user is waiting on, so arm the next wait before doing anything else. A surface only the user's own script reacts to — a filter that redraws a chart in the frame — never answers a wait and does not need one. Stop re-arming when the exchange is finished; the user can always type, which ends the wait.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "surfaceId": {
            "type": "string",
            "description": "Restrict the wait to one surface. Omit to accept any."
          },
          "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 30
          }
        }
      }
    },
    {
      "name": "create_task",
      "description": "Create a Maestro task in the Planning column of the current project. Use when the user asks for work to be tracked, or when you find follow-up work worth recording. The task is created immediately — say what you created and quote the id.",
      "inputSchema": {
        "type": "object",
        "required": ["title"],
        "properties": {
          "title": {
            "type": "string",
            "minLength": 3,
            "maxLength": 255
          },
          "description": {
            "type": "string",
            "description": "Markdown. What needs doing and why."
          },
          "priority": {
            "type": "string",
            "enum": ["Urgent", "High", "Medium", "Low", "None"]
          },
          "labels": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    {
      "name": "get_task",
      "description": "Read a Maestro task: its description, status, labels, and the outcome thread of what previous sessions concluded about it. Omit `id` for the task this session was started for — the prompt you were given is a paraphrase of a card, and the card is where the user put the detail, the acceptance criteria and the history. Read it before assuming what the work is. The thread carries the last 20 entries, newest kept.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Omit for the task this session is working on."
          }
        }
      }
    },
    {
      "name": "update_task",
      "description": "Change a Maestro task. Only the fields you pass are touched; omit `id` for the task this session is working on.\n\nMoving `status` is the same move as the user dragging the card, so make it when the work has actually reached that state — `Review` when you are finished and want it looked at, not when you expect to be finished. The user is watching the board, so a wrong move is visible and misleading rather than harmless.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Omit for the task this session is working on."
          },
          "status": {
            "type": "string",
            "enum": ["Planning", "Queue", "InProgress", "Review", "Done"]
          },
          "title": {
            "type": "string",
            "minLength": 3,
            "maxLength": 255
          },
          "description": {
            "type": "string",
            "description": "Markdown. Replaces what is there."
          },
          "priority": {
            "type": "string",
            "enum": ["Urgent", "High", "Medium", "Low", "None"]
          },
          "labels": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Replaces the whole list, so include the ones you are keeping."
          }
        }
      }
    },
    {
      "name": "comment_task",
      "description": "Add a note to a task's outcome thread: what you concluded, what you tried that did not work, what the next session needs to know. This transcript is discarded when the session closes and the thread is not, so anything worth keeping belongs here rather than only in your reply. Omit `id` for the task this session is working on.",
      "inputSchema": {
        "type": "object",
        "required": ["body"],
        "properties": {
          "id": {
            "type": "integer",
            "description": "Omit for the task this session is working on."
          },
          "body": {
            "type": "string",
            "description": "Markdown."
          }
        }
      }
    },
    {
      "name": "list_tasks",
      "description": "List the current project's tasks, newest first. Archived tasks are excluded. Use it to check whether work is already tracked before creating a task.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": ["Planning", "Queue", "InProgress", "Review", "Done"],
            "description": "Only tasks in this column."
          }
        }
      }
    },
    {
      "name": "list_automations",
      "description": "List the current project's automations: agents Maestro runs on a schedule, from a webhook, or on demand, each in a session of its own. Returns each one's id, name, trigger and whether it is enabled, plus `server_timezone`, the zone of the machine they run on.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "get_automation",
      "description": "Read one automation of the current project in full: its prompt, trigger, agent and workspace.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      }
    },
    {
      "name": "create_automation",
      "description": "Create an automation in the current project, from scratch or from a template (see list_templates). With `template_id` the template's name, prompt and trigger are filled in first and any field you pass replaces them. Anything not given comes from the project's defaults, as in the editor. A schedule or webhook is live as soon as it is created, so tell the user what you made.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "template_id": {
            "type": "string",
            "description": "A template id such as `builtin-daily-summary` or `user-3`."
          },
          "name": {
            "type": "string",
            "description": "Shown on the Automations page and on every run."
          },
          "prompt": {
            "type": "string",
            "description": "What the agent is asked to do on every run. The whole contract of the run: say what to leave behind (a commit, a report, a task) and when to stop, since nobody is there to ask."
          },
          "agent_id": {
            "type": "string",
            "description": "Which agent runs it. Defaults to the project's default agent, else the one running this session."
          },
          "model": {
            "type": ["string", "null"],
            "description": "The agent's model id. Null leaves it to the agent."
          },
          "permission_mode": {
            "type": ["string", "null"],
            "description": "The agent's session mode id. Null leaves it to the agent."
          },
          "effort": {
            "type": ["string", "null"],
            "description": "The agent's effort level. Null leaves it to the agent."
          },
          "cron": {
            "type": ["string", "null"],
            "description": "Five-field cron (minute hour day-of-month month day-of-week, Sunday is 0), read in `timezone`. Null for no schedule. Cannot be set together with `webhook_enabled`."
          },
          "timezone": {
            "type": "string",
            "description": "IANA zone the schedule is read in. Defaults to the zone of the machine the automation runs on (`server_timezone` in list_automations)."
          },
          "enabled": {
            "type": "boolean",
            "description": "Whether the schedule or webhook is live. False pauses it; the automation can still be run by hand."
          },
          "workspace": {
            "type": "object",
            "description": "Where the agent works. `{\"mode\":\"new_worktree\",\"base_branch\":\"main\"}` makes a fresh worktree per run, `{\"mode\":\"repository\"}` uses the project directory, `{\"mode\":\"path\",\"path\":\"...\"}` an existing directory. Defaults to the project's default workspace.",
            "required": ["mode"],
            "properties": {
              "mode": {
                "type": "string",
                "enum": ["new_worktree", "repository", "path"]
              },
              "base_branch": {
                "type": "string"
              },
              "path": {
                "type": "string"
              }
            }
          },
          "webhook_enabled": {
            "type": "boolean",
            "description": "Whether POST /hooks/<id> on the machine's webhook listener starts a run. The request body is attached to the prompt. Cannot be set together with `cron`."
          },
          "webhook_overlap": {
            "type": "string",
            "enum": ["refuse", "queue", "parallel"],
            "description": "What a webhook delivery does while a run of this automation is already going."
          }
        }
      }
    },
    {
      "name": "update_automation",
      "description": "Change an automation of the current project. Only the fields you pass are touched; pass null to clear `cron`. Set `enabled` to turn its schedule or webhook on or off.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string",
            "description": "Shown on the Automations page and on every run."
          },
          "prompt": {
            "type": "string",
            "description": "What the agent is asked to do on every run. The whole contract of the run: say what to leave behind (a commit, a report, a task) and when to stop, since nobody is there to ask."
          },
          "agent_id": {
            "type": "string",
            "description": "Which agent runs it. Defaults to the project's default agent, else the one running this session."
          },
          "model": {
            "type": ["string", "null"],
            "description": "The agent's model id. Null leaves it to the agent."
          },
          "permission_mode": {
            "type": ["string", "null"],
            "description": "The agent's session mode id. Null leaves it to the agent."
          },
          "effort": {
            "type": ["string", "null"],
            "description": "The agent's effort level. Null leaves it to the agent."
          },
          "cron": {
            "type": ["string", "null"],
            "description": "Five-field cron (minute hour day-of-month month day-of-week, Sunday is 0), read in `timezone`. Null for no schedule. Cannot be set together with `webhook_enabled`."
          },
          "timezone": {
            "type": "string",
            "description": "IANA zone the schedule is read in. Defaults to the zone of the machine the automation runs on (`server_timezone` in list_automations)."
          },
          "enabled": {
            "type": "boolean",
            "description": "Whether the schedule or webhook is live. False pauses it; the automation can still be run by hand."
          },
          "workspace": {
            "type": "object",
            "description": "Where the agent works. `{\"mode\":\"new_worktree\",\"base_branch\":\"main\"}` makes a fresh worktree per run, `{\"mode\":\"repository\"}` uses the project directory, `{\"mode\":\"path\",\"path\":\"...\"}` an existing directory. Defaults to the project's default workspace.",
            "required": ["mode"],
            "properties": {
              "mode": {
                "type": "string",
                "enum": ["new_worktree", "repository", "path"]
              },
              "base_branch": {
                "type": "string"
              },
              "path": {
                "type": "string"
              }
            }
          },
          "webhook_enabled": {
            "type": "boolean",
            "description": "Whether POST /hooks/<id> on the machine's webhook listener starts a run. The request body is attached to the prompt. Cannot be set together with `cron`."
          },
          "webhook_overlap": {
            "type": "string",
            "enum": ["refuse", "queue", "parallel"],
            "description": "What a webhook delivery does while a run of this automation is already going."
          }
        }
      }
    },
    {
      "name": "delete_automation",
      "description": "Delete an automation of the current project. Its schedule and webhook stop. Past runs stay in the run history.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      }
    },
    {
      "name": "run_automation",
      "description": "Run an automation of the current project now, whatever its trigger. The user is asked to confirm first, and this call waits up to a minute for the answer. Returns `{started}` once they approve, `{denied}` if they refuse, or `{pending}` if they have not answered yet. A pending question stays on their screen and the run starts if they approve it later, so do not ask again. The run happens in a session of its own; follow it with list_automation_runs.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      }
    },
    {
      "name": "list_automation_runs",
      "description": "List the current project's automation runs, newest first: status (running, succeeded, failed), what triggered each, and when. Pass `automation_id` for one automation's runs only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "automation_id": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          }
        }
      }
    },
    {
      "name": "get_automation_run",
      "description": "Read one run of the current project: its status, its error if it failed, `result` (the agent's last message), and the worktree it made if that is still on disk.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      }
    },
    {
      "name": "list_templates",
      "description": "List automation templates: the user's own (ids `user-<n>`) and the ones built into Maestro (ids `builtin-<name>`, read-only). Templates are shared by every project. Returns each one's name, tag and trigger, without the prompt.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "get_template",
      "description": "Read one template in full, prompt included.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      }
    },
    {
      "name": "update_template",
      "description": "Change one of the user's own templates. Only the fields you pass are touched. Built-in templates cannot be changed. Automations already made from a template are copies and do not change with it.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "tag": {
            "type": ["string", "null"],
            "description": "A topic the template is filed under. Null clears it."
          },
          "prompt": {
            "type": "string",
            "description": "What the agent is asked to do on every run. The whole contract of the run: say what to leave behind (a commit, a report, a task) and when to stop, since nobody is there to ask."
          },
          "cron": {
            "type": ["string", "null"],
            "description": "Five-field cron (minute hour day-of-month month day-of-week, Sunday is 0), read in `timezone`. Null for no schedule. Cannot be set together with `webhook_enabled`."
          },
          "timezone": {
            "type": "string",
            "description": "IANA zone the schedule is read in. Defaults to the zone of the machine the automation runs on (`server_timezone` in list_automations)."
          },
          "webhook_enabled": {
            "type": "boolean",
            "description": "Whether POST /hooks/<id> on the machine's webhook listener starts a run. The request body is attached to the prompt. Cannot be set together with `cron`."
          },
          "webhook_overlap": {
            "type": "string",
            "enum": ["refuse", "queue", "parallel"],
            "description": "What a webhook delivery does while a run of this automation is already going."
          }
        }
      }
    },
    {
      "name": "save_as_template",
      "description": "Save an automation of the current project as a new template, keeping its name, prompt and trigger. Agent and workspace are left out, since they belong to the project.",
      "inputSchema": {
        "type": "object",
        "required": ["automation_id"],
        "properties": {
          "automation_id": {
            "type": "string"
          },
          "name": {
            "type": "string",
            "description": "Defaults to the automation's name."
          },
          "tag": {
            "type": "string",
            "description": "A topic to file it under."
          }
        }
      }
    },
    {
      "name": "delete_template",
      "description": "Delete one of the user's own templates. Built-in templates cannot be deleted.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      }
    },
    {
      "name": "list_prompts",
      "description": "List the user's saved prompts available in this project: its own and the ones shared with all projects, favorites first. A prompt is text the user keeps to hand to an agent. When the user asks you to use, apply or run one of their prompts (\"use my review prompt\"), find it here and read it with get_prompt. The list carries titles and tags, not the text.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "tag": {
            "type": "string",
            "description": "Only prompts carrying this tag."
          }
        }
      }
    },
    {
      "name": "get_prompt",
      "description": "Read one saved prompt in full. To use it, treat its text as instructions from the user for the work at hand, exactly as if they had typed it in this conversation, and carry them out; do not merely summarise it.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "integer"
          }
        }
      }
    },
    {
      "name": "create_prompt",
      "description": "Save a new prompt for the user. It belongs to this project unless `shared`. Write it as instructions to an agent, in markdown, self-contained enough to work in a later session that has none of this conversation.",
      "inputSchema": {
        "type": "object",
        "required": ["title", "body"],
        "properties": {
          "title": {
            "type": "string",
            "minLength": 1
          },
          "body": {
            "type": "string",
            "description": "The prompt itself, markdown."
          },
          "tags": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Lowercased and deduplicated. Reuse tags list_prompts already shows rather than inventing near-duplicates."
          },
          "shared": {
            "type": "boolean",
            "description": "Shared with all projects rather than only this one. Defaults to false."
          },
          "favorite": {
            "type": "boolean",
            "description": "Starred in this project. Defaults to false."
          }
        }
      }
    },
    {
      "name": "update_prompt",
      "description": "Change a saved prompt, for example to improve its wording when the user asks. Only the fields you pass are touched; `tags` replaces the whole list. Read it with get_prompt first, and keep what the user wrote unless asked to change it. Setting `shared` to false gives the prompt to this project alone, and removes it from every other project.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "integer"
          },
          "title": {
            "type": "string",
            "minLength": 1
          },
          "body": {
            "type": "string"
          },
          "tags": {
            "type": "array",
            "items": { "type": "string" }
          },
          "shared": {
            "type": "boolean"
          },
          "favorite": {
            "type": "boolean",
            "description": "Starred in this project. Other projects' stars are left alone."
          }
        }
      }
    },
    {
      "name": "delete_prompt",
      "description": "Delete a saved prompt. A shared prompt disappears from every project, so only delete one the user asked you to.",
      "inputSchema": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": {
            "type": "integer"
          }
        }
      }
    }
  ]
}
