{
  "$comment": "Maestro's own MCP tool surface, served by `maestro-server mcp` (see mcp_stdio.rs). Descriptions are the ONLY documentation the agent receives, so prose here is behaviour, not commentary. Adding a tool here also needs an arm in the host's `host_tools::handle`, or the gateway will forward calls nobody answers.",
  "tools": [
    {
      "name": "canvas_create",
      "description": "Open a canvas surface in Maestro's side panel. A surface is an HTML document you write — HTML, CSS and JavaScript — rendered in a sandboxed frame beside the conversation. Always the first call for a new surfaceId, before canvas_data or canvas_update. Reuse one surfaceId for a whole answer; a new surface per tool call produces disconnected panels. Never narrate these calls to the user — they see the rendered surface, not the machinery.\n\nWRITING THE DOCUMENT\n- Give every meaningful element an `id`. Controls with an id answer canvas_await, and elements with an id are the ones the user can annotate and point you at.\n- theme `maestro` (the default) gives you Tailwind compiled in the frame with Maestro's own tokens: `bg-card`, `text-muted-foreground`, `border-border`, `rounded-lg`, `bg-primary`, `text-destructive`, `bg-chart-1`. Writing shadcn-flavoured Tailwind produces a surface that looks like the rest of the app — `<div class='rounded-lg border bg-card p-4 shadow-sm'>` is a Maestro card.\n- theme `tailwind` is Tailwind with its own stock tokens, for mocking up a project that uses Tailwind. theme `none` injects nothing, for a mockup carrying the target project's own CSS.\n- Assets are `https:` URLs or `data:` URIs. Relative paths and `file://` do NOT resolve — the frame has no base URL and no access to the disk. Icons are inline `<svg>`, not image files.\n- Charts: inline `<svg>` for bars, lines and sparklines, which always works. For real axes, scales and tooltips add a CDN library — and guard it, because the user may be offline:\n  `<script src='https://cdn.jsdelivr.net/npm/uplot@1.6.32/dist/uPlot.iife.min.js' onerror=\"document.getElementById('chart').textContent='Chart library unavailable offline'\"></script>`\n- `window.maestro` is available in the frame: `maestro.emit(id, kind, value)` answers a pending canvas_await from your own code, `maestro.record(id, value)` notes a value without answering, `maestro.data` / `maestro.onData(cb)` read what canvas_data pushed, and `maestro.fetch(url, init)` performs a request from the host machine against the origins you declared in `sources`.\n- Anything the frame fails to load or run — a blocked asset, an offline CDN, a thrown exception — comes back to you on the result of your next canvas call on this surface.",
      "inputSchema": {
        "type": "object",
        "required": ["surfaceId", "title", "html"],
        "properties": {
          "surfaceId": {
            "type": "string",
            "description": "Your identifier for this surface; reuse it in every later call."
          },
          "title": { "type": "string", "description": "Shown above the surface." },
          "html": {
            "type": "string",
            "description": "The document. A full `<html>` page or a fragment — Maestro prepends its own head either way."
          },
          "theme": {
            "type": "string",
            "enum": ["maestro", "tailwind", "none"],
            "default": "maestro",
            "description": "`maestro` for anything that should look like the app. `none` for a mockup of someone else's product."
          },
          "sources": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Origins `maestro.fetch` may reach, e.g. [\"https://api.example.com\"]. Anything else is refused. The user is shown this list. Secrets in request headers end up in the transcript and in the saved file."
          }
        }
      }
    },
    {
      "name": "canvas_update",
      "description": "Change a surface. With `target`, the element with that id is replaced in the live frame: no reload, so scroll position, focus and anything the user typed survive — prefer this. Without `target`, the whole document is replaced and the frame reloads, losing all of that.\n\nOrder is canvas_create, then canvas_data, then canvas_update.\n\nControls report back only while a canvas_await is pending. Outside that window the user can still fill them in and nothing is sent to you, which is the right shape for a UI mock. Element ids are scoped to their surface, so two forms may both use `branch` without colliding.",
      "inputSchema": {
        "type": "object",
        "required": ["surfaceId", "html"],
        "properties": {
          "surfaceId": { "type": "string" },
          "html": {
            "type": "string",
            "description": "With `target`, the replacement for that element — include the element itself, with its id, or you lose the anchor. Without `target`, the whole document."
          },
          "target": {
            "type": "string",
            "description": "Id of the element to replace. Omit to replace the document."
          }
        }
      }
    },
    {
      "name": "canvas_data",
      "description": "Store data on a surface at a JSON Pointer path. The document reads it as `maestro.data['/rows']`, or subscribes with `maestro.onData(function (data) { ... })`, which fires immediately and again on every change. Use it for values that change while the surface stays the same — a table's rows, a chart's series — so a refresh is one small call instead of a document rewrite.",
      "inputSchema": {
        "type": "object",
        "required": ["surfaceId", "path", "value"],
        "properties": {
          "surfaceId": { "type": "string" },
          "path": { "type": "string", "description": "JSON Pointer, e.g. /rows" },
          "value": { "description": "Any JSON value." }
        }
      }
    },
    {
      "name": "canvas_await",
      "description": "Wait for the user to act on a surface's controls, and return what they did. This is what makes the controls answer — render the form with canvas_create or canvas_update first, then call this. Returns `{event:{surfaceId,componentId,kind,value,values}}`, where `componentId` is the element's `id` and `values` holds every field on that surface, so one form is one round trip. A `button[id]` click, a `form[id]` submit, and any `maestro.emit` call all answer it; inputs with an `id` or `name` fill `values` without answering. Omit `surfaceId` to take whichever canvas the user acts on: they can page between every surface you have drawn, and the event names the one they used. Pass `surfaceId` only to insist on a particular canvas — one that does not match a canvas you created waits for nothing. Returns `{timeout:true}` if nothing happened in the window; call again to keep waiting.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "surfaceId": {
            "type": "string",
            "description": "Restrict the wait to one surface. Omit to accept any."
          },
          "timeoutSeconds": { "type": "integer", "minimum": 1, "maximum": 60, "default": 30 }
        }
      }
    },
    {
      "name": "create_task",
      "description": "Create a Maestro task in the Planning column of the current project. Use when the user asks for work to be tracked, or when you find follow-up work worth recording. The task is created immediately — say what you created and quote the id.",
      "inputSchema": {
        "type": "object",
        "required": ["title"],
        "properties": {
          "title": { "type": "string", "minLength": 3, "maxLength": 255 },
          "description": {
            "type": "string",
            "description": "Markdown. What needs doing and why."
          },
          "priority": {
            "type": "string",
            "enum": ["Urgent", "High", "Medium", "Low", "None"]
          },
          "labels": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    {
      "name": "list_tasks",
      "description": "List the current project's tasks, newest first. Archived tasks are excluded. Use it to check whether work is already tracked before creating a task.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": ["Planning", "Queue", "InProgress", "Review", "Done"],
            "description": "Only tasks in this column."
          }
        }
      }
    }
  ]
}
